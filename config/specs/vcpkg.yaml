# Converted from argc's vcpkg.sh by tools/argc-to-spec. Do not edit by hand.
name: vcpkg
flags:
  "-h, --help": ""
  "-V, --version": ""
commands:
  - name: export
    description: "Creates a standalone deployment of installed ports"
    flags:
      "--7zip": "Exports to a 7zip (.7z) file"
      "--x-all-installed": "Exports all installed packages"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--x-chocolatey": "Exports a Chocolatey package (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Does not actually export"
      "--host-triplet=": "Host triplet."
      "--ifw": "Exports to an IFW-based installer"
      "--ifw-configuration-file-path=": "The temporary file path for the installer configuration"
      "--ifw-installer-file-path=": "The file path for the exported installer"
      "--ifw-packages-directory-path=": "The temporary directory path for the repacked packages"
      "--ifw-repository-directory-path=": "The directory path for the exported repository"
      "--ifw-repository-url=": "The remote repository URL for the online installer"
      "--x-install-root=": "Installed directory (experimental)"
      "--x-maintainer=": "The maintainer for the exported Chocolatey package (experimental)"
      "--nuget": "Exports a NuGet package"
      "--nuget-description=": "Description for the exported NuGet package"
      "--nuget-id=": "Id for the exported NuGet package (overrides --output)"
      "--nuget-version=": "The version for the exported NuGet package"
      "--output-dir=": "The output directory for produced artifacts"
      "--output=": "The output name (used to construct filename)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--prefab": "Exports to Prefab format"
      "--prefab-artifact-id=": "Artifact Id is the name of the project according Maven specifications"
      "--prefab-debug": "Enables prefab debug"
      "--prefab-group-id=": "GroupId uniquely identifies your project according Maven specifications"
      "--prefab-maven": "Enables Maven"
      "--prefab-min-sdk=": "The Android minimum supported SDK version"
      "--prefab-target-sdk=": "The Android target sdk version"
      "--prefab-version=": "Version is the name of the project according Maven specifications"
      "--raw": "Exports to an uncompressed directory"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--x-version-suffix": "string The version suffix to add for the exported Chocolatey package (experimental)"
      "--zip": "Exports to a zip file"
      "-h, --help": "Print help"
  - name: install
    description: "Installs a package"
    flags:
      "--allow-unsupported": "Continues with a warning on unsupported ports, rather than failing"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--clean-after-build": "Cleans buildtrees, packages and downloads after building each package"
      "--clean-buildtrees-after-build": "Cleans buildtrees after building each package"
      "--clean-downloads-after-build": "Cleans downloads after building each package"
      "--clean-packages-after-build": "Cleans packages after building each package"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Does not actually build or install"
      "--editable": "Disables source re-extraction and binary caching for libraries on the command line (classic mode)"
      "--enforce-port-checks": "Fails install if a port has detected problems or attempts to use a deprecated feature"
      "--x-feature=": "Additional features from the top-level manifest to install (manifest mode)"
      "--head": "Installs the libraries on the command line using the latest upstream sources (classic mode)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--keep-going": "Continues installing packages on failure"
      "--x-no-default-features": "Does not install the default features from the top-level manifest (manifest mode)"
      "--no-downloads": "Does not download new sources"
      "--no-print-usage": "Does not print CMake usage information after install"
      "--only-binarycaching": "Fails if cached binaries are not available"
      "--only-downloads": "Makes best-effort attempt to download sources without building"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--recurse": "Allows removal of packages as part of installation"
      "--triplet=": "Target triplet."
      "--x-use-aria2": "Uses aria2 to perform download tasks"
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--x-write-nuget-packages-config=": "Writes a NuGet packages.config-formatted file for use with external binary caching."
      "-h, --help": "Print help"
  - name: remove
    description: "Uninstalls a package"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Prints the packages to be removed, but does not remove them"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--outdated": "Removes all packages with versions that do not match the built-in registry"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--recurse": "Allows removal of dependent packages not explicitly specified"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: x-set-installed
    description: "Installs, upgrades, or removes packages such that that installed matches exactly those supplied"
    flags:
      "--allow-unsupported": "Continues with a warning on unsupported ports, rather than failing"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Does not actually build or install"
      "--enforce-port-checks": "Fails install if a port has detected problems or attempts to use a deprecated feature"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--keep-going": "Continues installing packages on failure"
      "--no-print-usage": "Does not print CMake usage information after install"
      "--only-downloads": "Makes best-effort attempt to download sources without building"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--x-write-nuget-packages-config=": "Writes a NuGet packages.config-formatted file for use with external binary caching."
      "-h, --help": "Print help"
  - name: upgrade
    description: "Rebuilds all outdated packages"
    flags:
      "--allow-unsupported": "Continues with a warning on unsupported ports, rather than failing"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--no-dry-run": "Actually upgrade"
      "--no-keep-going": "Stop installing packages on failure"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: x-check-support
    description: "Tests whether a port is supported without building it"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--x-json": "Prints JSON rather than plain text"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: depend-info
    description: "Displays a list of dependencies for ports"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--format=": "Chooses output format, one of `list`, `tree`, `mermaid`, `dot`, or `dgml`"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--max-recurse=": "Sets max recursion depth."
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--show-depth": "Shows recursion depth in `list` output"
      "--sort=": "Chooses sort order for the `list` format, one of `lexicographical`, `topological` (default), `reverse`"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: list
    description: "Lists installed libraries"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--x-full-desc": "Does not truncate long text"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--x-json": "Prints JSON rather than plain text"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: owns
    description: "Searches for the owner of a file in installed packages"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: x-package-info
    description: "Display detailed information on packages"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--x-installed": "(experimental) Reports installed packages rather than available"
      "--x-json": "Prints JSON rather than plain text"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--x-transitive": "(experimental) Also reports dependencies of installed packages"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: portsdiff
    description: "Diffs changes in port versions between commits"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: search
    description: "Searches for packages available to be built"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--x-full-desc": "Does not truncate long text"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--x-json": "Prints JSON rather than plain text"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: update
    description: "Lists packages that can be upgraded"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: add
    description: "Adds dependency to manifest"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--version=": "A version or version range to match; only valid for artifacts"
      "-h, --help": "Print help"
  - name: x-add-version
    description: "Adds a version to the version database"
    flags:
      "--all": "Processes versions for all ports"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--overwrite-version": "Overwrites git-tree of an existing version"
      "--x-packages-root=": "Packages directory (experimental)"
      "--skip-formatting-check": "Skips the formatting check of vcpkg.json files"
      "--skip-version-format-check": "Skips the version format check"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--verbose": "Prints success messages rather than only errors"
      "-h, --help": "Print help"
  - name: create
    description: "Creates a new port"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: edit
    description: "Edits a port, optionally with $EDITOR, defaults to \"code\""
    flags:
      "--all": "Opens editor into the port as well as the port-specific buildtree subfolder"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--buildtrees": "Opens editor into the port-specific buildtree subfolder"
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: env
    description: "Creates a clean shell environment for development or compiling"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--bin": "Adds installed bin/ to $PATH"
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--debug-bin": "Adds installed debug/bin/ to $PATH"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--include": "Adds installed include/ to $INCLUDE"
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--python": "Adds installed python/ to $PYTHONPATH"
      "--tools": "Adds installed tools/*/ to $PATH"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: format-manifest
    description: "Prettyfies vcpkg.json"
    flags:
      "--all": "Formats all ports' manifest files"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--convert-control": "Converts CONTROL files to manifest files"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: hash
    description: "Gets a file's SHA256 or SHA512"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: x-init-registry
    description: "Creates a blank git registry"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: new
    description: "Creates a new manifest"
    flags:
      "--application": "Creates an application manifest (don't require name or version)"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--name=": "Name for the new manifest"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--single-file": "Embeds vcpkg-configuration.json into vcpkg.json"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--version-date": "Interprets --version as an ISO 8601 date."
      "--version-relaxed": "Interprets --version as a relaxed-numeric version (Nonnegative numbers separated by dots)"
      "--version-string": "Interprets --version as a string with no ordering behavior"
      "--version=": "Version for the new manifest"
      "-h, --help": "Print help"
  - name: x-update-baseline
    description: "Updates baselines of git registries in a manifest to those registries' HEAD commit"
    flags:
      "--add-initial-baseline": "Adds a `builtin-baseline` to a vcpkg.json that doesn't already have it"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Prints out plan without execution"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: ci
    description: "Tries building all ports for CI testing"
    flags:
      "--allow-unexpected-passing": "Suppresses 'Passing, remove from fail list' results"
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--ci-baseline=": "Path to the ci.baseline.txt file."
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--dry-run": "Prints out plan without execution"
      "--exclude=": "Comma separated list of ports to skip"
      "--failure-logs=": "Directory to which failure logs will be copied"
      "--host-exclude=": "Comma separated list of ports to skip for the host triplet"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--output-hashes=": "File to output all determined package hashes"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--parent-hashes=": "File to read package hashes for a parent CI state, to reduce the set of changed packages"
      "--x-randomize": "Randomizes the install order"
      "--skip-failures": "Skips ports marked `=fail` in ci.baseline.txt"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--x-xunit-all": "Reports unchanged ports in the XUnit output"
      "--x-xunit=": "File to output results in XUnit format"
      "-h, --help": "Print help"
  - name: x-ci-verify-versions
    description: "Checks integrity of the version database"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "--verbose": "Prints result for each port rather than only just errors"
      "--verify-git-trees": "Verifies that each git tree object matches its declared version (this is very slow)"
      "-h, --help": "Print help"
  - name: contact
    description: "Displays contact information to send feedback"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: fetch
    description: "Fetches something from the system or the internet"
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--x-stderr-status": "Prints status/downloading messages to stderr rather than stdout (Errors/failures still go to stdout)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
  - name: integrate
    description: "Integrate vcpkg with shells and buildsystems."
    flags:
      "--x-asset-sources=": "Asset caching sources."
      "--binarysource=": "Binary caching sources."
      "--x-buildtrees-root=": "Buildtrees directory (experimental)"
      "--downloads-root=": "Downloads directory (default: $VCPKG_DOWNLOADS)"
      "--host-triplet=": "Host triplet."
      "--x-install-root=": "Installed directory (experimental)"
      "--overlay-ports=": "Directories of overlay ports (also: $VCPKG_OVERLAY_PORTS)"
      "--overlay-triplets": "string Directories of overlay triplets (also: $VCPKG_OVERLAY_TRIPLETS)"
      "--x-packages-root=": "Packages directory (experimental)"
      "--triplet=": "Target triplet."
      "--vcpkg-root=": "The vcpkg root directory (default: $VCPKG_ROOT)"
      "-h, --help": "Print help"
