# Converted from argc's s3cmd.sh by tools/argc-to-spec. Do not edit by hand.
name: s3cmd
flags:
  "-h, --help": "show this help message and exit"
  "--configure": "Invoke interactive (re)configuration tool."
  "-c, --config=": "Config file name."
  "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
  "--access_key=": "AWS Access Key"
  "--secret_key=": "AWS Secret Key"
  "--access_token=": "AWS Access Token"
  "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
  "-s, --ssl": "Use HTTPS connection when communicating with S3."
  "--no-ssl": "Don't use HTTPS."
  "-e, --encrypt": "Encrypt files before uploading to S3."
  "--no-encrypt": "Don't encrypt files."
  "-f, --force": "Force overwrite and other dangerous operations."
  "--continue": "Continue getting a partially downloaded file (only for [get] command)."
  "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
  "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
  "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
  "-r, --recursive": "Recursive upload, download or removal."
  "--check-md5": "Check MD5 sums when comparing files for [sync]."
  "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
  "-P, --acl-public": "Store objects with ACL allowing read for anyone."
  "--acl-private": "Store objects with default ACL allowing access for you only."
  "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
  "--acl-revoke=": "Revoke stated permission for a given amazon user."
  "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
  "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
  "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
  "--no-delete-removed": "Don't delete destination objects [sync]"
  "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
  "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
  "--max-delete=": "Do not delete more than NUM files."
  "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
  "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
  "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
  "-p, --preserve=": "Preserve filesystem attributes."
  "--no-preserve": "Don't store FS attributes"
  "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
  "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
  "--exclude-from=": "Read --exclude GLOBs from FILE"
  "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
  "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
  "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
  "--include-from=": "Read --include GLOBs from FILE"
  "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
  "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
  "--files-from=": "Read list of source-file names from FILE."
  "--region=": "Region to create bucket in."
  "--bucket-location=": "Region to create bucket in."
  "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
  "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
  "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
  "--rr=": "Store object with 'Reduced redundancy'."
  "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
  "--no-rr=": "Store object without 'Reduced redundancy'."
  "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
  "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
  "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
  "--default-mime-type=": "Default MIME-type for stored objects."
  "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
  "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
  "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
  "-m, --mime-type=": "Force MIME-type."
  "--add-header=": "Add a given HTTP header to the upload request."
  "--remove-header=": "Remove a given HTTP header."
  "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
  "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
  "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
  "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
  "--verbatim": "Use the S3 name as given on the command line."
  "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
  "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
  "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
  "--list-allow-unordered": "Not an AWS standard."
  "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
  "--ws-index=": "Name of index-document (only for [ws-create] command)"
  "--ws-error=": "Name of error-document (only for [ws-create] command)"
  "--expiry-date=": "Indicates when the expiration rule takes effect."
  "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
  "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
  "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
  "--progress": "Display progress meter (default on TTY)."
  "--no-progress": "Don't display progress meter (default on non-TTY)."
  "--stats": "Give some file-transfer stats."
  "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
  "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
  "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
  "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
  "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
  "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
  "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
  "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
  "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
  "-v, --verbose": "Enable verbose output."
  "-d, --debug": "Enable debug output."
  "--version": "Show s3cmd version (2.4.0) and exit."
  "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
  "--cache-file=": "Cache FILE containing local source MD5 values"
  "-q, --quiet": "Silence output on stdout"
  "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
  "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
  "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
  "--check-certificate": "Check SSL certificate validity"
  "--no-check-certificate": "Do not check SSL certificate validity"
  "--check-hostname": "Check SSL certificate hostname validity"
  "--no-check-hostname": "Do not check SSL certificate hostname validity"
  "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
  "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
  "--no-connection-pooling": "Disable connection reuse"
  "--requester-pays": "Set the REQUESTER PAYS flag for operations"
  "-l, --long-listing": "Produce long listing [ls]"
  "--stop-on-error": "stop if error in transfer"
  "--max-retries=": "Maximum number of times to retry a failed request before giving up."
  "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
  "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
completion:
  flag:
    restore-priority: [bulk, standard, expedited]
    preserve: [mode, ownership, timestamps]
    region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
    bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
    reduced-redundancy: [put, cp, mv]
    rr: [put, cp, mv]
    no-reduced-redundancy: [put, cp, mv]
    no-rr: [put, cp, mv]
    storage-class: [put, cp, mv]
    server-side-encryption: [put, sync, cp, modify]
    server-side-encryption-kms-id: [put, sync, cp, modify]
    add-encoding-exts: [css, js, html]
commands:
  - name: mb
    description: "Make bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: rb
    description: "Remove bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: ls
    description: "List objects or buckets"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: la
    description: "List all object in all buckets"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: put
    description: "Put file into bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: get
    description: "Get file from bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: del
    description: "Delete file from bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: rm
    description: "Delete file from bucket (alias for del)"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: restore
    description: "Restore file from Glacier storage"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: sync
    description: "Synchronize a directory tree to S3 (checks files freshness using size and md5 checksum, unless overridden by options, see below)"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: du
    description: "Disk usage by buckets"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: info
    description: "Get various information about Buckets or Files"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cp
    description: "Copy object"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: modify
    description: "Modify object metadata"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: mv
    description: "Move object"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setacl
    description: "Modify Access control list for Bucket or Files"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setversioning
    description: "Modify Bucket Versioning"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
      positional:
        - [enabled, disabled]
  - name: setownership
    description: "Modify Bucket Object Ownership"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
      positional:
        - [BucketOwnerPreferred, BucketOwnerEnforced, ObjectWriter]
  - name: setblockpublicaccess
    description: "Modify Block Public Access rules"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
      positional:
        - [BlockPublicAcls, IgnorePublicAcls, BlockPublicPolicy, RestrictPublicBuckets]
  - name: setobjectlegalhold
    description: "Modify Object Legal Hold"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setobjectretention
    description: "Modify Object Retention"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setpolicy
    description: "Modify Bucket Policy"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: delpolicy
    description: "Delete Bucket Policy"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setcors
    description: "Modify Bucket CORS"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: delcors
    description: "Delete Bucket CORS"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: payer
    description: "Modify Bucket Requester Pays policy"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: multipart
    description: "Show multipart uploads"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: abortmp
    description: "Abort a multipart upload"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: listmp
    description: "List parts of a multipart upload"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: accesslog
    description: "Enable/disable bucket access logging"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: sign
    description: "Sign arbitrary string using the secret key"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: signurl
    description: "Sign an S3 URL to provide limited public access with expiry"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: fixbucket
    description: "Fix invalid file names in a bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: settagging
    description: "Modify tagging for Bucket or Files"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: gettagging
    description: "Get tagging for Bucket or Files"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: deltagging
    description: "Delete tagging for Bucket or Files"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: ws-create
    description: "Create Website from bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: ws-delete
    description: "Delete Website"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: ws-info
    description: "Info about Website"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: expire
    description: "Set or delete expiration rule for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setlifecycle
    description: "Upload a lifecycle policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: getlifecycle
    description: "Get a lifecycle policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: dellifecycle
    description: "Remove a lifecycle policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: setnotification
    description: "Upload a notification policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: getnotification
    description: "Get a notification policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: delnotification
    description: "Remove a notification policy for the bucket"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cflist
    description: "List CloudFront distribution points"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfinfo
    description: "Display CloudFront distribution point parameters"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfcreate
    description: "Create CloudFront distribution point"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfdelete
    description: "Delete CloudFront distribution point"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfmodify
    description: "Change CloudFront distribution point parameters"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfinval
    description: "Invalidate CloudFront objects"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
  - name: cfinvalinfo
    description: "Display CloudFront invalidation request(s) status"
    flags:
      "-h, --help": ""
    persistentflags:
      "-h, --help": "show this help message and exit"
      "--configure": "Invoke interactive (re)configuration tool."
      "-c, --config=": "Config file name."
      "--dump-config": "Dump current configuration after parsing config files and command line options and exit."
      "--access_key=": "AWS Access Key"
      "--secret_key=": "AWS Secret Key"
      "--access_token=": "AWS Access Token"
      "-n, --dry-run": "Only show what should be uploaded or downloaded but don't actually do it."
      "-s, --ssl": "Use HTTPS connection when communicating with S3."
      "--no-ssl": "Don't use HTTPS."
      "-e, --encrypt": "Encrypt files before uploading to S3."
      "--no-encrypt": "Don't encrypt files."
      "-f, --force": "Force overwrite and other dangerous operations."
      "--continue": "Continue getting a partially downloaded file (only for [get] command)."
      "--continue-put": "Continue uploading partially uploaded files or multipart upload parts."
      "--upload-id=": "UploadId for Multipart Upload, in case you want continue an existing upload (equivalent to --continue-put) and there are multiple partial uploads."
      "--skip-existing": "Skip over files that exist at the destination (only for [get] and [sync] commands)."
      "-r, --recursive": "Recursive upload, download or removal."
      "--check-md5": "Check MD5 sums when comparing files for [sync]."
      "--no-check-md5": "Do not check MD5 sums when comparing files for [sync]."
      "-P, --acl-public": "Store objects with ACL allowing read for anyone."
      "--acl-private": "Store objects with default ACL allowing access for you only."
      "--acl-grant=": "or USER_CANONICAL_ID Grant stated permission to a given amazon user."
      "--acl-revoke=": "Revoke stated permission for a given amazon user."
      "-D, --restore-days=": "Number of days to keep restored file available (only for 'restore' command)."
      "--restore-priority=": "Priority for restoring files from S3 Glacier (only for 'restore' command)."
      "--delete-removed": "Delete destination objects with no corresponding source file [sync]"
      "--no-delete-removed": "Don't delete destination objects [sync]"
      "--delete-after": "Perform deletes AFTER new uploads when delete-removed is enabled [sync]"
      "--delay-updates": "*OBSOLETE* Put all updated files into place at end [sync]"
      "--max-delete=": "Do not delete more than NUM files."
      "--limit=": "Limit number of objects returned in the response body (only for [ls] and [la] commands)"
      "--add-destination=": "Additional destination for parallel uploads, in addition to last arg."
      "--delete-after-fetch": "Delete remote objects after fetching to local file (only for [get] and [sync] commands)."
      "-p, --preserve=": "Preserve filesystem attributes."
      "--no-preserve": "Don't store FS attributes"
      "--keep-dirs": "Preserve all local directories as remote objects including empty directories."
      "--exclude=": "Filenames and paths matching GLOB will be excluded from sync"
      "--exclude-from=": "Read --exclude GLOBs from FILE"
      "--rexclude=": "Filenames and paths matching REGEXP (regular expression) will be excluded from sync"
      "--rexclude-from=": "Read --rexclude REGEXPs from FILE"
      "--include=": "Filenames and paths matching GLOB will be included even if previously excluded by one of --(r)exclude(-from) patterns"
      "--include-from=": "Read --include GLOBs from FILE"
      "--rinclude=": "Same as --include but uses REGEXP (regular expression) instead of GLOB"
      "--rinclude-from=": "Read --rinclude REGEXPs from FILE"
      "--files-from=": "Read list of source-file names from FILE."
      "--region=": "Region to create bucket in."
      "--bucket-location=": "Region to create bucket in."
      "--host=": "HOSTNAME:PORT for S3 endpoint (default: s3.amazonaws.com, alternatives such as s3-eu-west-1.amazonaws.com)."
      "--host-bucket=": "DNS-style bucket+hostname:port template for accessing a bucket (default: %(bucket)s.s3.amazonaws.com)"
      "--reduced-redundancy=": "Store object with 'Reduced redundancy'."
      "--rr=": "Store object with 'Reduced redundancy'."
      "--no-reduced-redundancy=": "Store object without 'Reduced redundancy'."
      "--no-rr=": "Store object without 'Reduced redundancy'."
      "--storage-class=": "Store object with specified CLASS (STANDARD, STANDARD_IA, ONEZONE_IA, INTELLIGENT_TIERING, GLACIER or DEEP_ARCHIVE)."
      "--access-logging-target-prefix=": "Target prefix for access logs (S3 URI) (for [cfmodify] and [accesslog] commands)"
      "--no-access-logging": "Disable access logging (for [cfmodify] and [accesslog] commands)"
      "--default-mime-type=": "Default MIME-type for stored objects."
      "-M, --guess-mime-type": "Guess MIME-type of files by their extension or mime magic."
      "--no-guess-mime-type": "Don't guess MIME-type and use the default type instead."
      "--no-mime-magic": "Don't use mime magic when guessing MIME-type."
      "-m, --mime-type=": "Force MIME-type."
      "--add-header=": "Add a given HTTP header to the upload request."
      "--remove-header=": "Remove a given HTTP header."
      "--server-side-encryption=": "Specifies that server-side encryption will be used when putting objects."
      "--server-side-encryption-kms-id=": "Specifies the key id used for server-side encryption with AWS KMS-Managed Keys (SSE-KMS) when putting objects."
      "--encoding=": "Override autodetected terminal and filesystem encoding (character set)."
      "--add-encoding-exts=": "Add encoding to these comma delimited extensions i.e."
      "--verbatim": "Use the S3 name as given on the command line."
      "--disable-multipart": "Disable multipart upload on files bigger than --multipart-chunk-size-mb"
      "--multipart-chunk-size-mb=": "Size of each chunk of a multipart upload."
      "--list-md5": "Include MD5 sums in bucket listings (only for 'ls' command)."
      "--list-allow-unordered": "Not an AWS standard."
      "-H, --human-readable-sizes": "Print sizes in human readable form (eg 1kB instead of 1234)."
      "--ws-index=": "Name of index-document (only for [ws-create] command)"
      "--ws-error=": "Name of error-document (only for [ws-create] command)"
      "--expiry-date=": "Indicates when the expiration rule takes effect."
      "--expiry-days=": "Indicates the number of days after object creation the expiration rule takes effect."
      "--expiry-prefix=": "Identifying one or more objects with the prefix to which the expiration rule applies."
      "--skip-destination-validation": "Skips validation of Amazon SQS, Amazon SNS, and AWS Lambda destinations when applying notification configuration."
      "--progress": "Display progress meter (default on TTY)."
      "--no-progress": "Don't display progress meter (default on non-TTY)."
      "--stats": "Give some file-transfer stats."
      "--enable": "Enable given CloudFront distribution (only for [cfmodify] command)"
      "--disable": "Disable given CloudFront distribution (only for [cfmodify] command)"
      "--cf-invalidate": "Invalidate the uploaded filed in CloudFront."
      "--cf-invalidate-default-index": "When using Custom Origin and S3 static website, invalidate the default index file."
      "--cf-no-invalidate-default-index-root": "When using Custom Origin and S3 static website, don't invalidate the path to the default index file."
      "--cf-add-cname=": "Add given CNAME to a CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-remove-cname=": "Remove given CNAME from a CloudFront distribution (only for [cfmodify] command)"
      "--cf-comment=": "Set COMMENT for a given CloudFront distribution (only for [cfcreate] and [cfmodify] commands)"
      "--cf-default-root-object=": "Set the default root object to return when no object is specified in the URL."
      "-v, --verbose": "Enable verbose output."
      "-d, --debug": "Enable debug output."
      "--version": "Show s3cmd version (2.4.0) and exit."
      "-F, --follow-symlinks": "Follow symbolic links as if they are regular files"
      "--cache-file=": "Cache FILE containing local source MD5 values"
      "-q, --quiet": "Silence output on stdout"
      "--ca-certs=": "Path to SSL CA certificate FILE (instead of system default)"
      "--ssl-cert=": "Path to client own SSL certificate CRT_FILE"
      "--ssl-key=": "Path to client own SSL certificate private key KEY_FILE"
      "--check-certificate": "Check SSL certificate validity"
      "--no-check-certificate": "Do not check SSL certificate validity"
      "--check-hostname": "Check SSL certificate hostname validity"
      "--no-check-hostname": "Do not check SSL certificate hostname validity"
      "--signature-v2": "Use AWS Signature version 2 instead of newer signature methods."
      "--limit-rate=": "Limit the upload or download speed to amount bytes per second."
      "--no-connection-pooling": "Disable connection reuse"
      "--requester-pays": "Set the REQUESTER PAYS flag for operations"
      "-l, --long-listing": "Produce long listing [ls]"
      "--stop-on-error": "stop if error in transfer"
      "--max-retries=": "Maximum number of times to retry a failed request before giving up."
      "--content-disposition=": "Provide a Content-Disposition for signed URLs, e.g., \"inline; filename=myvideo.mp4\""
      "--content-type=": "Provide a Content-Type for signed URLs, e.g., \"video/mp4\""
    completion:
      flag:
        restore-priority: [bulk, standard, expedited]
        preserve: [mode, ownership, timestamps]
        region: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        bucket-location: [us-east-1, us-west-1, us-west-2, eu-west-1, eu-central-1, ap-northeast-1, ap-southeast-1, ap-southeast-2, sa-east-1]
        reduced-redundancy: [put, cp, mv]
        rr: [put, cp, mv]
        no-reduced-redundancy: [put, cp, mv]
        no-rr: [put, cp, mv]
        storage-class: [put, cp, mv]
        server-side-encryption: [put, sync, cp, modify]
        server-side-encryption-kms-id: [put, sync, cp, modify]
        add-encoding-exts: [css, js, html]
