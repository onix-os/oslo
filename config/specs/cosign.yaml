# Converted from Fig's cosign.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: cosign
flags:
  "--output-file=": "Log output to a file"
  "--timeout, -t=": "Timeout for commands"
  "--verbose, -d": "Log debug output"
  "--help, -h": "Help for cosign"
commands:
  - name: attach
    description: "Provides utilities for attaching artifacts to other artifacts in a registry"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for attach"
    commands:
      - name: attestation
        description: "Attach attestation to the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--attestation=": "Path to the attestation envelope"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for attestation"
      - name: sbom
        description: "Attach sbom to the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--sbom=": "Path to the sbom, or {-} for stdin"
          "--type=": "Type of sbom (spdx|cyclonedx|syft)"
          "--help, -h": "Help for sbom"
      - name: signature
        description: "Attach signatures to the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--payload=": "Path to the payload covered by the signature (if using another format)"
          "--signature=": "The signature, path to the signature, or {-} for stdin"
          "--help, -h": "Help for signature"
  - name: attest
    description: "Attest the supplied container image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--cert=": "Path to the x509 certificate to include in the Signature"
      "--force, -f": "Skip warnings and confirmations"
      "--fulcio-url=": "[EXPERIMENTAL] address of sigstore PKI server"
      "--identity-token=": "[EXPERIMENTAL] identity token to use for certificate from fulcio"
      "--insecure-skip-verify": "[EXPERIMENTAL] skip verifying fulcio published to the SCT (this should only be used for testing)"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the private key file, KMS URI or Kubernetes Secret"
      "--no-upload": "Do not upload the generated attestation"
      "--oidc-client-id=": "[EXPERIMENTAL] OIDC client ID for application"
      "--oidc-client-secret=": "[EXPERIMENTAL] OIDC client secret for application"
      "--oidc-issuer=": "[EXPERIMENTAL] OIDC provider to be used to issue ID token"
      "--predicate=": "Path to the predicate file"
      "--recursive, -r": "If a multi-arch image is specified, additionally sign each discrete image"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--replace": ""
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--type=": "Specify a predicate type (slsaprovenance|link|spdx|vuln|custom) or an URI"
      "--help, -h": "Help for attest"
  - name: clean
    description: "Remove all signatures from an image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--force, -f": "Do not prompt for confirmation"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--type=": "A type of clean: <signature|attestation|sbom|all> (default: all)"
      "--help, -h": "Help for clean"
  - name: completion
    description: "Generate completion script"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for completion"
  - name: copy
    description: "Copy the supplied container image and signatures"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--force, -f": "Overwrite destination image(s), if necessary"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--sig-only": "Only copy the image signature"
      "--help, -h": "Help for copy"
  - name: dockerfile
    description: "Provides utilities for discovering images in and performing operations on Dockerfiles"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for dockerfile"
    commands:
      - name: verify
        description: "Verify a signature on the base image specified in the Dockerfile"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--annotations, -a=": "Extra key=value pairs to sign"
          "--attachment=": "Related image attachment to sign (sbom), default none"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--base-image-only": "Only verify the base image (the last FROM image in the Dockerfile)"
          "--cert=": "Path to the public certificate"
          "--cert-email=": "The email expected in a valid Fulcio certificate"
          "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
          "--check-claims": "Whether to check the claims found"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
          "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
          "--output, -o=": "Output format for the signing image information (json|text)"
          "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
          "--signature=": "Signature content or path or remote URL"
          "--signature-digest-algorithm=": "Digest algorithm to use when processing a signature (sha224|sha256|sha384|sha512)"
          "--sk": "Whether to use a hardware security key"
          "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
          "--help, -h": "Help for verify"
  - name: download
    description: "Provides utilities for downloading artifacts and attached artifacts in a registry"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for download"
    commands:
      - name: attestation
        description: "Download in-toto attestations from the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for attestation"
      - name: sbom
        description: "Download SBOMs from the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for sbom"
      - name: signature
        description: "Download signatures from the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for signature"
  - name: generate
    description: "Generates (unsigned) signature payloads from the supplied container image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--annotations, -a=": "Extra key=value pairs to sign"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--help, -h": "Help for generate"
  - name: generate-key-pair
    description: "Generates a key-pair"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--kms=": "Create key pair in KMS service to use for signing"
      "--help, -h": "Help for generate-key-pair"
  - name: import-key-pair
    description: "Imports a PEM-encoded RSA or EC private key"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--key=": "Import key pair to use for signing"
      "--help, -h": "Help for import-key-pair"
  - name: initialize
    description: "Initializes SigStore root to retrieve trusted certificate and key targets for verification"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--mirror=": "GCS bucket to a SigStore TUF repository or HTTP(S) base URL"
      "--root=": "Path to trusted initial root. defaults to embedded root"
      "--help, -h": "Help for initialize"
  - name: load
    description: "Load a signed image on disk to a remote registry"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--dir=!": "Path to directory where the signed image is stored on disk"
      "--help, -h": "Help for load"
  - name: login
    description: "Log in to a registry"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--password, -p=": Password
      "--password-stdin": "Take the password from stdin"
      "--username, -u=": Username
      "--help, -h": "Help for login"
  - name: manifest
    description: "Provides utilities for discovering images in and performing operations on Kubernetes manifests"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for manifest"
    commands:
      - name: verify
        description: "Verify all signatures of images specified in the manifest"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--annotations, -a=": "Extra key=value pairs to sign"
          "--attachment=": "Related image attachment to sign (sbom), default none"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--cert=": "Path to the public certificate"
          "--cert-email=": "The email expected in a valid Fulcio certificate"
          "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
          "--check-claims": "Whether to check the claims found"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
          "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
          "--output, -o=": "Output format for the signing image information (json|text)"
          "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
          "--signature=": "Signature content or path or remote URL"
          "--signature-digest-algorithm=": "Digest algorithm to use when processing a signature (sha224|sha256|sha384|sha512)"
          "--sk": "Whether to use a hardware security key"
          "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
          "--help, -h": "Help for verify"
  - name: piv-tool
    description: "This cosign was not built with piv-tool support!"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for piv-tool"
  - name: pkcs11-tool
    description: "This cosign was not built with pkcs11-tool support!"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for pkcs11-tool"
  - name: policy
    description: "Subcommand to manage a keyless policy"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for policy"
    commands:
      - name: init
        description: "Generate a new keyless policy"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--expires=": "Total expire duration in days"
          "--issuer=": "Trusted issuer to use for identity tokens, e.g. https://accounts.google.com"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--maintainers, -m=": "List of maintainers to add to the root policy"
          "--namespace=": "Registry namespace that the root policy belongs to"
          "--out=": "Output policy locally"
          "--threshold=": "Threshold for root policy signers"
          "--help, -h": "Help for init"
      - name: sign
        description: "Sign a keyless policy"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--fulcio-url=": "[EXPERIMENTAL] address of sigstore PKI server"
          "--identity-token=": "[EXPERIMENTAL] identity token to use for certificate from fulcio"
          "--insecure-skip-verify": "[EXPERIMENTAL] skip verifying fulcio published to the SCT (this should only be used for testing)"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--namespace=": "Registry namespace that the root policy belongs to"
          "--oidc-client-id=": "[EXPERIMENTAL] OIDC client ID for application"
          "--oidc-client-secret=": "[EXPERIMENTAL] OIDC client secret for application"
          "--oidc-issuer=": "[EXPERIMENTAL] OIDC provider to be used to issue ID token"
          "--out=": "Output policy locally"
          "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
          "--help, -h": "Help for sign"
  - name: public-key
    description: "Gets a public key from the key-pair"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--key=": "Path to the private key file, KMS URI or Kubernetes Secret"
      "--outfile=": "Path to a payload file to use rather than generating one"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--help, -h": "Help for public-key"
  - name: save
    description: "Save the container image and associated signatures to disk at the specified directory"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--dir=!": "Path to dir where the signed image should be stored on disk"
      "--help, -h": "Help for save"
  - name: sign
    description: "Sign the supplied container image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--annotations, -a=": "Extra key=value pairs to sign"
      "--attachment=": "Related image attachment to sign (sbom), default none"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--cert=": "Path to the x509 certificate to include in the Signature"
      "--force, -f": "Skip warnings and confirmations"
      "--fulcio-url=": "[EXPERIMENTAL] address of sigstore PKI server"
      "--identity-token=": "[EXPERIMENTAL] identity token to use for certificate from fulcio"
      "--insecure-skip-verify": "[EXPERIMENTAL] skip verifying fulcio published to the SCT (this should only be used for testing)"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the private key file, KMS URI or Kubernetes Secret"
      "--oidc-client-id=": "[EXPERIMENTAL] OIDC client ID for application"
      "--oidc-client-secret=": "[EXPERIMENTAL] OIDC client secret for application"
      "--oidc-issuer=": "[EXPERIMENTAL] OIDC provider to be used to issue ID token"
      "--output-certificate=": "Write the certificate to FILE"
      "--output-signature=": "Write the signature to FILE"
      "--payload=": "Path to a payload file to use rather than generating one"
      "--recursive, -r": "If a multi-arch image is specified, additionally sign each discrete image"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--upload": "Whether to upload the signature"
      "--help, -h": "Help for sign"
  - name: sign-blob
    description: "Sign the supplied blob, outputting the base64-encoded signature to stdout"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--b64": "Whether to base64 encode the output"
      "--bundle=": "Write everything required to verify the blob to a FILE"
      "--fulcio-url=": "[EXPERIMENTAL] address of sigstore PKI server"
      "--identity-token=": "[EXPERIMENTAL] identity token to use for certificate from fulcio"
      "--insecure-skip-verify": "[EXPERIMENTAL] skip verifying fulcio published to the SCT (this should only be used for testing)"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the private key file, KMS URI or Kubernetes Secret"
      "--oidc-client-id=": "[EXPERIMENTAL] OIDC client ID for application"
      "--oidc-client-secret=": "[EXPERIMENTAL] OIDC client secret for application"
      "--oidc-issuer=": "[EXPERIMENTAL] OIDC provider to be used to issue ID token"
      "--output=": "Write the signature to FILE"
      "--output-certificate=": "Write the certificate to FILE"
      "--output-signature=": "Write the signature to FILE"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--help, -h": "Help for sign-blob"
  - name: tree
    description: "Display supply chain security related artifacts for an image such as signatures, SBOMs and attestations"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--help, -h": "Help for tree"
  - name: triangulate
    description: "Outputs the located cosign image reference. This is the location cosign stores the specified artifact type"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--type=": "Related attachment to triangulate (attestation|sbom|signature), default signature"
      "--help, -h": "Help for triangulate"
  - name: upload
    description: "Provides utilities for uploading artifacts to a registry"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for upload"
    commands:
      - name: blob
        description: "Upload one or more blobs to the supplied container image address"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--ct=": "Content type to set"
          "--files, -f=": "<filepath>:[platform/arch]"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for blob"
      - name: wasm
        description: "Upload a wasm module to the supplied container image reference"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
          "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
          "--file, -f=!": "Path to the wasm file to upload"
          "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
          "--help, -h": "Help for wasm"
  - name: verify
    description: "Verify a signature on the supplied container image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--annotations, -a=": "Extra key=value pairs to sign"
      "--attachment=": "Related image attachment to sign (sbom), default none"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--cert=": "Path to the public certificate"
      "--cert-email=": "The email expected in a valid Fulcio certificate"
      "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
      "--check-claims": "Whether to check the claims found"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
      "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
      "--output, -o=": "Output format for the signing image information (json|text)"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--signature=": "Signature content or path or remote URL"
      "--signature-digest-algorithm=": "Digest algorithm to use when processing a signature (sha224|sha256|sha384|sha512)"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--help, -h": "Help for verify"
  - name: verify-attestation
    description: "Verify an attestation on the supplied container image"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--cert=": "Path to the public certificate"
      "--cert-email=": "The email expected in a valid Fulcio certificate"
      "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
      "--check-claims": "Whether to check the claims found"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
      "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
      "--output, -o=": "Output format for the signing image information (json|text)"
      "--policy=": "Specify CUE or Rego files will be using for validation"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--type=": "Specify a predicate type (slsaprovenance|link|spdx|vuln|custom) or an URI"
      "--help, -h": "Help for verify-attestation"
  - name: verify-blob
    description: "Verify a signature on the supplied blob"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
      "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
      "--bundle=": "Path to bundle FILE"
      "--cert=": "Path to the public certificate"
      "--cert-email=": "The email expected in a valid Fulcio certificate"
      "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
      "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
      "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
      "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
      "--signature=": "Signature content or path or remote URL"
      "--sk": "Whether to use a hardware security key"
      "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
      "--help, -h": "Help for verify-blob"
  - name: version
    description: "Prints the version"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--json": "Print JSON instead of text"
      "--help, -h": "Help for version"
  - name: help
    description: "Help about any command"
    flags:
      "--output-file=": "Log output to a file"
      "--timeout, -t=": "Timeout for commands"
      "--verbose, -d": "Log debug output"
      "--help, -h": "Help for help"
    commands:
      - name: attach
        description: "Provides utilities for attaching artifacts to other artifacts in a registry"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: attestation
            description: "Attach attestation to the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--attestation=": "Path to the attestation envelope"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for attestation"
          - name: sbom
            description: "Attach sbom to the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--sbom=": "Path to the sbom, or {-} for stdin"
              "--type=": "Type of sbom (spdx|cyclonedx|syft)"
              "--help, -h": "Help for sbom"
          - name: signature
            description: "Attach signatures to the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--payload=": "Path to the payload covered by the signature (if using another format)"
              "--signature=": "The signature, path to the signature, or {-} for stdin"
              "--help, -h": "Help for signature"
      - name: attest
        description: "Attest the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: clean
        description: "Remove all signatures from an image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: completion
        description: "Generate completion script"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: copy
        description: "Copy the supplied container image and signatures"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: dockerfile
        description: "Provides utilities for discovering images in and performing operations on Dockerfiles"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: verify
            description: "Verify a signature on the base image specified in the Dockerfile"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--annotations, -a=": "Extra key=value pairs to sign"
              "--attachment=": "Related image attachment to sign (sbom), default none"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--base-image-only": "Only verify the base image (the last FROM image in the Dockerfile)"
              "--cert=": "Path to the public certificate"
              "--cert-email=": "The email expected in a valid Fulcio certificate"
              "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
              "--check-claims": "Whether to check the claims found"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
              "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
              "--output, -o=": "Output format for the signing image information (json|text)"
              "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
              "--signature=": "Signature content or path or remote URL"
              "--signature-digest-algorithm=": "Digest algorithm to use when processing a signature (sha224|sha256|sha384|sha512)"
              "--sk": "Whether to use a hardware security key"
              "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
              "--help, -h": "Help for verify"
      - name: download
        description: "Provides utilities for downloading artifacts and attached artifacts in a registry"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: attestation
            description: "Download in-toto attestations from the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for attestation"
          - name: sbom
            description: "Download SBOMs from the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for sbom"
          - name: signature
            description: "Download signatures from the supplied container image"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for signature"
      - name: generate
        description: "Generates (unsigned) signature payloads from the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: generate-key-pair
        description: "Generates a key-pair"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: import-key-pair
        description: "Imports a PEM-encoded RSA or EC private key"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: initialize
        description: "Initializes SigStore root to retrieve trusted certificate and key targets for verification"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: load
        description: "Load a signed image on disk to a remote registry"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: login
        description: "Log in to a registry"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: manifest
        description: "Provides utilities for discovering images in and performing operations on Kubernetes manifests"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: verify
            description: "Verify all signatures of images specified in the manifest"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--annotations, -a=": "Extra key=value pairs to sign"
              "--attachment=": "Related image attachment to sign (sbom), default none"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--cert=": "Path to the public certificate"
              "--cert-email=": "The email expected in a valid Fulcio certificate"
              "--cert-oidc-issuer=": "The OIDC issuer expected in a valid Fulcio certificate, e.g. https://token.actions.githubusercontent.com or https://oauth2.sigstore.dev/auth"
              "--check-claims": "Whether to check the claims found"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--key=": "Path to the public key file, KMS URI or Kubernetes Secret"
              "--local-image": "Whether the specified image is a path to an image saved locally via 'cosign save'"
              "--output, -o=": "Output format for the signing image information (json|text)"
              "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
              "--signature=": "Signature content or path or remote URL"
              "--signature-digest-algorithm=": "Digest algorithm to use when processing a signature (sha224|sha256|sha384|sha512)"
              "--sk": "Whether to use a hardware security key"
              "--slot=": "Security key slot to use for generated key (default: signature) (authentication|signature|card-authentication|key-management)"
              "--help, -h": "Help for verify"
      - name: piv-tool
        description: "This cosign was not built with piv-tool support!"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: pkcs11-tool
        description: "This cosign was not built with pkcs11-tool support!"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: policy
        description: "Subcommand to manage a keyless policy"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: init
            description: "Generate a new keyless policy"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--expires=": "Total expire duration in days"
              "--issuer=": "Trusted issuer to use for identity tokens, e.g. https://accounts.google.com"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--maintainers, -m=": "List of maintainers to add to the root policy"
              "--namespace=": "Registry namespace that the root policy belongs to"
              "--out=": "Output policy locally"
              "--threshold=": "Threshold for root policy signers"
              "--help, -h": "Help for init"
          - name: sign
            description: "Sign a keyless policy"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--fulcio-url=": "[EXPERIMENTAL] address of sigstore PKI server"
              "--identity-token=": "[EXPERIMENTAL] identity token to use for certificate from fulcio"
              "--insecure-skip-verify": "[EXPERIMENTAL] skip verifying fulcio published to the SCT (this should only be used for testing)"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--namespace=": "Registry namespace that the root policy belongs to"
              "--oidc-client-id=": "[EXPERIMENTAL] OIDC client ID for application"
              "--oidc-client-secret=": "[EXPERIMENTAL] OIDC client secret for application"
              "--oidc-issuer=": "[EXPERIMENTAL] OIDC provider to be used to issue ID token"
              "--out=": "Output policy locally"
              "--rekor-url=": "[EXPERIMENTAL] address of rekor STL server"
              "--help, -h": "Help for sign"
      - name: public-key
        description: "Gets a public key from the key-pair"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: save
        description: "Save the container image and associated signatures to disk at the specified directory"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: sign
        description: "Sign the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: sign-blob
        description: "Sign the supplied blob, outputting the base64-encoded signature to stdout"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: tree
        description: "Display supply chain security related artifacts for an image such as signatures, SBOMs and attestations"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: triangulate
        description: "Outputs the located cosign image reference. This is the location cosign stores the specified artifact type"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: upload
        description: "Provides utilities for uploading artifacts to a registry"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
        commands:
          - name: blob
            description: "Upload one or more blobs to the supplied container image address"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--ct=": "Content type to set"
              "--files, -f=": "<filepath>:[platform/arch]"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for blob"
          - name: wasm
            description: "Upload a wasm module to the supplied container image reference"
            flags:
              "--output-file=": "Log output to a file"
              "--timeout, -t=": "Timeout for commands"
              "--verbose, -d": "Log debug output"
              "--allow-insecure-registry": "Whether to allow insecure connections to registries. Don't use this for anything but testing"
              "--attachment-tag-prefix=": "Optional custom prefix to use for attached image tags. Attachment images are tagged as: `[AttachmentTagPrefix]sha256-[TargetImageDigest].[AttachmentName]`"
              "--file, -f=!": "Path to the wasm file to upload"
              "--k8s-keychain": "Whether to use the kubernetes keychain instead of the default keychain (supports workload identity)"
              "--help, -h": "Help for wasm"
      - name: verify
        description: "Verify a signature on the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: verify-attestation
        description: "Verify an attestation on the supplied container image"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: verify-blob
        description: "Verify a signature on the supplied blob"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
      - name: version
        description: "Prints the version"
        flags:
          "--output-file=": "Log output to a file"
          "--timeout, -t=": "Timeout for commands"
          "--verbose, -d": "Log debug output"
          "--help, -h": "Help for version"
