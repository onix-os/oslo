# Converted from argc's yj.sh by tools/argc-to-spec. Do not edit by hand.
name: yj
flags:
  "-y, -yj": "YAML to JSON (default)"
  "-yy": "YAML to YAML"
  "-yt": "YAML to TOML"
  "-yc": "YAML to HCL"
  "-t, -tj": "TOML to JSON"
  "-ty": "TOML to YAML"
  "-tt": "TOML to TOML"
  "-tc": "TOML to HCL"
  "-jj": "JSON to JSON"
  "-r, -jy": "JSON to YAML"
  "-jt": "JSON to TOML"
  "-jc": "JSON to HCL"
  "-cy": "HCL to YAML"
  "-ct": "HCL to TOML"
  "-c, -cj": "HCL to JSON"
  "-cc": "HCL to HCL"
  "-n": "Do not covert inf, -inf, and NaN to/from strings (YAML or TOML only)"
  "-e": "Escape HTML (JSON out only)"
  "-i": "Indent output (JSON or TOML out only)"
  "-k": "Attempt to parse keys as objects or numeric types (YAML out only)"
  "-h": "Show this help message"
  "-v": "Show version"
  "-help": "Print help"
  "-V, -version": "Print version"
