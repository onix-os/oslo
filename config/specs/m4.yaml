# Converted from argc's m4.sh by tools/argc-to-spec. Do not edit by hand.
name: m4
flags:
  "--help": "display this help and exit"
  "--version": "output version information and exit"
  "-E, --fatal-warnings": "once: warnings become errors, twice: stop execution at first error"
  "-i, --interactive": "unbuffer output, ignore interrupts"
  "-P, --prefix-builtins": "force a `m4_' prefix to all builtins"
  "-Q": "suppress some warnings for builtins"
  "--quiet": "suppress some warnings for builtins"
  "--silent": "suppress some warnings for builtins"
  "--warn-macro-sequence=": "warn if macro definition matches REGEXP, default \\$\\({[^}]*}\\|[0-9][0-9]+\\)"
  "-D, --define=": "define NAME as having VALUE, or empty"
  "-I, --include=": "append DIRECTORY to include path"
  "-s, --synclines": "generate `#line NUM \"FILE\"' lines"
  "-U, --undefine=": "undefine NAME"
  "-g, --gnu": "override -G to re-enable GNU extensions"
  "-G, --traditional": "suppress all GNU extensions"
  "-H, --hashsize=": "set symbol lookup hash table size [509]"
  "-L, --nesting-limit=": "change nesting limit, 0 for unlimited [0]"
  "-F, --freeze-state=": "produce a frozen state on FILE at end"
  "-R, --reload-state=": "reload a frozen state from FILE at start"
  "-d, --debug=": "set debug level (no FLAGS implies `aeq')"
  "--debugfile=": "redirect debug and trace output to FILE (default stderr, discard if empty string)"
  "-l, --arglength=": "restrict macro tracing size"
  "-t, --trace=": "trace NAME when it is defined"
