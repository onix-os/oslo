# Converted from argc's nix-env.sh by tools/argc-to-spec. Do not edit by hand.
name: nix-env
flags:
  "-h, --help": ""
  "-V, --version": ""
commands:
  - name: "--install"
    aliases: ["-i"]
    description: "add packages to user environment"
    flags:
      "-b, --prebuilt-only": "Use only derivations for which a substitute is registered, i.e., there is a pre-built binary available that can be downloaded in lieu of building the derivation."
      "-P, --preserve-installed": "Do not remove derivations with a name matching one of the derivations being installed."
      "-r, --remove-all": "Remove all previously installed packages first."
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--upgrade"
    aliases: ["-u"]
    description: "upgrade packages in user environment"
    flags:
      "--lt": "Only upgrade a derivation to newer versions."
      "--leq": "In addition to upgrading to newer versions, also “upgrade” to derivations that have the same version."
      "--eq": "Only “upgrade” to derivations that have the same version."
      "--always": "In addition to upgrading to newer versions, also “upgrade” to derivations that have the same or a lower version."
      "-b, --prebuilt-only": "Use only derivations for which a substitute is registered, i.e., there is a pre-built binary available that can be downloaded in lieu of building the derivation."
      "-P, --preserve-installed": "Do not remove derivations with a name matching one of the derivations being installed."
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--uninstall"
    description: "remove packages from user environment"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--set"
    description: "set profile to contain a specified derivation"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--set-flag"
    description: "modify meta attributes of installed packages"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--query"
    aliases: ["-q"]
    description: "display information about packages"
    flags:
      "--installed": "The query operates on the store paths that are installed in the current generation of the active profile."
      "-a, --available": "The query operates on the derivations that are available in the active Nix expression."
      "--xml": "Print the result in an XML representation suitable for automatic processing by other tools."
      "--json": "Print the result in a JSON representation suitable for automatic processing by other tools."
      "-b, --prebuilt-only": "Show only derivations for which a substitute is registered, i.e., there is a pre-built binary available that can be downloaded in lieu of building the derivation."
      "-s, --status": "Print the status of the derivation."
      "-P, --attr-path": "Print the attribute path of the derivation, which can be used to unambiguously select it using the --attr option available in commands that install derivations like nix-env --install."
      "--no-name": "Suppress printing of the name attribute of each derivation."
      "-c, --compare-versions": "Compare installed versions to available versions, or vice versa (if --available is given)."
      "--system": "Print the system attribute of the derivation."
      "--drv-path": "Print the path of the store derivation."
      "--out-path": "Print the output path of the derivation."
      "--description": "Print a short (one-line) description of the derivation, if available."
      "--meta": "Print all of the meta-attributes of the derivation."
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--switch-profile"
    aliases: ["-S"]
    description: "set user environment to given profile"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--list-generations"
    description: "list profile generations"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--delete-generations"
    description: "delete profile generations"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--switch-generation"
    description: "set user environment to given profile generation"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
  - name: "--rollback"
    description: "set user environment to previous generation"
    flags:
      "-f, --file=": "Specifies the Nix expression (designated below as the active Nix expression) used by the --install, --upgrade, and --query --available operations to obtain derivations."
      "-p, --profile=": "Specifies the profile to be used by those operations that operate on a profile (designated below as the active profile)."
      "--dry-run": "For the --install, --upgrade, --uninstall, --switch-generation, --delete-generations and --rollback operations, this flag will cause nix-env to print what would be done if this flag had not been specified, without actually doing it."
      "--system-filter=": "By default, operations such as --query --available show derivations matching any platform."
      "--help": "Prints out a summary of the command syntax and exits."
      "--version": "Prints out the Nix version number on standard output and exits."
      "-v, --verbose=": "Increases the level of verbosity of diagnostic messages printed on standard error."
      "--quiet": "Decreases the level of verbosity of diagnostic messages printed on standard error."
      "--log-format=": "This option can be used to change the output of the log format, with format being one of:"
      "-Q, --no-build-output": "By default, output written by builders to standard output and standard error is echoed to the Nix command’s standard error."
      "-j, --max-jobs=": "Sets the maximum number of build jobs that Nix will perform in parallel to the specified number."
      "--cores": "Sets the value of the NIX_BUILD_CORES environment variable in the invocation of builders."
      "--max-silent-time": "Sets the maximum number of seconds that a builder can go without producing any data on standard output or standard error."
      "--timeout": "Sets the maximum number of seconds that a builder can run."
      "-k, --keep-going": "Keep going in case of failed builds, to the greatest extent possible."
      "-K, --keep-failed": "Specifies that in case of a build failure, the temporary directory (usually in /tmp) in which the build takes place should not be deleted."
      "--fallback": "Whenever Nix attempts to build a derivation for which substitutes are known for each output path, but realising the output paths through the substitutes fails, fall back on building the derivation."
      "--readonly-mode": "When this option is used, no attempt is made to open the Nix database."
      "--arg=*": {description: "This option is accepted by nix-env, nix-instantiate, nix-shell and nix-build.", nargs: -1}
      "--argstr=*": {description: "This option is like --arg, only the value is not a Nix expression but a string.", nargs: -1}
      "-A, --attr=*": {description: "Select an attribute from the top-level Nix expression being evaluated.", nargs: -1}
      "-E, --expr": "Interpret the command line arguments as a list of Nix expressions to be parsed and evaluated, rather than as a list of file names of Nix expressions."
      "-I, --include=": "Add an entry to the list of search paths used to resolve lookup paths."
      "--option=": {description: "Set the Nix configuration option name to value.", nargs: -1}
      "--repair": "Fix corrupted or missing store paths by redownloading or rebuilding them."
