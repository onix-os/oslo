# Converted from Fig's head.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: head
description: "Output the first part of files"
flags:
  "-c, --bytes=": "Print the first [numBytes] bytes of each file"
  "-n, --lines=": "Print the first [numLines] lines instead of the first 10"
  "-q, --quiet, --silent": "Never print headers giving file names"
  "-v, --verbose": "Always print headers giving file names"
  "--help": "Display this help and exit"
  "--version": "Output version information and exit"
completion:
  positional:
    - ["$files"]
