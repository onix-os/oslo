# Converted from argc's ngrep.sh by tools/argc-to-spec. Do not edit by hand.
name: ngrep
flags:
  "-h": "Display help/usage information."
  "-N": "Show sub-protocol number along with single-character identifier (useful when observing raw or unknown protocols)."
  "-X": "Treat the match expression as a hexadecimal string."
  "-V": "Display version information."
  "-i": "Ignore case for the regex expression."
  "-w": "Match the regex expression as a word."
  "-q": "Be quiet; don't output any information other than packet headers and their payloads (if relevant)."
  "-p": "Don't put the interface into promiscuous mode."
  "-e": "Show empty packets."
  "-v": "Invert the match; only display packets that don't match."
  "-x": "Dump packet contents as hexadecimal as well as ASCII."
  "-l": "Make stdout line buffered."
  "-D": "When reading pcap_dump files, replay them at their recorded time intervals (mimic realtime)."
  "-t": "Print a timestamp in the form of YYYY/MM/DD HH:MM:SS.UUUUUU everytime a packet is matched."
  "-T": "Print a timestamp in the form of +S.UUUUUU, indicating the delta between packet matches."
  "-R": "Do not try to drop privileges to the DROPPRIVS_USER."
  "-c": "cols Explicitly set the console width to ``cols''."
  "-F": "file Read in the bpf filter from the specified filename."
  "-P": "char Specify an alternate character to signify non-printable characters when displayed."
  "-K=": "Kill matching TCP connections (like tcpkill)."
  "-W": "normal|byline|single|none Specify an alternate manner for displaying packets, when not in hexadecimal mode."
  "-s": "snaplen Set the bpf caplen to snaplen (default 65536)."
  "-S": "limitlen Set the upper limit on the size of packets that ngrep will look at."
  "-I": "pcap_dump Input file pcap_dump into ngrep."
  "-O": "pcap_dump Output matched packets to a pcap-compatible dump file."
  "-n=": "Match only num packets total, then exit."
  "-d=": "By default ngrep will select a default interface to listen on."
  "-A=": "Dump num packets of trailing context after matching a packet."
  "-help": "Print help"
  "-version": "Print version"
