# Converted from Fig's django-admin.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: django-admin
description: "Utility script for the Django Web framework"
commands:
  - name: help
    description: "Usage and help information for django-admin"
    completion:
      positional:
        - ["changepassword\tChange a user's password for django.contrib.auth", "createsuperuser\tUsed to create a superuser", remove_stale_contenttypes, "check\tChecks the entire Django project for potential problems", "compilemessages\tCompiles .po files to .mo files for use with builtin gettext support", "createcachetable\tCreates the tables needed to use the SQL cache backend", "dbshell\tRuns the command-line client for specified database, or the default database", "diffsettings\tDisplays differences between the current settings.py and Django's default settings", "dumpdata\tOutput the contents of the database as a fixture of the given format (using each model's default manager unless --all is specified)", "flush\tRemoves ALL DATA from the database, including data added during migrations", "inspectdb\tIntrospects the database tables in the given database and outputs a Django", "loaddata\tInstalls the named fixture(s) in the database", "makemessages\tRuns over the entire source tree of the current directory and pulls out all strings marked for translation. It creates (or updates) a message file in the conf/locale (in the django tree) or locale (for projects and applications) directory. You must run this command with one of either the --locale, --exclude, or --all options", "makemigrations\tCreates new migration(s) for apps", "migrate\tUpdates database schema. Manages both apps with migrations and those without", "sendtestemail\tSends a test email to the email addresses specified as arguments", "shell\tRuns a Python interactive interpreter. Tries to use IPython or bpython, if one of them is available. Any standard input is executed as code", "showmigrations\tShows all available migrations for the current project", "sqlflush\tReturns a list of the SQL statements required to return all tables in the database to the state they were in just after they were installed", "sqlmigrate\tPrints the SQL statements for the named migration", "sqlsequencereset\tPrints the SQL statements for resetting sequences for the given app name(s)", "squashmigrations\tSquashes an existing set of migrations (from first until specified) into a single new one", "startapp\tCreates a Django app directory structure for the given app name in the current directory or optionally in the given directory", "startproject\tCreates a Django project directory structure for the given project name in the current directory or optionally in the given directory", "test\tDiscover and run tests in the specified modules or the current directory", "testserver\tRuns a development server with data from the given fixture(s)", test_mail, "clearsessions\tCan be run as a cronjob or directly to clean out expired sessions (only with the database backend at the moment)", "collectstatic\tCollect static files in a single location", "findstatic\tFinds the absolute paths for the given static file(s)", "runserver\tStarts a lightweight Web server for development and also serves static files"]
  - name: changepassword
    description: "Change a user's password for django.contrib.auth"
    flags:
      "--database=": "Specifies the database to use. Default is \"default\""
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: createsuperuser
    description: "Used to create a superuser"
    flags:
      "--username=": "Specifies the login for the superuser"
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--database=": "Specifies the database to use. Default is \"default\""
      "--email=": "Specifies the email for the superuser"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: remove_stale_contenttypes
    flags:
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--database=": "Nominates the database to use. Defaults to the \"default\" database"
      "--include-stale-apps": "Deletes stale content types including ones from previously installed apps that have been removed from INSTALLED_APPS"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: check
    description: "Checks the entire Django project for potential problems"
    flags:
      "--tag, -t=": "Run only checks labeled with given tag"
      "--list-tags": "List available tags"
      "--deploy": "Check deployment settings"
      "--fail-level=": "Message level that will cause the command to exit with a non-zero status. Default is ERROR"
      "--database=": "Run database related checks against these aliases"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        fail-level: [CRITICAL, ERROR, WARNING, INFO, DEBUG]
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: compilemessages
    description: "Compiles .po files to .mo files for use with builtin gettext support"
    flags:
      "--locale, -l=": "Locale(s) to process (e.g. de_AT). Default is to process all. Can be used multiple times"
      "--exclude, -x=": "Locales to exclude. Default is none. Can be used multiple times"
      "--use-fuzzy, -f": "Use fuzzy translations"
      "--ignore, -i=": "Ignore directories matching this glob-style pattern. Use multiple times to ignore more"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: createcachetable
    description: "Creates the tables needed to use the SQL cache backend"
    flags:
      "--database=": "Nominates a database onto which the cache tables will be installed. Defaults to the \"default\" database"
      "--dry-run": "Does not create the table, just prints the SQL that would be run"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: dbshell
    description: "Runs the command-line client for specified database, or the default database"
    flags:
      "--database=": "Nominates a database onto which to open a shell"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: diffsettings
    description: "Displays differences between the current settings.py and Django's default settings"
    flags:
      "--all": "Display all settings, regardless of their value. In \"hash\" mode, default values are prefixed by \"###\""
      "--default=": "The settings module to compare the current settings against. Leave empty to compare against Django's default settings"
      "--output=": "Selects the output format. 'hash' mode displays each changed setting, with the settings that don't appear in the defaults followed by ###. 'unified' mode prefixes the default setting with a minus sign, followed by the changed setting prefixed with a plus sign"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        output: [hash, unified]
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: dumpdata
    description: "Output the contents of the database as a fixture of the given format (using each model's default manager unless --all is specified)"
    flags:
      "--format=": "Specifies the output serialization format for fixtures"
      "--indent=": "Specifies the indent level to use when pretty-printing output"
      "--database=": "Nominates a specific database to dump fixtures from. Defaults to the \"default\" database"
      "-e, --exclude=": "An app_label or app_label.ModelName to exclude (use multiple --exclude to exclude multiple apps/models)"
      "--natural-foreign": "Use natural foreign keys if they are available"
      "--natural-primary": "Use natural primary keys if they are available"
      "-a, --all": "Use Django's base manager to dump all models stored in the database, including those that would otherwise be filtered or modified by a custom manager"
      "--pks=": "Only dump objects with given primary keys. Accepts a comma-separated list of keys. This option only works when you specify one model"
      "-o, --output=": "Specifies file to which the output is written"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: flush
    description: "Removes ALL DATA from the database, including data added during migrations"
    flags:
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--database=": "Nominates a database to flush. Defaults to the \"default\" database"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: inspectdb
    description: "Introspects the database tables in the given database and outputs a Django"
    flags:
      "--database=": "Nominates a database to introspect. Defaults to using the \"default\" database"
      "--include-partitions": "Also output models for partition tables"
      "--include-views": "Also output models for database views"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: loaddata
    description: "Installs the named fixture(s) in the database"
    flags:
      "--database=": "Nominates a specific database to load fixtures into. Defaults to the \"default\" database"
      "--app=": "Only look for fixtures in the specified app"
      "--ignorenonexistent, -i": "Ignores entries in the serialized data for fields that do not currently exist on the model"
      "-e, --exclude=": "An app_label or app_label.ModelName to exclude. Can be used multiple times"
      "--format=": "Format of serialized data when reading from stdin"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: makemessages
    description: "Runs over the entire source tree of the current directory and pulls out all strings marked for translation. It creates (or updates) a message file in the conf/locale (in the django tree) or locale (for projects and applications) directory. You must run this command with one of either the --locale, --exclude, or --all options"
    flags:
      "--locale, -l=": "Creates or updates the message files for the given locale(s) (e.g. pt_BR). Can be used multiple times"
      "--exclude, -x=": "Locales to exclude. Default is none. Can be used multiple times"
      "--domain, -d=": "The domain of the message files (default: \"django\")"
      "--all, -a": "Updates the message files for all existing locales"
      "--extension, -e=": "The file extension(s) to examine (default: \"html,txt,py\", or \"js\" if the domain is \"djangojs\"). Separate multiple extensions with commas, or use -e multiple times"
      "--symlinks, -s": "Follows symlinks to directories when examining source code and templates for translation strings"
      "--ignore, -i=": "Ignore files or directories matching this glob-style pattern. Use multiple times to ignore more"
      "--no-default-ignore": "Don't ignore the common glob-style patterns 'CVS', '.*', '*~' and '*.pyc'"
      "--no-wrap": "Don't break long message lines into several lines"
      "--no-location": "Don't write '#: filename:line' lines"
      "--add-location?": "[{full,file,never}] Controls '#: filename:line' lines. If the option is 'full' (the default if not given), the lines include both file name and line number. If it's 'file', the line number is omitted. If it's 'never', the lines are suppressed (same as --no-location). --add-location requires gettext 0.19 or newer"
      "--no-obsolete": "Remove obsolete message strings"
      "--keep-pot": "Keep .pot file after making messages. Useful when debugging"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        add-location: ["full\tThe lines include both file name and line number", "file\tThe line number is omitted", "never\tThe lines are suppressed (same as --no-location)"]
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: makemigrations
    description: "Creates new migration(s) for apps"
    flags:
      "--dry-run": "Just show what migrations would be made; don't actually write them"
      "--merge": "Enable fixing of migration conflicts"
      "--empty": "Create an empty migration"
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "-n, --name=": "Use this name for migration file(s)"
      "--no-header": "Do not add header comments to new migration file(s)"
      "--check": "Exit with a non-zero status if model changes are missing migrations"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: migrate
    description: "Updates database schema. Manages both apps with migrations and those without"
    flags:
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--database=": "Nominates a database to synchronize. Defaults to the \"default\" database"
      "--fake": "Mark migrations as run without actually running them"
      "--fake-initial": "Detect if tables already exist and fake-apply initial migrations if so. Make sure that the current database schema matches your initial migration before using this flag. Django will only check for an existing table name"
      "--plan": "Shows a list of the migration actions that will be performed"
      "--run-syncdb": "Creates tables for apps without migrations"
      "--check": "Exits with a non-zero status if unapplied migrations exist"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: sendtestemail
    description: "Sends a test email to the email addresses specified as arguments"
    flags:
      "--managers": "Send a test email to the addresses specified in settings.MANAGERS"
      "--admins": "Send a test email to the addresses specified in settings.ADMINS"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: shell
    description: "Runs a Python interactive interpreter. Tries to use IPython or bpython, if one of them is available. Any standard input is executed as code"
    flags:
      "--no-startup": "When using plain Python, ignore the PYTHONSTARTUP environment variable and ~/.pythonrc.py script"
      "-i, --interface=": "Specify an interactive interpreter interface. Available options: \"ipython\", \"bpython\", and \"python\""
      "-c, --command=": "Instead of opening an interactive shell, run a command as Django and exit"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        interface: [ipython, bpython, python]
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: showmigrations
    description: "Shows all available migrations for the current project"
    flags:
      "--database=": "Nominates a database to synchronize. Defaults to the \"default\" database"
      "--list, -l": "Shows a list of all migrations and which are applied. With a verbosity level of 2 or above, the applied datetimes will be included"
      "--plan, -p": "Shows all migrations in the order they will be applied. With a verbosity level of 2 or above all direct migration dependencies and reverse dependencies (run_before) will be included"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: sqlflush
    description: "Returns a list of the SQL statements required to return all tables in the database to the state they were in just after they were installed"
    flags:
      "--database=": "Nominates a database to print the SQL for. Defaults to the \"default\" database"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: sqlmigrate
    description: "Prints the SQL statements for the named migration"
    flags:
      "--database=": "Nominates a database to create SQL for. Defaults to the \"default\" database"
      "--backwards": "Creates SQL to unapply the migration, rather than to apply it"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: sqlsequencereset
    description: "Prints the SQL statements for resetting sequences for the given app name(s)"
    flags:
      "--database=": "Nominates a database to print the SQL for. Defaults to the \"default\" database"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: squashmigrations
    description: "Squashes an existing set of migrations (from first until specified) into a single new one"
    flags:
      "--no-optimize": "Do not try to optimize the squashed operations"
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--squashed-name=": "Sets the name of the new squashed migration"
      "--no-header": "Do not add a header comment to the new squashed migration"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: startapp
    description: "Creates a Django app directory structure for the given app name in the current directory or optionally in the given directory"
    flags:
      "--template=": "The path or URL to load the template from"
      "--extension, -e=": "The file extension(s) to render (default: \"py\"). Separate multiple extensions with commas, or use -e multiple times"
      "--name, -n=": "The file name(s) to render. Separate multiple file names with commas, or use -n multiple times"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: startproject
    description: "Creates a Django project directory structure for the given project name in the current directory or optionally in the given directory"
    flags:
      "--template=": "The path or URL to load the template from"
      "--extension, -e=": "The file extension(s) to render (default: \"py\"). Separate multiple extensions with commas, or use -e multiple times"
      "--name, -n=": "The file name(s) to render. Separate multiple file names with commas, or use -n multiple times"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: test
    description: "Discover and run tests in the specified modules or the current directory"
    flags:
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--failfast": "Tells Django to stop running the test suite after first failed test"
      "--testrunner=": "Tells Django to use specified test runner class instead of the one specified by the TEST_RUNNER setting"
      "-t, --top-level-directory=": "Top level of project for unittest discovery"
      "-p, --pattern=": "The test matching pattern. Defaults to test*.py"
      "--keepdb": "Preserves the test DB between runs"
      "-r, --reverse": "Reverses test cases order"
      "--debug-mode": "Sets settings.DEBUG to True"
      "-d, --debug-sql": "Prints logged SQL queries on failure"
      "--parallel=": "Run tests using up to N parallel processes"
      "--tag=": "Run only tests with the specified tag. Can be used multiple times"
      "--exclude-tag=": "Do not run tests with the specified tag. Can be used multiple times"
      "--pdb": "Runs a debugger (pdb, or ipdb if installed) on error or failure"
      "-b, --buffer": "Discard output from passing tests"
      "-k=": "Only run test methods and classes that match the pattern or substring. Can be used multiple times. Same as unittest -k option"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        top-level-directory: ["$directories"]
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: testserver
    description: "Runs a development server with data from the given fixture(s)"
    flags:
      "--noinput, --no-input": "Tells Django to NOT prompt the user for input of any kind"
      "--addrport=": "Port number or ipaddr:port to run the server on"
      "--ipv6, -6": "Tells Django to use an IPv6 address"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: test_mail
    flags:
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: clearsessions
    description: "Can be run as a cronjob or directly to clean out expired sessions (only with the database backend at the moment)"
    flags:
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: collectstatic
    description: "Collect static files in a single location"
    flags:
      "--noinput, --no-input": "Do NOT prompt the user for input of any kind"
      "--no-post-process": "Do NOT post process collected files"
      "-i, --ignore=": "Ignore files or directories matching this glob-style pattern. Use multiple times to ignore more"
      "-n, --dry-run": "Do everything except modify the filesystem"
      "-c, --clear": "Clear the existing files using the storage before trying to copy or link the original file"
      "-l, --link": "Create a symbolic link to each file instead of copying"
      "--no-default-ignore": "Don't ignore the common private glob-style patterns (defaults to 'CVS', '.*' and '*~')"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: findstatic
    description: "Finds the absolute paths for the given static file(s)"
    flags:
      "--first": "Only return the first match for each static file"
      "--skip-checks": "Skip system checks"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
  - name: runserver
    description: "Starts a lightweight Web server for development and also serves static files"
    flags:
      "--ipv6, -6": "Tells Django to use an IPv6 address"
      "--nothreading": "Tells Django to NOT use threading"
      "--noreload": "Tells Django to NOT use the auto-reloader"
      "--nostatic": "Tells Django to NOT automatically serve static files at STATIC_URL"
      "--insecure": "Allows serving static files even if DEBUG is False"
      "-h, --help": "Show this help message and exit"
      "--version": "Show program's version number and exit"
      "-v, --verbosity=": "Verbosity level; 0=minimal output, 1=normal output, 2=verbose output, 3=very verbose output"
      "--settings=": "The Python path to a settings module, e.g. \"myproject.settings.main\". If this isn't provided, the DJANGO_SETTINGS_MODULE environment variable will be used"
      "--pythonpath=": "A directory to add to the Python path, e.g. \"/home/djangoprojects/myproject\""
      "--traceback": "Raise on CommandError exceptions"
      "--no-color": "Don't colorize the command output"
      "--force-color": "Force colorization of the command output"
    completion:
      flag:
        verbosity: [0, 1, 2, 3]
        pythonpath: ["$directories"]
