# Converted from Fig's doggo.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: doggo
description: "DNS Client for Humans"
flags:
  "--version": "Output version"
  "--help, -h": "Output help"
  "-q, --query": "Hostname to query the DNS records for (e.g., example.com)"
  "-t, --type": "Type of the DNS Record (A, MX, NS, etc.)"
  "-n, --nameserver": "Address of a specific nameserver to send queries to (e.g., 9.9.9.9, 8.8.8.8)"
  "-c, --class": "Network class of the DNS record (IN, CH, HS, etc.)"
  "-x, --reverse": "Performs a reverse DNS lookup for an IPv4 or IPv6 address"
  "--any": "Query all supported DNS record types (A, AAAA, CNAME, MX, NS, PTR, SOA, SRV, TXT, CAA)"
  "--strategy": "Specify strategy to query nameservers (all, random, first)"
  "--ndots": "Specify ndots parameter"
  "--search": "Use the search list defined in resolv.conf (default: true)"
  "--timeout": "Specify timeout (in seconds) for the resolver to return a response"
  "-4, --ipv4": "Use IPv4 only"
  "-6, --ipv6": "Use IPv4 only"
  "--tls-hostname": "Provide a hostname for TLS certificate verification"
  "--skip-hostname-verification": "Skip TLS Hostname Verification for DoT lookups"
  "--aa": "Set Authoritative Answer flag"
  "--ad": "Set Authenticated Data flag"
  "--cd": "Set Checking Disabled flag"
  "--rd": "Set Recursion Desired flag (default: true)"
  "--z": "Set Z flag (reserved for future use)"
  "--do": "Set DNSSEC OK flag"
  "-J, --json": "Format the output as JSON"
  "--short": "Short output format (shows only the response section)"
  "--color": "Enable/disable colored output (default: true)"
  "--debug": "Enable debug logging"
  "--time": "Show query response time"
