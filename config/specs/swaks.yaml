# Converted from argc's swaks.sh by tools/argc-to-spec. Do not edit by hand.
name: swaks
flags:
  "--config=": "This option provides a path to a specific configuration file to be used."
  "-s, --server=": "Explicitly tell Swaks to use network sockets and specify the hostname or IP address to which to connect, or prompt if no argument is given."
  "-p, --port=": "Specify which TCP port on the target is to be used, or prompt if no argument is listed."
  "-li=": "Use argument as the local interface for the outgoing SMTP connection, or prompt user if no argument given."
  "--local-interface=": "Use argument as the local interface for the outgoing SMTP connection, or prompt user if no argument given."
  "-lp=": "Specify the outgoing port from which to originate the transaction."
  "--local-port=": "Specify the outgoing port from which to originate the transaction."
  "--lport=": "Specify the outgoing port from which to originate the transaction."
  "--copy-routing=": "The argument is interpreted as the domain part of an email address and it is used to find the target server using the same logic that would be used to look up the target server for a recipient email address."
  "-4": "Force IPv4 or IPv6."
  "-6": "Force IPv4 or IPv6."
  "--socket=": "This option takes as its argument a UNIX-domain socket file."
  "--pipe=": "Provide a process name and arguments to the process."
  "-t, --to=": "Tells Swaks to use argument(s) as the envelope-recipient for the email, or prompt for recipient if no argument provided."
  "-f, --from=": "Use argument as envelope-sender for email, or prompt user if no argument specified."
  "-h=": "String to use as argument to HELO/EHLO/LHLO command, or prompt user if no argument is specified."
  "--ehlo=": "String to use as argument to HELO/EHLO/LHLO command, or prompt user if no argument is specified."
  "--lhlo=": "String to use as argument to HELO/EHLO/LHLO command, or prompt user if no argument is specified."
  "--helo=": "String to use as argument to HELO/EHLO/LHLO command, or prompt user if no argument is specified."
  "-q=": "Point at which the transaction should be stopped."
  "--quit=": "Point at which the transaction should be stopped."
  "--quit-after=": "Point at which the transaction should be stopped."
  "--da=": "The option is similar to \"--quit-after\", but instead of trying to cleanly shut down the session it simply terminates the session."
  "--drop-after=": "The option is similar to \"--quit-after\", but instead of trying to cleanly shut down the session it simply terminates the session."
  "--das=": "This option is similar to \"--drop-after\", but instead of dropping the connection after reading a response to the stop-point, it drops the connection immediately after sending stop-point."
  "--drop-after-send=": "This option is similar to \"--drop-after\", but instead of dropping the connection after reading a response to the stop-point, it drops the connection immediately after sending stop-point."
  "--timeout=": "Use argument as the SMTP transaction timeout, or prompt user if no argument given."
  "--protocol=": "Specify which protocol to use in the transaction."
  "--pipeline": "If the remote server supports it, attempt SMTP PIPELINING (RFC 2920)."
  "--prdr": "If the server supports it, attempt Per-Recipient Data Response (PRDR) (<https://tools.ietf.org/html/draft-hall-prdr-00.txt>)."
  "--force-getpwuid": "Tell Swaks to use the getpwuid method of finding the default sender local-part instead of trying $LOGNAME first."
  "-tls": "Require connection to use STARTTLS."
  "-tlso": "Attempt to use STARTTLS if available, continue with normal transaction if TLS was unable to be negotiated for any reason."
  "--tls-optional": "Attempt to use STARTTLS if available, continue with normal transaction if TLS was unable to be negotiated for any reason."
  "-tlsos": "Attempt to use STARTTLS if available."
  "--tls-optional-strict": "Attempt to use STARTTLS if available."
  "-tlsc": "Initiate a TLS connection immediately on connection."
  "--tls-on-connect": "Initiate a TLS connection immediately on connection."
  "-tlsp=": "Specify which protocols to use (or not use) when negotiating TLS."
  "--tls-protocol=": "Specify which protocols to use (or not use) when negotiating TLS."
  "--tls-cipher=": "The argument to this option is passed to the underlying OpenSSL library to set the list of acceptable ciphers to the be used for the connection."
  "--tls-verify": "Tell Swaks to attempt to verify the server's certificate."
  "--tls-ca-path=": "Specify an alternate location for CA information for verifying server certificates."
  "--tls-cert=": "Provide a path to a file containing the local certificate Swaks should use if TLS is negotiated."
  "--tls-key=": "Provide a path to a file containing the local private key Swaks should use if TLS is negotiated."
  "--tls-get-peer-cert=": "Get a copy of the TLS peer's certificate."
  "--tls-sni=": "Specify the Server Name Indication field to send when the TLS connection is initiated."
  "-a, --auth=": "Require Swaks to authenticate."
  "-ao=": "This option behaves identically to \"--auth\" except that it requests authentication rather than requiring it."
  "--auth-optional=": "This option behaves identically to \"--auth\" except that it requests authentication rather than requiring it."
  "-aos=": "This option is a compromise between \"--auth\" and \"--auth-optional\"."
  "--auth-optional-strict=": "This option is a compromise between \"--auth\" and \"--auth-optional\"."
  "-au=": "Provide the username to be used for authentication."
  "--auth-user=": "Provide the username to be used for authentication."
  "-ap=": "Provide the password to be used for authentication."
  "--auth-password=": "Provide the password to be used for authentication."
  "-ae=": "Some of the authentication types allow extra information to be included in the authentication process."
  "--auth-extra=": "Some of the authentication types allow extra information to be included in the authentication process."
  "-am=": "Provides a way to map alternate names onto base authentication types."
  "--auth-map=": "Provides a way to map alternate names onto base authentication types."
  "-apt": "Instead of showing AUTH strings base64 encoded as they are transmitted, translate them to plaintext before printing on screen."
  "--auth-plaintext": "Instead of showing AUTH strings base64 encoded as they are transmitted, translate them to plaintext before printing on screen."
  "-ahp=": "If this option is specified, any time a readable password would be printed to the terminal (specifically AUTH PLAIN and AUTH LOGIN) the password is replaced with the string 'PROVIDED_BUT_REMOVED' (or the contents of <replacement-string> if provided)."
  "--auth-hide-password=": "If this option is specified, any time a readable password would be printed to the terminal (specifically AUTH PLAIN and AUTH LOGIN) the password is replaced with the string 'PROVIDED_BUT_REMOVED' (or the contents of <replacement-string> if provided)."
  "--xclient-addr=": ""
  "--xclient-name=": ""
  "--xclient-port=": ""
  "--xclient-proto=": ""
  "--xclient-destaddr=": ""
  "--xclient-destport=": ""
  "--xclient-helo=": ""
  "--xclient-login=": ""
  "--xclient-reverse-name=": "These options specify XCLIENT attributes that should be sent to the target server."
  "--xclient-delim": "When this option is specified, it indicates a break in XCLIENT attributes to be sent."
  "--xclient=": "This is the \"free form\" XCLIENT option."
  "--xclient-no-verify": "Do not enforce the requirement that an XCLIENT attribute must be advertised by the server in order for Swaks to send it in an XCLIENT command."
  "--xclient-before-starttls": "If Swaks is configured to attempt both XCLIENT and STARTTLS, it will do STARTTLS first."
  "--xclient-optional": ""
  "--xclient-optional-strict": "In normal operation, setting one of the \"--xclient*\" options will require a successful XCLIENT transaction to take place in order to proceed (that is, XCLIENT needs to be advertised, all the user-requested attributes need to have been advertised, and the server needs to have accepted Swaks' XCLIENT request)."
  "--proxy-version=": "Whether to use version 1 (human readable) or version 2 (binary) of the Proxy protocol."
  "--proxy=": "If this option is used, its argument is passed unchanged after the \"PROXY \" portion (or the 12-byte protocol header for version 2) of the Proxy exchange."
  "--proxy-family=": "For version 1, specifies both the address family and transport protocol."
  "--proxy-protocol=": "For version 2, specifies the transport protocol."
  "--proxy-command=": "For version 2, specifies the transport protocol."
  "--proxy-source=": "Specify the source address of the proxied connection."
  "--proxy-source-port=": "Specify the source port of the proxied connection."
  "--proxy-dest=": "Specify the destination address of the proxied connection."
  "--proxy-dest-port=": "Specify the destination port of the proxied connection."
  "-d, --data=": "Use argument as the entire contents of DATA."
  "-dab=": "If \"--dump-as-body\" is used and no other option is used to change the default body of the message, the body is replaced with output similar to the output of what is provided by \"--dump\"."
  "--dump-as-body=": "If \"--dump-as-body\" is used and no other option is used to change the default body of the message, the body is replaced with output similar to the output of what is provided by \"--dump\"."
  "-dabsp": "Cause \"--dump-as-body\" to include plaintext passwords."
  "--dump-as-body-shows-password": "Cause \"--dump-as-body\" to include plaintext passwords."
  "--body=": "Specify the body of the email."
  "--attach=": "When one or more \"--attach\" option is supplied, the message is changed into a multipart/mixed MIME message."
  "--attach-body=": "This is a variation on \"--attach\" that is specifically for the body part of the email."
  "--attach-type=": "By default, content that gets MIME attached to a message with the \"--attach\" option is encoded as application/octet-stream (except for the body, which is text/plain by default)."
  "--attach-name=": "This option sets the filename that will be included in the MIME part created for the next \"--attach\" option."
  "-ah=": "This option allows headers to be added to the DATA."
  "--add-header=": "This option allows headers to be added to the DATA."
  "--header=": "These options allow a way to change headers that already exist in the DATA."
  "--h=": "These options allow a way to change headers that already exist in the DATA."
  "-g=": "This option is a direct alias to \"--data -\" (read DATA from \"STDIN\")."
  "--no-data-fixup": "This option forces Swaks to do no massaging of the DATA portion of the email."
  "-ndf": "This option forces Swaks to do no massaging of the DATA portion of the email."
  "--no-strip-from": "Don't strip the From_ line from the DATA portion, if present."
  "-nsf": "Don't strip the From_ line from the DATA portion, if present."
  "-n, --suppress-data": "Summarizes the DATA portion of the SMTP transaction instead of printing every line."
  "-stl=": "Display time lapse between send/receive pairs."
  "--show-time-lapse=": "Display time lapse between send/receive pairs."
  "-nih": "Don't display the transaction hint for informational transactions."
  "--no-info-hints": "Don't display the transaction hint for informational transactions."
  "-nsh": ""
  "--no-send-hints": ""
  "-nrh": ""
  "--no-receive-hints": ""
  "-nth": "\"--no-send-hints\" and \"--no-receive-hints\" suppress the transaction hints from send and receive lines, respectively."
  "--no-hints": "\"--no-send-hints\" and \"--no-receive-hints\" suppress the transaction hints from send and receive lines, respectively."
  "-raw": "This option will print a hex dump of raw data sent and received by Swaks."
  "--show-raw-text": "This option will print a hex dump of raw data sent and received by Swaks."
  "--output=": ""
  "--output-file=": ""
  "--output-file-stdout=": ""
  "--output-file-stderr=": "These options allow the user to send output to files instead of \"STDOUT\"/\"STDERR\"."
  "-pp": "Don't echo user input on prompts that are potentially sensitive (right now only authentication password)."
  "--protect-prompt": "Don't echo user input on prompts that are potentially sensitive (right now only authentication password)."
  "-hr": "Don't display lines sent from the remote server being received by Swaks."
  "--hide-receive": "Don't display lines sent from the remote server being received by Swaks."
  "-hs": "Don't display lines being sent by Swaks to the remote server."
  "--hide-send": "Don't display lines being sent by Swaks to the remote server."
  "-hi": "Don't display non-error informational lines from Swaks itself."
  "--hide-informational": "Don't display non-error informational lines from Swaks itself."
  "-ha": "Do not display any content to the terminal."
  "--hide-all": "Do not display any content to the terminal."
  "-S, --silent=": "Cause Swaks to be silent."
  "--support": "Print capabilities and exit."
  "--dump-mail": "Cause Swaks to process all options to generate the message it would send, then print that message to \"STDOUT\" instead of sending it."
  "--dump=": "This option causes Swaks to print the results of option processing, immediately before mail would have been sent."
  "--help": "Display this help information and exit."
  "--version": "Display version information and exit."
completion:
  flag:
    port: [25, 465, 587]
    tlsp: [sslv2, sslv3, tlsv1, tlsv1_1, tlsv1_2]
    tls-protocol: [sslv2, sslv3, tlsv1, tlsv1_1, tlsv1_2]
    ap: [Arg-Required, From-Prompt, Sensitive]
    auth-password: [Arg-Required, From-Prompt, Sensitive]
    body: [Arg-Required, From-Prompt, From-File]
    attach: [Arg-Required, From-File]
    attach-body: [Arg-Required, From-File]
    g: [Arg-None, Deprecated]
    silent: [1, 2, 3]
