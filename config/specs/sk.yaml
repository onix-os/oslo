# Converted from argc's sk.sh by tools/argc-to-spec. Do not edit by hand.
name: sk
flags:
  "-e, --exact": "Enable exact-match"
  "--regex": "Search with regular expression instead of fuzzy match"
  "--algo=": "Fuzzy matching algorithm (default: skim_v2)"
  "--case=": "To ignore case on matching or not."
  "-n, --nth=": "Comma-separated list of field index expressions for limiting search scope."
  "--with-nth=": "Transform the presentation of each line using field index expressions"
  "-d, --delimiter=": "Field delimiter regex for --nth and --with-nth (default: AWK-style)"
  "--tac": "Reverse the order of the search result(normally used together with --no-sort)"
  "--no-sort": "Do not sort the search result(normally used together with --tac)"
  "--tiebreak=*": {description: "Comma-separated list of sort criteria to apply when the scores are tied.", nargs: -1}
  "-i, --interactive": "Start the finder in the command query"
  "-c, --cmd=": "Specify the command to invoke for fetching options"
  "-I=": "Replace replstr with the selected item"
  "-m, --multi": "Enable multi-select with tab/shift-tab"
  "--no-multi": "Disable multi-select"
  "--bind=": "Comma-separated list of custom key bindings."
  "--no-hscroll": "Disable horizontal scroll"
  "--height=": "Display sk window below the cursor with the given height instead of using the full screen."
  "--min-height=": "Minimum height when --height is given in percent (default: 10)."
  "--layout=": "Choose the layout (default: default)"
  "--reverse": "A synonym for --layout=reverse"
  "--margin=": "Comma-separated expression for margins around the finder."
  "--inline-info": "Display finder info inline with the query"
  "-p, --prompt=": "Input prompt (default: '> ')"
  "--cmd-prompt=": "Command prompt (default: 'c> ')"
  "--header=": "The given string will be printed as the sticky header."
  "--header-lines=": "The first N lines of the input are treated as the sticky header."
  "--keep-right": "Keep the right end of the line visible when it's too long."
  "--skip-to-pattern": "Line will start with the start of the matched pattern."
  "--no-clear-if-empty": "Do not clear previous items if new command returns empty result."
  "--show-cmd-error": "If the command fails, send the error messages and show them as items."
  "--tabstop=": "Number of spaces for a tab character (default: 8)"
  "--color=": "Color configuration."
  "--history=": "Load search history from the specified file and update the file on completion."
  "--history-size=": "Maximum number of entries in the history file (default: 1000)."
  "--cmd-history=": "Load command query history from the specified file and update the file on completion."
  "--cmd-history-size=": "Maximum number of command query entries in the history file (default: 1000)."
  "--preview=": "Execute the given command for the current line and display the result on the preview window."
  "--preview-window=": "Determine the layout of the preview window."
  "-q, --query=": "Start the finder with the given query"
  "--cmd-query=": "Specify the initial query for the command query"
  "--print-query": "Print query as the first line"
  "-f, --filter=": "Filter mode."
  "--expect=": "Comma-separated list of keys that can be used to complete sk in addition to the default enter key."
  "--read0": "Read input delimited by ASCII NUL characters instead of newline characters"
  "--print0": "Print output delimited by ASCII NUL characters instead of newline characters"
  "--no-clear": "Do not clear finder interface on exit."
  "-1, --select-1": "Automatically select the only match"
  "-0, --exit-0": "Exit immediately when there's no match"
  "--sync": "Synchronous search for multi-staged filtering."
  "--pre-select-n=": "Pre-select the first NUM items in the multi-selection mode."
  "--pre-select-pat=": "Pre-select the items that matches the REGEX specified in multi-selection mode."
  "--pre-select-items=": "Pre-select the specified items (separated by newline character) in multi-selection mode."
  "--pre-select-file=": "Pre-select the items read from FILENAME (separated by newline character) in multi-selection mode."
  "--version": "Display version information and exit"
  "-h, -help": "Print help"
completion:
  flag:
    case: [smart, respect, ignore]
