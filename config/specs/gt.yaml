# Converted from Fig's gt.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: gt
description: "Graphite.dev CLI"
persistentflags:
  "--version": "Show Graphite CLI version"
  "--help, -h": "Show help for Graphite CLI"
  "--interactive": "Prompt the user. Disable with --no-interactive"
  "--no-interactive": "Run non interactive"
  "--quiet, -q": "Minimize output to the terminal. Implies --no-interactive"
  "--verify": "Run git hooks. Disable with --no-verify"
  "--no-verify": "Disable git hooks"
  "--debug": "Display debug output"
  "--cwd": "Working directory in which to perform operations"
commands:
  - name: aliases
    description: "Edit your command aliases"
    flags:
      "--reset": "Reset your alias configuration"
  - name: auth
    description: "Add your auth token to enable Graphite CLI to create and update your PRs on GitHub"
    flags:
      "--token, -t=": "Auth token. Get it from: https://app.graphite.dev/activate"
  - name: bottom
    aliases: [b]
    description: "Switch to the first branch from trunk in the current stack"
  - name: changelog
    description: "Show the Graphite CLI changelog"
  - name: checkout
    aliases: [co]
    description: "Switch to a branch. If no branch is provided, opens an interactive selector"
    flags:
      "--show-untracked, -u": "Include untracked branched in interactive selection"
  - name: children
    description: "Show the children of the current branch"
  - name: completion
    description: "Set up bash or zsh tab completion"
  - name: config
    description: "Configure the Graphite CLI"
  - name: continue
    aliases: [cont]
    description: "Continues the most recent Graphite command halted by a merge conflict"
    flags:
      "--all, -a": "Stage all changes before continuing"
  - name: create
    aliases: [c]
    description: "Create a new branch stacked on top of the current branch and commit staged changes. If no branch name is specified but a commit message is passed, generate a branch name from the commit message"
    flags:
      "--message, -m=": "Commit staged changes on the new branch with this message"
      "--all, -a": "Stage all unstaged changes on the new branch with this message"
      "--patch, -p": "Pick hunks to stage before committing"
      "--insert, -i": "When true, any existing children of the current branch will become children of the new branch"
  - name: dash
    description: "Open the web dashboard"
  - name: delete
    aliases: [dl]
    description: "Delete a branch and its corresponding Graphite metadata"
    flags:
      "--force, -f": "Delete the branch even if it is not merged or closed"
  - name: docs
    description: "Show the Graphite CLI docs"
  - name: down
    aliases: [d]
    description: "Switch to the parent of the current branch"
    flags:
      "--steps, -n=": "The number of levels to traverse downstack"
  - name: feedback
    description: "Commands for providing feedback and debug state"
    flags:
      "--print-debug-context": "Print a debug summary of your repository. Useful for creating bug report details"
      "--with-debug-context": "Include a blob of json describing your repo's state to help with debugging. Run 'gt feedback debug-context' to see what would be included"
  - name: fish
    description: "Set up 'fish' tab completion"
  - name: fold
    aliases: [f]
    description: "Fold a branch's changes into its parent, update dependencies of descendants of the new combined branch, and restack"
    flags:
      "--keep, -k": "Keeps the name of the current branch instead of using the name of its parent"
  - name: get
    description: "Get branches from trunk to the specified branch from remote, prompting the user to resolve conflicts. If no branch is provided, get downstack from the current branch"
    flags:
      "--force, -f": "Overwrite all fetched branches with remote source of truth"
  - name: info
    aliases: [i]
    description: "Display information about the current branch"
    flags:
      "--patch, -p": "Show the changes made by each commit"
      "--diff, -d": "Show the diff between this branch and its parent. Takes precedence over patch"
      "--body, -b": "Show the PR body, if it exists"
      "--stat, -s": "Show a diffstat instead of a full diff. Modifies either --patch or --diff. If neither is passed, implies --diff"
  - name: init
    description: "Initialize Graphite in this repository by selecting a trunk branch. Can also be used to change the trunk branch of the repository"
    flags:
      "--reset": "Untrack all branches"
      "--trunk?": "The name of your trunk branch. If no name is passed, you will be prompted to select one interactively"
  - name: log
    aliases: [l]
    description: "Commands that log your stacks"
    flags:
      "--classic": "Use the old short logging style, which runs out of screen real estate more quickly. Other options will not work in classic mode"
      "--reverse, -r": "Print the log upside down. Handy when you have a lot of branches"
      "--show-untracked, -u": "Include untracked branched in interactive selection"
      "--stack, -s": "Only show ancestors and descendants of the current branch"
      "--steps, -n=": "Only show this many levels upstack and downstack. Implies --stack"
    commands:
      - name: long
        description: "Display a graph of the commit ancestry of all branches"
      - name: short
        description: "Log all stacks tracked by Graphite, arranged to show dependencies"
  - name: merge
    description: "Merge the pull requests associated with all branches from trunk to the current branch via Graphite"
    flags:
      "--confirm, -c": "Asks for confirmation before merging branches. Prompts for confirmation if the local branches differ from remote, regardless of the value of this flag"
      "--dry-run": "Reports the PRs that would be merged and terminates. No branches are merged"
  - name: modify
    aliases: [m]
    description: "Modify the current branch by amending its commit or creating a new commit. Automatically restacks descendants. If you have any unstaged changes, you will be asked whether you'd like to stage them"
    flags:
      "--all, -a": "Stage all changes before committing"
      "--commit, -c": "Create a new commit instead of amending the current commit. If this branch has no commits, this command always creates a new commit"
      "--edit, -e": "If passed, open an editor to edit the commit message. When creating a new commit, this flag is ignored"
      "--interactive-rebase": "Ignore all other flags and start a git interactive rebase on the commits in this branch"
      "--message, -m=": "The message for the new or amended commit. If passed, no editor is opened"
      "--patch, -p": "Pick hunks to stage before committing"
  - name: move
    description: "Rebase the current branch onto the target branch and restack all of its descendants. If no branch is passed in, opens an interactive selector"
    flags:
      "--onto, -o=": "Branch to move the current branch onto"
      "--source=": "Branch to move (defaults to current branch)"
  - name: parent
    description: "Show the parent of the current branch"
  - name: pop
    description: "Delete the current branch but reaint the state of files in the working tree"
  - name: pr
    description: "Opens the pull request page for a branch or PR number. If no branch is passed, the current branch's PR is opened"
  - name: rename
    aliases: [rn]
    description: "Rename a branch and update metadata referencing it. If no branch name is supplied, you will be prompted for a new branch name. Note that this removes any associated GitHub pull request"
    flags:
      "--force, -f": "Allow renaming a branch that is already associated with an open GitHub pull request"
  - name: reorder
    description: "Reorder branches between trunk and the current branch, restacking all of their descendants. Opens an editor where you can reorder branches by moving around a line corresponding to each branch"
  - name: restack
    aliases: [r]
    description: "Ensure the current branch is based on its parent, rebasing if necessary. If conflicts are encountered, you will be prompted to resolve them via an interactive Git rebase"
    flags:
      "--branch=": "Which branch to run this command from. Defaults to the current branch"
      "--downstack": "Only restack this branch and its ancestors"
      "--only": "Only restack this branch"
      "--upstack": "Only restack this branch and its descendants"
  - name: revert
    description: "Create a branch that reverts a commit on the trunk branch. Currently experimental"
    flags:
      "--edit, -e": "Edit the commit message"
  - name: split
    aliases: [sp]
    description: "Split the current branch into multiple single-commit branches"
    flags:
      "--by-commit, --commit, -c": "Split by commit - slice up the history of this branch"
      "--by-hunk, --hunk, -h": "Split by hunk - split into new single-commit branches"
  - name: squash
    aliases: [sq]
    description: "Squash all commits in the current branch and restack upstack branches"
    flags:
      "--message, -m=": "The updated message for the commit"
      "--edit": "Modify the existing commit message"
      "--no-edit, -n": "Don't modify the existing commit message. Takes precedence over --edit"
  - name: submit
    aliases: [s]
    description: "Idempotently force push the current branch to GitHub, creating or updating a pull request. Opens an interactive prompt that allows you to input pull request metadata. 'gt ss' is a default alias for 'gt submit --stack'"
    flags:
      "--always": "Always push updates, even if the branch has not changed. Can be helpful for fixing an inconsistent Graphite stack view on Web/GitHub resulting from downtime/a bug"
      "--branch=": "Which branch to run this command from. Defaults to the current branch"
      "--confirm, -c": "Reports the PRs that would be submitted and asks for confirmation before pushing branches and opening/updating PRs. If either of --no-interactive or --dry-run is passed, this flag is ignored"
      "--draft, -d": "If set, marks PR as draft. If --no-interactive is true, new PRs will be created in draft mode"
      "--dry-run": "Reports the PRs that would be submitted and terminates. No branches are pushed and no PRs are opened or updated"
      "--edit, -e": "Input metadata for all PRs interactively. If neither --edit nor --no-edit is passed, only prompts for new PRs"
      "--force, -f": "Force push: overwrites the remote branch with your local branch. Otherwise defaults to --force-with-lease"
      "--merge-when-ready, -m": "If set, marks all PRs being submitted as merge when ready, which will let them automatically merge as soon as all merge requirements are met"
      "--no-edit, -n": "Don't edit PR fields inline. Takes precedence over --edit"
      "--publish, -p": "If set, publishes PR. If --no-interactive is true, new PRs will be created in draft mode"
      "--rerequest-review": "Rerequest review from current reviewers"
      "--restack": "Restack branches before submitting. If there are conflicts, output the branch names that could not be restacked"
      "--reviewers, -r?": "If set without an argument, prompt to manually set reviewers. Alternatively, accepts a comma separated string of reviewers"
      "--stack, -s": "Submit descendants of the current branch in addition to its ancestors"
      "--update-only, -u": "Only update the PRs that have been already been submitted"
  - name: sync
    description: "Pull the trunk branch from remote and prompt to delete any branches that have been merged. Restacks all branches in your repository that can be restacked without conflicts. If trunk cannot be fast-forwarded to match remote, overwrites trunk with the remote version"
    flags:
      "--delete": "Delete merged branches (true by default; skip with --no-delete)"
      "--force, -f": "Don't prompt for confirmation before deleting a branch or overwriting trunk to match remote if it can't be fast-forwarded"
      "--pull": "Pull the trunk branch from remote (true by default; skip with --no-pull)"
      "--restack": "Restack any branches that can be restacked without conflicts (true by default; skip with --no-restack)"
      "--show-delete-progress": "Show progress through merged branches"
  - name: top
    aliases: [t]
    description: "Switch to the tip branch of the current stack. Prompts if ambiguous"
  - name: track
    aliases: [tr]
    description: "Start tracking the current (or provided) branch with Graphite by selecting its parent. This command can also be used to fix corrupted Graphite metadata"
    flags:
      "--parent, -p=": "The tracked branch's parent. If unset, prompts for a parent branch"
      "--force, -f": "Sets the parent to the most recent tracked ancestor of the branch being tracked. Takes precedence over `--parent`"
  - name: trunk
    description: "Switch to the trunk branch"
  - name: untrack
    aliases: [utr]
    description: "Stop tracking a branch with Graphite. If the branch has children, they will also be untracked. Default to the current branch if none is passed in"
    flags:
      "--force, -f": "Will not prompt for confirmation before untracking a branch with children"
  - name: up
    aliases: [u]
    description: "Switch to the child of the current branch. Prompts if ambiguous"
    flags:
      "--steps, -n=": "The number of levels to traverse upstack"
