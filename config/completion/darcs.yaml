# Converted from argc's darcs.sh by tools/argc-to-spec. Do not edit by hand.
name: darcs
flags:
  "-h, --help": ""
  "-V, --version": ""
commands:
  - name: initialize
    description: "Create an empty repository."
    flags:
      "--darcs-3": "New darcs patch format"
      "--darcs-2": "Standard darcs patch format [DEFAULT]"
      "--darcs-1": "Older patch format (for compatibility)"
      "--with-working-dir": "Create a working tree (normal repository) [DEFAULT]"
      "--no-working-dir": "Do not create a working tree (bare repository)"
      "--repo-name=": ""
      "--repodir=": "path of output directory"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--with-patch-index": "build patch index"
      "--no-patch-index": "don't build patch index [DEFAULT]"
      "--hashed": "deprecated, use --darcs-1 instead"
      "--umask=": "specify umask to use when writing"
      "--with-prefs-templates": "create template-filled preferences [DEFAULT]"
      "--no-prefs-templates": "create empty preferences"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: add
    description: "Add new files to version control."
    flags:
      "--boring": "don't skip boring files"
      "--no-boring": "skip boring files [DEFAULT]"
      "--case-ok": "don't refuse to add files differing only in case"
      "--no-case-ok": "refuse to add files whose name differ only in case [DEFAULT]"
      "--reserved-ok": "don't refuse to add files with Windows-reserved names"
      "--no-reserved-ok": "refuse to add files with Windows-reserved names [DEFAULT]"
      "-r, --recursive": "recurse into subdirectories"
      "--not-recursive": ""
      "--no-recursive": "don't recurse into subdirectories [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "-n, --dry-run": "don't actually take the action"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: whatsnew
    description: "List unrecorded changes in the working tree."
    flags:
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "-u, --unified": "output changes in a darcs-specific format similar to diff -u"
      "--no-unified": "output changes in darcs' usual format [DEFAULT]"
      "--human-readable": "normal human-readable output [DEFAULT]"
      "--machine-readable": "machine-readable output"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added [DEFAULT]"
      "-l, --look-for-adds": "look for files that could be added"
      "--boring": "look for any file that could be added, even boring files"
      "--look-for-replaces": "look for replaces that could be marked"
      "--dont-look-for-replaces": ""
      "--no-look-for-replaces": "don't look for any replaces [DEFAULT]"
      "--look-for-moves": "look for files that may be moved/renamed"
      "--dont-look-for-moves": ""
      "--no-look-for-moves": "don't look for any files that could be moved/renamed [DEFAULT]"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: record
    description: "Create a patch from unrecorded changes."
    flags:
      "-m, --name=": "name of patch"
      "-A, --author=": "specify author id"
      "--test": "run the test script"
      "--no-test": "don't run the test script [DEFAULT]"
      "--leave-test-directory": "don't remove the test directory"
      "--remove-test-directory": "remove the test directory [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--pipe": "ask user interactively for the patch metadata"
      "--ask-deps": "manually select dependencies"
      "--no-ask-deps": "automatically select dependencies [DEFAULT]"
      "--edit-long-comment": "edit the long comment by default"
      "--skip-long-comment": "don't give a long comment"
      "--prompt-long-comment": "prompt for whether to edit the long comment"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added [DEFAULT]"
      "-l, --look-for-adds": "look for files that could be added"
      "--boring": "look for any file that could be added, even boring files"
      "--look-for-replaces": "look for replaces that could be marked"
      "--dont-look-for-replaces": ""
      "--no-look-for-replaces": "don't look for any replaces [DEFAULT]"
      "--look-for-moves": "look for files that may be moved/renamed"
      "--dont-look-for-moves": ""
      "--no-look-for-moves": "don't look for any files that could be moved/renamed [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--logfile=": "give patch name and comment in file"
      "--delete-logfile": "delete the logfile when done"
      "--no-delete-logfile": "keep the logfile when done [DEFAULT]"
      "--umask=": "specify umask to use when writing"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: clone
    description: "Make a copy of an existing repository."
    flags:
      "--repo-name=": ""
      "--repodir=": "path of output directory"
      "--lazy": "get patch files only as needed"
      "--complete": "get a complete copy of the repository"
      "--to-match=": "select changes up to a patch matching PATTERN"
      "--to-patch=": "select changes up to a patch matching REGEXP"
      "--to-hash=": "select changes up to a patch whose hash prefix matches HASH"
      "-t, --tag=": "select tag matching REGEXP"
      "--context=": "version specified by the context in FILENAME"
      "--set-default": "set default repository"
      "--no-set-default": "don't set default repository"
      "--inherit-default": "inherit default repository"
      "--no-inherit-default": "don't inherit default repository [DEFAULT]"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--with-working-dir": "Create a working tree (normal repository) [DEFAULT]"
      "--no-working-dir": "Do not create a working tree (bare repository)"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--packs": "use repository packs [DEFAULT]"
      "--no-packs": "don't use repository packs"
      "--with-patch-index": "build patch index"
      "--no-patch-index": "don't build patch index [DEFAULT]"
      "--umask=": "specify umask to use when writing"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--with-prefs-templates": "create template-filled preferences [DEFAULT]"
      "--no-prefs-templates": "create empty preferences"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: pull
    description: "Copy and apply patches from another repository to this one."
    flags:
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--reorder-patches": "put local-only patches on top of remote ones"
      "--no-reorder-patches": "put remote-only patches on top of local ones [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--mark-conflicts": "mark conflicts [DEFAULT]"
      "--allow-conflicts": "allow conflicts, but don't mark them"
      "--external-merge=": "use external tool to merge conflicts"
      "--dont-allow-conflicts": ""
      "--no-allow-conflicts": ""
      "--no-resolve-conflicts": "fail if there are patches that would create conflicts"
      "--skip-conflicts": "filter out any patches that would create conflicts"
      "--test": "run the test script"
      "--no-test": "don't run the test script [DEFAULT]"
      "--leave-test-directory": "don't remove the test directory"
      "--remove-test-directory": "remove the test directory [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "--set-default": "set default repository"
      "--no-set-default": "don't set default repository"
      "--inherit-default": "inherit default repository"
      "--no-inherit-default": "don't inherit default repository [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--ignore-unrelated-repos": "do not check if repositories are unrelated"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--intersection": "take intersection of all repositories"
      "--union": "take union of all repositories [DEFAULT]"
      "--complement": "take complement of repositories (in order listed)"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--umask=": "specify umask to use when writing"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--pause-for-gui": "pause for an external diff or merge command to finish [DEFAULT]"
      "--no-pause-for-gui": "return immediately after external diff or merge command finishes"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: push
    description: "Copy and apply patches from this repository to another one."
    flags:
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--sign": "sign the patch with your gpg key"
      "--sign-as=": "sign the patch with a given keyid"
      "--sign-ssl=": "sign the patch using openssl with a given private key"
      "--dont-sign": ""
      "--no-sign": "don't sign the patch [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "--repodir=": "specify the repository directory in which to run"
      "--set-default": "set default repository"
      "--no-set-default": "don't set default repository"
      "--inherit-default": "inherit default repository"
      "--no-inherit-default": "don't inherit default repository [DEFAULT]"
      "--ignore-unrelated-repos": "do not check if repositories are unrelated"
      "--reorder-patches": "put remote-only patches on top of local ones"
      "--no-reorder-patches": "put local-only patches on top of remote ones [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--apply-as=": "apply patch as another user using sudo"
      "--no-apply-as": "don't use sudo to apply as another user [DEFAULT]"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--compress": "compress patch data [DEFAULT]"
      "--dont-compress": ""
      "--no-compress": "don't compress patch data"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: move
    description: "Move or rename files."
    flags:
      "--case-ok": "don't refuse to add files differing only in case"
      "--no-case-ok": "refuse to add files whose name differ only in case [DEFAULT]"
      "--reserved-ok": "don't refuse to add files with Windows-reserved names"
      "--no-reserved-ok": "refuse to add files with Windows-reserved names [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: remove
    description: "Remove files from version control."
    flags:
      "--repodir=": "specify the repository directory in which to run"
      "-r, --recursive": "recurse into subdirectories"
      "--not-recursive": ""
      "--no-recursive": "don't recurse into subdirectories [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: replace
    description: "Substitute one word for another."
    flags:
      "--token-chars=": "define token to contain these characters"
      "-f, --force": "proceed with replace even if 'new' token already exists"
      "--no-force": "don't force the replace if it looks scary [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: log
    description: "List patches in the repository."
    flags:
      "--to-match=": "select changes up to a patch matching PATTERN"
      "--to-patch=": "select changes up to a patch matching REGEXP"
      "--to-hash=": "select changes up to a patch whose hash prefix matches HASH"
      "--to-tag=": "select changes up to a tag matching REGEXP"
      "--from-match=": "select changes starting with a patch matching PATTERN"
      "--from-patch=": "select changes starting with a patch matching REGEXP"
      "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
      "--from-tag=": "select changes starting with a tag matching REGEXP"
      "--last=": "select the last NUMBER patches"
      "-n, --index=": "select a range of patches"
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--max-count=": "return only NUMBER results"
      "--only-to-files": "show only changes to specified files"
      "--no-only-to-files": "show changes to all files [DEFAULT]"
      "--context": "produce output suitable for clone --context"
      "--xml-output": "generate XML formatted output"
      "--human-readable": "normal human-readable output"
      "--machine-readable": "machine-readable output"
      "--number": "number the changes"
      "--count": "output count of changes"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--repo=": "specify the repository URL"
      "--repodir=": "specify the repository directory in which to run"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--with-patch-index": "build patch index [DEFAULT]"
      "--no-patch-index": "don't build patch index"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: annotate
    description: "Annotate lines of a file with the last patch that modified it."
    flags:
      "--human-readable": "normal human-readable output [DEFAULT]"
      "--machine-readable": "machine-readable output"
      "--match=": "select a single patch matching PATTERN"
      "-p, --patch=": "select a single patch matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "-t, --tag=": "select tag matching REGEXP"
      "-n, --index=": "select one patch"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--with-patch-index": "build patch index [DEFAULT]"
      "--no-patch-index": "don't build patch index"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: diff
    description: "Create a diff between two versions of the repository."
    flags:
      "--match=": "select a single patch matching PATTERN"
      "-p, --patch=": "select a single patch matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--to-match=": "select changes up to a patch matching PATTERN"
      "--to-patch=": "select changes up to a patch matching REGEXP"
      "--to-hash=": "select changes up to a patch whose hash prefix matches HASH"
      "--to-tag=": "select changes up to a tag matching REGEXP"
      "--from-match=": "select changes starting with a patch matching PATTERN"
      "--from-patch=": "select changes starting with a patch matching REGEXP"
      "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
      "--from-tag=": "select changes starting with a tag matching REGEXP"
      "--last=": "select the last NUMBER patches"
      "-n, --index=": "select a range of patches"
      "--diff-command=": "specify diff command (ignores --diff-opts)"
      "--diff-opts=": "options to pass to diff"
      "-u, --unified": "pass -u option to diff [DEFAULT]"
      "--no-unified": "output patch in diff's dumb format"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added [DEFAULT]"
      "-l, --look-for-adds": "look for files that could be added"
      "--boring": "look for any file that could be added, even boring files"
      "--look-for-moves": "look for files that may be moved/renamed"
      "--dont-look-for-moves": ""
      "--no-look-for-moves": "don't look for any files that could be moved/renamed [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--store-in-memory": "do patch application in memory rather than on disk"
      "--no-store-in-memory": "do patch application on disk [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--pause-for-gui": "pause for an external diff or merge command to finish [DEFAULT]"
      "--no-pause-for-gui": "return immediately after external diff or merge command finishes"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: show
    description: "Show information about the given repository."
    flags:
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
    commands:
      - name: contents
        description: "Outputs a specific version of a file."
        flags:
          "--match=": "select a single patch matching PATTERN"
          "-p, --patch=": "select a single patch matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "-t, --tag=": "select tag matching REGEXP"
          "-n, --index=": "select one patch"
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: dependencies
        description: "Generate the graph of dependencies."
        flags:
          "--to-match=": "select changes up to a patch matching PATTERN"
          "--to-patch=": "select changes up to a patch matching REGEXP"
          "--to-hash=": "select changes up to a patch whose hash prefix matches HASH"
          "--to-tag=": "select changes up to a tag matching REGEXP"
          "--from-match=": "select changes starting with a patch matching PATTERN"
          "--from-patch=": "select changes starting with a patch matching REGEXP"
          "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
          "--from-tag=": "select changes starting with a tag matching REGEXP"
          "--last=": "select the last NUMBER patches"
          "-n, --index=": "select a range of patches"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: files
        description: "Show version-controlled files in the working tree."
        flags:
          "--files": "include files in output [DEFAULT]"
          "--no-files": "don't include files in output"
          "--directories": "include directories in output [DEFAULT]"
          "--no-directories": "don't include directories in output"
          "--pending": "reflect pending patches in output [DEFAULT]"
          "--no-pending": "only include recorded patches in output"
          "-0, --null": "separate file names by NUL characters"
          "--match=": "select a single patch matching PATTERN"
          "-p, --patch=": "select a single patch matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "-t, --tag=": "select tag matching REGEXP"
          "-n, --index=": "select one patch"
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: index
        description: "Dump contents of working tree index."
        flags:
          "-0, --null": "separate file names by NUL characters"
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: pristine
        description: "Dump contents of pristine cache."
        flags:
          "-0, --null": "separate file names by NUL characters"
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: repo
        description: "Show repository summary information"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--xml-output": "generate XML formatted output"
          "--enum-patches": "include statistics requiring enumeration of patches [DEFAULT]"
          "--no-enum-patches": "don't include statistics requiring enumeration of patches"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: authors
        description: "List authors by patch count."
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: tags
        description: "Show all tags in the repository."
        flags:
          "--repo=": "specify the repository URL"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: patch-index
        description: "Check integrity of patch index"
        flags:
          "-0, --null": "separate file names by NUL characters"
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
  - name: test
    description: "Run tests and search for the patch that introduced a bug."
    flags:
      "--once": "run test on current version only [DEFAULT]"
      "--linear": "locate the most recent version lacking an error"
      "--backoff": "exponential backoff search"
      "--bisect": "binary instead of linear search"
      "--leave-test-directory": "don't remove the test directory"
      "--remove-test-directory": "remove the test directory [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--shrink-failure": "try to cut down the set of patches causing a test failure [DEFAULT]"
      "--no-shrink-failure": "don't try to cut down the set of patches causing a test failure"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: revert
    description: "Discard unrecorded changes."
    flags:
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added [DEFAULT]"
      "-l, --look-for-adds": "look for files that could be added"
      "--boring": "look for any file that could be added, even boring files"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: unrevert
    description: "Undo the last revert."
    flags:
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: amend
    description: "Improve a patch before it leaves your repository."
    flags:
      "--unrecord": "remove changes from the patch"
      "--record": "add more changes to the patch [DEFAULT]"
      "--not-in-remote=": "select all patches not in the default push/pull repository or at location URL/PATH"
      "--match=": "select a single patch matching PATTERN"
      "-p, --patch=": "select a single patch matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--test": "run the test script"
      "--no-test": "don't run the test script [DEFAULT]"
      "--leave-test-directory": "don't remove the test directory"
      "--remove-test-directory": "remove the test directory [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "-A, --author=": "specify author id"
      "--select-author": "select author id from a menu"
      "-m, --name=": "name of patch"
      "--ask-deps": "manually select dependencies"
      "--no-ask-deps": "automatically select dependencies [DEFAULT]"
      "--edit-long-comment": "edit the long comment by default"
      "--skip-long-comment": "don't give a long comment"
      "--prompt-long-comment": "prompt for whether to edit the long comment"
      "--keep-date": "keep the date of the original patch"
      "--no-keep-date": "use the current date for the amended patch [DEFAULT]"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added [DEFAULT]"
      "-l, --look-for-adds": "look for files that could be added"
      "--boring": "look for any file that could be added, even boring files"
      "--look-for-replaces": "look for replaces that could be marked"
      "--dont-look-for-replaces": ""
      "--no-look-for-replaces": "don't look for any replaces [DEFAULT]"
      "--look-for-moves": "look for files that may be moved/renamed"
      "--dont-look-for-moves": ""
      "--no-look-for-moves": "don't look for any files that could be moved/renamed [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: rebase
    description: "Edit several patches at once."
    flags:
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
    commands:
      - name: pull
        description: "Copy and apply patches from another repository, suspending any local patches that conflict."
        flags:
          "--matches=": "select patches matching PATTERN"
          "-p, --patches=": "select patches matching REGEXP"
          "-t, --tags=": "select tags matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "--reorder-patches": "put local-only patches on top of remote ones"
          "--no-reorder-patches": "put remote-only patches on top of local ones [DEFAULT]"
          "-a, --all": ""
          "--no-interactive": "answer yes to all patches"
          "-i, --interactive": "prompt user interactively"
          "--mark-conflicts": "mark conflicts [DEFAULT]"
          "--allow-conflicts": "allow conflicts, but don't mark them"
          "--external-merge=": "use external tool to merge conflicts"
          "--dont-allow-conflicts": ""
          "--no-allow-conflicts": ""
          "--no-resolve-conflicts": "fail if there are patches that would create conflicts"
          "--skip-conflicts": "filter out any patches that would create conflicts"
          "--test": "run the test script"
          "--no-test": "don't run the test script [DEFAULT]"
          "--leave-test-directory": "don't remove the test directory"
          "--remove-test-directory": "remove the test directory [DEFAULT]"
          "-n, --dry-run": "don't actually take the action"
          "--xml-output": "generate XML formatted output"
          "-s, --summary": "summarize changes"
          "--no-summary": "don't summarize changes"
          "--no-deps": "don't automatically fulfill dependencies"
          "--auto-deps": ""
          "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
          "--prompt-deps": ""
          "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
          "--repodir=": "specify the repository directory in which to run"
          "--ignore-unrelated-repos": "do not check if repositories are unrelated"
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--intersection": "take intersection of all repositories"
          "--union": "take union of all repositories [DEFAULT]"
          "--complement": "take complement of repositories (in order listed)"
          "--set-scripts-executable": "make scripts executable"
          "--dont-set-scripts-executable": ""
          "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
          "--umask=": "specify umask to use when writing"
          "--reverse": "show/consider changes in reverse order"
          "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
          "--remote-darcs=": "name of the darcs executable on the remote server"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: apply
        description: "Apply a patch bundle, suspending any local patches that conflict."
        flags:
          "--verify=": "verify that the patch was signed by a key in PUBRING"
          "--verify-ssl=": "verify using openSSL with authorized keys from file KEYS"
          "--no-verify": "don't verify patch signature [DEFAULT]"
          "--reorder-patches": "put local-only patches on top of remote ones"
          "--no-reorder-patches": "put remote-only patches on top of local ones [DEFAULT]"
          "-a, --all": ""
          "--no-interactive": "answer yes to all patches"
          "-i, --interactive": "prompt user interactively"
          "-n, --dry-run": "don't actually take the action"
          "--xml-output": "generate XML formatted output"
          "--matches=": "select patches matching PATTERN"
          "-p, --patches=": "select patches matching REGEXP"
          "-t, --tags=": "select tags matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "--repodir=": "specify the repository directory in which to run"
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--set-scripts-executable": "make scripts executable"
          "--dont-set-scripts-executable": ""
          "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
          "--umask=": "specify umask to use when writing"
          "--reverse": "show/consider changes in reverse order"
          "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
          "--pause-for-gui": "pause for an external diff or merge command to finish [DEFAULT]"
          "--no-pause-for-gui": "return immediately after external diff or merge command finishes"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: suspend
        description: "Select patches to move into a suspended state at the end of the repo."
        flags:
          "--not-in-remote=": "select all patches not in the default push/pull repository or at location URL/PATH"
          "--from-match=": "select changes starting with a patch matching PATTERN"
          "--from-patch=": "select changes starting with a patch matching REGEXP"
          "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
          "--from-tag=": "select changes starting with a tag matching REGEXP"
          "--last=": "select the last NUMBER patches"
          "--matches=": "select patches matching PATTERN"
          "-p, --patches=": "select patches matching REGEXP"
          "-t, --tags=": "select tags matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "--no-deps": "don't automatically fulfill dependencies"
          "--auto-deps": ""
          "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
          "--prompt-deps": ""
          "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
          "-a, --all": ""
          "--no-interactive": "answer yes to all patches"
          "-i, --interactive": "prompt user interactively"
          "-s, --summary": "summarize changes"
          "--no-summary": "don't summarize changes"
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--reverse": "show/consider changes in reverse order"
          "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: unsuspend
        description: "Select suspended patches to restore to the end of the repo."
        flags:
          "--mark-conflicts": "mark conflicts [DEFAULT]"
          "--allow-conflicts": "allow conflicts, but don't mark them"
          "--external-merge=": "use external tool to merge conflicts"
          "--dont-allow-conflicts": ""
          "--no-allow-conflicts": ""
          "--no-resolve-conflicts": "fail if there are patches that would create conflicts"
          "--skip-conflicts": "filter out any patches that would create conflicts"
          "--to-match=": "select changes up to a patch matching PATTERN"
          "--to-patch=": "select changes up to a patch matching REGEXP"
          "--to-hash=": "select changes up to a patch whose hash prefix matches HASH"
          "--to-tag=": "select changes up to a tag matching REGEXP"
          "--last=": "select the last NUMBER patches"
          "--matches=": "select patches matching PATTERN"
          "-p, --patches=": "select patches matching REGEXP"
          "-t, --tags=": "select tags matching REGEXP"
          "-h, --hash=": "select a single patch whose hash prefix matches HASH"
          "-a, --all": ""
          "--no-interactive": "answer yes to all patches"
          "-i, --interactive": "prompt user interactively"
          "-s, --summary": "summarize changes"
          "--no-summary": "don't summarize changes"
          "-A, --author=": "specify author id"
          "--select-author": "select author id from a menu"
          "-m, --name=": "name of patch"
          "--ask-deps": "manually select dependencies"
          "--no-ask-deps": "automatically select dependencies [DEFAULT]"
          "--edit-long-comment": "edit the long comment by default"
          "--skip-long-comment": "don't give a long comment"
          "--prompt-long-comment": "prompt for whether to edit the long comment"
          "--keep-date": "keep the date of the original patch"
          "--no-keep-date": "use the current date for the amended patch [DEFAULT]"
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: edit
        description: "Edit suspended patches."
        flags:
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "-s, --summary": "summarize changes"
          "--no-summary": "don't summarize changes"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: obliterate
        description: "Obliterate a patch that is currently suspended."
        flags:
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: log
        description: "List the currently suspended changes."
        flags:
          "-s, --summary": "summarize changes"
          "--no-summary": "don't summarize changes"
          "-a, --all": ""
          "--no-interactive": "answer yes to all patches"
          "-i, --interactive": "prompt user interactively"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: upgrade
        description: "Upgrade a repo with an old-style rebase in progress."
        flags:
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
  - name: rollback
    description: "Apply the inverse of recorded changes to the working tree."
    flags:
      "--from-match=": "select changes starting with a patch matching PATTERN"
      "--from-patch=": "select changes starting with a patch matching REGEXP"
      "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
      "--from-tag=": "select changes starting with a tag matching REGEXP"
      "--last=": "select the last NUMBER patches"
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: unrecord
    description: "Remove recorded patches without changing the working tree."
    flags:
      "--not-in-remote=": "select all patches not in the default push/pull repository or at location URL/PATH"
      "--from-match=": "select changes starting with a patch matching PATTERN"
      "--from-patch=": "select changes starting with a patch matching REGEXP"
      "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
      "--from-tag=": "select changes starting with a tag matching REGEXP"
      "--last=": "select the last NUMBER patches"
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: obliterate
    description: "Delete selected patches from the repository."
    flags:
      "--not-in-remote=": "select all patches not in the default push/pull repository or at location URL/PATH"
      "--from-match=": "select changes starting with a patch matching PATTERN"
      "--from-patch=": "select changes starting with a patch matching REGEXP"
      "--from-hash=": "select changes starting with a patch whose hash prefix matches HASH"
      "--from-tag=": "select changes starting with a tag matching REGEXP"
      "--last=": "select the last NUMBER patches"
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "-o, --output=": "specify output filename"
      "-O, --output-auto-name=": "output to automatically named file in DIRECTORY, default: current directory"
      "--minimize": "minimize context of patch bundle [DEFAULT]"
      "--no-minimize": "don't minimize context of patch bundle"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: clean
    description: "Alias for `darcs revert -l`."
    flags:
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--dont-look-for-adds": ""
      "--no-look-for-adds": "don't look for files that could be added"
      "-l, --look-for-adds": "look for files that could be added [DEFAULT]"
      "--boring": "look for any file that could be added, even boring files"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: tag
    description: "Name the current repository state for future reference."
    flags:
      "-m, --name=": "name of patch"
      "-A, --author=": "specify author id"
      "--pipe": "ask user interactively for the patch metadata"
      "--edit-long-comment": "edit the long comment by default"
      "--skip-long-comment": "don't give a long comment"
      "--prompt-long-comment": "prompt for whether to edit the long comment"
      "--ask-deps": "manually select dependencies"
      "--no-ask-deps": "automatically select dependencies [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: setpref
    description: "Set a preference (test, predist, boringfile, or binariesfile)."
    flags:
      "--repodir=": "specify the repository directory in which to run"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: send
    description: "Prepare a bundle of patches to be applied to some target repository."
    flags:
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "--to=": "specify destination email"
      "--cc=": "mail results to additional EMAIL(s)"
      "--from=": "specify email address"
      "--subject=": "specify mail subject"
      "--in-reply-to=": "specify in-reply-to header"
      "-A, --author=": "specify author id"
      "--charset=": "specify mail charset"
      "--mail": "send patch using sendmail"
      "--sendmail-command=": "specify sendmail command"
      "-o, --output=": "specify output filename"
      "-O, --output-auto-name=": "output to automatically named file in DIRECTORY, default: current directory"
      "--sign": "sign the patch with your gpg key"
      "--sign-as=": "sign the patch with a given keyid"
      "--sign-ssl=": "sign the patch using openssl with a given private key"
      "--dont-sign": ""
      "--no-sign": "don't sign the patch [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "--edit-description": "edit the patch bundle description [DEFAULT]"
      "--dont-edit-description": ""
      "--no-edit-description": "don't edit the patch bundle description"
      "--set-default": "set default repository"
      "--no-set-default": "don't set default repository"
      "--inherit-default": "inherit default repository"
      "--no-inherit-default": "don't inherit default repository [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--minimize": "minimize context of patch bundle [DEFAULT]"
      "--no-minimize": "don't minimize context of patch bundle"
      "--ignore-unrelated-repos": "do not check if repositories are unrelated"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--logfile=": "give patch name and comment in file"
      "--delete-logfile": "delete the logfile when done"
      "--no-delete-logfile": "keep the logfile when done [DEFAULT]"
      "--context=": "send to context stored in FILENAME"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: apply
    description: "Apply a patch bundle created by `darcs send'."
    flags:
      "--verify=": "verify that the patch was signed by a key in PUBRING"
      "--verify-ssl=": "verify using openSSL with authorized keys from file KEYS"
      "--no-verify": "don't verify patch signature [DEFAULT]"
      "--reorder-patches": "put local-only patches on top of remote ones"
      "--no-reorder-patches": "put remote-only patches on top of local ones [DEFAULT]"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "--mark-conflicts": "mark conflicts"
      "--allow-conflicts": "allow conflicts, but don't mark them"
      "--external-merge=": "use external tool to merge conflicts"
      "--dont-allow-conflicts": ""
      "--no-allow-conflicts": ""
      "--no-resolve-conflicts": "fail if there are patches that would create conflicts [DEFAULT]"
      "--skip-conflicts": "filter out any patches that would create conflicts"
      "--test": "run the test script"
      "--no-test": "don't run the test script [DEFAULT]"
      "--leave-test-directory": "don't remove the test directory"
      "--remove-test-directory": "remove the test directory [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--umask=": "specify umask to use when writing"
      "--reverse": "show/consider changes in reverse order"
      "--no-reverse": "show/consider changes in the usual order [DEFAULT]"
      "--pause-for-gui": "pause for an external diff or merge command to finish [DEFAULT]"
      "--no-pause-for-gui": "return immediately after external diff or merge command finishes"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: optimize
    description: "Optimize the repository."
    flags:
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
    commands:
      - name: clean
        description: "Garbage collect pristine, inventories and patches"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: http
        description: "Optimize repository for getting over network"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: reorder
        description: "Reorder the patches in the repository"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--deep": "also optimize clean tags in the complete history"
          "--shallow": "only reorder recent patches (works with lazy repo) [DEFAULT]"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: enable-patch-index
        description: "Enable patch index"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: disable-patch-index
        description: "Disable patch index"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: compress
        description: "Compress hashed files"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: uncompress
        description: "Uncompress hashed files (for debugging)"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: relink
        description: "Replace copies of hashed files with hard links"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--sibling=": "specify a sibling directory"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: upgrade
        description: "Upgrade repository to latest compatible format"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: cache
        description: "Garbage collect global cache"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--umask=": "specify umask to use when writing"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
  - name: dist
    description: "Create a distribution archive."
    flags:
      "-d, --dist-name=": "name of version"
      "--zip": "generate zip archive instead of gzip'ed tar"
      "--repodir=": "specify the repository directory in which to run"
      "--match=": "select a single patch matching PATTERN"
      "-p, --patch=": "select a single patch matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "-t, --tag=": "select tag matching REGEXP"
      "-n, --index=": "select one patch"
      "--set-scripts-executable": "make scripts executable"
      "--dont-set-scripts-executable": ""
      "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
      "--store-in-memory": "do patch application in memory rather than on disk"
      "--no-store-in-memory": "do patch application on disk [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: mark-conflicts
    description: "Mark unresolved conflicts in working tree, for manual resolution."
    flags:
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--xml-output": "generate XML formatted output"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: repair
    description: "Repair a corrupted repository."
    flags:
      "--repodir=": "specify the repository directory in which to run"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "-n, --dry-run": "don't actually take the action"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--umask=": "specify umask to use when writing"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
  - name: convert
    description: "Convert repositories between various formats."
    flags:
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
    commands:
      - name: darcs-2
        description: "Convert darcs-1 repository to the darcs-2 patch format"
        flags:
          "--repo-name=": ""
          "--repodir=": "path of output directory"
          "--set-scripts-executable": "make scripts executable"
          "--dont-set-scripts-executable": ""
          "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
          "--with-working-dir": "Create a working tree (normal repository) [DEFAULT]"
          "--no-working-dir": "Do not create a working tree (bare repository)"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--remote-darcs=": "name of the darcs executable on the remote server"
          "--with-patch-index": "build patch index"
          "--no-patch-index": "don't build patch index [DEFAULT]"
          "--umask=": "specify umask to use when writing"
          "--darcs-3": "New darcs patch format"
          "--darcs-2": "Standard darcs patch format [DEFAULT]"
          "--darcs-1": "Older patch format (for compatibility)"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: export
        description: "Export a darcs repository to a git-fast-import stream"
        flags:
          "--repodir=": "specify the repository directory in which to run"
          "--read-marks=": "continue conversion, previously checkpointed by --write-marks"
          "--write-marks=": "checkpoint conversion to continue it later"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--remote-darcs=": "name of the darcs executable on the remote server"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
      - name: import
        description: "Import from a git-fast-export stream into darcs"
        flags:
          "--repo-name=": ""
          "--repodir=": "path of output directory"
          "--set-scripts-executable": "make scripts executable"
          "--dont-set-scripts-executable": ""
          "--no-set-scripts-executable": "don't make scripts executable [DEFAULT]"
          "--darcs-3": "New darcs patch format"
          "--darcs-2": "Standard darcs patch format [DEFAULT]"
          "--darcs-1": "Older patch format (for compatibility)"
          "--with-working-dir": "Create a working tree (normal repository) [DEFAULT]"
          "--no-working-dir": "Do not create a working tree (bare repository)"
          "--help": "show a description of the command and its options"
          "--list-options": "show plain list of available options and commands, for auto-completion"
          "--disable": "disable this command"
          "-q, --quiet": "suppress informational output"
          "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
          "-v, --verbose": "enable verbose output"
          "--myers": "use myers diff algorithm"
          "--patience": "use patience diff algorithm [DEFAULT]"
          "--with-patch-index": "build patch index"
          "--no-patch-index": "don't build patch index [DEFAULT]"
          "--umask=": "specify umask to use when writing"
          "--with-prefs-templates": "create template-filled preferences [DEFAULT]"
          "--no-prefs-templates": "create empty preferences"
          "--no-cache": "don't use patch caches"
          "--ignore-times": "don't trust the file modification times"
          "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
          "--prehook=": "specify command to run before this darcs command"
          "--no-prehook": "don't run prehook command [DEFAULT]"
          "--prompt-prehook": "prompt before running prehook"
          "--run-prehook": "run prehook command without prompting [DEFAULT]"
          "--posthook=": "specify command to run after this darcs command"
          "--no-posthook": "don't run posthook command [DEFAULT]"
          "--prompt-posthook": "prompt before running posthook"
          "--run-posthook": "run posthook command without prompting [DEFAULT]"
          "--debug": "enable general debug output"
          "--timings": "provide debugging timings information"
  - name: fetch
    description: "Fetch patches from another repository, but don't apply them."
    flags:
      "--matches=": "select patches matching PATTERN"
      "-p, --patches=": "select patches matching REGEXP"
      "-t, --tags=": "select tags matching REGEXP"
      "-h, --hash=": "select a single patch whose hash prefix matches HASH"
      "-a, --all": ""
      "--no-interactive": "answer yes to all patches"
      "-i, --interactive": "prompt user interactively"
      "-n, --dry-run": "don't actually take the action"
      "-s, --summary": "summarize changes"
      "--no-summary": "don't summarize changes"
      "--no-deps": "don't automatically fulfill dependencies"
      "--auto-deps": ""
      "--dont-prompt-for-dependencies": "don't ask about patches that are depended on by matched patches (with --match or --patch)"
      "--prompt-deps": ""
      "--prompt-for-dependencies": "prompt about patches that are depended on by matched patches [DEFAULT]"
      "--set-default": "set default repository"
      "--no-set-default": "don't set default repository"
      "--inherit-default": "inherit default repository"
      "--no-inherit-default": "don't inherit default repository [DEFAULT]"
      "--repodir=": "specify the repository directory in which to run"
      "-o, --output=": "specify output filename"
      "-O, --output-auto-name=": "output to automatically named file in DIRECTORY, default: current directory"
      "--ignore-unrelated-repos": "do not check if repositories are unrelated"
      "--myers": "use myers diff algorithm"
      "--patience": "use patience diff algorithm [DEFAULT]"
      "--help": "show a description of the command and its options"
      "--list-options": "show plain list of available options and commands, for auto-completion"
      "--disable": "disable this command"
      "-q, --quiet": "suppress informational output"
      "--standard-verbosity": "neither verbose nor quiet output [DEFAULT]"
      "-v, --verbose": "enable verbose output"
      "--intersection": "take intersection of all repositories"
      "--union": "take union of all repositories [DEFAULT]"
      "--complement": "take complement of repositories (in order listed)"
      "--remote-darcs=": "name of the darcs executable on the remote server"
      "--no-cache": "don't use patch caches"
      "--ignore-times": "don't trust the file modification times"
      "--no-ignore-times": "trust modification times to find modified files [DEFAULT]"
      "--prehook=": "specify command to run before this darcs command"
      "--no-prehook": "don't run prehook command [DEFAULT]"
      "--prompt-prehook": "prompt before running prehook"
      "--run-prehook": "run prehook command without prompting [DEFAULT]"
      "--posthook=": "specify command to run after this darcs command"
      "--no-posthook": "don't run posthook command [DEFAULT]"
      "--prompt-posthook": "prompt before running posthook"
      "--run-posthook": "run posthook command without prompting [DEFAULT]"
      "--debug": "enable general debug output"
      "--timings": "provide debugging timings information"
