# Converted from argc's bfs.sh by tools/argc-to-spec. Do not edit by hand.
name: bfs
flags:
  "-H": "Follow symbolic links on the command line, but not while searching."
  "-L": "Follow all symbolic links."
  "-P": "Never follow symbolic links (the default)."
  "-E": "Use extended regular expressions (same as -regextype posix-extended)."
  "-X": "Filter out files with non-xargs(1)-safe names."
  "-d": "Search in post-order (same as -depth)."
  "-s": "Visit directory entries in sorted order."
  "-x": "Don't descend into other mount points (same as -xdev)."
  "-f=": "Treat PATH as a path to search (useful if it begins with a dash)."
  "-D=": "Turn on a debugging flag (see -D help)."
  "-O=": "Enable optimization level N (default: 3)."
  "-S=": "Choose the search strategy."
  "-jN": "Search with N threads in parallel (default: number of CPUs, up to 8)."
  "-not=": "The \"not\" operator: returns the negation of the truth value of the expression."
  "-exclude=": "Exclude all paths matching the expression from the search."
  "-help": "Print usage information, and exit immediately (without parsing the rest of the command line or processing any files)."
  "--help": "Print usage information, and exit immediately (without parsing the rest of the command line or processing any files)."
  "-version": "Print version information, and exit immediately."
  "--version": "Print version information, and exit immediately."
  "-color": "Turn colors on or off (default: -color if outputting to a terminal, -nocolor otherwise)."
  "-nocolor": "Turn colors on or off (default: -color if outputting to a terminal, -nocolor otherwise)."
  "-daystart": "Measure time relative to the start of today."
  "-follow": "Follow all symbolic links (same as -L)."
  "-files0-from=": "Treat the NUL ('\\0')-separated paths in FILE as starting points for the search."
  "-ignore_readdir_race": "Whether to report an error if bfs detects that the file tree is modified during the search (default: -noignore_readdir_race)."
  "-noignore_readdir_race": "Whether to report an error if bfs detects that the file tree is modified during the search (default: -noignore_readdir_race)."
  "-maxdepth=": "Ignore files deeper/shallower than N."
  "-mindepth=": "Ignore files deeper/shallower than N."
  "-mount": "Don't descend into other mount points (same as -xdev for now, but will skip mount points entirely in the future)."
  "-nohidden": "Exclude hidden files and directories."
  "-noleaf": "Ignored; for compatibility with GNU find."
  "-regextype=": "Use TYPE-flavored regular expressions."
  "-status": "Display a status bar while searching."
  "-unique": "Skip any files that have already been seen."
  "-warn": "Turn on or off warnings about the command line."
  "-nowarn": "Turn on or off warnings about the command line."
  "-xdev": "Don't descend into other mount points."
  "-acl": "Find files with a non-trivial Access Control List (acl(5))."
  "-amin=": "Find files accessed/Birthed/changed/modified N minutes ago."
  "-Bmin=": "Find files accessed/Birthed/changed/modified N minutes ago."
  "-cmin=": "Find files accessed/Birthed/changed/modified N minutes ago."
  "-mmin=": "Find files accessed/Birthed/changed/modified N minutes ago."
  "-anewer=": "Find files accessed/Birthed/changed/modified more recently than FILE was modified."
  "-Bnewer=": "Find files accessed/Birthed/changed/modified more recently than FILE was modified."
  "-cnewer=": "Find files accessed/Birthed/changed/modified more recently than FILE was modified."
  "-mnewer=": "Find files accessed/Birthed/changed/modified more recently than FILE was modified."
  "-asince=": "Find files accessed/Birthed/changed/modified more recently than the ISO 8601-style timestamp TIME."
  "-Bsince=": "Find files accessed/Birthed/changed/modified more recently than the ISO 8601-style timestamp TIME."
  "-csince=": "Find files accessed/Birthed/changed/modified more recently than the ISO 8601-style timestamp TIME."
  "-msince=": "Find files accessed/Birthed/changed/modified more recently than the ISO 8601-style timestamp TIME."
  "-atime=": "Find files accessed/Birthed/changed/modified N days ago."
  "-Btime=": "Find files accessed/Birthed/changed/modified N days ago."
  "-ctime=": "Find files accessed/Birthed/changed/modified N days ago."
  "-mtime=": "Find files accessed/Birthed/changed/modified N days ago."
  "-capable": "Find files with POSIX.1e capabilities(7) set."
  "-depth=": "Find files with depth N."
  "-empty": "Find empty files/directories."
  "-executable": "Find files the current user can execute/read/write."
  "-readable": "Find files the current user can execute/read/write."
  "-writable": "Find files the current user can execute/read/write."
  "-false": "Always false/true."
  "-true": "Always false/true."
  "-fstype=": "Find files on file systems with the given TYPE."
  "-gid=": "Find files owned by group/user ID N."
  "-uid=": "Find files owned by group/user ID N."
  "-group=": "Find files owned by the group/user NAME."
  "-user=": "Find files owned by the group/user NAME."
  "-hidden": "Find hidden files (those beginning with .)."
  "-ilname=": "Case-insensitive versions of -lname/-name/-path/-regex/-wholename."
  "-iname=": "Case-insensitive versions of -lname/-name/-path/-regex/-wholename."
  "-ipath=": "Case-insensitive versions of -lname/-name/-path/-regex/-wholename."
  "-iregex=": "Case-insensitive versions of -lname/-name/-path/-regex/-wholename."
  "-iwholename=": "Case-insensitive versions of -lname/-name/-path/-regex/-wholename."
  "-inum=": "Find files with inode number N."
  "-links=": "Find files with N hard links."
  "-lname=": "Find symbolic links whose target matches the GLOB."
  "-name=": "Find files whose name matches the GLOB."
  "-newer=": "Find files newer than FILE."
  "-newerXY=": "Find files whose X time is newer than the Y time of REFERENCE."
  "-nogroup": "Find files owned by nonexistent groups/users."
  "-nouser": "Find files owned by nonexistent groups/users."
  "-path=": "Find files whose entire path matches the GLOB."
  "-wholename=": "Find files whose entire path matches the GLOB."
  "-perm=": "Find files with a matching mode."
  "-regex=": "Find files whose entire path matches the regular expression REGEX."
  "-samefile=": "Find hard links to FILE."
  "-since=": "Find files modified since the ISO 8601-style timestamp TIME."
  "-size=": "Find files with the given size."
  "-sparse": "Find files that occupy fewer disk blocks than expected."
  "-type=*": {description: "Find files of the given type.", nargs: -1}
  "-used=": "Find files last accessed N days after they were changed."
  "-xattr": "Find files with extended attributes (xattr(7))."
  "-xattrname=": "Find files with the extended attribute NAME."
  "-xtype=": "Find files of the given type, following links when -type would not, and vice versa."
  "-delete": "Delete any found files (implies -depth)."
  "-rm": "Delete any found files (implies -depth)."
  "-exec=": {description: "command ... {} ; Execute a command.", nargs: -1}
  "-ok=": {description: "command ... {} ; Prompt the user whether to execute a command.", nargs: -1}
  "-execdir=": {description: "command ... {} ;, -execdir command ... {} +, -okdir command ... {} ; Like -exec/-ok, but run the command in the same directory as the found file(s).", nargs: -1}
  "-exit=": "Exit immediately with the given status (0 if unspecified)."
  "-fls=": {description: "Like -ls/-print/-print0/-printf, but write to FILE instead of standard output.", nargs: -1}
  "-fprint=": {description: "Like -ls/-print/-print0/-printf, but write to FILE instead of standard output.", nargs: -1}
  "-fprint0=": {description: "Like -ls/-print/-print0/-printf, but write to FILE instead of standard output.", nargs: -1}
  "-fprintf=": {description: "Like -ls/-print/-print0/-printf, but write to FILE instead of standard output.", nargs: -1}
  "-ls": "List files like ls -dils."
  "-print": "Print the path to the found file."
  "-print0": "Like -print, but use the null character ('\\0') as a separator rather than newlines."
  "-printf=": "Print according to a format string (see find(1))."
  "-printx": "Like -print, but escape whitespace and quotation characters, to make the output safe for xargs(1)."
  "-prune": "Don't descend into this directory."
  "-quit": "Quit immediately."
completion:
  flag:
    S: [bfs, dfs, ids, eds]
