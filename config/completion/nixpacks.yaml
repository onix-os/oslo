# Converted from argc's nixpacks.sh by tools/argc-to-spec. Do not edit by hand.
name: nixpacks
flags:
  "--json-plan=": "Specify an entire build plan in json format that should be used to configure the build"
  "-i, --install-cmd=": "Specify the install command to use"
  "-b, --build-cmd=": "Specify the build command to use"
  "-s, --start-cmd=": "Specify the start command to use"
  "-p, --pkgs=": "Provide additional nix packages to install in the environment"
  "-a, --apt=": "Provide additional apt packages to install in the environment"
  "--libs=": "Provide additional nix libraries to install in the environment"
  "-e, --env=": "Provide environment variables to your build"
  "-c, --config=": "Path to config file"
  "-h, --help": "Print help information"
  "-V, --version": "Print version information"
commands:
  - name: plan
    description: "Generate a build plan for an app"
    flags:
      "-f, --format=": "Specify the output format of the build plan [default: json]"
      "--json-plan=": "Specify an entire build plan in json format that should be used to configure the build"
      "-i, --install-cmd=": "Specify the install command to use"
      "-b, --build-cmd=": "Specify the build command to use"
      "-s, --start-cmd=": "Specify the start command to use"
      "-p, --pkgs=": "Provide additional nix packages to install in the environment"
      "-a, --apt=": "Provide additional apt packages to install in the environment"
      "--libs=": "Provide additional nix libraries to install in the environment"
      "-e, --env=": "Provide environment variables to your build"
      "-c, --config=": "Path to config file"
      "-h, --help": "Print help information"
      "-V, --version": "Print version information"
    completion:
      flag:
        format: [json, toml]
  - name: detect
    description: "List all of the providers that will be used to build the app"
    flags:
      "--json-plan=": "Specify an entire build plan in json format that should be used to configure the build"
      "-i, --install-cmd=": "Specify the install command to use"
      "-b, --build-cmd=": "Specify the build command to use"
      "-s, --start-cmd=": "Specify the start command to use"
      "-p, --pkgs=": "Provide additional nix packages to install in the environment"
      "-a, --apt=": "Provide additional apt packages to install in the environment"
      "--libs=": "Provide additional nix libraries to install in the environment"
      "-e, --env=": "Provide environment variables to your build"
      "-c, --config=": "Path to config file"
      "-h, --help": "Print help information"
      "-V, --version": "Print version information"
  - name: build
    description: "Build an app"
    flags:
      "-n, --name=": "Name for the built image"
      "-o, --out=": "Save output directory instead of building it with Docker"
      "--json-plan=": "Specify an entire build plan in json format that should be used to configure the build"
      "-t, --tag=": "Additional tags to add to the output image"
      "-i, --install-cmd=": "Specify the install command to use"
      "-l, --label=": "Additional labels to add to the output image"
      "-b, --build-cmd=": "Specify the build command to use"
      "--platform=": "Set target platform for your output image"
      "--cache-key=": "Unique identifier to key cache by."
      "-s, --start-cmd=": "Specify the start command to use"
      "--current-dir": "Output Nixpacks related files to the current directory"
      "-p, --pkgs=": "Provide additional nix packages to install in the environment"
      "-a, --apt=": "Provide additional apt packages to install in the environment"
      "--no-cache": "Disable building with the cache"
      "--incremental-cache-image=": "Image to hold the cached directories between builds"
      "--libs=": "Provide additional nix libraries to install in the environment"
      "--cache-from=": "Image to consider as cache sources"
      "-e, --env=": "Provide environment variables to your build"
      "-c, --config=": "Path to config file"
      "--docker-host=": "Specify host for Docker client"
      "--add-host=": "Adds hosts to the Docker build"
      "--docker-tls-verify=": "Specify if Docker client should verify the TLS (Transport Layer Security) certificates"
      "--docker-output=": "Specify output destination for Docker build."
      "--inline-cache": "Enable writing cache metadata into the output image"
      "--no-error-without-start": "Do not error when no start command can be found"
      "--cpu-quota=": "Limit the CPU CFS (Completely Fair Scheduler) quota."
      "--memory=": "Memory limit."
      "-v, --verbose": "Display more info during build"
      "-h, --help": "Print help information"
      "-V, --version": "Print version information"
