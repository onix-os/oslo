# Converted from Fig's hx.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: hx
description: "A post-modern text editor"
flags:
  "-h, --help": "Show help"
  "--tutor": "Open the tutorial"
  "--health?": "Check for errors in editor setup"
  "-v*": "Increases logging verbosity"
  "-g, --grammar=": "Fetch or build tree-sitter grammars"
  "-V, --version": "Print version information"
completion:
  flag:
    grammar: [fetch, build]
  positionalany: ["$files", "$directories"]
