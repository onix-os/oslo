# Converted from argc's mysqldump.sh by tools/argc-to-spec. Do not edit by hand.
name: mysqldump
flags:
  "--print-defaults": "Print the program argument list and exit."
  "--no-defaults": "Don't read default options from any option file."
  "--defaults-file=": "Only read default options from the given file ♯."
  "--defaults-extra-file=": "Read this file after the global files are read."
  "--defaults-group-suffix=": "Additionally read default groups with ♯ appended as a suffix."
  "-A, --all-databases": "Dump all the databases."
  "-Y, --all-tablespaces": "Dump all the tablespaces."
  "-y, --no-tablespaces": "Do not dump any tablespace information."
  "--add-drop-database": "Add a DROP DATABASE before each create."
  "--add-drop-table": "Add a DROP TABLE before each create."
  "--add-drop-trigger": "Add a DROP TRIGGER before each create."
  "--add-locks": "Add locks around INSERT statements."
  "--allow-keywords": "Allow creation of column names that are keywords."
  "--apply-slave-statements": "Adds 'STOP SLAVE' prior to 'CHANGE MASTER' and 'START SLAVE' to bottom of dump."
  "--as-of=": "Dump system versioned table(s) as of specified timestamp."
  "--character-sets-dir=": "Directory for character set files."
  "-i, --comments": "Write additional information."
  "--compatible=": "Change the dump to be compatible with a given mode."
  "--compact": "Give less verbose output (useful for debugging)."
  "-c, --complete-insert": "Use complete insert statements."
  "-C, --compress": "Use compression in server/client protocol."
  "--copy-s3-tables": "If 'no' S3 tables will be ignored, otherwise S3 tables will be copied as Aria tables and then altered to S3"
  "-a, --create-options": "Include all MariaDB specific create options."
  "-B, --databases": "Dump several databases."
  "-#, --debug=": "This is a non-debug version."
  "--debug-check": "Check memory and open file usage at exit."
  "--debug-info": "Print some debug info at exit."
  "--default-character-set=": "Set the default character set."
  "--delayed-insert": "Insert rows with INSERT DELAYED."
  "--delete-master-logs": "Delete logs on master after backup."
  "-K, --disable-keys": "'/*!40000 ALTER TABLE tb_name DISABLE KEYS */; and '/*!40000 ALTER TABLE tb_name ENABLE KEYS */; will be put in the output."
  "--dump-date": "Put a dump date to the end of the output."
  "-H, --dump-history": "Dump system-versioned tables with history (only for timestamp based versioning)"
  "--dump-slave=": "This causes the binary log position and filename of the master to be appended to the dumped data output."
  "-E, --events": "Dump events."
  "-e, --extended-insert": "Use multiple-row INSERT syntax that include several VALUES lists."
  "--fields-terminated-by=": "Fields in the output file are terminated by the given string."
  "--fields-enclosed-by=": "Fields in the output file are enclosed by the given character."
  "--fields-optionally-enclosed-by=": "Fields in the output file are optionally enclosed by the given character."
  "--fields-escaped-by=": "Fields in the output file are escaped by the given character."
  "-F, --flush-logs": "Flush logs file in server before starting dump."
  "--flush-privileges": "Emit a FLUSH PRIVILEGES statement after dumping the mysql database."
  "-f, --force": "Continue even if we get an SQL error."
  "--gtid": "Used together with --master-data=1 or --dump-slave=1.When enabled, the output from those options will set the GTID position instead of the binlog file and offset; the file/offset will appear only as a comment."
  "--header": "Used together with --tab."
  "-?, --help": "Display this help message and exit."
  "--hex-blob=": "Dump binary strings in hexadecimal format."
  "-h, --host=": "Connect to host."
  "--ignore-database=": "Do not dump the specified database."
  "--ignore-table-data=": "Do not dump the specified table data."
  "--ignore-table=": "Do not dump the specified table."
  "--include-master-host-port": "Adds 'MASTER_HOST=<host>, MASTER_PORT=<port>' to 'CHANGE MASTER TO..' in dump produced with --dump-slave."
  "--insert-ignore": "Insert rows with INSERT IGNORE."
  "--lines-terminated-by=": "Lines in the output file are terminated by the given string."
  "-x, --lock-all-tables": "Locks all tables across all databases."
  "-l, --lock-tables": "Lock all tables for read."
  "--log-error=": "Append warnings and errors to given file."
  "--log-queries": "When restoring the dump, the server will, if logging turned on, log the queries to the general and slow query log."
  "--master-data=": "This causes the binary log position and filename to be appended to the output."
  "--max-allowed-packet=": "The maximum packet length to send to or receive from server."
  "--max-statement-time=": "Max statement execution time."
  "--net-buffer-length=": "The buffer size for TCP/IP and socket communication."
  "--no-autocommit": "Wrap tables with autocommit/commit statements."
  "-n, --no-create-db": "Suppress the CREATE DATABASE ... IF EXISTS statement that normally is output for each dumped database if --all-databases or --databases is given."
  "-t, --no-create-info": "Don't write table creation info."
  "-d, --no-data": "No row information."
  "--no-data-med=": "No row information for engines that Manage External Data."
  "-N, --no-set-names": "Same as --skip-set-charset."
  "--opt": "Same as --add-drop-table, --add-locks, --create-options, --quick, --extended-insert, --lock-tables, --set-charset, and --disable-keys."
  "--order-by-primary": "Sorts each table's rows by primary key, or first unique key, if such a key exists."
  "--order-by-size": "Dump tables in the order of their size, smaller first."
  "-p, --password=": "Password to use when connecting to server."
  "-P, --port=": "Port number to use for connection."
  "--protocol=": "The protocol to use for connection."
  "-q, --quick": "Don't buffer query, dump directly to stdout."
  "-Q, --quote-names": "Quote table and column names with backticks (`)."
  "--replace": "Use REPLACE INTO instead of INSERT INTO."
  "-r, --result-file=": "Direct output to a given file."
  "-R, --routines": "Dump stored routines (functions and procedures)."
  "--set-charset": "Add 'SET NAMES default_character_set' to the output."
  "--single-transaction": "Creates a consistent snapshot by dumping all tables in a single transaction."
  "--skip-opt": "Disable --opt."
  "-S, --socket=": "The socket file to use for connection."
  "--ssl": "Enable SSL for connection (automatically enabled with other flags)."
  "--ssl-ca=": "CA file in PEM format (check OpenSSL docs, implies --ssl)."
  "--ssl-capath=": "CA directory (check OpenSSL docs, implies --ssl)."
  "--ssl-cert=": "X509 cert in PEM format (implies --ssl)."
  "--ssl-cipher=": "SSL cipher to use (implies --ssl)."
  "--ssl-key=": "X509 key in PEM format (implies --ssl)."
  "--ssl-crl=": "Certificate revocation list (implies --ssl)."
  "--ssl-crlpath=": "Certificate revocation list path (implies --ssl)."
  "--tls-version=": "TLS protocol version for secure connection."
  "--ssl-verify-server-cert": "Verify server's \"Common Name\" in its cert against hostname used when connecting."
  "--system=": "Dump system tables as portable SQL."
  "-T, --tab=": "Create tab-separated textfile for each table to given path."
  "--tables": "Overrides option --databases (-B)."
  "--triggers": "Dump triggers for each dumped table."
  "--tz-utc": "Set connection time zone to UTC before commencing the dump and add SET TIME_ZONE=´+00:00´ to the top of the dump file."
  "-u, --user=": "User for login if not current user."
  "-v, --verbose": "Print info about the various stages."
  "-V, --version": "Output version information and exit."
  "-w, --where=": "Dump only selected records."
  "-X, --xml": "Dump a database as well formed XML."
  "--plugin-dir=": "Directory for client-side plugins."
  "--default-auth=": "Default authentication client-side plugin to use."
completion:
  flag:
    compatible: [ansi, mysql323, mysql40, postgresql, oracle, mssql, db2, maxdb, no_key_options, no_table_options, no_field_options]
    hex-blob: [BINARY, VARBINARY, BLOB]
    no-data-med: [MRG_MyISAM, MRG_ISAM, CONNECT, OQGRAPH, SPIDER, VP, FEDERATED]
    protocol: [tcp, socket, pipe]
    system: [all, users, plugins, udfs, servers, stats, timezones]
