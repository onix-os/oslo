# Converted from argc's rga.sh by tools/argc-to-spec. Do not edit by hand.
name: rga
flags:
  "-e, --regexp=": "A pattern to search for."
  "-f, --file=": "Search for patterns from the given file, with one pattern per line."
  "--pre=": "For each input PATH, this flag causes ripgrep to search the standard output of COMMAND PATH instead of the contents of PATH."
  "--pre-glob=": "This flag works in conjunction with the --pre flag."
  "-z, --search-zip": "This flag instructs ripgrep to search in compressed files."
  "-s, --case-sensitive": "Execute the search case sensitively."
  "--crlf": "When enabled, ripgrep will treat CRLF (\\r\\n) as a line terminator instead of just \\n."
  "--dfa-size-limit=": "The upper size limit of the regex DFA."
  "-E, --encoding=": "Specify the text encoding that ripgrep will use on all files searched."
  "--engine=": "Specify which regular expression engine to use."
  "-F, --fixed-strings": "Treat all patterns as literals instead of as regular expressions."
  "-i, --ignore-case": "When this flag is provided, all patterns will be searched case insensitively."
  "-v, --invert-match": "This flag inverts matching."
  "-x, --line-regexp": "When enabled, ripgrep will only show matches surrounded by line boundaries."
  "-m, --max-count=": "Limit the number of matching lines per file searched to NUM."
  "--mmap": "When enabled, ripgrep will search using memory maps when possible."
  "-U, --multiline": "This flag enable searching across multiple lines."
  "--multiline-dotall": "This flag enables \"dot all\" mode in all regex patterns."
  "--no-unicode": "This flag disables Unicode mode for all patterns given to ripgrep."
  "--null-data": "Enabling this flag causes ripgrep to use NUL as a line terminator instead of the default of \\n."
  "-P, --pcre2": "When this flag is present, ripgrep will use the PCRE2 regex engine instead of its default regex engine."
  "--regex-size-limit=": "The size limit of the compiled regex, where the compiled regex generally corresponds to a single object in memory that can match all of the patterns provided to ripgrep."
  "-S, --smart-case": "This flag instructs ripgrep to searches case insensitively if the pattern is all lowercase."
  "--stop-on-nonmatch": "Enabling this option will cause ripgrep to stop reading a file once it encounters a non-matching line after it has encountered a matching line."
  "-a, --text": "This flag instructs ripgrep to search binary files as if they were text."
  "-j, --threads=": "This flag sets the approximate number of threads to use."
  "-w, --word-regexp": "When enabled, ripgrep will only show matches surrounded by word boundaries."
  "--auto-hybrid-regex": DEPRECATED.
  "--no-pcre2-unicode": DEPRECATED.
  "--binary": "Enabling this flag will cause ripgrep to search binary files."
  "-L, --follow": "This flag instructs ripgrep to follow symbolic links while traversing directories."
  "-g, --glob=": "Include or exclude files and directories for searching that match the given glob."
  "--glob-case-insensitive": "Process all glob patterns given with the -g/--glob flag case insensitively."
  "-., --hidden": "Search hidden files and directories."
  "--iglob=": "Include or exclude files and directories for searching that match the given glob."
  "--ignore-file=": "Specifies a path to one or more gitignore formatted rules files."
  "--ignore-file-case-insensitive": "Process ignore files (.gitignore, .ignore, etc.) case insensitively."
  "-d, --max-depth=": "This flag limits the depth of directory traversal to NUM levels beyond the paths given."
  "--max-filesize=": "Ignore files larger than NUM in size."
  "--no-ignore": "When set, ignore files such as .gitignore, .ignore and .rgignore will not be respected."
  "--no-ignore-dot": "Don't respect filter rules from .ignore or .rgignore files."
  "--no-ignore-exclude": "Don't respect filter rules from files that are manually configured for the repository."
  "--no-ignore-files": "When set, any --ignore-file flags, even ones that come after this flag, are ignored."
  "--no-ignore-global": "Don't respect filter rules from ignore files that come from \"global\" sources such as git's core.excludesFile configuration option (which defaults to $HOME/.config/git/ignore)."
  "--no-ignore-parent": "When this flag is set, filter rules from ignore files found in parent directories are not respected."
  "--no-ignore-vcs": "When given, filter rules from source control ignore files (e.g., .gitignore) are not respected."
  "--no-require-git": "When this flag is given, source control ignore files such as .gitignore are respected even if no git repository is present."
  "--one-file-system": "When enabled, ripgrep will not cross file system boundaries relative to where the search started from."
  "-t, --type=": "This flag limits ripgrep to searching files matching TYPE."
  "-T, --type-not=": "Do not search files matching TYPE."
  "--type-add=": "This flag adds a new glob for a particular file type."
  "--type-clear=": "Clear the file type globs previously defined for TYPE."
  "-u, --unrestricted": "This flag reduces the level of \"smart\" filtering."
  "-A, --after-context=": "Show NUM lines after each match."
  "-B, --before-context=": "Show NUM lines before each match."
  "--block-buffered": "When enabled, ripgrep will use block buffering."
  "-b, --byte-offset": "Print the 0-based byte offset within the input file before each line of output."
  "--color=": "This flag controls when to use colors."
  "--colors=": "This flag specifies color settings for use in the output."
  "--column": "Show column numbers (1-based)."
  "-C, --context=": "Show NUM lines before and after each match."
  "--context-separator=": "The string used to separate non-contiguous context lines in the output."
  "--field-context-separator=": "Set the field context separator."
  "--field-match-separator=": "Set the field match separator."
  "--heading": "This flag prints the file path above clusters of matches from each file instead of printing the file path as a prefix for each matched line."
  "--hostname-bin=": "This flag controls how ripgrep determines this system's hostname."
  "--hyperlink-format=": "Set the format of hyperlinks to use when printing results."
  "--include-zero": "When used with -c/--count or --count-matches, this causes ripgrep to print the number of matches for each file even if there were zero matches."
  "--line-buffered": "When enabled, ripgrep will always use line buffering."
  "-n, --line-number": "Show line numbers (1-based)."
  "-N, --no-line-number": "Suppress line numbers."
  "-M, --max-columns=": "When given, ripgrep will omit lines longer than this limit in bytes."
  "--max-columns-preview": "Prints a preview for lines exceeding the configured max column limit."
  "-0, --null": "Whenever a file path is printed, follow it with a NUL byte."
  "-o, --only-matching": "Print only the matched (non-empty) parts of a matching line, with each such part on a separate output line."
  "--path-separator=": "Set the path separator to use when printing file paths."
  "--passthru": "Print both matching and non-matching lines."
  "-p, --pretty": "This is a convenience alias for --color=always --heading --line-number."
  "-q, --quiet": "Do not print anything to stdout."
  "-r, --replace=": "Replaces every match with the text given when printing results."
  "--sort=": "This flag enables sorting of results in ascending order."
  "--sortr=": "This flag enables sorting of results in descending order."
  "--trim": "When set, all ASCII whitespace at the beginning of each line printed will be removed."
  "--vimgrep": "This flag instructs ripgrep to print results with every match on its own line, including line numbers and column numbers."
  "-H, --with-filename": "This flag instructs ripgrep to print the file path for each matching line."
  "-I, --no-filename": "This flag instructs ripgrep to never print the file path with each matching line."
  "--sort-files": DEPRECATED.
  "-c, --count": "This flag suppresses normal output and shows the number of lines that match the given patterns for each file searched."
  "--count-matches": "This flag suppresses normal output and shows the number of individual matches of the given patterns for each file searched."
  "-l, --files-with-matches": "Print only the paths with at least one match and suppress match contents."
  "--files-without-match": "Print the paths that contain zero matches and suppress match contents."
  "--json": "Enable printing results in a JSON Lines format."
  "--debug": "Show debug messages."
  "--no-ignore-messages": "When this flag is enabled, all error messages related to parsing ignore files are suppressed."
  "--no-messages": "This flag suppresses some error messages."
  "--stats": "When enabled, ripgrep will print aggregate statistics about the search."
  "--trace": "Show trace messages."
  "--files": "Print each file that would be searched without actually performing the search."
  "--generate=": "This flag instructs ripgrep to generate some special kind of output identified by KIND and then quit without searching."
  "--no-config": "When set, ripgrep will never read configuration files."
  "--pcre2-version": "When this flag is present, ripgrep will print the version of PCRE2 in use, along with other information, and then exit."
  "--type-list": "Show all supported file types and their corresponding globs."
  "--rga-accurate": "Use more accurate but slower matching by mime type"
  "--rga-no-cache": "Disable caching of results"
  "-h, --help": "Prints help information"
  "--rga-list-adapters": "List all known adapters"
  "--rga-no-prefix-filenames": "Don't prefix lines of files within archive with the path inside the archive."
  "--rga-print-config-schema": "Print the JSON Schema of the configuration file"
  "--rg-help": "Show help for ripgrep itself"
  "--rg-version": "Show version of ripgrep itself"
  "-V, --version": "Prints version information"
  "--rga-adapters=*": {description: "Change which adapters to use and in which priority order (descending)", nargs: -1}
  "--rga-cache-compression-level=": "ZSTD compression level to apply to adapter outputs before storing in cache db"
  "--rga-config-file=": ""
  "--rga-max-archive-recursion=": "Maximum nestedness of archives to recurse into"
  "--rga-cache-max-blob-len=": "Max compressed size to cache"
  "--rga-cache-path=": "Path to store cache db [default: /home/sigo/.cache/ripgrep-all]"
