# Converted from argc's icdiff.sh by tools/argc-to-spec. Do not edit by hand.
name: icdiff
flags:
  "--version": "show program's version number and exit"
  "-h, --help": "show this help message and exit"
  "--cols=": "specify the width of the screen."
  "--encoding=": "specify the file encoding; defaults to utf8"
  "-E, --exclude-lines=": "Do not diff lines that match this regex."
  "--head=": "consider only the first N lines of each file"
  "-H, --highlight": "color by changing the background color instead of the foreground color."
  "-L, --label=": "override file labels with arbitrary tags."
  "-N, --line-numbers": "generate output with line numbers."
  "--no-bold": "use non-bold colors; recommended for solarized"
  "--no-headers": "don't label the left and right sides with their file names"
  "--output-encoding=": "specify the output encoding; defaults to utf8"
  "-r, --recursive": "recursively compare subdirectories"
  "-s, --report-identical-files": "report when two files are the same"
  "--show-all-spaces": "color all non-matching whitespace including that which is not needed for drawing the eye to changes."
  "--show-no-spaces": "don't color whitespace-only changes"
  "--tabsize=": "tab stop spacing"
  "-t, --truncate": "truncate long lines instead of wrapping them"
  "-u, --patch": "generate patch."
  "-U=": "how many lines of context to print; can't be combined with --whole-file"
  "--unified=": "how many lines of context to print; can't be combined with --whole-file"
  "--numlines=": "how many lines of context to print; can't be combined with --whole-file"
  "-W, --whole-file": "show the whole file instead of just changed lines and context"
  "-P, --permissions": "compare the file permissions as well as the content of the file"
  "--strip-trailing-cr": "strip any trailing carriage return at the end of an input line"
  "--color-map=": "choose which colors are used for which items."
  "--is-git-diff": "Show the real file name when displaying git-diff result"
  "-x, --exclude=": "exclude files that match PAT"
