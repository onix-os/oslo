# Converted from argc's lychee.sh by tools/argc-to-spec. Do not edit by hand.
name: lychee
flags:
  "-c, --config=": "Configuration file to use"
  "-v, --verbose*": "Set verbosity level; more output per occurrence (e.g. `-v` or `-vv`)"
  "-q, --quiet*": "Less output per occurrence (e.g. `-q` or `-qq`)"
  "-n, --no-progress": "Do not show progress bar."
  "--cache": "Use request cache stored on disk at `.lycheecache`"
  "--max-cache-age=": "Discard all cached requests older than this duration"
  "--dump": "Don't perform any link checking."
  "--dump-inputs": "Don't perform any link extraction and checking."
  "--archive=": "Specify the use of a specific web archive."
  "--suggest": "Suggest link replacements for broken links, using a web archive."
  "-m, --max-redirects=": "Maximum number of allowed redirects"
  "--max-retries=": "Maximum number of retries per request"
  "--max-concurrency=": "Maximum number of concurrent network requests"
  "-T, --threads=": "Number of threads to utilize."
  "-u, --user-agent=": "User agent"
  "-i, --insecure": "Proceed for server connections considered insecure (invalid TLS)"
  "-s, --scheme=": "Only test links with the given schemes (e.g. https)."
  "--offline": "Only check local files and block network requests"
  "--include=": "URLs to check (supports regex)."
  "--exclude=": "Exclude URLs and mail addresses from checking (supports regex)"
  "--exclude-file=": "Deprecated; use `--exclude-path` instead"
  "--exclude-path=": "Exclude file path from getting checked"
  "-E, --exclude-all-private": "Exclude all private IPs from checking."
  "--exclude-private": "Exclude private IP address ranges from checking"
  "--exclude-link-local": "Exclude link-local IP address range from checking"
  "--exclude-loopback": "Exclude loopback IP address range and localhost from checking"
  "--exclude-mail": "Exclude all mail addresses from checking (deprecated; excluded by default)"
  "--include-mail": "Also check email addresses"
  "--remap=": "Remap URI matching pattern to different URI"
  "--header=": "Custom request header"
  "-a, --accept=": "A List of accepted status codes for valid links"
  "--include-fragments": "Enable the checking of fragments in links"
  "-t, --timeout=": "Website timeout in seconds from connect to response finished"
  "-r, --retry-wait-time=": "Minimum wait time in seconds between retries of failed requests"
  "-X, --method=": "Request method"
  "-b, --base=": "Base URL or website root directory to check relative URLs e.g. https://example.com or `/path/to/public`"
  "--basic-auth=": "Basic authentication support."
  "--github-token=": "GitHub API token to use when checking github.com links, to avoid rate limiting"
  "--skip-missing": "Skip missing input files (default is to error if they don't exist)"
  "--include-verbatim": "Find links in verbatim sections like `pre`- and `code` blocks"
  "--glob-ignore-case": "Ignore case when expanding filesystem path glob inputs"
  "-o, --output=": "Output file of status report"
  "-f, --format=": "Output format of final status report"
  "--require-https": "When HTTPS is available, treat HTTP links as errors"
  "--cookie-jar=": "Tell lychee to read cookies from the given file."
  "-h, --help": "Print help (see a summary with '-h')"
  "-V, --version": "Print version"
completion:
  flag:
    archive: [wayback]
    format: [compact, detailed, json, markdown]
