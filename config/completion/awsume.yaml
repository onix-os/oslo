# Converted from Fig's awsume.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: awsume
description: Awsume
flags:
  "--help, -h": "Show help for awsume"
  "--version, -v": "Display the current version of awsume"
  "--output-profile, -o=": "A profile to output credentials to"
  "--clean": "Clean expired output profiles"
  "--refresh, -r": "Force refresh credentials"
  "--show-commands, -s": "Show the commands to set the credentials"
  "--unset, -u": "Unset your aws environment variables"
  "--auto-refresh, -a": "Auto refresh credentials"
  "--kill-refresher, -k": "Kill autoawsume"
  "--list-profiles, -l=": "List profiles, \"more\" for detail (slow)"
  "--refresh-autocomplete": "Refresh all plugin autocomplete profiles"
  "--role-arn=": "Role ARN or <partition>:<account_id>:<role_name>"
  "--principal-arn=": "Principal ARN or <partition>:<account_id>:<provider_name>"
  "--source-profile=": "Source_profile to use(role-arn only)"
  "--external-id=": "External ID to pass to the assume_role"
  "--mfa-token=": "Your mfa token"
  "--region=": "The region you want to awsume into"
  "--session-name=": "Set a custom role session name"
  "--role-duration=": "Seconds to get role creds for"
  "--with-saml": "Use saml (requires plugin)"
  "--with-web-identity": "Use web identity (requires plugin)"
  "--json=": "Use json credentials"
  "--credentials-file=": "Target a shared credentials file"
  "--config-file=": "Target a config file"
  "--config=": {description: "Configure awsume", nargs: -1}
  "--list-plugins": "List installed plugins"
  "--info": "Print any info logs to stderr"
  "--debug": "Print any debug logs to stderr"
  "--console, -c": "Open AWS console"
  "--console-link, -cl": "Get a sign-on url"
  "--console-service, -cs=": "Open the console to a specific service"
  "-cls, -csl=": "Get a sign-on url to a specific service"
