# Converted from argc's msfdb.sh by tools/argc-to-spec. Do not edit by hand.
name: msfdb
flags:
  "--component=": "Component used with provided command (default: database)"
  "-d, --debug": "Enable debug output"
  "-h, --help": "Show this help message"
  "--use-defaults": "Accept all defaults and do not prompt for options during an init"
  "--msf-db-name=": "Database name (default: msf)"
  "--msf-db-user-name=": "Database username (default: msf)"
  "--msf-test-db-name=": "Test database name (default: msftest)"
  "--msf-test-db-user-name=": "Test database username (default: msftest)"
  "--db-port=": "Database port (default: 5433)"
  "--db-pool=": "Database connection pool size (default: 200)"
  "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
  "-a, --address=": "Bind to host address (default: localhost)"
  "-p, --port=": "Web service port (default: 5443)"
  "--daemon": "Enable daemon"
  "--no-daemon": "Enable daemon"
  "--ssl": "Enable SSL (default: true)"
  "--no-ssl": "Enable SSL (default: true)"
  "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
  "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
  "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
  "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
  "--environment=": "Web service framework environment (default: production)"
  "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
  "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
  "--user=": "Initial web service admin username"
  "--pass=": "Initial web service admin password"
  "--msf-data-service=": "Local msfconsole data service connection name"
  "--no-msf-data-service=": "Local msfconsole data service connection name"
  "-V, --version": "Print version"
completion:
  flag:
    component: [database, webservice]
    environment: [production, development]
commands:
  - name: init
    description: "initialize the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: reinit
    description: "delete and reinitialize the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: delete
    description: "delete and stop the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: status
    description: "check component status"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: start
    description: "start the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: stop
    description: "stop the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
  - name: restart
    description: "restart the component"
    flags:
      "-h, --help": ""
    persistentflags:
      "--component=": "Component used with provided command (default: database)"
      "-d, --debug": "Enable debug output"
      "-h, --help": "Show this help message"
      "--use-defaults": "Accept all defaults and do not prompt for options during an init"
      "--msf-db-name=": "Database name (default: msf)"
      "--msf-db-user-name=": "Database username (default: msf)"
      "--msf-test-db-name=": "Test database name (default: msftest)"
      "--msf-test-db-user-name=": "Test database username (default: msftest)"
      "--db-port=": "Database port (default: 5433)"
      "--db-pool=": "Database connection pool size (default: 200)"
      "--connection-string=": "Use a pre-existing database cluster for initialization Example: --connection-string=postgresql://postgres:mysecretpassword@localhost:5432/postgres"
      "-a, --address=": "Bind to host address (default: localhost)"
      "-p, --port=": "Web service port (default: 5443)"
      "--daemon": "Enable daemon"
      "--no-daemon": "Enable daemon"
      "--ssl": "Enable SSL (default: true)"
      "--no-ssl": "Enable SSL (default: true)"
      "--ssl-key-file=": "Path to private key (default: /home/sigo/.msf4/msf-ws-key.pem)"
      "--ssl-cert-file=": "Path to certificate (default: /home/sigo/.msf4/msf-ws-cert.pem)"
      "--ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--no-ssl-disable-verify": "Disables (optional) client cert requests (default: true)"
      "--environment=": "Web service framework environment (default: production)"
      "--retry-max=": "Maximum number of web service connect attempts (default: 10)"
      "--retry-delay=": "Delay in seconds between web service connect attempts (default: 5.0)"
      "--user=": "Initial web service admin username"
      "--pass=": "Initial web service admin password"
      "--msf-data-service=": "Local msfconsole data service connection name"
      "--no-msf-data-service=": "Local msfconsole data service connection name"
    completion:
      flag:
        component: [database, webservice]
        environment: [production, development]
