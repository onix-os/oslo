# Converted from argc's ansible-vault.sh by tools/argc-to-spec. Do not edit by hand.
name: ansible-vault
flags:
  "--version": "show program's version number, config file location, configured module search path, module location, executable location and exit"
  "-h, --help": "show this help message and exit"
  "-v, --verbose": "Causes Ansible to print more debug messages."
commands:
  - name: create
    description: "Create new vault encrypted file"
    flags:
      "-h, --help": "show this help message and exit"
      "--encrypt-vault-id=": "the vault id used to encrypt (required if more than one vault-id is provided)"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
      "--skip-tty-check": "allows editor to be opened when no tty attached"
  - name: decrypt
    description: "Decrypt vault encrypted file"
    flags:
      "-h, --help": "show this help message and exit"
      "--output=": "output file name for encrypt or decrypt; use - for stdout"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
  - name: edit
    description: "Edit vault encrypted file"
    flags:
      "-h, --help": "show this help message and exit"
      "--encrypt-vault-id=": "the vault id used to encrypt (required if more than one vault-id is provided)"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
  - name: view
    description: "View vault encrypted file"
    flags:
      "-h, --help": "show this help message and exit"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
  - name: encrypt
    description: "Encrypt YAML file"
    flags:
      "-h, --help": "show this help message and exit"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
      "--output=": "output file name for encrypt or decrypt; use - for stdout"
      "--encrypt-vault-id=": "the vault id used to encrypt (required if more than one vault-id is provided)"
  - name: encrypt_string
    aliases: [encrypt-string]
    description: "Encrypt a string"
    flags:
      "-h, --help": "show this help message and exit"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
      "--output=": "output file name for encrypt or decrypt; use - for stdout"
      "--encrypt-vault-id=": "the vault id used to encrypt (required if more than one vault-id is provided)"
      "-p, --prompt": "Prompt for the string to encrypt"
      "--show-input": "Do not hide input when prompted for the string to encrypt"
      "-n, --name=": "Specify the variable name."
      "--stdin-name=": "Specify the variable name for stdin"
  - name: rekey
    description: "Re-key a vault encrypted file"
    flags:
      "-h, --help": "show this help message and exit"
      "--vault-id=": "the vault identity to use."
      "-J": "ask for vault password"
      "--ask-vault-password": "ask for vault password"
      "--ask-vault-pass": "ask for vault password"
      "--vault-password-file=": "vault password file"
      "--vault-pass-file=": "vault password file"
      "-v, --verbose": "Causes Ansible to print more debug messages."
      "--encrypt-vault-id=": "the vault id used to encrypt (required if more than one vault-id is provided)"
      "--new-vault-password-file=": "new vault password file for rekey"
      "--new-vault-id=": "the new vault identity to use for rekey"
