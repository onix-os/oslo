# Converted from Fig's bun.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: bun
description: "A fast bundler, transpiler, JavaScript Runtime and package manager for web software"
flags:
  "--inspect?": "Activate Bun's debugger for a file"
  "--inspect-wait?": "Activate Bun's Debugger, wait for a connection before executing"
  "--inspect-brk?": "Activate Bun's Debugger, set breakpoint on first line of code and wait"
  "--hot": "Enable auto reload in the Bun runtime, test runner, or bundler"
  "--watch": "Automatically restart the process on file change"
completion:
  flag:
    inspect: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
    inspect-wait: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
    inspect-brk: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
commands:
  - name: c
    aliases: [create]
    description: "Start a new project from a template"
    flags:
      "--force": "Overwrite existing files"
      "--no-install": "Don't install node_modules"
      "--no-git": "Don't create a git repository"
      "--verbose": "Too many logs"
      "--no-package-json": "Disable package.json transforms"
      "--open": "On finish, start bun and open in browser"
    completion:
      positional:
        - [react, next, hono, discord-interactions, blank, bun-bakery, react-native-app, completion-spec, next-app, t3-app, discord-bot, video, remix, "$(command ls -1 .bun-create)"]
        - ["$directories"]
  - name: run
    description: "Run a package.json script or executable"
  - name: i
    aliases: [install]
    description: "Install dependencies for a package.json"
    flags:
      "-c, --config?": "Load config (bunfig.toml)"
      "-y, --yarn": "Write a yarn.lock file (yarn v1)"
      "-p, --production": "Don't install devDependencies"
      "--no-save": "Don't save a lockfile"
      "--save": "Save to package.json"
      "--dry-run": "Don't install anything"
      "--frozen-lockfile": "Disallow changes to lockfile"
      "-f, --force": "Always request the latest versions from the registry & reinstall all dependencies"
      "--cache-dir=": "Store & load cached data from a specific directory path"
      "--no-cache": "Ignore manifest cache entirely"
      "--silent": "Don't log anything"
      "--verbose": "Excessively verbose logging"
      "-g, --global": "Install globally"
      "--cwd=": "Set a specific cwd"
      "--backend=": "Platform-specific optimizations for installing dependencies. For macOS, 'clonefile' (default), 'copyfile'"
      "--link-native-bins=": {description: "Link 'bin' from a matching platform-specific 'optionalDependencies' instead. Default: esbuild, turbo", nargs: -1}
      "--help": "Print the help menu"
      "--no-progress": "Disable the progress bar"
      "--no-summary": "Don't print a summary"
      "--no-verify": "Skip verifying integrity of newly downloaded packages"
      "--ignore-scripts": "Skip lifecycle scripts in the project's package.json (dependency scripts are never run)"
      "-d, --dev, -D, --development": "Install as devDependency"
      "--exact": "Install exact version"
      "--optional": "Install as optionalDependency"
    completion:
      flag:
        config: ["$files"]
        cache-dir: ["$directories"]
        cwd: ["$directories"]
        backend: [clonefile, copyfile]
  - name: a
    aliases: [add]
    description: "Add a dependency to package.json"
    flags:
      "-c, --config?": "Load config (bunfig.toml)"
      "-y, --yarn": "Write a yarn.lock file (yarn v1)"
      "-p, --production": "Don't install devDependencies"
      "--no-save": "Don't save a lockfile"
      "--save": "Save to package.json"
      "--dry-run": "Don't install anything"
      "--frozen-lockfile": "Disallow changes to lockfile"
      "-f, --force": "Always request the latest versions from the registry & reinstall all dependencies"
      "--cache-dir=": "Store & load cached data from a specific directory path"
      "--no-cache": "Ignore manifest cache entirely"
      "--silent": "Don't log anything"
      "--verbose": "Excessively verbose logging"
      "-g, --global": "Install globally"
      "--cwd=": "Set a specific cwd"
      "--backend=": "Platform-specific optimizations for installing dependencies. For macOS, 'clonefile' (default), 'copyfile'"
      "--link-native-bins=": {description: "Link 'bin' from a matching platform-specific 'optionalDependencies' instead. Default: esbuild, turbo", nargs: -1}
      "--help": "Print the help menu"
      "--no-progress": "Disable the progress bar"
      "--no-summary": "Don't print a summary"
      "--no-verify": "Skip verifying integrity of newly downloaded packages"
      "--ignore-scripts": "Skip lifecycle scripts in the project's package.json (dependency scripts are never run)"
      "-d, --dev, -D, --development": "Install as devDependency"
      "--exact": "Install exact version"
      "--optional": "Install as optionalDependency"
    completion:
      flag:
        config: ["$files"]
        cache-dir: ["$directories"]
        cwd: ["$directories"]
        backend: [clonefile, copyfile]
  - name: rm
    aliases: [remove]
    description: "Remove a dependency from package.json"
    flags:
      "-c, --config?": "Load config (bunfig.toml)"
      "-y, --yarn": "Write a yarn.lock file (yarn v1)"
      "-p, --production": "Don't install devDependencies"
      "--no-save": "Don't save a lockfile"
      "--save": "Save to package.json"
      "--dry-run": "Don't install anything"
      "--frozen-lockfile": "Disallow changes to lockfile"
      "-f, --force": "Always request the latest versions from the registry & reinstall all dependencies"
      "--cache-dir=": "Store & load cached data from a specific directory path"
      "--no-cache": "Ignore manifest cache entirely"
      "--silent": "Don't log anything"
      "--verbose": "Excessively verbose logging"
      "-g, --global": "Install globally"
      "--cwd=": "Set a specific cwd"
      "--backend=": "Platform-specific optimizations for installing dependencies. For macOS, 'clonefile' (default), 'copyfile'"
      "--link-native-bins=": {description: "Link 'bin' from a matching platform-specific 'optionalDependencies' instead. Default: esbuild, turbo", nargs: -1}
      "--help": "Print the help menu"
      "--no-progress": "Disable the progress bar"
      "--no-summary": "Don't print a summary"
      "--no-verify": "Skip verifying integrity of newly downloaded packages"
      "--ignore-scripts": "Skip lifecycle scripts in the project's package.json (dependency scripts are never run)"
      "-d, --dev, -D, --development": "Install as devDependency"
      "--exact": "Install exact version"
      "--optional": "Install as optionalDependency"
    completion:
      flag:
        config: ["$files"]
        cache-dir: ["$directories"]
        cwd: ["$directories"]
        backend: [clonefile, copyfile]
  - name: build
    aliases: [bun]
    description: "Bundle files using Bun's native bundler"
    flags:
      "-h, --help": "Display this help and exit"
      "-b, --bun": "Force a script or package to use Bun's runtime instead of Node.js (via symlinking node)"
      "--cwd=": ""
      "-c, --config?": "Load config (bunfig.toml)"
      "--extension-order=": {description: "", nargs: -1}
      "--jsx-factory=": "Changes the function called when compiling JSX elements using the classic JSX runtime"
      "--jsx-fragment?": "Changes the function called when compiling JSX fragments"
      "--jsx-import-source=": "Declares the module specifier to be used for importing the jsx and jsxs factory functions. Default: \"react\""
      "--jsx-runtime=": "\"automatic\" (default) or \"classic\""
      "-r, --preload=": {description: "", nargs: -1}
      "--main-fields=": {description: "", nargs: -1}
      "--no-summary": "Don't print a summary (when generating .bun)"
      "-v, --version": "Print version and exit"
      "--revision": "Print version with revision and exit"
      "--tsconfig-override=": ""
      "-d, --define=": {description: "", nargs: -1}
      "-e, --external=": {description: "", nargs: -1}
      "-l, --loader=": {description: "", nargs: -1}
      "-u, --origin=": ""
      "-p, --port=": ""
      "--smol": "Use less memory, but run garbage collection more often"
      "--minify": "Minify (experimental)"
      "--minify-syntax": "Minify syntax and inline data (experimental)"
      "--minify-whitespace": "Minify whitespace (experimental)"
      "--minify-identifiers": "Minify identifiers"
      "--no-macros": "Disable macros from being executed in the bundler, transpiler and runtime"
      "--target=": "Target environment"
      "--inspect?": "Activate Bun's debugger for a file"
      "--inspect-wait?": "Activate Bun's Debugger, wait for a connection before executing"
      "--inspect-brk?": "Activate Bun's Debugger, set breakpoint on first line of code and wait"
      "--if-present": "Exit if the entrypoint does not exist"
      "--hot": "Enable auto reload in the Bun runtime, test runner, or bundler"
      "--watch": "Automatically restart the process on file change"
      "--no-install": "Disable auto install in the Bun runtime"
      "-i": "Automatically install dependencies and use global cache in Bun's runtime, equivalent to --install=fallback"
      "--install=": ""
      "--prefer-offline": "Skip staleness checks for packages in the Bun runtime and resolve from disk"
      "--prefer-latest": "Use the latest matching versions of packages in the Bun runtime, always checking npm"
      "--silent": "Don't repeat the command for bun run"
      "--format=": "Specifies the module format to build to. Only esm is supported currently"
      "--outdir=": "Default to \"dist\" if multiple files"
      "--outfile=": "Write to a file"
      "--root=": "Root directory used for multiple entry points"
      "--splitting": "Enable code splitting"
      "--public-path=": "A prefix to be appended to any import paths in bundled code"
      "--sourcemap?": "Build with sourcemaps - 'inline', 'external', or 'none'"
      "--entry-naming=": "Customize entry point filenames. Defaults to \"[dir]/[name].[ext]\""
      "--chunk-naming=": "Customize chunk filenames. Defaults to \"[name]-[hash].[ext]\""
      "--asset-naming=": "Customize asset filenames. Defaults to \"[name]-[hash].[ext]\""
      "--server-components": "Enable React Server Components (experimental)"
      "--no-bundle": "Transpile file only, do not bundle"
      "--dump-environment-variables": "Dump environment variables from .env and process as JSON and quit. Useful for debugging"
      "--dump-limits": "Dump system limits. Useful for debugging"
    completion:
      flag:
        config: ["$files"]
        jsx-factory: [React.createElement, h, preact.h]
        jsx-import-source: [react]
        jsx-runtime: [automatic, classic]
        inspect: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        inspect-wait: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        inspect-brk: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        install: [auto, force, fallback, disable]
        format: [esm]
        sourcemap: [inline, external, none]
  - name: update
    description: "Update outdated dependencies"
    flags:
      "-c, --config?": "Load config (bunfig.toml)"
      "-y, --yarn": "Write a yarn.lock file (yarn v1)"
      "-p, --production": "Don't install devDependencies"
      "--no-save": "Don't save a lockfile"
      "--save": "Save to package.json"
      "--dry-run": "Don't install anything"
      "--frozen-lockfile": "Disallow changes to lockfile"
      "-f, --force": "Always request the latest versions from the registry & reinstall all dependencies"
      "--cache-dir=": "Store & load cached data from a specific directory path"
      "--no-cache": "Ignore manifest cache entirely"
      "--silent": "Don't log anything"
      "--verbose": "Excessively verbose logging"
      "-g, --global": "Install globally"
      "--cwd=": "Set a specific cwd"
      "--backend=": "Platform-specific optimizations for installing dependencies. For macOS, 'clonefile' (default), 'copyfile'"
      "--link-native-bins=": {description: "Link 'bin' from a matching platform-specific 'optionalDependencies' instead. Default: esbuild, turbo", nargs: -1}
      "--help": "Print the help menu"
      "--no-progress": "Disable the progress bar"
      "--no-summary": "Don't print a summary"
      "--no-verify": "Skip verifying integrity of newly downloaded packages"
      "--ignore-scripts": "Skip lifecycle scripts in the project's package.json (dependency scripts are never run)"
      "-d, --dev, -D, --development": "Install as devDependency"
      "--exact": "Install exact version"
      "--optional": "Install as optionalDependency"
    completion:
      flag:
        config: ["$files"]
        cache-dir: ["$directories"]
        cwd: ["$directories"]
        backend: [clonefile, copyfile]
  - name: link
    description: "Run without an argument to register this package to the global package registry. Uses the name field from package.json"
    flags:
      "--save": "Save to package.json"
  - name: unlink
    description: "Unlink this package from the global package registry"
  - name: upgrade
    description: "Get the latest version of bun"
    flags:
      "--canary": "Install the latest canary release"
  - name: test
    description: "Run unit tests with Bun"
    flags:
      "-h, --help": "Display this help and exit"
      "-b, --bun": "Force a script or package to use Bun's runtime instead of Node.js (via symlinking node)"
      "--cwd=": ""
      "-c, --config?": "Load config (bunfig.toml)"
      "--extension-order=": {description: "", nargs: -1}
      "--jsx-factory=": "Changes the function called when compiling JSX elements using the classic JSX runtime"
      "--jsx-fragment?": "Changes the function called when compiling JSX fragments"
      "--jsx-import-source=": "Declares the module specifier to be used for importing the jsx and jsxs factory functions. Default: \"react\""
      "--jsx-runtime=": "\"automatic\" (default) or \"classic\""
      "-r, --preload=": {description: "", nargs: -1}
      "--main-fields=": {description: "", nargs: -1}
      "--no-summary": "Don't print a summary (when generating .bun)"
      "-v, --version": "Print version and exit"
      "--revision": "Print version with revision and exit"
      "--tsconfig-override=": ""
      "-d, --define=": {description: "", nargs: -1}
      "-e, --external=": {description: "", nargs: -1}
      "-l, --loader=": {description: "", nargs: -1}
      "-u, --origin=": ""
      "-p, --port=": ""
      "--smol": "Use less memory, but run garbage collection more often"
      "--minify": "Minify (experimental)"
      "--minify-syntax": "Minify syntax and inline data (experimental)"
      "--minify-whitespace": "Minify whitespace (experimental)"
      "--minify-identifiers": "Minify identifiers"
      "--no-macros": "Disable macros from being executed in the bundler, transpiler and runtime"
      "--target=": "Target environment"
      "--inspect?": "Activate Bun's debugger for a file"
      "--inspect-wait?": "Activate Bun's Debugger, wait for a connection before executing"
      "--inspect-brk?": "Activate Bun's Debugger, set breakpoint on first line of code and wait"
      "--if-present": "Exit if the entrypoint does not exist"
      "--hot": "Enable auto reload in the Bun runtime, test runner, or bundler"
      "--watch": "Automatically restart the process on file change"
      "--no-install": "Disable auto install in the Bun runtime"
      "-i": "Automatically install dependencies and use global cache in Bun's runtime, equivalent to --install=fallback"
      "--install=": ""
      "--prefer-offline": "Skip staleness checks for packages in the Bun runtime and resolve from disk"
      "--prefer-latest": "Use the latest matching versions of packages in the Bun runtime, always checking npm"
      "--silent": "Don't repeat the command for bun run"
      "--timeout=": "Set the per-test timeout in milliseconds, default is 5000"
      "--update-snapshots": "Update snapshot files"
      "--rerun-each=": "Re-run each test file <NUMBER> times, helps catch certain bugs"
      "--only": "Only run tests that are marked with \"test.only()\""
      "--todo": "Include tests that are marked with \"test.todo()\""
      "--coverage": "Generate a coverage profile"
      "--bail?": "Exit the test suite after <NUMBER> failures. If you do not specify a number, it defaults to 1"
      "-t, --test-name-pattern=": "Run only tests with a name that matches the given regex"
      "--dump-environment-variables": "Dump environment variables from .env and process as JSON and quit. Useful for debugging"
      "--dump-limits": "Dump system limits. Useful for debugging"
    completion:
      flag:
        config: ["$files"]
        jsx-factory: [React.createElement, h, preact.h]
        jsx-import-source: [react]
        jsx-runtime: [automatic, classic]
        inspect: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        inspect-wait: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        inspect-brk: ["3000\tServe on port 3000", "8080\tServe on port 8080"]
        install: [auto, force, fallback, disable]
  - name: pm
    description: "Set of utilities for working with Bun's package manager"
  - name: completions
    description: "Install shell completions"
  - name: discord
    description: "Open bun's Discord server"
  - name: help
    description: "Print the help menu"
  - name: x
    description: "Run an npx command"
    completion:
      positional:
        - [autocannon, vite, babel, create-react-native-app, react-native, tailwindcss, next, nuxi, gltfjsx, prisma, eslint, prettier, tsc, typeorm, "@withfig/autocomplete-tools", create-completion-spec, "@fig/publish-spec-to-team", "fig-teams@latest", create-next-app, create-t3-app, create-discord-bot, create-video, remotion, create-remix, remix, playwright, ignite-cli, vsce, degit, "@preset/cli", mikro-orm, pod-install, capacitor, cap, "@magnolia/cli", stencil, swagger-typescript-api, sta, "@wordpress/create-block", astro, ampx]
  - name: repl
    description: "Run a REPL (read eval print loop) with the Bun runtime.(experimental)"
  - name: init
    description: "Initialize a new bun project"
    flags:
      "-y, --yes": "Answer yes to all prompts"
