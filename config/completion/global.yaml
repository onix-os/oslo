# Converted from argc's global.sh by tools/argc-to-spec. Do not edit by hand.
name: global
flags:
  "-c, --completion=": {description: "Print symbols which start with prefix.", nargs: -1}
  "-f, --file=": "Print all tags in the files."
  "-g, --grep=": {description: "Print all lines which match to the pattern.", nargs: -1}
  "--help": "Print a usage message."
  "-I, --idutils=": "Print all lines which match to pattern."
  "-P, --path=": "Print path names which match to pattern."
  "-p, --print-dbpath": "Print location of ´GTAGS´."
  "--print=": "Print location of name, which may be one of: ´root´, ´dbpath´ or ´conf´."
  "-u, --update": "Update tag files incrementally."
  "--version": "Show version number."
  "-a, --absolute": "Print absolute path names."
  "--color=": "Use color to highlight the pattern within the line; when may be one of: never, always or auto (default)."
  "-C, --directory=": "Change the directory before doing all the work including parameter analysis."
  "-d, --definition": "Print locations of definitions."
  "-e, --regexp=": "Use pattern as the pattern; useful to protect patterns starting with ´-´."
  "-E, --extended-regexp": "Interpret pattern as a extended regular expression."
  "--encode-path=": "Convert path characters in chars into a ´%´ symbol, followed by the two-digit hexadecimal representation of the character."
  "-F, --first-match": "End the search without going through all the tag files listed in GTAGSLIBPATH when tags are found in a tag file."
  "--from-here=": "Decide tag type by context."
  "-G, --basic-regexp": "Interpret pattern as a basic regular expression."
  "--gtagsconf=": "Set environment variable GTAGSCONF to file."
  "--gtagslabel=": "Set environment variable GTAGSLABEL to label."
  "-i, --ignore-case": "Ignore case distinctions in the pattern."
  "-L, --file-list=": "Obtain files from file in addition to the arguments."
  "-l, --local": "Print only tags which exist under the current directory."
  "--literal": "Execute literal search instead of regular expression search."
  "-M, --match-case": "Search is case-sensitive."
  "--match-part=": "Specify how path name completion should match, where part is one of: ´first´, ´last´ or ´all´ (default)."
  "-n, --nofilter": "Suppress sort filter and path conversion filter."
  "-N, --nearness=": "Use Nearness sort method (sorting by closest from start) for the output."
  "-O, --only-other": "Treat only text files other than source code, like ´README´."
  "-o, --other": "Treat not only source files but also text files other than source code, like ´README´."
  "--path-style=": "Print path names using format, which may be one of: ´relative´, ´absolute´, ´shorter´, ´abslib´ or ´through´."
  "--print0": "Print each record followed by a null character instead of a newline."
  "-q, --quiet": "Quiet mode."
  "-r": "Print reference tags."
  "--reference": "Print reference tags."
  "--rootdir": "Print reference tags."
  "--result=": "Print out using format, which may be one of: ´path´ (default), ´ctags´, ´ctags-x´, ´grep´ or ´cscope´."
  "--single-update=": "Update tag files using gtags(1) with the --single-update option."
  "-s, --symbol": "Print other symbol tags."
  "-S, --scope=": "Print only tags which exist under dir directory."
  "-T, --through": "Go through all the tag files listed in GTAGSLIBPATH."
  "-t, --tags": "Use standard ctags format."
  "-V, --invert-match": "Invert the sense of matching, to select non-matching lines."
  "-v, --verbose": "Verbose mode."
  "-x, --cxref": "Use standard ctags cxref (with -x) format."
