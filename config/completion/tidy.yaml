# Converted from argc's tidy.sh by tools/argc-to-spec. Do not edit by hand.
name: tidy
flags:
  "-o, -output=": "write output to the specified <file>"
  "-config=": "set configuration options from the specified <file>"
  "-f, -file=": "write errors and warnings to the specified <file>"
  "-m, -modify": "modify the original input files"
  "-i, -indent": "indent element content"
  "-w, -wrap=": "wrap text at the specified <column>."
  "-u, -upper": "force tags to upper case"
  "-c, -clean": "replace FONT, NOBR and CENTER tags with CSS"
  "-b, -bare": "strip out smart quotes and em dashes, etc."
  "-g, -gdoc": "produce clean version of html exported by Google Docs"
  "-n, -numeric": "output numeric rather than named entities"
  "-e, -errors": "show only errors and warnings"
  "-q, -quiet": "suppress nonessential output"
  "-omit": "omit optional start tags and end tags"
  "-xml": "specify the input is well formed XML"
  "-asxml": "convert HTML to well formed XHTML"
  "-asxhtml": "convert HTML to well formed XHTML"
  "-ashtml": "force XHTML to well formed HTML"
  "-access=": "do additional accessibility checks (<level> = 0, 1, 2, 3)."
  "-raw": "output values above 127 without conversion to entities"
  "-ascii": "use ISO-8859-1 for input, US-ASCII for output"
  "-latin0": "use ISO-8859-15 for input, US-ASCII for output"
  "-latin1": "use ISO-8859-1 for both input and output"
  "-iso2022": "use ISO-2022 for both input and output"
  "-utf8": "use UTF-8 for both input and output"
  "-mac": "use MacRoman for input, US-ASCII for output"
  "-win1252": "use Windows-1252 for input, US-ASCII for output"
  "-ibm858": "use IBM-858 (CP850+Euro) for input, US-ASCII for output"
  "-utf16le": "use UTF-16LE for both input and output"
  "-utf16be": "use UTF-16BE for both input and output"
  "-utf16": "use UTF-16 for both input and output"
  "-big5": "use Big5 for both input and output"
  "-shiftjis": "use Shift_JIS for both input and output"
  "-v, -version": "show the version of Tidy"
  "-h": "list the command line options"
  "-?": "list the command line options"
  "-help": "list the command line options"
  "-help-config": "list all configuration options"
  "-help-env": "show information about the environment and runtime configuration"
  "-show-config": "list the current configuration settings"
  "-export-config": "list the current configuration settings, suitable for a config file"
  "-export-default-config": "list the default configuration settings, suitable for a config file"
  "-help-option=": "show a description of the <option>"
  "-language=": "set Tidy's output language to <lang>."
  "-xml-help": "list the command line options in XML format"
  "-xml-config": "list all configuration options in XML format"
  "-xml-strings": "output all of Tidy's strings in XML format"
  "-xml-error-strings": "output error constants and strings in XML format"
  "-xml-options-strings": "output option descriptions in XML format"
  "--gnu-emacs": "This option specifies if Tidy should change the format for reporting errors and warnings to a format that is more easily parsed by GNU Emacs."
  "--markup": "This option specifies if Tidy should generate a pretty printed version of the markup."
  "--mute=": "Use this option to prevent Tidy from displaying certain types of report output, for example, for conditions that you wish to ignore."
  "--mute-id": "This option indicates whether or not Tidy should display message ID's with each of its error reports."
  "--quiet": "When enabled, this option limits Tidy's non-document output to report only document warnings and errors."
  "--show-body-only=": "This option specifies if Tidy should print only the contents of the body tag as an HTML fragment."
  "--show-errors=": "This option specifies the number Tidy uses to determine if further errors should be shown."
  "--show-info": "This option specifies if Tidy should display info-level messages."
  "--show-warnings": "This option specifies if Tidy should suppress warnings."
  "--add-meta-charset": "This option, when enabled, adds a <meta> element and sets the charset attribute to the encoding of the document."
  "--add-xml-decl": "This option specifies if Tidy should add the XML declaration when outputting XML or XHTML."
  "--add-xml-space": "This option specifies if Tidy should add xml:space=\"preserve\" to elements such as <pre>, <style> and <script> when generating XML."
  "--doctype=": "This option specifies the DOCTYPE declaration generated by Tidy."
  "--input-xml": "This option specifies if Tidy should use the XML parser rather than the error correcting HTML parser."
  "--output-html": "This option specifies if Tidy should generate pretty printed output, writing it as HTML."
  "--output-xhtml": "This option specifies if Tidy should generate pretty printed output, writing it as extensible HTML."
  "--output-xml": "This option specifies if Tidy should pretty print output, writing it as well-formed XML."
  "--error-file=": "This option specifies the error file Tidy uses for errors and warnings."
  "--keep-time": "This option specifies if Tidy should keep the original modification time of files that Tidy modifies in place."
  "--output-file=": "This option specifies the output file Tidy uses for markup."
  "--write-back": "This option specifies if Tidy should write back the tidied markup to the same file it read from."
  "--accessibility-check=": "Enum if unset) This option specifies what level of accessibility checking, if any, that Tidy should perform."
  "--force-output": "This option specifies if Tidy should produce output even if errors are encountered."
  "--show-meta-change": "This option enables a message whenever Tidy changes the content attribute of a meta charset declaration to match the encoding of the document."
  "--warn-proprietary-attributes": "This option specifies if Tidy should warn on proprietary attributes."
  "--char-encoding=": "This option specifies the character encoding Tidy uses for input, and when set, automatically chooses an appropriate character encoding to be used for output."
  "--input-encoding=": "This option specifies the character encoding Tidy uses for input."
  "--newline=": "The default is appropriate to the current platform."
  "--output-bom=": "This option specifies if Tidy should write a Unicode Byte Order Mark character (BOM; also known as Zero Width No-Break Space; has value of U+FEFF) to the beginning of the output, and only applies to UTF-8 and UTF-16 output encodings."
  "--output-encoding=": "This option specifies the character encoding Tidy uses for output."
  "--bare": "This option specifies if Tidy should strip Microsoft specific HTML from Word 2000 documents, and output spaces rather than non-breaking spaces where they exist in the input."
  "--clean": "This option specifies if Tidy should perform cleaning of some legacy presentational tags (currently <i>, <b>, <center> when enclosed within appropriate inline tags, and <font>)."
  "--drop-empty-elements": "This option specifies if Tidy should discard empty elements."
  "--drop-empty-paras": "This option specifies if Tidy should discard empty paragraphs."
  "--drop-proprietary-attributes": "This option specifies if Tidy should strip out proprietary attributes, such as Microsoft data binding attributes."
  "--gdoc": "This option specifies if Tidy should enable specific behavior for cleaning up HTML exported from Google Docs."
  "--logical-emphasis": "This option specifies if Tidy should replace any occurrence of <i> with <em> and any occurrence of <b> with <strong>."
  "--merge-divs=": "This option can be used to modify the behavior of clean when set to yes."
  "--merge-spans=": "This option can be used to modify the behavior of clean when set to yes."
  "--word-2000": "This option specifies if Tidy should go to great pains to strip out all the surplus stuff Microsoft Word 2000 inserts when you save Word documents as \"Web pages\"."
  "--ascii-chars": "Can be used to modify behavior of the clean option when set to yes."
  "--ncr": "This option specifies if Tidy should allow numeric character references."
  "--numeric-entities": "This option specifies if Tidy should output entities other than the built-in HTML entities (&amp;, &lt;, &gt;, and &quot;) in the numeric rather than the named entity form."
  "--preserve-entities": "This option specifies if Tidy should preserve well-formed entities as found in the input."
  "--quote-ampersand": "This option specifies if Tidy should output unadorned & characters as &amp;."
  "--quote-marks": "This option specifies if Tidy should output \" characters as &quot; as is preferred by some editing environments."
  "--quote-nbsp": "This option specifies if Tidy should output non-breaking space characters as entities, rather than as the Unicode character value 160 (decimal)."
  "--alt-text=": "This option specifies the default alt= text Tidy uses for <img> attributes when the alt= attribute is missing."
  "--anchor-as-name": "This option controls the deletion or addition of the name attribute in elements where it can serve as anchor."
  "--assume-xml-procins": "This option specifies if Tidy should change the parsing of processing instructions to require ?> as the terminator rather than >."
  "--coerce-endtags": "This option specifies if Tidy should coerce a start tag into an end tag in cases where it looks like an end tag was probably intended; for example, given"
  "--css-prefix=": "This option specifies the prefix that Tidy uses for styles rules."
  "--custom-tags=": "This option enables the use of tags for autonomous custom elements, e.g. <flag-icon> with Tidy."
  "--enclose-block-text": "This option specifies if Tidy should insert a <p> element to enclose any text it finds in any element that allows mixed content for HTML transitional but not HTML strict."
  "--enclose-text": "This option specifies if Tidy should enclose any text it finds in the body element within a <p> element."
  "--escape-scripts": "This option causes items that look like closing tags, like </g to be escaped to <\\/g."
  "--fix-backslash": "This option specifies if Tidy should replace backslash characters \\ in URLs with forward slashes /."
  "--fix-bad-comments=": "This option specifies if Tidy should replace unexpected hyphens with = characters when it comes across adjacent hyphens."
  "--fix-style-tags": "This option specifies if Tidy should move all style tags to the head of the document."
  "--fix-uri": "This option specifies if Tidy should check attribute values that carry URIs for illegal characters and if such are found, escape them as HTML4 recommends."
  "--literal-attributes": "This option specifies how Tidy deals with whitespace characters within attribute values."
  "--lower-literals": "This option specifies if Tidy should convert the value of an attribute that takes a list of predefined values to lower case."
  "--repeated-attributes=": "This option specifies if Tidy should keep the first or last attribute, if an attribute is repeated, e.g. has two align attributes."
  "--skip-nested": "This option specifies that Tidy should skip nested tags when parsing script and style data."
  "--strict-tags-attributes": "This options ensures that tags and attributes are applicable for the version of HTML that Tidy outputs."
  "--uppercase-attributes=": "This option specifies if Tidy should output attribute names in upper case."
  "--uppercase-tags": "This option specifies if Tidy should output tag names in upper case."
  "--decorate-inferred-ul": "This option specifies if Tidy should decorate inferred <ul> elements with some CSS markup to avoid indentation to the right."
  "--escape-cdata": "This option specifies if Tidy should convert <![CDATA[]]> sections to normal text."
  "--hide-comments": "This option specifies if Tidy should not print out comments."
  "--join-classes": "This option specifies if Tidy should combine class names to generate a single, new class name if multiple class assignments are detected on an element."
  "--join-styles": "This option specifies if Tidy should combine styles to generate a single, new style if multiple style values are detected on an element."
  "--merge-emphasis": "This option specifies if Tidy should merge nested <b> and <i> elements; for example, for the case"
  "--replace-color": "This option specifies if Tidy should replace numeric values in color attributes with HTML/XHTML color names where defined, e.g. replace ♯ffffff with white."
  "--new-blocklevel-tags": "Tag Names This option specifies new block-level tags."
  "--new-empty-tags": "Tag Names This option specifies new empty inline tags."
  "--new-inline-tags": "Tag Names This option specifies new non-empty inline tags."
  "--new-pre-tags": "Tag Names This option specifies new tags that are to be processed in exactly the same way as HTML's <pre> element."
  "--break-before-br": "This option specifies if Tidy should output a line break before each <br> element."
  "--indent=": "This option specifies if Tidy should indent block-level tags."
  "--indent-attributes": "This option specifies if Tidy should begin each attribute on a new line."
  "--indent-cdata": "This option specifies if Tidy should indent <![CDATA[]]> sections."
  "--indent-spaces=": "This option specifies the number of spaces or tabs that Tidy uses to indent content when indent is enabled."
  "--indent-with-tabs": "This option specifies if Tidy should indent with tabs instead of spaces, assuming indent is yes."
  "--keep-tabs": "With the default no Tidy will replace all source tabs with spaces, controlled by the option tab-size, and the current line offset."
  "--omit-optional-tags": "This option specifies if Tidy should omit optional start tags and end tags when generating output."
  "--priority-attributes": "Attributes Names This option allows prioritizing the writing of attributes in tidied documents, allowing them to written before the other attributes of an element."
  "--punctuation-wrap": "This option specifies if Tidy should line wrap after some Unicode or Chinese punctuation characters."
  "--sort-attributes=": "This option specifies that Tidy should sort attributes within an element using the specified sort algorithm."
  "--tab-size=": "This option specifies the number of columns that Tidy uses between successive tab stops."
  "--tidy-mark": "This option specifies if Tidy should add a meta element to the document head to indicate that the document has been tidied."
  "--vertical-space=": "This option specifies if Tidy should add some extra empty lines for readability."
  "--wrap=": "This option specifies the right margin Tidy uses for line wrapping."
  "--wrap-asp": "This option specifies if Tidy should line wrap text contained within ASP pseudo elements, which look like: <% ... %>."
  "--wrap-attributes": "This option specifies if Tidy should line-wrap attribute values, meaning that if the value of an attribute causes a line to exceed the width specified by wrap, Tidy will add one or more line breaks to the value, causing it to be wrapped into multiple lines."
  "--wrap-jste": "This option specifies if Tidy should line wrap text contained within JSTE pseudo elements, which look like: <# ... ♯>."
  "--wrap-php": "This option specifies if Tidy should line wrap text contained within PHP pseudo elements, which look like: <?php ... ?>."
  "--wrap-script-literals": "This option specifies if Tidy should line wrap string literals that appear in script attributes."
  "--wrap-sections": "This option specifies if Tidy should line wrap text contained within <![ ... ]> section tags."
completion:
  flag:
    show-body-only: ["no", "yes", auto]
    newline: [LF, CRLF, CR]
    output-bom: ["no", "yes", auto]
    merge-divs: ["no", "yes", auto]
    merge-spans: ["no", "yes", auto]
    custom-tags: ["no", blocklevel, empty, inline, pre]
    fix-bad-comments: ["no", "yes", auto]
    indent: ["no", "yes", auto]
    sort-attributes: [none, alpha]
    vertical-space: ["no", "yes", auto]
