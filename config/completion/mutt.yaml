# Converted from argc's mutt.sh by tools/argc-to-spec. Do not edit by hand.
name: mutt
flags:
  "-A=": "expand the given alias"
  "-a=": "attach file(s) to the message the list of files must be terminated with the \"--\" sequence"
  "-b=": "specify a blind carbon-copy (BCC) address"
  "-c=": "specify a carbon-copy (CC) address"
  "-D": "print the value of all variables to stdout"
  "-d=": "log debugging output to ~/.muttdebug0 0 => no debugging; <0 => do not rotate .muttdebug files"
  "-E": "edit the draft (-H) or include (-i) file"
  "-e=": "specify a command to be executed after initialization"
  "-f=": "specify which mailbox to read"
  "-F=": "specify an alternate muttrc file"
  "-H=": "specify a draft file to read header and body from"
  "-i=": "specify a file which Mutt should include in the body"
  "-m=": "specify a default mailbox type"
  "-n": "causes Mutt not to read the system Muttrc"
  "-p": "recall a postponed message"
  "-Q=": "query a configuration variable"
  "-R": "open mailbox in read-only mode"
  "-s=": "specify a subject (must be in quotes if it has spaces)"
  "-v": "show version and compile-time definitions"
  "-x": "simulate the mailx send mode"
  "-y": "select a mailbox specified in your `mailboxes' list"
  "-z": "exit immediately if there are no messages in the mailbox"
  "-Z": "open the first folder with new message, exit immediately if none"
  "-h": "this help message"
  "-help": "Print help"
  "-V, -version": "Print version"
