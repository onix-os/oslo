# Converted from argc's cssh.sh by tools/argc-to-spec. Do not edit by hand.
name: cssh
flags:
  "-a, --action=": "Run the command in each session, e.g. \"-a 'vi /etc/hosts'\" to drop straight into a vi session."
  "-K, --autoclose=": "Number of seconds to wait before closing finished terminal windows."
  "-q, --autoquit": "Toggle automatically quitting after the last client window has closed (overriding the config file)."
  "-c, --cluster-file=": "Use supplied file as additional cluster file (see also \"FILES\")."
  "-x, --cols=": "Number of columns"
  "-C, --config-file=": "Use supplied file as additional configuration file (see also \"FILES\")."
  "--debug=*": {description: "Enable debugging.", nargs: -1}
  "-d, --dump-config": "Dump the current configuration in the same format used by the $HOME/.clusterssh/config file."
  "-e, --evaluate=": "Display and evaluate the terminal and connection arguments to display any potential errors."
  "--fillscreen": "Resize terminal windows to fill the whole available screen"
  "-f, --font=": "Specify the font to use in the terminal windows."
  "-h, --help": "Show basic help text and exit"
  "-L, --list=": "List available cluster tags."
  "-H, --man": "Show full help text (the man page) and exit"
  "-o, --options=": "Specify arguments to be passed to ssh when making the connection."
  "-p, --port=": "Specify an alternate port for connections."
  "-Q, --quiet": "Do not output extra text when using some options"
  "-y, --rows=": "Number of rows"
  "-s, --show-history": "Show history within console window."
  "-r, --tag-file=": "Use supplied file as additional tag file (see also \"FILES\")"
  "-t, --term-args=": "Specify arguments to be passed to terminals being used."
  "-g, --tile": "Toggle window tiling (overriding the config file)."
  "-T, --title=": "Specify the initial part of the title used in the console and client windows."
  "-u, --unique-servers": "Toggle connecting to each host only once when a hostname has been specified multiple times."
  "-?, --usage": "Show synopsis and exit"
  "-A, --use-all-a-records": "If a hostname resolves to multiple IP addresses, toggle whether or not to connect to all of them, or just the first one (see also config file entry)."
  "-l, --username=": "Specify the default username to use for connections (if different from the currently logged in user)."
  "-v, --version": "Show version information and exit"
