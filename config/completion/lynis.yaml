# Converted from argc's lynis.sh by tools/argc-to-spec. Do not edit by hand.
name: lynis
flags:
  "--forensics": "Perform forensics on a running or mounted system"
  "--pentest": "Non-privileged, show points of interest for pentesting"
  "--no-colors": "Don't use colors in output"
  "-q, --quiet": "No output"
  "--reverse-colors": "Optimize color display for light backgrounds"
  "--reverse-colours": "Optimize colour display for light backgrounds"
  "--debug": "Debug logging to screen"
  "--no-log": "Don't create a log file"
  "--profile=": "Scan the system with the given profile file"
  "--view-manpage": "View man page"
  "--man": "View man page"
  "--verbose": "Show more details on screen"
  "-V, --version": "Display version number and quit"
  "--wait": "Wait between a set of tests"
  "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
  "--plugindir=": "Define path of available plugins"
  "--upload": "Upload data to central node"
  "-h, --help": "Print help"
commands:
  - name: audit
    flags:
      "-h, --help": ""
    persistentflags:
      "--forensics": "Perform forensics on a running or mounted system"
      "--pentest": "Non-privileged, show points of interest for pentesting"
      "--no-colors": "Don't use colors in output"
      "-q, --quiet": "No output"
      "--reverse-colors": "Optimize color display for light backgrounds"
      "--reverse-colours": "Optimize colour display for light backgrounds"
      "--debug": "Debug logging to screen"
      "--no-log": "Don't create a log file"
      "--profile=": "Scan the system with the given profile file"
      "--view-manpage": "View man page"
      "--man": "View man page"
      "--verbose": "Show more details on screen"
      "-V, --version": "Display version number and quit"
      "--wait": "Wait between a set of tests"
      "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
      "--plugindir=": "Define path of available plugins"
      "--upload": "Upload data to central node"
    commands:
      - name: system
        description: "Perform local security scan"
        flags:
          "-h, --help": ""
        persistentflags:
          "--forensics": "Perform forensics on a running or mounted system"
          "--pentest": "Non-privileged, show points of interest for pentesting"
          "--no-colors": "Don't use colors in output"
          "-q, --quiet": "No output"
          "--reverse-colors": "Optimize color display for light backgrounds"
          "--reverse-colours": "Optimize colour display for light backgrounds"
          "--debug": "Debug logging to screen"
          "--no-log": "Don't create a log file"
          "--profile=": "Scan the system with the given profile file"
          "--view-manpage": "View man page"
          "--man": "View man page"
          "--verbose": "Show more details on screen"
          "-V, --version": "Display version number and quit"
          "--wait": "Wait between a set of tests"
          "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
          "--plugindir=": "Define path of available plugins"
          "--upload": "Upload data to central node"
        commands:
          - name: remote
            description: "Remote security scan"
            flags:
              "-h, --help": ""
            persistentflags:
              "--forensics": "Perform forensics on a running or mounted system"
              "--pentest": "Non-privileged, show points of interest for pentesting"
              "--no-colors": "Don't use colors in output"
              "-q, --quiet": "No output"
              "--reverse-colors": "Optimize color display for light backgrounds"
              "--reverse-colours": "Optimize colour display for light backgrounds"
              "--debug": "Debug logging to screen"
              "--no-log": "Don't create a log file"
              "--profile=": "Scan the system with the given profile file"
              "--view-manpage": "View man page"
              "--man": "View man page"
              "--verbose": "Show more details on screen"
              "-V, --version": "Display version number and quit"
              "--wait": "Wait between a set of tests"
              "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
              "--plugindir=": "Define path of available plugins"
              "--upload": "Upload data to central node"
      - name: dockerfile
        description: "Analyze Dockerfile"
        flags:
          "-h, --help": ""
        persistentflags:
          "--forensics": "Perform forensics on a running or mounted system"
          "--pentest": "Non-privileged, show points of interest for pentesting"
          "--no-colors": "Don't use colors in output"
          "-q, --quiet": "No output"
          "--reverse-colors": "Optimize color display for light backgrounds"
          "--reverse-colours": "Optimize colour display for light backgrounds"
          "--debug": "Debug logging to screen"
          "--no-log": "Don't create a log file"
          "--profile=": "Scan the system with the given profile file"
          "--view-manpage": "View man page"
          "--man": "View man page"
          "--verbose": "Show more details on screen"
          "-V, --version": "Display version number and quit"
          "--wait": "Wait between a set of tests"
          "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
          "--plugindir=": "Define path of available plugins"
          "--upload": "Upload data to central node"
  - name: show
    flags:
      "-h, --help": ""
    persistentflags:
      "--forensics": "Perform forensics on a running or mounted system"
      "--pentest": "Non-privileged, show points of interest for pentesting"
      "--no-colors": "Don't use colors in output"
      "-q, --quiet": "No output"
      "--reverse-colors": "Optimize color display for light backgrounds"
      "--reverse-colours": "Optimize colour display for light backgrounds"
      "--debug": "Debug logging to screen"
      "--no-log": "Don't create a log file"
      "--profile=": "Scan the system with the given profile file"
      "--view-manpage": "View man page"
      "--man": "View man page"
      "--verbose": "Show more details on screen"
      "-V, --version": "Display version number and quit"
      "--wait": "Wait between a set of tests"
      "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
      "--plugindir=": "Define path of available plugins"
      "--upload": "Upload data to central node"
    commands:
      - name: version
        description: "Show Lynis version"
        flags:
          "-h, --help": ""
        persistentflags:
          "--forensics": "Perform forensics on a running or mounted system"
          "--pentest": "Non-privileged, show points of interest for pentesting"
          "--no-colors": "Don't use colors in output"
          "-q, --quiet": "No output"
          "--reverse-colors": "Optimize color display for light backgrounds"
          "--reverse-colours": "Optimize colour display for light backgrounds"
          "--debug": "Debug logging to screen"
          "--no-log": "Don't create a log file"
          "--profile=": "Scan the system with the given profile file"
          "--view-manpage": "View man page"
          "--man": "View man page"
          "--verbose": "Show more details on screen"
          "-V, --version": "Display version number and quit"
          "--wait": "Wait between a set of tests"
          "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
          "--plugindir=": "Define path of available plugins"
          "--upload": "Upload data to central node"
  - name: update
    flags:
      "-h, --help": ""
    persistentflags:
      "--forensics": "Perform forensics on a running or mounted system"
      "--pentest": "Non-privileged, show points of interest for pentesting"
      "--no-colors": "Don't use colors in output"
      "-q, --quiet": "No output"
      "--reverse-colors": "Optimize color display for light backgrounds"
      "--reverse-colours": "Optimize colour display for light backgrounds"
      "--debug": "Debug logging to screen"
      "--no-log": "Don't create a log file"
      "--profile=": "Scan the system with the given profile file"
      "--view-manpage": "View man page"
      "--man": "View man page"
      "--verbose": "Show more details on screen"
      "-V, --version": "Display version number and quit"
      "--wait": "Wait between a set of tests"
      "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
      "--plugindir=": "Define path of available plugins"
      "--upload": "Upload data to central node"
    commands:
      - name: info
        description: "Show update details"
        flags:
          "-h, --help": ""
        persistentflags:
          "--forensics": "Perform forensics on a running or mounted system"
          "--pentest": "Non-privileged, show points of interest for pentesting"
          "--no-colors": "Don't use colors in output"
          "-q, --quiet": "No output"
          "--reverse-colors": "Optimize color display for light backgrounds"
          "--reverse-colours": "Optimize colour display for light backgrounds"
          "--debug": "Debug logging to screen"
          "--no-log": "Don't create a log file"
          "--profile=": "Scan the system with the given profile file"
          "--view-manpage": "View man page"
          "--man": "View man page"
          "--verbose": "Show more details on screen"
          "-V, --version": "Display version number and quit"
          "--wait": "Wait between a set of tests"
          "--slow-warning=": "Threshold for slow test warning in seconds (default 10)"
          "--plugindir=": "Define path of available plugins"
          "--upload": "Upload data to central node"
