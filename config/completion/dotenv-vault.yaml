# Converted from Fig's dotenv-vault.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: dotenv-vault
description: "CLI for dotenv-vault"
persistentflags:
  "--help": "Get documentation for the command"
commands:
  - name: new
    description: "Create your project"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
  - name: login
    description: "Log in to dotenv-vault"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
  - name: logout
    description: "Log out from dotenv-vault"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
  - name: open
    description: "Opens project page in a browser"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
  - name: push
    description: "Push .env securely"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
      "-m, --dotenvMe=": "Pass .env.me (DOTENV_ME) credential directly (rather than reading from .env.me file)"
  - name: pull
    description: "Pull .env securely"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
      "-m, --dotenvMe=": "Pass .env.me (DOTENV_ME) credential directly (rather than reading from .env.me file)"
  - name: versions
    description: "List version history"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
      "-m, --dotenvMe=": "Pass .env.me (DOTENV_ME) credential directly (rather than reading from .env.me file)"
  - name: whoami
    description: "Display the current logged in user"
    flags:
      "-m, --dotenvMe=": "Pass .env.me (DOTENV_ME) credential directly (rather than reading from .env.me file)"
  - name: status
    description: "Check dotenv-operational status"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
  - name: help
    description: "Display help for dotenv-vault"
  - name: update
    description: "Update the dotenv-vault CLI"
    flags:
      "--yes, -y": "Automatic yes to prompts. Assume yes to all prompts and run non-interactively"
