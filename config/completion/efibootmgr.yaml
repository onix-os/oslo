# Converted from argc's efibootmgr.sh by tools/argc-to-spec. Do not edit by hand.
name: efibootmgr
flags:
  "-a, --active": "sets bootnum active"
  "-A, --inactive": "sets bootnum inactive"
  "-b, --bootnum=": "modify BootXXXX (hex)"
  "-B, --delete-bootnum": "delete bootnum"
  "-c, --create": "create new variable bootnum and add to bootorder"
  "-C, --create-only": "create new variable bootnum and do not add to bootorder"
  "-D, --remove-dups": "remove duplicate values from BootOrder"
  "-d, --disk=": "(defaults to /dev/sda) containing loader"
  "-r, --driver": "Operate on Driver variables, not Boot Variables."
  "-e, --edd=": "force EDD 1.0 or 3.0 creation variables, or guess"
  "-E, --device=": "EDD 1.0 device number (defaults to 0x80)"
  "-g, --gpt": "force disk with invalid PMBR to be treated as GPT"
  "-i, --iface=": "create a netboot entry for the named interface"
  "-l, --loader=": "(defaults to \"\\EFI\\ubuntu\\grub.efi\")"
  "-L, --label=": "Boot manager display label (defaults to \"Linux\")"
  "-m, --mirror-below-4G": "t|f mirror memory below 4GB"
  "-M, --mirror-above-4G": "X percentage memory to mirror above 4GB"
  "-n, --bootnext=": "set BootNext to XXXX (hex)"
  "-N, --delete-bootnext": "delete BootNext"
  "-o, --bootorder=*": {description: "explicitly set BootOrder (hex)", nargs: -1}
  "-O, --delete-bootorder": "delete BootOrder"
  "-p, --part=": "partition containing loader (defaults to 1 on partitioned devices)"
  "-q, --quiet": "be quiet"
  "-t, --timeout=": "set boot manager timeout waiting for user input."
  "-T, --delete-timeout": "delete Timeout."
  "-u": "handle extra args as UCS-2 (default is ASCII)"
  "--unicode": "handle extra args as UCS-2 (default is ASCII)"
  "--UCS-2": "handle extra args as UCS-2 (default is ASCII)"
  "-v, --verbose": "print additional information"
  "-V, --version": "return version and exit"
  "-w, --write-signature": "write unique sig to MBR if needed"
  "-y, --sysprep": "Operate on SysPrep variables, not Boot Variables."
  "-@, --append-binary-args=": "append extra args from file (use \"-\" for stdin)"
  "-h, --help": "show help/usage"
