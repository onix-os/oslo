# Converted from Fig's sam.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: sam
description: "AWS Serverless Application Model CLI"
flags:
  "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
  "--help, -h": "Show help for sam"
  "--version": "Show the version and exit"
  "--info": ""
commands:
  - name: init
    description: "Init an AWS SAM application"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--help, -h": "Show help for sam"
      "--no-interactive": "Disable interactive prompting for init parameters, and fail if any required values are missing"
      "-a, --architecture=": "Architectures your Lambda function will run on"
      "-l, --location=": "Template location (git, mercurial, http(s), zip, path)"
      "-r, --runtime=": "Lambda Runtime for your app"
      "-p, --package-type=": "Package type for your app"
      "-i, --base-image=": "Lambda Image of your app"
      "-d, --dependency-manager=": "Dependency manager for your Lambda runtime"
      "-o, --output-dir=": "Where to output the initialized app into"
      "-n, --name=": "Name of your project to be generated as a folder"
      "--app-template=": "Identifier of the managed application template you want to use"
      "--no-input": "Disable Cookiecutter prompting and accept default values defined template config"
      "--extra-context=": "Override any custom parameters in the template's cookiecutter.json configuration"
    completion:
      flag:
        architecture: [arm64, x86_64]
        runtime: [dotnet8, dotnet6, go1.x, java21, java17, java11, java8.al2, nodejs20.x, nodejs18.x, nodejs16.x, provided.al2023, provided.al2, provided, python3.12, python3.11, python3.10, python3.9, python3.8, ruby3.2]
        package-type: [zip, image]
        base-image: [amazon/dotnet8-base, amazon/dotnet6-base, amazon/go1.x-base, amazon/go-provided.al2-base, amazon/go-provided.al2023-base, amazon/java21-base, amazon/java17-base, amazon/java11-base, amazon/java8.al2-base, amazon/nodejs20.x-base, amazon/nodejs18.x-base, amazon/nodejs16.x-base, amazon/python3.12-base, amazon/python3.11-base, amazon/python3.10-base, amazon/python3.9-base, amazon/python3.8-base, amazon/ruby3.2-base]
        dependency-manager: [npm, mod, gradle, cli-package, bundler, pip, maven]
  - name: validate
    description: "Validate an AWS SAM template"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--help, -h": "Show help for sam"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "-t, --template-file, --template=": "AWS SAM template file"
    completion:
      flag:
        template-file: ["$files"]
  - name: build
    description: "Build your Lambda function code"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--docker-network=": "Specifies the name or id of an existing docker network to lambda docker containers should connect to, along with the default bridge network. If not specified, the Lambda containers will only connect to the default bridge docker network."
      "--help, -h": "Show help for sam"
      "--parameter-overrides=": "A string that contains AWS CloudFormation parameter overrides encoded as key=value pairs"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "--skip-pull-image": "Specify whether CLI should skip pulling down the latest Docker image for Lambda runtime"
      "-t, --template-file, --template=": "AWS SAM template file"
      "-u, --use-container": "If your functions depend on packages that have natively compiled dependencies, use this flag to build your function inside an AWS Lambda-like Docker container"
      "-e, --container-env-var=*": "Input environment variables through command line to pass into build containers, you can either input function specific format (FuncName.VarName=Value) or global format (VarName=Value)"
      "-ef, --container-env-var-file=": "Path to environment variable json file (e.g., env_vars.json) to pass into build containers"
      "-bi, --build-image=*": "Container image URIs"
      "-p, --parallel": "Enabled parallel builds"
      "-b, --build-dir=": "Path to a folder where the built artifacts will be stored"
      "-cd, --cache-dir=": "The folder where the cached artifacts are stored with `--cached` is specified"
      "-s, --base-dir=": "Resolve relative paths to function's source code with respect to this folder"
      "-m, --manifest=": "Path to a custom dependency manifest to use instead of the default one"
      "-c, --cached": "Enable cached builds. Use this flag to reuse build artifacts that have not changed from previous builds"
      "--beta-features, --no-beta-features": "Should beta features be enabled"
    completion:
      flag:
        template-file: ["$files"]
        container-env-var-file: ["$files"]
        build-dir: ["$directories"]
        cache-dir: ["$directories"]
        base-dir: ["$directories"]
        manifest: ["$directories"]
  - name: local
    description: "Run your Serverless application locally for quick development & testing"
    flags:
      "--help, -h": "Show help for sam"
    commands:
      - name: generate-event
        description: "You can use this command to generate sample payloads from different event sources such as S3, API Gateway, and SNS"
        flags:
          "--help, -h": "Show help for sam"
      - name: invoke
        description: "Invokes a local Lambda function once"
        flags:
          "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
          "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
          "--container-env-vars=": "JSON file containing environment variables to be set within the container environment"
          "--container-host-interface=": "IP address of the host network interface that container ports should bind to. Use 0.0.0.0 to bind to all interfaces"
          "--container-host=": "Host of locally emulated Lambda container"
          "--debugger-args=": "Additional arguments to be passed to the debugger"
          "--debugger-path=": "Host path to a debugger to mount into the Lambda container"
          "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
          "-d, --debug-port=*": "When specified, Lambda function will start in debug mode and expose this port to localhost"
          "--docker-network=": "Specifies the name or id of an existing docker network to lambda docker containers should connect to, along with the default bridge network. If not specified, the Lambda containers will only connect to the default bridge docker network."
          "-v, --docker-volume-basedir=": "Location basedir where the SAM file exists"
          "-n, --env-vars=": "JSON file containing values for Lambda function environment variables"
          "--force-image-build": "Whether the image should be rebuilt"
          "--help, -h": "Show help for sam"
          "-ii, --invoke-image=": "Container image URIs for invoking functions or starting api and function"
          "--layer-cache-basedir=": "Location where Layers your template uses will be downloaded to"
          "-l, --log-file=": "Log file to send runtime logs to"
          "--parameter-overrides=": "A string that contains AWS CloudFormation parameter overrides encoded as key=value pairs"
          "--profile=": "Select a specific profile from your credential file to get AWS credentials"
          "--region=": "Set the AWS Region of the service"
          "--shutdown": "If set, will emulate a shutdown event after the invoke completes, in order to test extension handling of shutdown behavior"
          "--skip-pull-image": "Specify whether CLI should skip pulling down the latest Docker image for Lambda runtime"
          "-t, --template-file, --template=": "AWS SAM template file"
          "-e, --event=": "JSON file containing event data passed to the Lambda function"
        completion:
          flag:
            container-env-vars: ["$files"]
            env-vars: ["$files"]
            layer-cache-basedir: ["$directories"]
            template-file: ["$files"]
            event: ["$files"]
      - name: start-api
        description: "Sets up a local endpoint you can use to test your API"
        flags:
          "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
          "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
          "--container-env-vars=": "JSON file containing environment variables to be set within the container environment"
          "--container-host-interface=": "IP address of the host network interface that container ports should bind to. Use 0.0.0.0 to bind to all interfaces"
          "--container-host=": "Host of locally emulated Lambda container"
          "--debug-function=": "Specifies the Lambda Function logicalId to apply debug options to when --warm-containers is specified. This parameter applies to --debug-port, --debugger-path, and --debug-args"
          "--debugger-args=": "Additional arguments to be passed to the debugger"
          "--debugger-path=": "Host path to a debugger to mount into the Lambda container"
          "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
          "-d, --debug-port=*": "When specified, Lambda function will start in debug mode and expose this port to localhost"
          "--docker-network=": "Specifies the name or id of an existing docker network to lambda docker containers should connect to, along with the default bridge network. If not specified, the Lambda containers will only connect to the default bridge docker network."
          "-v, --docker-volume-basedir=": "Location basedir where the SAM file exists"
          "-n, --env-vars=": "JSON file containing values for Lambda function environment variables"
          "--force-image-build": "Whether the image should be rebuilt"
          "--help, -h": "Show help for sam"
          "--host=": "Local hostname or IP address to bind to"
          "-ii, --invoke-image=": "Container image URIs for invoking functions or starting api and function"
          "--layer-cache-basedir=": "Location where Layers your template uses will be downloaded to"
          "-l, --log-file=": "Log file to send runtime logs to"
          "--parameter-overrides=": "A string that contains AWS CloudFormation parameter overrides encoded as key=value pairs"
          "-p, --port=": "Local port to listen on"
          "--shutdown": "If set, will emulate a shutdown event after the invoke completes, in order to test extension handling of shutdown behavior"
          "--skip-pull-image": "Specify whether CLI should skip pulling down the latest Docker image for Lambda runtime"
          "-t, --template-file, --template=": "AWS SAM template file"
          "--warm-containers=": "Specific how SAM manages the containers for each function"
          "-s, --static-dir=": "Any static assets files locates in this directory will be presented at /"
        completion:
          flag:
            container-env-vars: ["$files"]
            env-vars: ["$files"]
            layer-cache-basedir: ["$directories"]
            template-file: ["$files"]
            warm-containers: ["eager\tContainers for all functions are loaded at startup and persist between invocations", "lazy\tContainers are only loaded when each function is first invoked. Those containers persist for additional invocations"]
            static-dir: ["$files"]
      - name: start-lambda
        description: "Starts a local endpoint you can use to invoke your local Lambda functions"
        flags:
          "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
          "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
          "--container-env-vars=": "JSON file containing environment variables to be set within the container environment"
          "--container-host-interface=": "IP address of the host network interface that container ports should bind to. Use 0.0.0.0 to bind to all interfaces"
          "--container-host=": "Host of locally emulated Lambda container"
          "--debug-function=": "Specifies the Lambda Function logicalId to apply debug options to when --warm-containers is specified. This parameter applies to --debug-port, --debugger-path, and --debug-args"
          "--debugger-args=": "Additional arguments to be passed to the debugger"
          "--debugger-path=": "Host path to a debugger to mount into the Lambda container"
          "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
          "-d, --debug-port=*": "When specified, Lambda function will start in debug mode and expose this port to localhost"
          "--docker-network=": "Specifies the name or id of an existing docker network to lambda docker containers should connect to, along with the default bridge network. If not specified, the Lambda containers will only connect to the default bridge docker network."
          "-v, --docker-volume-basedir=": "Location basedir where the SAM file exists"
          "-n, --env-vars=": "JSON file containing values for Lambda function environment variables"
          "--force-image-build": "Whether the image should be rebuilt"
          "--help, -h": "Show help for sam"
          "--host=": "Local hostname or IP address to bind to"
          "-ii, --invoke-image=": "Container image URIs for invoking functions or starting api and function"
          "--layer-cache-basedir=": "Location where Layers your template uses will be downloaded to"
          "-l, --log-file=": "Log file to send runtime logs to"
          "--parameter-overrides=": "A string that contains AWS CloudFormation parameter overrides encoded as key=value pairs"
          "-p, --port=": "Local port to listen on"
          "--profile=": "Select a specific profile from your credential file to get AWS credentials"
          "--region=": "Set the AWS Region of the service"
          "--shutdown": "If set, will emulate a shutdown event after the invoke completes, in order to test extension handling of shutdown behavior"
          "--skip-pull-image": "Specify whether CLI should skip pulling down the latest Docker image for Lambda runtime"
          "-t, --template-file, --template=": "AWS SAM template file"
          "--warm-containers=": "Specific how SAM manages the containers for each function"
        completion:
          flag:
            container-env-vars: ["$files"]
            env-vars: ["$files"]
            layer-cache-basedir: ["$directories"]
            template-file: ["$files"]
            warm-containers: ["eager\tContainers for all functions are loaded at startup and persist between invocations", "lazy\tContainers are only loaded when each function is first invoked. Those containers persist for additional invocations"]
  - name: package
    description: "Package an AWS SAM application"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--force-upload": "Indicates whether to override existing files in the S3 bucket. Specify this flag to upload artifacts even if they match existing artifacts in the S3 bucket"
      "--help, -h": "Show help for sam"
      "--image-repositories=*": "Specify mapping of Function Logical ID to ECR Repo uri"
      "--image-repository=": "ECR repo uri where this command uploads the image artifacts that are referenced in your template"
      "--kms-key-id=": "The ID of an AWS KMS key that the command uses to encrypt artifacts that are at rest in the S3 bucket"
      "--metadata=": "A map of metadata to attach to ALL the artifacts that are referenced in your template"
      "--no-progressbar": "Does not showcase a progress bar when uploading artifacts to s3"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "--resolve-s3": "Automatically resolve s3 bucket for non-guided deployments. Enabling this option will also create a managed default s3 bucket for you"
      "--s3-bucket=": "The name of the S3 bucket where this command uploads the artifacts that are referenced in your template"
      "--s3-prefix=": "A prefix name that the command adds to the artifacts name when it uploads them to the S3 bucket"
      "--signing-profiles=": "A string that contains Code Sign configuration parameters"
      "-t, --template-file, --template=": "AWS SAM template file"
      "--use-json": "Indicates whether to use JSON as the format for the output AWS CloudFormation template. YAML is used by default"
      "--output-template-file=": "The path to the file where the command writes the output AWS CloudFormation template. If unspecified, the command writes the template to the standard output"
    completion:
      flag:
        template-file: ["$files"]
  - name: deploy
    description: "Deploy an AWS SAM application"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "-g, --guided": "Specify this flag to allow SAM CLI to guide you through the deployment using guided prompts"
      "-t, --template-file, --template=": "AWS SAM template file"
      "--stack-name=": "The name of the AWS CloudFormation stack you're deploying to. If you specify an existing stack, the command updates the stack. If you specify a new stack, the command creates it"
      "--s3-bucket=": "The name of the S3 bucket where this command uploads the artifacts that are referenced in your template"
      "--image-repository=": "ECR repo uri where this command uploads the image artifacts that are referenced in your template"
      "--image-repositories=*": "Specify mapping of Function Logical ID to ECR Repo uri"
      "--force-upload": "Indicates whether to override existing files in the S3 bucket. Specify this flag to upload artifacts even if they match existing artifacts in the S3 bucket"
      "--s3-prefix=": "A prefix name that the command adds to the artifacts name when it uploads them to the S3 bucket"
      "--kms-key-id=": "The ID of an AWS KMS key that the command uses to encrypt artifacts that are at rest in the S3 bucket"
      "--use-json": "Indicates whether to use JSON as the format for the output AWS CloudFormation template. YAML is used by default"
      "--resolve-s3": "Automatically resolve s3 bucket for non-guided deployments. Enabling this option will also create a managed default s3 bucket for you"
      "--metadata=": "A map of metadata to attach to ALL the artifacts that are referenced in your template"
      "--parameter-overrides=": "A string that contains AWS CloudFormation parameter overrides encoded as key=value pairs"
      "--signing-profiles=": "A string that contains Code Sign configuration parameters"
      "--no-progressbar": "Does not showcase a progress bar when uploading artifacts to s3"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--help, -h": "Show help for sam"
    completion:
      flag:
        template-file: ["$files"]
  - name: delete
    description: "Delete an AWS SAM application and the artifacts created by `sam deploy`"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--help, -h": "Show help for sam"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "--stack-name=": "The name of the AWS CloudFormation stack you're deploying to. If you specify an existing stack, the command updates the stack. If you specify a new stack, the command creates it"
      "--no-prompts": "Specify this flag to allow SAM CLI to skip through the guided prompts"
  - name: logs
    description: "Fetch logs for a function"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--stack-name=": "The name of the AWS CloudFormation stack you're deploying to. If you specify an existing stack, the command updates the stack. If you specify a new stack, the command creates it"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "--help, -h": "Show help for sam"
  - name: publish
    description: "Publish a packaged AWS SAM template to the AWS Serverless Application Repository"
    flags:
      "--config-env=": "The environment name specifying the default parameter values in the configuration file to use"
      "--config-file=": "The path and file name of the configuration file containing default parameter values to use"
      "--debug": "Turn on debug logging to print debug message generated by SAM CLI and display timestamps"
      "--help, -h": "Show help for sam"
      "--profile=": "Select a specific profile from your credential file to get AWS credentials"
      "--region=": "Set the AWS Region of the service"
      "-t, --template-file, --template=": "AWS SAM template file"
      "--semantic-version=": "The value provided here overrides SemanticVersion in the template metadata"
    completion:
      flag:
        template-file: ["$files"]
  - name: traces
    description: "[Beta Feature] Fetch AWS X-Ray traces"
  - name: sync
    description: "[Beta Feature] Sync a project to AWS"
  - name: pipeline
    description: "Manage the continuous delivery of the application"
    flags:
      "--help, -h": "Show help for sam"
    commands:
      - name: init
        description: "Generates a CI/CD pipeline configuration file"
      - name: bootstrap
        description: "Generates the required AWS resources to connect your CI/CD system"
