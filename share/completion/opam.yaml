# Converted from argc's opam.sh by tools/argc-to-spec. Do not edit by hand.
name: opam
flags:
  "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
  "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
  "--color=": "Colorize the output."
  "--confirm-level=": "Confirmation level"
  "--criteria=": "Specify user preferences for dependency solving for this run."
  "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
  "--debug": "Print debug message to stderr."
  "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
  "--git-version": "Print the git version of opam, if set (i.e."
  "--help=": "Show this help in format FMT."
  "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
  "--json=": "Save the results of the opam run in a computer-readable file."
  "--no": "Answer no to all opam yes/no questions without prompting."
  "--no-aspcud": "Removed in 2.1."
  "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
  "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
  "-q, --quiet": "Disables --verbose."
  "--root=": "Use ROOT as the current root path."
  "--safe": "Make sure nothing will be automatically updated or rewritten."
  "--readonly": "Make sure nothing will be automatically updated or rewritten."
  "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
  "--strict": "Fail whenever an error is found in a package definition or a configuration file."
  "--switch=": "Use SWITCH as the current compiler switch."
  "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
  "-v, --verbose": "Be more verbose."
  "--version": "Show version information."
  "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
  "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
completion:
  flag:
    color: [always, never, auto]
    confirm-level: [ask, "no", "yes", unsafe-yes]
    help: [auto, pager, groff, plain]
commands:
  - name: admin
    description: "Tools for repository administrators"
    flags:
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
    commands:
      - name: add-constraint
        description: "Adds version constraints on all dependencies towards a given package"
        flags:
          "--force": "Force updating of constraints even if the resulting constraint is unsatisfiable (e.g. when adding >3 to the constraint <2)."
          "-p, --packages=": "Only add constraints for the given packages"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: add-hashes
        description: "Add archive hashes to an opam repository."
        flags:
          "-p, --packages=": "Only add hashes for the given packages"
          "--replace": "Replace the existing hashes rather than adding to them"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: cache
        description: "Fills a local cache of package archives"
        flags:
          "-c, --check-all": "Run a full integrity check on the existing cache."
          "-j, --jobs=": "Number of parallel downloads"
          "--link=": "Create reverse symbolic links to the archives within DIR, in the form DIR/PKG.VERSION/FILENAME."
          "-n, --no-repo-update": "Don't check, create or update the 'repo' file to point to the generated cache ('archive-mirrors:' field)."
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: check
        description: "Runs some consistency checks on a repository"
        flags:
          "--cycles": "Do the cycles check (and disable the others by default)"
          "-i, --ignore-test-doc": "By default, {with-test} and {with-doc} dependencies are included."
          "--installability": "Do the installability check (and disable the others by default)"
          "--obsolete": "Analyse for obsolete packages"
          "-s, --short": "Only output a list of uninstallable packages"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: filter
        description: "Filters a repository to only keep selected packages"
        flags:
          "--dry-run": "List the removal commands, without actually performing them"
          "--environment=": "Use the given opam environment, in the form of a list of comma-separated 'var=value' bindings, when resolving variables."
          "--remove": "Invert the behaviour and remove the matching packages, keeping the ones that don't match."
          "-a, --available": "List only packages that are available according to the defined environment."
          "-A, --all": "Include all, even uninstalled or unavailable packages"
          "--coinstallable-with=": "Only list packages that are compatible with all of PACKAGES."
          "--conflicts-with=": "List packages that have declared conflicts with at least one of the given list."
          "--depends-on=": "List only packages that depend on one of (comma-separated) PACKAGES."
          "--depopts": "Include optional dependencies in dependency requests."
          "--dev": "Include development packages in dependencies."
          "--field-match=": "Filter packages with a match for PATTERN on the given FIELD"
          "--has-flag=": "Only include packages which have the given flag set."
          "--has-tag=": "Only includes packages which have the given tag set"
          "--installable": "List only packages that are installable according to the defined environment (this calls the solver and may be more costly; a package depending on an unavailable one may be available, but is never installable)"
          "--nobuild": "Exclude build dependencies (they are included by default)."
          "--or": "Instead of selecting packages that match all the criteria, select packages that match any of them"
          "--post": "Include dependencies tagged as post."
          "--recursive": "With `--depends-on' and `--required-by', display all transitive dependencies rather than just direct dependencies."
          "--required-by=": "List only the dependencies of (comma-separated) PACKAGES."
          "--resolve=": "Restrict to a solution to install (comma-separated) PACKAGES, i.e."
          "-t": "Include test-only dependencies."
          "--with-test": "Include test-only dependencies."
          "--test": "Include test-only dependencies."
          "--with-dev-setup": "Include developer only dependencies."
          "--with-doc": "Include doc-only dependencies."
          "--doc": "Include doc-only dependencies."
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            has-flag: [light-uninstall, verbose, plugin, compiler, conf]
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: index
        aliases: [make]
        description: "Generate an inclusive index file for serving over HTTP."
        flags:
          "--full-urls-txt": "Generate an inclusive 'urls.txt', for a repository that will be used by opam versions earlier than 2.0."
          "--minimal-urls-txt": "Generate a minimal 'urls.txt' file, that only includes the 'repo' file."
          "--no-urls-txt": "Don't generate a 'urls.txt' file."
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: lint
        description: "Runs 'opam lint' and reports on a whole repository"
        flags:
          "-i, --ignore-packages=": "Ignore any packages having one of these warnings or errors"
          "-l, --list": "Only list package names, without warning details"
          "-s, --short": "Print only packages and warning/error numbers, without explanations"
          "-W, --warn-error": "Return failure on any warnings, not only on errors"
          "-x, --exclude=": "Exclude the given warnings or errors"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: list
        description: "Lists packages from a repository"
        flags:
          "--environment=": "Use the given opam environment, in the form of a list of comma-separated 'var=value' bindings, when resolving variables."
          "-a, --available": "List only packages that are available according to the defined environment."
          "-A, --all": "Include all, even uninstalled or unavailable packages"
          "--coinstallable-with=": "Only list packages that are compatible with all of PACKAGES."
          "--conflicts-with=": "List packages that have declared conflicts with at least one of the given list."
          "--depends-on=": "List only packages that depend on one of (comma-separated) PACKAGES."
          "--depopts": "Include optional dependencies in dependency requests."
          "--dev": "Include development packages in dependencies."
          "--field-match=": "Filter packages with a match for PATTERN on the given FIELD"
          "--has-flag=": "Only include packages which have the given flag set."
          "--has-tag=": "Only includes packages which have the given tag set"
          "--installable": "List only packages that are installable according to the defined environment (this calls the solver and may be more costly; a package depending on an unavailable one may be available, but is never installable)"
          "--nobuild": "Exclude build dependencies (they are included by default)."
          "--or": "Instead of selecting packages that match all the criteria, select packages that match any of them"
          "--post": "Include dependencies tagged as post."
          "--recursive": "With `--depends-on' and `--required-by', display all transitive dependencies rather than just direct dependencies."
          "--required-by=": "List only the dependencies of (comma-separated) PACKAGES."
          "--resolve=": "Restrict to a solution to install (comma-separated) PACKAGES, i.e."
          "-t": "Include test-only dependencies."
          "--with-test": "Include test-only dependencies."
          "--test": "Include test-only dependencies."
          "--with-dev-setup": "Include developer only dependencies."
          "--with-doc": "Include doc-only dependencies."
          "--doc": "Include doc-only dependencies."
          "--columns=": "Select the columns to display among: name, version, package, synopsis, synopsis-or-target, description, <field>:, installed-version, pin, source-hash, opam-file, all-installed-versions, available-versions, all-versions, repository, installed-files, vc-ref, depexts."
          "--normalise": "Print the values of opam fields normalised"
          "-S, --sort": "Sort the packages in dependency order (i.e."
          "-s, --short": "Don't print a header, and sets the default columns to name only."
          "--separator=": "Set the column-separator string"
          "-V, --all-versions": "Normally, when multiple versions of a package match, only one is shown in the output (the installed one, the pinned-to one, or, failing that, the highest one available or the highest one)."
          "--wrap": "Wrap long lines, the default being to truncate when displaying on a terminal, or to keep as is otherwise"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            has-flag: [light-uninstall, verbose, plugin, compiler, conf]
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: update-extrafiles
        description: "Add extra-files to an opam repository."
        flags:
          "--hash=": "The hash, or hashes to be added"
          "-p, --packages=": "Only add extra files for the given packages"
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
      - name: upgrade
        description: "Upgrades repository from earlier opam versions."
        flags:
          "--clear-cache": "Instead of running the upgrade, clear the cache of archive hashes (held in ~/.cache), that is used to avoid re-downloading files to obtain their hashes at every run."
          "-m, --mirror=": "Don't overwrite the current repository, but put an upgraded mirror in place in a subdirectory, with proper redirections."
          "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
          "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
          "--color=": "Colorize the output."
          "--confirm-level=": "Confirmation level"
          "--criteria=": "Specify user preferences for dependency solving for this run."
          "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
          "--debug": "Print debug message to stderr."
          "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
          "--git-version": "Print the git version of opam, if set (i.e."
          "--help=": "Show this help in format FMT."
          "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
          "--json=": "Save the results of the opam run in a computer-readable file."
          "--no": "Answer no to all opam yes/no questions without prompting."
          "--no-aspcud": "Removed in 2.1."
          "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
          "-q, --quiet": "Disables --verbose."
          "--root=": "Use ROOT as the current root path."
          "--safe": "Make sure nothing will be automatically updated or rewritten."
          "--readonly": "Make sure nothing will be automatically updated or rewritten."
          "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
          "--strict": "Fail whenever an error is found in a package definition or a configuration file."
          "--switch=": "Use SWITCH as the current compiler switch."
          "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
          "-v, --verbose": "Be more verbose."
          "--version": "Show version information."
          "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
          "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
        completion:
          flag:
            color: [always, never, auto]
            confirm-level: [ask, "no", "yes", unsafe-yes]
            help: [auto, pager, groff, plain]
  - name: clean
    description: "Cleans up opam caches"
    flags:
      "-a, --all-switches": "Run the switch cleanup commands in all switches."
      "-c, --download-cache": "Clear the cache of downloaded files ($OPAMROOT/download-cache), as well as the obsolete $OPAMROOT/archives, if that exists."
      "--dry-run": "Print the removal commands, but don't execute them"
      "--logs": "Clear the logs directory."
      "-r, --repo-cache": "Clear the repository cache."
      "-s, --switch-cleanup": "Run the switch-specific cleanup: clears backups, build dirs, uncompressed package sources of non-dev packages, local metadata of previously pinned packages, etc."
      "--untracked": "Clean untracked file and directories"
      "--unused-repositories": "Clear any configured repository that is not used by any switch nor the default."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: config
    description: "Display configuration options for packages."
    flags:
      "--inplace-path": "When updating the PATH variable, replace any pre-existing opam path in-place rather than putting the new path in front."
      "--set-root": "With the env and exec subcommands, also sets the OPAMROOT variable, making sure further calls to opam will use the same root."
      "--set-switch": "With the env and exec subcommands, also sets the OPAMSWITCH variable, making sure further calls to opam will use the same switch as this one."
      "--sexp": "Print environment as an s-expression rather than in shell format"
      "--shell=": "Sets the configuration mode for opam environment appropriate for SHELL."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        shell: [bash, sh, csh, zsh, fish]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
    commands:
      - name: env
        description: "Returns the bindings for the environment variables set in the current switch, e.g. PATH, in a format intended to be evaluated by a shell."
        flags:
          "-h, --help": ""
      - name: revert-env
        description: "Reverts environment changes made by opam, e.g. eval $(opam config revert-env) undoes what eval $(opam config env) did, as much as possible."
        flags:
          "-h, --help": ""
      - name: list
        description: "Without argument, prints a documented list of all available variables."
        flags:
          "-h, --help": ""
      - name: expand
        description: "Expand variable interpolations in the given string"
        flags:
          "-h, --help": ""
      - name: subst
        description: "Substitute variables in the given files."
        flags:
          "-h, --help": ""
      - name: report
        description: "Prints a summary of your setup, useful for bug-reports."
        flags:
          "-h, --help": ""
      - name: cudf-universe
        description: "Outputs the current available package universe in CUDF format."
        flags:
          "-h, --help": ""
      - name: pef-universe
        description: "Outputs the current package universe in PEF format."
        flags:
          "-h, --help": ""
      - name: exec
        description: "Removed in 2.1, use opam exec instead."
        flags:
          "-h, --help": ""
      - name: set
        description: "Removed in 2.1, use opam var instead."
        flags:
          "-h, --help": ""
      - name: unset
        description: "Removed in 2.1, use opam var instead."
        flags:
          "-h, --help": ""
      - name: set-global
        description: "Removed in 2.1, use opam var instead."
        flags:
          "-h, --help": ""
      - name: unset-global
        description: "Removed in 2.1, use opam var instead."
        flags:
          "-h, --help": ""
      - name: var
        description: "Removed in 2.1, use opam var instead."
        flags:
          "-h, --help": ""
  - name: env
    description: "Prints appropriate shell variable assignments to stdout"
    flags:
      "--check": "Exits with 0 if the environment is already up-to-date, 1 otherwise, after printing the list of not up-to-date variables."
      "--inplace-path": "When updating the PATH variable, replace any pre-existing opam path in-place rather than putting the new path in front."
      "--revert": "Output the environment with updates done by opam reverted instead."
      "--set-root": "With the env and exec subcommands, also sets the OPAMROOT variable, making sure further calls to opam will use the same root."
      "--set-switch": "With the env and exec subcommands, also sets the OPAMSWITCH variable, making sure further calls to opam will use the same switch as this one."
      "--sexp": "Print environment as an s-expression rather than in shell format"
      "--shell=": "Sets the configuration mode for opam environment appropriate for SHELL."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        shell: [bash, sh, csh, zsh, fish]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: exec
    description: "Executes a command in the proper opam environment"
    flags:
      "--inplace-path": "When updating the PATH variable, replace any pre-existing opam path in-place rather than putting the new path in front."
      "--no-switch": "Execute the command with the updates to the environment done by opam reverted instead."
      "--set-root": "With the env and exec subcommands, also sets the OPAMROOT variable, making sure further calls to opam will use the same root."
      "--set-switch": "With the env and exec subcommands, also sets the OPAMSWITCH variable, making sure further calls to opam will use the same switch as this one."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: init
    description: "Initialize opam state, or set init options."
    flags:
      "-a, --auto-setup": "Automatically do a full setup, including adding a line to your shell init files."
      "--bare": "Initialise the opam state, but don't setup any compiler switch yet."
      "--bypass-checks": "Skip checks on required or recommended tools, and assume everything is fine"
      "-c, --compiler=": "Set the compiler to install (when creating an initial switch)"
      "--config=": "Use the given init config file."
      "--cygwin-extra-packages=": "Windows only (Experimental)."
      "--cygwin-internal-install": "Windows only (Experimental)."
      "--cygwin-local-install": "Windows only (Experimental)."
      "--cygwin-location=": "Windows only (Experimental)."
      "--disable-completion": "Disable shell completion in opam init scripts."
      "--disable-sandboxing": "Use a default configuration with sandboxing disabled (note that this may be overridden by `opamrc' if --no-opamrc is not specified or --config is used)."
      "--disable-shell-hook": "Disable registration of a shell hook in opam init scripts."
      "--dot-profile=": "Name of the configuration file to update instead of ~/.profile or ~/.zshrc based on shell detection."
      "--enable-completion": "Setup shell completion in opam init scripts, for supported shells."
      "--enable-shell-hook": "Setup opam init scripts to register a shell hook that will automatically keep the shell environment up-to-date at every prompt."
      "--git-location=": "Windows only (Experimental)."
      "-i, --interactive": "Run the setup interactively (this is the default for an initial run, or when no more specific options are specified)"
      "-k, --kind=": "Specify the kind of the repository to be used (one of http, local, git, darcs or hg)."
      "-n, --no-setup": "Do not update the user shell configuration to setup opam."
      "--no-cygwin-setup": Experimental.
      "--no-git-location": Experimental.
      "--no-opamrc": "Don't read `/etc/opamrc' or `~/.opamrc': use the default settings and the files specified through --config only"
      "--reinit": "Re-run the initial checks and setup, according to opamrc, even if this is not a new opam root"
      "--shell=": "Sets the configuration mode for opam environment appropriate for SHELL."
      "--shell-setup": "Automatically setup the user shell configuration for opam, e.g. adding a line to the `~/.profile' file."
      "--show-default-opamrc": "Print the built-in default configuration to stdout and exit"
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        kind: [http, local, git, darcs, hg]
        shell: [bash, sh, csh, zsh, fish]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: install
    description: "Install a list of packages."
    flags:
      "--assume-built": "For use on locally-pinned packages: assume they have already been correctly built, and only run their installation instructions, directly from their source directory."
      "--check": "Exit with 0 if all the dependencies of PACKAGES are already installed."
      "--depext-only": "Resolves the package installation normally, but only installs the required system dependencies, without affecting the opam switch state or installing opam packages."
      "--deps-only": "Install all its dependencies, but don't actually install the package."
      "--destdir=": "Copy the files installed by the given package within the current opam switch below the prefix DIR, respecting their hierarchy, after installation."
      "--download-only": "Fetch the sources of the packages, but don't build or install anything."
      "--formula=": Experimental.
      "--ignore-conflicts": "Used with --deps-only, ignores conflicts of given package"
      "--recursive": "Allow recursive lookups of *.opam files."
      "--restore": "Attempt to restore packages that were marked for installation but have been removed due to errors"
      "--set-root": "Mark given packages as installed roots."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--unset-root": "Mark given packages as \"installed automatically\"."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: lint
    description: "Checks and validate package description ('opam') files."
    flags:
      "--check-upstream": "Check upstream, archive availability and checksum(s)"
      "--normalise": "Output a normalised version of the opam file to stdout"
      "--package=": "Lint the current definition of the given package instead of specifying an opam file directly."
      "--recursive": "Allow recursive lookups of *.opam files."
      "-s, --short": "Only print the warning/error numbers, space-separated, if any"
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "-W, --warnings=": "Select the warnings to show or hide."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: list
    description: "Display the list of available packages."
    flags:
      "-a, --available": "List only packages that are available on the current system"
      "-A, --all": "Include all, even uninstalled or unavailable packages"
      "--base": "Removed in 2.1, use --invariant instead."
      "--coinstallable-with=": "Only list packages that are compatible with all of PACKAGES."
      "--conflicts-with=": "List packages that have declared conflicts with at least one of the given list."
      "--depends-on=": "List only packages that depend on one of (comma-separated) PACKAGES."
      "--depopts": "Include optional dependencies in dependency requests."
      "--dev": "Include development packages in dependencies."
      "--field-match=": "Filter packages with a match for PATTERN on the given FIELD"
      "--has-flag=": "Only include packages which have the given flag set."
      "--has-tag=": "Only includes packages which have the given tag set"
      "-i, --installed": "List installed packages only."
      "--installable": "List only packages that can be installed on the current switch (this calls the solver and may be more costly; a package depending on an unavailable package may be available, but is never installable)"
      "--invariant": "List only the immutable base of the current switch (i.e."
      "--no-switch": "List what is available from the repositories, without consideration for the current (or any other) switch (installed or pinned packages, etc.)"
      "--nobuild": "Exclude build dependencies (they are included by default)."
      "--or": "Instead of selecting packages that match all the criteria, select packages that match any of them"
      "--owns-file=": "Finds installed packages responsible for installing the given file"
      "--pinned": "List only the pinned packages"
      "--post": "Include dependencies tagged as post."
      "--recursive": "With `--depends-on' and `--required-by', display all transitive dependencies rather than just direct dependencies."
      "--repos=": "Include only packages that took their origin from one of the given repositories (unless no-switch is also specified, this excludes pinned packages)."
      "--required-by=": "List only the dependencies of (comma-separated) PACKAGES."
      "--resolve=": "Restrict to a solution to install (comma-separated) PACKAGES, i.e."
      "--roots": "List only packages that were explicitly installed, excluding the ones installed as dependencies"
      "--installed-roots": "List only packages that were explicitly installed, excluding the ones installed as dependencies"
      "--search": "Match PATTERNS against the full descriptions of packages, and require all of them to match, instead of requiring at least one to match against package names (unless --or is also specified)."
      "-t": "Include test-only dependencies."
      "--with-test": "Include test-only dependencies."
      "--test": "Include test-only dependencies."
      "--with-dev-setup": "Include developer only dependencies."
      "--with-doc": "Include doc-only dependencies."
      "--doc": "Include doc-only dependencies."
      "--columns=": "Select the columns to display among: name, version, package, synopsis, synopsis-or-target, description, <field>:, installed-version, pin, source-hash, opam-file, all-installed-versions, available-versions, all-versions, repository, installed-files, vc-ref, depexts."
      "-e": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--external": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--depexts": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--normalise": "Print the values of opam fields normalised"
      "-S, --sort": "Sort the packages in dependency order (i.e."
      "-s, --short": "Don't print a header, and sets the default columns to name only."
      "--separator=": "Set the column-separator string"
      "-V, --all-versions": "Normally, when multiple versions of a package match, only one is shown in the output (the installed one, the pinned-to one, or, failing that, the highest one available or the highest one)."
      "--vars=*": {description: "Define the given variable bindings.", nargs: -1}
      "--wrap": "Wrap long lines, the default being to truncate when displaying on a terminal, or to keep as is otherwise"
      "--check": "Don't write anything in the output, exit with return code 0 if the list is not empty, 1 otherwise."
      "--no-depexts": "Disable external dependencies handling for the query."
      "--silent": "Removed in 2.1, use --check instead."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        has-flag: [light-uninstall, verbose, plugin, compiler, conf]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: lock
    description: "Create locked opam files to share build environments across hosts."
    flags:
      "-d, --direct-only": "Only lock direct dependencies, rather than the whole dependency tree."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: option
    description: "Global and switch configuration options settings"
    flags:
      "--global": "Act on global configuration"
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: pin
    description: "Pin a given package to a specific version or source."
    flags:
      "--all": "When unpinning, removes all pins in the given switch."
      "--current": "When pinning, use the currently installed version and metadata of the package: this will avoid reinstallations due to upstream metadata changes, and may also be used to keep a package that was removed upstream."
      "--dev-repo": "Pin to the upstream package source for the latest development version"
      "-e, --edit": "With opam pin add, edit the opam file as with `opam pin edit' after pinning."
      "-k, --kind=": "Sets the kind of pinning."
      "-n, --no-action": "Just record the new pinning status, and don't prompt for (re)installation or removal of affected packages."
      "--normalise": "Print list of available package to pin in format `name.version^url`, that is comprehensible by `opam pin add`."
      "--recursive": "Allow recursive lookups of *.opam files."
      "-s, --short": "Output raw lists of names, one per line, skipping any details."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--with-version=": "Set the pinning version to VERSION for named PACKAGES or packages retrieved from TARGET."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        kind: [http, local, git, darcs, hg]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
    commands:
      - name: list
        description: "Lists pinned packages."
        flags:
          "-h, --help": ""
      - name: scan
        description: "Lists available packages to pin in directory."
        flags:
          "-h, --help": ""
      - name: add
        description: "Pins package PACKAGE to TARGET, which may be a version, a path, or a URL."
        flags:
          "-h, --help": ""
      - name: remove
        description: "Unpins packages PACKAGES, restoring their definition from the repository, if any."
        flags:
          "-h, --help": ""
      - name: edit
        description: "Opens an editor giving you the opportunity to change the package definition that opam will locally use for package NAME, including its version and source URL."
        flags:
          "-h, --help": ""
  - name: reinstall
    description: "Reinstall a list of packages."
    flags:
      "--assume-built": "For use on locally-pinned packages: assume they have already been correctly built, and only run their installation instructions, directly from their source directory."
      "--forget-pending": "Forget about pending reinstallations of listed packages."
      "--list-pending": "List packages that have been changed since installed and are marked for reinstallation"
      "--pending": "Perform pending reinstallations, i.e."
      "--recursive": "Allow recursive lookups of *.opam files."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: remove
    aliases: [uninstall]
    description: "Remove a list of packages."
    flags:
      "-a, --auto-remove": "Remove all the packages which have not been explicitly installed and which are not necessary anymore."
      "--destdir=": "Instead of uninstalling the packages, reverts the action of opam install --destdir: remove files corresponding to what the listed packages installed to the current switch from the given DIR."
      "--force": "Execute the remove commands of given packages directly, even if they are not considered installed by opam."
      "--formula=": Experimental.
      "--recursive": "Allow recursive lookups of *.opam files."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: repository
    aliases: [remote]
    description: "Manage opam repositories."
    flags:
      "-a, --all-switches": "Act on the selections of all configured switches"
      "--dont-select": "Don't update any selections"
      "--on-switches=": "Act on the selections of the given list of switches"
      "--set-default": "Act on the default repository selection that is used for newly created switches"
      "--this-switch": "Act on the selections for the current switch (this is the default)"
      "-k, --kind=": "Specify the kind of the repository to be used (one of http, local, git, darcs or hg)."
      "--rank=": "Set the rank of the repository in the list of configured repositories."
      "-s, --short": "Output raw lists of names, one per line, skipping any details."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        kind: [http, local, git, darcs, hg]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
    commands:
      - name: add
        description: "Adds under NAME the repository at address ADDRESS to the list of configured repositories, if not already registered, and sets this repository for use in the current switch (or the specified scope)."
        flags:
          "-h, --help": ""
      - name: remove
        description: "Removes the given repositories from the selected scopes (the default scope is the current switch, see --this-switch)."
        flags:
          "-h, --help": ""
      - name: set-repos
        description: "Explicitly selects the list of repositories to look up package definitions from, in the specified priority order (overriding previous selection and ranks), according to the specified scope."
        flags:
          "-h, --help": ""
      - name: set-url
        description: "Updates the URL and trust anchors associated with a given repository name."
        flags:
          "-h, --help": ""
      - name: list
        description: "Lists the currently selected repositories in priority order from rank 1."
        flags:
          "-h, --help": ""
      - name: priority
        description: "Synonym to add NAME --rank RANK"
        flags:
          "-h, --help": ""
  - name: show
    aliases: [info]
    description: "Display information about specific packages."
    flags:
      "--all-versions": "Display information of all packages matching PACKAGES, not restrained to a single package matching PACKAGES constraints."
      "--empty-fields": "Show fields that are empty."
      "-f, --field=": "Only display the values of these fields."
      "--file=": "Removed in 2.1, use --just-file instead."
      "--just-file": "Load and display information from the given files (allowed PACKAGES are file or directory paths), without consideration for the repositories or state of the package."
      "--list-files": "List the files installed by the package."
      "--no-lint": "Don't output linting warnings or errors when reading from files"
      "--normalise": "Print the values of opam fields normalised (no newlines, no implicit brackets)"
      "--raw": "Print the raw opam file for this package"
      "--sort": "Sort opam fields"
      "--where": "Print the location of the opam file used for this package"
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: source
    description: "Get the source of an opam package."
    flags:
      "--dev-repo": "Get the latest version-controlled source rather than the release archive"
      "--dir=": "The directory where to put the source."
      "--no-switch": "Choose package without consideration for the current (or any other) switch (installed or pinned packages, etc.)"
      "--pin": "Pin the package to the downloaded source (see `opam pin')."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: switch
    description: "Manage multiple installation prefixes."
    flags:
      "-A, --alias-of=": "Removed in 2.1."
      "--deps-only": "When creating a local switch in a project directory (i.e."
      "--description=": "Attach the given description to a switch when creating it."
      "--empty": "Allow creating an empty switch, with no invariant."
      "--force": "Only for set-invariant: force setting the invariant, bypassing consistency checks."
      "--formula=": "Allows specifying a complete \"dependency formula\", possibly including disjunction cases, as the switch invariant."
      "--freeze": "When exporting, locks all VCS urls to their current commit, failing if it can not be retrieved."
      "--full": "When exporting, include the metadata of all installed packages, allowing to re-import even if they don't exist in the repositories (the default is to include only the metadata of pinned packages)."
      "-n, --no-action": "Only for set-invariant: set the invariant, but don't enforce it right away: wait for the next install, upgrade or similar command."
      "--no-autoinstall": "Removed in 2.1."
      "--no-install": "When creating a local switch, don't look for any local package definitions to install."
      "--no-switch": "Don't automatically select newly installed switches."
      "--packages=": "When installing a switch, explicitly define the set of packages to enforce as the switch invariant."
      "--repositories=": "When creating a new switch, use the given selection of repositories instead of the default."
      "-s, --short": "Output raw lists of names, one per line, skipping any details."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
    commands:
      - name: create
        description: "Create a new switch, and install the given compiler there."
        flags:
          "-h, --help": ""
      - name: set
        description: "Set the currently active switch, among the installed switches."
        flags:
          "-h, --help": ""
      - name: remove
        description: "Remove the given switch from disk."
        flags:
          "-h, --help": ""
      - name: export
        description: "Save the current switch state to a file."
        flags:
          "-h, --help": ""
      - name: import
        description: "Import a saved switch state."
        flags:
          "-h, --help": ""
      - name: reinstall
        description: "Reinstall the given compiler switch and all its packages."
        flags:
          "-h, --help": ""
      - name: list
        description: "Lists installed switches."
        flags:
          "-h, --help": ""
      - name: list-available
        description: "Lists all the possible packages that are advised for installation when creating a new switch, i.e."
        flags:
          "-h, --help": ""
      - name: show
        description: "Prints the name of the current switch."
        flags:
          "-h, --help": ""
      - name: invariant
        description: "Prints the active switch invariant."
        flags:
          "-h, --help": ""
      - name: set-invariant
        description: "Updates the switch invariant, that is, the formula that the switch must keep verifying throughout all operations."
        flags:
          "-h, --help": ""
      - name: set-description
        description: "Sets the description for the selected switch."
        flags:
          "-h, --help": ""
      - name: link
        description: "Sets a local alias for a given switch, so that the switch gets automatically selected whenever in that directory or a descendant."
        flags:
          "-h, --help": ""
      - name: install
        description: "Removed in 2.1, use create instead."
        flags:
          "-h, --help": ""
      - name: set-base
        description: "Removed in 2.1, use set-invariant instead."
        flags:
          "-h, --help": ""
  - name: tree
    description: "Draw the dependency forest of installed packages."
    flags:
      "--deps": "Draw a dependency forest, starting from the packages not required by any other packages (this is the default)."
      "--rev-deps": "Draw a reverse-dependency forest, starting from the packages which have no dependencies."
      "--leads-to": "Display only the branches which leads to one of the PACKAGES."
      "--roots-from": "Display only the trees which roots from one of the PACKAGES (this is the default)."
      "--dev": "Include development packages in dependencies."
      "--no-switch": "Ignore active switch and simulate installing packages from an empty switch to draw the forest"
      "--post": "Include dependencies tagged as post."
      "-t": "Include test-only dependencies."
      "--with-test": "Include test-only dependencies."
      "--test": "Include test-only dependencies."
      "--with-dev-setup": "Include developer only dependencies."
      "--with-doc": "Include doc-only dependencies."
      "--doc": "Include doc-only dependencies."
      "--no-constraint": "Do not display the version constraints e.g. (>= 1.0.0)."
      "--recursive": "Allow recursive lookups of *.opam files."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: update
    description: "Update the list of available packages."
    flags:
      "-a, --all": "Update all configured repositories, not only what is set in the current switch"
      "--check": "Do the update, then return with code 0 if there were any upstream changes, 1 if there were none."
      "--depexts": "Request the system package manager to update its databases (skipping all opam packages, unless --development or --repositories is also specified)."
      "--development": "Update development packages (skipping repositories unless --repositories is also specified)."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "-R, --repositories": "Update repositories (skipping development packages unless --development is also specified)."
      "-u, --upgrade": "Automatically run opam upgrade after the update."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: upgrade
    description: "Upgrade the installed package to latest version."
    flags:
      "-a, --all": "Run an upgrade of all installed packages."
      "--check": "Don't run the upgrade: just check if anything could be upgraded."
      "--fixup": "Recover from a broken state (eg."
      "--formula=": Experimental.
      "--installed": "When a directory is provided as argument, do not install pinned package that are not yet installed."
      "--recursive": "Allow recursive lookups of *.opam files."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: var
    description: "Display and update the value associated with a given variable"
    flags:
      "--global": "Act on global configuration"
      "--package=": "List all variables defined for the given package"
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: search
    description: "An alias for list --search."
    flags:
      "-a, --available": "List only packages that are available on the current system"
      "-A, --all": "Include all, even uninstalled or unavailable packages"
      "--base": "Removed in 2.1, use --invariant instead."
      "--coinstallable-with=": "Only list packages that are compatible with all of PACKAGES."
      "--conflicts-with=": "List packages that have declared conflicts with at least one of the given list."
      "--depends-on=": "List only packages that depend on one of (comma-separated) PACKAGES."
      "--depopts": "Include optional dependencies in dependency requests."
      "--dev": "Include development packages in dependencies."
      "--field-match=": "Filter packages with a match for PATTERN on the given FIELD"
      "--has-flag=": "Only include packages which have the given flag set."
      "--has-tag=": "Only includes packages which have the given tag set"
      "-i, --installed": "List installed packages only."
      "--installable": "List only packages that can be installed on the current switch (this calls the solver and may be more costly; a package depending on an unavailable package may be available, but is never installable)"
      "--invariant": "List only the immutable base of the current switch (i.e."
      "--no-switch": "List what is available from the repositories, without consideration for the current (or any other) switch (installed or pinned packages, etc.)"
      "--nobuild": "Exclude build dependencies (they are included by default)."
      "--or": "Instead of selecting packages that match all the criteria, select packages that match any of them"
      "--owns-file=": "Finds installed packages responsible for installing the given file"
      "--pinned": "List only the pinned packages"
      "--post": "Include dependencies tagged as post."
      "--recursive": "With `--depends-on' and `--required-by', display all transitive dependencies rather than just direct dependencies."
      "--repos=": "Include only packages that took their origin from one of the given repositories (unless no-switch is also specified, this excludes pinned packages)."
      "--required-by=": "List only the dependencies of (comma-separated) PACKAGES."
      "--resolve=": "Restrict to a solution to install (comma-separated) PACKAGES, i.e."
      "--roots": "List only packages that were explicitly installed, excluding the ones installed as dependencies"
      "--installed-roots": "List only packages that were explicitly installed, excluding the ones installed as dependencies"
      "-t": "Include test-only dependencies."
      "--with-test": "Include test-only dependencies."
      "--test": "Include test-only dependencies."
      "--with-dev-setup": "Include developer only dependencies."
      "--with-doc": "Include doc-only dependencies."
      "--doc": "Include doc-only dependencies."
      "--columns=": "Select the columns to display among: name, version, package, synopsis, synopsis-or-target, description, <field>:, installed-version, pin, source-hash, opam-file, all-installed-versions, available-versions, all-versions, repository, installed-files, vc-ref, depexts."
      "-e": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--external": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--depexts": "Instead of displaying the packages, display their external dependencies that are associated with the current system."
      "--normalise": "Print the values of opam fields normalised"
      "-S, --sort": "Sort the packages in dependency order (i.e."
      "-s, --short": "Don't print a header, and sets the default columns to name only."
      "--separator=": "Set the column-separator string"
      "-V, --all-versions": "Normally, when multiple versions of a package match, only one is shown in the output (the installed one, the pinned-to one, or, failing that, the highest one available or the highest one)."
      "--vars=*": {description: "Define the given variable bindings.", nargs: -1}
      "--wrap": "Wrap long lines, the default being to truncate when displaying on a terminal, or to keep as is otherwise"
      "--check": "Don't write anything in the output, exit with return code 0 if the list is not empty, 1 otherwise."
      "--no-depexts": "Disable external dependencies handling for the query."
      "--silent": "Removed in 2.1, use --check instead."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        has-flag: [light-uninstall, verbose, plugin, compiler, conf]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
  - name: unpin
    description: "An alias for pin remove."
    flags:
      "--assume-depexts": "Skip the installation step for any missing system packages, and attempt to proceed with compilation of the opam packages anyway."
      "-b, --keep-build-dir": "Keep the build directories after compiling packages."
      "--build-doc": "Removed in 2.1, use --with-doc instead."
      "--build-test": "Removed in 2.1, use --with-test instead."
      "-d, --with-doc": "Build the package documentation."
      "--dry-run": "Simulate the command, but don't actually perform any changes."
      "--fake": "This option registers the actions into the opam database, without actually performing them."
      "--ignore-constraints-on=": "Forces opam to ignore version constraints on all dependencies to the listed packages."
      "--inplace-build": "When compiling a package which has its source bound to a local directory, process the build and install actions directly in that directory, rather than in a clean copy handled by opam."
      "-j, --jobs=": "Set the maximal number of concurrent jobs to use."
      "--lock-suffix=": "Set locked files suffix to SUFFIX."
      "--locked": "In commands that use opam files found from pinned sources, if a variant of the file with an added .locked extension is found (e.g. foo.opam.locked besides foo.opam), that will be used instead."
      "-m, --make=": "Removed in 2.1, use opam config set[-global] make MAKE instead."
      "--no-checksums": "Do not verify the checksum of downloaded archives.This is equivalent to setting $OPAMNOCHECKSUMS to \"true\"."
      "--no-depexts": "Temporarily disables handling of external dependencies."
      "--require-checksums": "Reject the installation of packages that don't provide a checksum for the upstream archives."
      "--reuse-build-dir": "Reuse existing build directories (kept by using --keep-build-dir), instead of compiling from a fresh clone of the source."
      "--show-actions": "Call the solver and display the actions."
      "--skip-updates": "When running an install, upgrade or reinstall on source-pinned packages, they are normally updated from their origin first."
      "-t, --with-test": "Build and run the package unit-tests."
      "--unlock-base": "Removed in 2.1, use --update-invariant instead."
      "--update-invariant": "Allow changes to the packages set as switch base (typically, the main compiler)."
      "--with-dev-setup": "Include development tools only dependencies."
      "--all": "When unpinning, removes all pins in the given switch."
      "--current": "When pinning, use the currently installed version and metadata of the package: this will avoid reinstallations due to upstream metadata changes, and may also be used to keep a package that was removed upstream."
      "--dev-repo": "Pin to the upstream package source for the latest development version"
      "-e, --edit": "With opam pin add, edit the opam file as with `opam pin edit' after pinning."
      "-k, --kind=": "Sets the kind of pinning."
      "-n, --no-action": "Just record the new pinning status, and don't prompt for (re)installation or removal of affected packages."
      "--normalise": "Print list of available package to pin in format `name.version^url`, that is comprehensible by `opam pin add`."
      "--recursive": "Allow recursive lookups of *.opam files."
      "-s, --short": "Output raw lists of names, one per line, skipping any details."
      "--subpath=": "*.opam files are retrieved from the given sub directory instead of top directory."
      "--with-version=": "Set the pinning version to VERSION for named PACKAGES or packages retrieved from TARGET."
      "--best-effort": "Don't fail if all requested packages can't be installed: try to install as many as possible."
      "--cli=": "Use the command-line interface syntax and semantics of MAJOR.MINOR."
      "--color=": "Colorize the output."
      "--confirm-level=": "Confirmation level"
      "--criteria=": "Specify user preferences for dependency solving for this run."
      "--cudf=": "Debug option: Save the CUDF requests sent to the solver to FILENAME-<n>.cudf where <n> is the n-th call to the solver during the opam run."
      "--debug": "Print debug message to stderr."
      "--debug-level=": "Like --debug, but allows specifying the debug level (--debug sets it to 1)."
      "--git-version": "Print the git version of opam, if set (i.e."
      "--help=": "Show this help in format FMT."
      "--ignore-pin-depends": "Ignore extra pins required by packages that get pinned, either manually through opam pin or through opam install DIR."
      "--json=": "Save the results of the opam run in a computer-readable file."
      "--no": "Answer no to all opam yes/no questions without prompting."
      "--no-aspcud": "Removed in 2.1."
      "--no-auto-upgrade": "When configuring or updating a repository that is written for an earlier opam version (1.2), opam internally converts it to the current format."
      "--no-self-upgrade": "Opam will replace itself with a newer binary found at OPAMROOT/opam if present."
      "-q, --quiet": "Disables --verbose."
      "--root=": "Use ROOT as the current root path."
      "--safe": "Make sure nothing will be automatically updated or rewritten."
      "--readonly": "Make sure nothing will be automatically updated or rewritten."
      "--solver=": "Specify the CUDF solver to use for resolving package installation problems."
      "--strict": "Fail whenever an error is found in a package definition or a configuration file."
      "--switch=": "Use SWITCH as the current compiler switch."
      "--use-internal-solver": "Disable any external solver, and use the built-in one (this requires that opam has been compiled with a built-in solver)."
      "-v, --verbose": "Be more verbose."
      "--version": "Show version information."
      "-w, --working-dir": "Whenever updating packages that are bound to a local, version-controlled directory, update to the current working state of their source instead of the last committed state, or the ref they are pointing to."
      "-y, --yes": "Answer yes to all opam yes/no questions without prompting."
    completion:
      flag:
        kind: [http, local, git, darcs, hg]
        color: [always, never, auto]
        confirm-level: [ask, "no", "yes", unsafe-yes]
        help: [auto, pager, groff, plain]
