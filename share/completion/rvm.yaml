# Converted from Fig's rvm.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: rvm
description: "Ruby version manager"
persistentflags:
  "--help, -h": "Show help for rvm"
commands:
  - name: use
    description: "Setup current shell to use a specific ruby version"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: reset
    description: "Remove default and current settings, exit the shell"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: info
    description: "Show information about the current ruby environment"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: list
    description: "List currently installed version"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: reload
    description: "Reload RVM source itself (useful after changing RVM source)"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: implode
    description: "Remove all ruby installations it manages"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: get
    description: "Upgrades RVM to the stable or git head branches"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: do
    description: "Runs a named ruby file against specified and/or all rubies"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: install
    description: "Install one or many ruby versions"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: upgrade
    description: "Install new ruby, copy gemsets, make gems pristine, remove old rubies"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: reinstall
    description: "Remove ruby, install it, make gems pristine"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: uninstall
    description: "Uninstall one or many ruby versions, leaves their sources"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: remove
    description: "Remove one or many ruby versions, including their sources"
    commands:
      - name: rbx
        description: Rubinius
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: jruby
        description: JRuby
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ruby
        description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: ree
        description: "Ruby Enterprise Edition"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: system
        description: "Use the system ruby (eg. pre-RVM state)"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
      - name: default
        description: "Use RVM set default ruby and system if it hasn't been set"
        flags:
          "--version, -v": "Show version of rvm"
          "--default": "When used with ruby selection, sets a default ruby for new shells"
          "--debug": "Enable debug mode"
          "--force": "Force install, removes old install & source before install"
          "--all": "Used with 'rvm list' to display 'most' available versions"
          "--summary": "Used with 'do' to print out a summary of the commands run"
          "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: rbx
    description: Rubinius
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: jruby
    description: JRuby
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: ruby
    description: "MRI/YARV Ruby (The Standard), defaults to 1.8.6"
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: ree
    description: "Ruby Enterprise Edition"
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: system
    description: "Use the system ruby (eg. pre-RVM state)"
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
  - name: default
    description: "Use RVM set default ruby and system if it hasn't been set"
    flags:
      "--version, -v": "Show version of rvm"
      "--default": "When used with ruby selection, sets a default ruby for new shells"
      "--debug": "Enable debug mode"
      "--force": "Force install, removes old install & source before install"
      "--all": "Used with 'rvm list' to display 'most' available versions"
      "--summary": "Used with 'do' to print out a summary of the commands run"
      "-C": "Custom configure options, comma separated, double quote args that need quoting, default"
