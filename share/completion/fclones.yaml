# Converted from argc's fclones.sh by tools/argc-to-spec. Do not edit by hand.
name: fclones
flags:
  "-q, --quiet": "Suppress progress reporting"
  "-h, --help": "Print help"
  "-V, --version": "Print version"
commands:
  - name: group
    description: "Produce a list of groups of identical files"
    flags:
      "-o, --output=": "Write the report to a file instead of the standard output"
      "-f, --format=": "Set output file format"
      "--stdin": "Read the list of input paths from the standard input instead of the arguments."
      "-d, --depth=": "Limit the recursion depth."
      "-., --hidden": "Include hidden files"
      "-A, --no-ignore": "Do not ignore files matching patterns listed in `.gitignore` and `.fdignore`"
      "-L, --follow-links": "Follow symbolic links."
      "-H, --match-links": "Treat files reachable from multiple paths through links as duplicates."
      "-S, --symbolic-links": "Don't ignore symbolic links to files."
      "-I, --isolate": "Don't count matching files found within the same directory argument as duplicates"
      "-1, --one-fs": "Don't match files on different filesystems or devices"
      "--transform=": "Transform each file by the specified program before matching."
      "--in-place": "Read the transform output from the same path as the transform input file."
      "--no-copy": "Don't copy the file to a temporary location before transforming, when `$IN` parameter is specified in the `--transform` command."
      "-n, --rf-over=": "Search for over-replicated files with replication factor above the specified value."
      "--rf-under=": "Search for under-replicated files with replication factor below the specified value."
      "--unique": "Instead of searching for duplicates, search for unique files"
      "-s, --min=": "Minimum file size in bytes (inclusive)."
      "--max=": "Maximum file size in bytes (inclusive)."
      "--max-prefix-size=": "Maximum prefix size to check in bytes"
      "--max-suffix-size=": "Maximum suffix size to check in bytes"
      "--name=": "Include only file names matched fully by any of the given patterns"
      "--path=": "Include only paths matched fully by any of the given patterns"
      "--exclude=": "Ignore paths matched fully by any of the given patterns"
      "-i, --ignore-case": "Make pattern matching case-insensitive"
      "-x, --regex": "Expect patterns as Perl compatible regular expressions instead of Unix globs"
      "--hash-fn=": "A hash function to use for computing file digests"
      "--skip-content-hash": "Skip the full contents hash step entirely."
      "--cache": "Enable caching of file hashes."
      "-t, --threads=": "Set the sizes of thread-pools"
      "--base-dir=": "Base directory to use when resolving relative input paths"
      "-h, --help": "Print help (see a summary with '-h')"
    completion:
      flag:
        format: [default, fdupes, csv, json]
  - name: link
    description: "Replace redundant files with links"
    flags:
      "--dry-run": "Don't perform any changes on the file-system, but writes a log of file operations to the standard output"
      "-o, --output=": "Write the `dry_run` report to a file instead of the standard output"
      "-m, --modified-before=": "Deduplicate only the files that were modified before the given time."
      "-n, --rf-over=": "Keep at least n replicas untouched."
      "--name=": "Restrict the set of files that can be removed or replaced by links to files with the name matching any given patterns"
      "--path=": "Restrict the set of files that can be removed or replaced by links to files with the path matching any given patterns"
      "--priority=": "Set the priority for files to be removed or replaced by links"
      "--keep-name=": "Keep files with names matching any given patterns untouched"
      "--keep-path=": "Keep files with paths matching any given patterns untouched"
      "--isolate=": "Specify a list of path prefixes."
      "-H, --match-links": "Treat files reachable from multiple paths through links as duplicates"
      "--no-lock": "Don't lock files before performing an action on them"
      "--no-check-size": "Allow the size of a file to be different than the size recorded during grouping."
      "-s, --soft": "Create soft (symbolic) links"
      "-h, --help": "Print help (see a summary with '-h')"
  - name: dedupe
    description: "Deduplicate file data using native filesystem deduplication capabilities"
    flags:
      "--dry-run": "Don't perform any changes on the file-system, but writes a log of file operations to the standard output"
      "-o, --output=": "Write the `dry_run` report to a file instead of the standard output"
      "-m, --modified-before=": "Deduplicate only the files that were modified before the given time."
      "-n, --rf-over=": "Keep at least n replicas untouched."
      "--name=": "Restrict the set of files that can be removed or replaced by links to files with the name matching any given patterns"
      "--path=": "Restrict the set of files that can be removed or replaced by links to files with the path matching any given patterns"
      "--priority=": "Set the priority for files to be removed or replaced by links"
      "--keep-name=": "Keep files with names matching any given patterns untouched"
      "--keep-path=": "Keep files with paths matching any given patterns untouched"
      "--isolate=": "Specify a list of path prefixes."
      "-H, --match-links": "Treat files reachable from multiple paths through links as duplicates"
      "--no-lock": "Don't lock files before performing an action on them"
      "--no-check-size": "Allow the size of a file to be different than the size recorded during grouping."
      "-h, --help": "Print help (see a summary with '-h')"
  - name: remove
    description: "Remove redundant files"
    flags:
      "--dry-run": "Don't perform any changes on the file-system, but writes a log of file operations to the standard output"
      "-o, --output=": "Write the `dry_run` report to a file instead of the standard output"
      "-m, --modified-before=": "Deduplicate only the files that were modified before the given time."
      "-n, --rf-over=": "Keep at least n replicas untouched."
      "--name=": "Restrict the set of files that can be removed or replaced by links to files with the name matching any given patterns"
      "--path=": "Restrict the set of files that can be removed or replaced by links to files with the path matching any given patterns"
      "--priority=": "Set the priority for files to be removed or replaced by links"
      "--keep-name=": "Keep files with names matching any given patterns untouched"
      "--keep-path=": "Keep files with paths matching any given patterns untouched"
      "--isolate=": "Specify a list of path prefixes."
      "-H, --match-links": "Treat files reachable from multiple paths through links as duplicates"
      "--no-lock": "Don't lock files before performing an action on them"
      "--no-check-size": "Allow the size of a file to be different than the size recorded during grouping."
      "-h, --help": "Print help (see a summary with '-h')"
  - name: move
    description: "Move redundant files to the given directory"
    flags:
      "--dry-run": "Don't perform any changes on the file-system, but writes a log of file operations to the standard output"
      "-o, --output=": "Write the `dry_run` report to a file instead of the standard output"
      "-m, --modified-before=": "Deduplicate only the files that were modified before the given time."
      "-n, --rf-over=": "Keep at least n replicas untouched."
      "--name=": "Restrict the set of files that can be removed or replaced by links to files with the name matching any given patterns"
      "--path=": "Restrict the set of files that can be removed or replaced by links to files with the path matching any given patterns"
      "--priority=": "Set the priority for files to be removed or replaced by links"
      "--keep-name=": "Keep files with names matching any given patterns untouched"
      "--keep-path=": "Keep files with paths matching any given patterns untouched"
      "--isolate=": "Specify a list of path prefixes."
      "-H, --match-links": "Treat files reachable from multiple paths through links as duplicates"
      "--no-lock": "Don't lock files before performing an action on them"
      "--no-check-size": "Allow the size of a file to be different than the size recorded during grouping."
      "-h, --help": "Print help (see a summary with '-h')"
