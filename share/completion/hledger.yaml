# Converted from argc's hledger.sh by tools/argc-to-spec. Do not edit by hand.
name: hledger
flags:
  "-f, --file=": "use a different input file."
  "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
  "--alias=": "rename accounts named OLD to NEW"
  "--pivot=": "use some other field/tag for account names"
  "-I, --ignore-assertions": "ignore any balance assertions"
  "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
  "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
  "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
  "-D, --daily": "multiperiod/multicolumn report by day"
  "-W, --weekly": "multiperiod/multicolumn report by week"
  "-M, --monthly": "multiperiod/multicolumn report by month"
  "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
  "-Y, --yearly": "multiperiod/multicolumn report by year"
  "-p, --period=": "set start date, end date, and/or report interval all at once"
  "--date2": "match the secondary date instead."
  "--today=": "override today's date (affects relative smart dates, for tests/examples)"
  "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
  "-P, --pending": "include only pending postings/txns"
  "-C, --cleared": "include only cleared postings/txns"
  "-R, --real": "include only non-virtual postings"
  "--depth=": "(or -NUM): hide accounts/postings deeper than this"
  "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
  "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
  "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
  "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
  "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
  "--infer-equity": "infer conversion equity postings from costs"
  "--infer-costs": "infer costs from conversion equity postings"
  "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
  "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
  "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
  "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
  "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
  "--color=": "Should color-supporting commands use ANSI color codes in text output."
  "--colour=": "Should color-supporting commands use ANSI color codes in text output."
  "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
  "-h, --help": "show general help (or after CMD, command help)"
  "--man": "show user manual with man"
  "--info": "show info manual with info"
  "--debug=": "show debug output (levels 1-9, default: 1)"
  "--version": "show version information"
commands:
  - name: add
    description: "add transactions using terminal prompts"
    flags:
      "--no-new-accounts": "don't allow creating new accounts"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: import
    description: "add new transactions from other files, eg CSV files"
    flags:
      "--catchup": "just mark all transactions as already imported"
      "--dry-run": "just show the transactions to be imported"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: close
    description: "generate balance-zeroing/restoring transactions"
    flags:
      "--close": "show a closing transaction (default)"
      "--open": "show an opening transaction"
      "--migrate": "show both closing and opening transactions"
      "--retain": "show a retain earnings transaction (for RX accounts)"
      "-x, --explicit": "show all amounts explicitly"
      "--show-costs": "show amounts with different costs separately"
      "--interleaved": "show source and destination postings together"
      "--close-desc=": "set closing transaction's description"
      "--close-acct=": "set closing transaction's destination account"
      "--open-desc=": "set opening transaction's description"
      "--open-acct=": "set opening transaction's source account"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: rewrite
    description: "generate auto postings, like print --auto"
    flags:
      "--add-posting=": "add a posting to ACCT, which may be parenthesised."
      "--diff": "generate diff suitable as an input for patch tool"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: check
    description: "check for various kinds of error in the data"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: diff
    description: "compare account transactions in two journal files"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: aregister
    aliases: [areg]
    description: "show transactions in a particular account"
    flags:
      "--txn-dates": "filter strictly by transaction date, not posting date."
      "--no-elide": "don't show only 2 commodities per amount"
      "-w, --width=": "set output width (default: terminal width or $COLUMNS)."
      "--align-all": "guarantee alignment across all lines (slower)"
      "-O, --output-format=": "select the output format."
      "-o, --output-file=": "write output to FILE."
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
    completion:
      flag:
        output-format: [txt, html, csv, tsv, json]
  - name: balancesheet
    aliases: [bs]
    description: "show assets, liabilities and net worth"
    flags:
      "--sum": "show sum of posting amounts (default)"
      "--valuechange": "show total change of period-end historical balance value (caused by deposits, withdrawals, market price fluctuations)"
      "--gain": "show unrealised capital gain/loss (historical balance value minus cost basis)"
      "--budget": "show sum of posting amounts compared to budget goals defined by periodic transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: balancesheetequity
    description: "show assets, liabilities and equity"
    flags:
      "--sum": "show sum of posting amounts (default)"
      "--valuechange": "show total change of period-end historical balance value (caused by deposits, withdrawals, market price fluctuations)"
      "--gain": "show unrealised capital gain/loss (historical balance value minus cost basis)"
      "--budget": "show sum of posting amounts compared to budget goals defined by periodic transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: cashflow
    aliases: [cf]
    description: "show changes in liquid assets"
    flags:
      "--sum": "show sum of posting amounts (default)"
      "--valuechange": "show total change of period-end historical balance value (caused by deposits, withdrawals, market price fluctuations)"
      "--gain": "show unrealised capital gain/loss (historical balance value minus cost basis)"
      "--budget": "show sum of posting amounts compared to budget goals defined by periodic transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: incomestatement
    aliases: [is]
    description: "show revenues and expenses"
    flags:
      "--sum": "show sum of posting amounts (default)"
      "--valuechange": "show total change of period-end historical balance value (caused by deposits, withdrawals, market price fluctuations)"
      "--gain": "show unrealised capital gain/loss (historical balance value minus cost basis)"
      "--budget": "show sum of posting amounts compared to budget goals defined by periodic transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: activity
    description: "show a simple bar chart of posting counts per period"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: balance
    aliases: [bal]
    description: "show balance changes, end balances, budgets, gains.."
    flags:
      "--sum": "show sum of posting amounts (default)"
      "--budget=": "show sum of posting amounts together with budget goals defined by periodic transactions."
      "--valuechange": "show total change of value of period-end historical balances (caused by deposits, withdrawals, market price fluctuations)"
      "--gain": "show unrealised capital gain/loss (historical balance value minus cost basis)"
      "--count": "show the count of postings"
      "--change": "accumulate amounts from column start to column end (in multicolumn reports, default)"
      "--cumulative": "accumulate amounts from report start (specified by e.g. -b/--begin) to column end"
      "-H, --historical": "accumulate amounts from journal start to column end (includes postings before report start date)"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: print
    description: "show transactions or export journal data"
    flags:
      "-x, --explicit": "show all amounts explicitly"
      "--show-costs": "show transaction prices even with conversion postings"
      "--round=": "how much rounding or padding should be done when displaying amounts ? none - show original decimal digits,"
      "--new": "show only newer-dated transactions added in each file since last run"
      "-m, --match=": "fuzzy search for one recent transaction with description closest to DESC"
      "-O, --output-format=": "select the output format."
      "-o, --output-file=": "write output to FILE."
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
    completion:
      flag:
        output-format: [txt, beancount, csv, tsv, json, sql]
  - name: register
    aliases: [reg]
    description: "show postings in one or more accounts & running total"
    flags:
      "--cumulative": "show running total from report start date (default)"
      "-H, --historical": "show historical running total/balance (includes postings before report start date)"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: roi
    description: "show return on investments"
    flags:
      "--cashflow": "show all amounts that were used to compute returns"
      "--investment=": "query to select your investment transactions"
      "--profit-loss=": "query to select profit-and-loss or appreciation/valuation transactions"
      "--pnl=": "query to select profit-and-loss or appreciation/valuation transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: accounts
    description: "show account names"
    flags:
      "-u, --used": "show only accounts used by transactions"
      "-d, --declared": "show only accounts declared by account directive"
      "--unused": "show only accounts declared but not used"
      "--undeclared": "show only accounts used but not declared"
      "--types": "also show account types when known"
      "--positions": "also show where accounts were declared"
      "--directives": "show as account directives, for use in journals"
      "--find": "find the first account matched by the first argument (a case-insensitive infix regexp or account name)"
      "-l, --flat": "show accounts as a flat list (default)"
      "-t, --tree": "show accounts as a tree"
      "--drop=": "flat mode: omit N leading account name parts"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: codes
    description: "show transaction codes"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: commodities
    description: "show commodity/currency symbols"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: descriptions
    description: "show full transaction descriptions (payee and note)"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: files
    description: "show data files in use"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: notes
    description: "show note part of transaction descriptions"
    flags:
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: payees
    description: "show payee names"
    flags:
      "--declared": "show payees declared with payee directives"
      "--used": "show payees referenced by transactions"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: prices
    description: "show historical market prices"
    flags:
      "--show-reverse": "also show the prices inferred by reversing known prices"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: stats
    description: "show journal statistics"
    flags:
      "-o, --output-file=": "write output to FILE."
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
  - name: tags
    description: "show tag names"
    flags:
      "--values": "list tag values instead of tag names"
      "--parsed": "show tags/values in the order they were parsed, including duplicates"
      "-f, --file=": "use a different input file."
      "--rules-file=": "CSV conversion rules file (default: FILE.rules)"
      "--alias=": "rename accounts named OLD to NEW"
      "--pivot=": "use some other field/tag for account names"
      "-I, --ignore-assertions": "ignore any balance assertions"
      "-s, --strict": "do extra error checking (check that all posted accounts are declared)"
      "-b, --begin=": "include postings/txns on or after this date (will be adjusted to preceding subperiod start when using a report interval)"
      "-e, --end=": "include postings/txns before this date (will be adjusted to following subperiod end when using a report interval)"
      "-D, --daily": "multiperiod/multicolumn report by day"
      "-W, --weekly": "multiperiod/multicolumn report by week"
      "-M, --monthly": "multiperiod/multicolumn report by month"
      "-Q, --quarterly": "multiperiod/multicolumn report by quarter"
      "-Y, --yearly": "multiperiod/multicolumn report by year"
      "-p, --period=": "set start date, end date, and/or report interval all at once"
      "--date2": "match the secondary date instead."
      "--today=": "override today's date (affects relative smart dates, for tests/examples)"
      "-U, --unmarked": "include only unmarked postings/txns (can combine with -P or -C)"
      "-P, --pending": "include only pending postings/txns"
      "-C, --cleared": "include only cleared postings/txns"
      "-R, --real": "include only non-virtual postings"
      "--depth=": "(or -NUM): hide accounts/postings deeper than this"
      "-E, --empty": "show items with zero amount, normally hidden (and vice-versa in hledger-ui/hledger-web)"
      "-B, --cost": "show amounts converted to their cost/selling amount, using the transaction price."
      "-V, --market": "show amounts converted to period-end market value in their default valuation commodity."
      "-X, --exchange=": "show amounts converted to current (single period reports) or period-end (multiperiod reports) market value in the specified commodity."
      "--value=": "show amounts converted with valuation TYPE, and optionally to specified commodity COMM."
      "--infer-equity": "infer conversion equity postings from costs"
      "--infer-costs": "infer costs from conversion equity postings"
      "--infer-market-prices": "use costs as additional market prices, as if they were P directives"
      "--forecast=": "Generate transactions from periodic rules, between the latest recorded txn and 6 months from today, or during the specified PERIOD (= is required)."
      "--auto": "Generate extra postings by applying auto posting rules to all txns (not just forecast txns)."
      "--verbose-tags": "Add visible tags indicating transactions or postings which have been generated/modified."
      "-c, --commodity-style=": "Override the commodity style in the output for the specified commodity."
      "--color=": "Should color-supporting commands use ANSI color codes in text output."
      "--colour=": "Should color-supporting commands use ANSI color codes in text output."
      "--pretty=": "Show prettier output, e.g. using unicode box-drawing characters."
      "-h, --help": "show general help (or after CMD, command help)"
      "--man": "show user manual with man"
      "--info": "show info manual with info"
      "--debug=": "show debug output (levels 1-9, default: 1)"
      "--version": "show version information"
