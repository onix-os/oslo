# Converted from Fig's sake.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: sake
description: "🍶 Swift-based utility for managing project commands, inspired by Make"
flags:
  "--version": "Show the version"
persistentflags:
  "--help, -h": "Show help information"
commands:
  - name: init
    description: "Initialize a new SakeApp project template for defining commands"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
  - name: clean
    description: "Remove all build artifacts generated by the SakeApp to ensure a clean state for future builds"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
  - name: edit
    description: "Open the SakeApp in Xcode (macOS only)"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
  - name: list
    description: "List all available commands defined in the SakeApp"
    flags:
      "--case-converting-strategy=": "Specify the strategy for converting command names' case"
      "--sake-app-prebuilt-binary-path, -b=": "Specify the path to the prebuilt SakeApp binary"
      "--json, -j": "Output the list of commands in JSON format"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
        case-converting-strategy: [keepOriginal, toSnakeCase, toKebabCase]
        sake-app-prebuilt-binary-path: ["$files"]
  - name: build
    description: "Manually trigger a rebuild of the SakeApp"
    flags:
      "--show-bin-path": "Print the path to the built SakeApp executable"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
  - name: run
    description: "Run the specified command from the SakeApp"
    flags:
      "--case-converting-strategy=": "Specify the strategy for converting command names' case"
      "--sake-app-prebuilt-binary-path, -b=": "Specify the path to the prebuilt SakeApp binary"
    persistentflags:
      "--config-path, -c=": "Specify the path to the configuration file"
      "--sake-app-path, -s=": "Specify the path for the SakeApp package"
    completion:
      flag:
        config-path: ["$files"]
        sake-app-path: ["$directories"]
        case-converting-strategy: [keepOriginal, toSnakeCase, toKebabCase]
        sake-app-prebuilt-binary-path: ["$files"]
