# Converted from Fig's dog.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: dog
description: "A command-line DNS client"
flags:
  "-q, --query=": "Host name or IP address to query"
  "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
  "-n, --nameserver=": "Address of the nameserver to send packets to"
  "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
  "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
  "--txid=": "Set the transaction ID to a specific value"
  "-Z=": "Set uncommon protocol-level tweaks"
  "-U, --udp": "Use the DNS protocol over UDP"
  "-T, --tcp": "Use the DNS protocol over TCP"
  "-S, --tls": "Use the DNS-over-TLS protocol"
  "-H, --https": "Use the DNS-over-HTTPS protocol"
  "-1, --short": "Short mode: display nothing but the first result"
  "-J, --json": "Display the output as JSON"
  "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
  "--seconds": "Do not format durations, display them as seconds"
  "--time": "Print how long the response took to arrive"
commands:
  - name: A
    description: "Query Domain A Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: MX
    description: "Query Domain MX Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: CNAME
    description: "Query Domain CNAME Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: TXT
    description: "Query Domain TXT Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: NS
    description: "Query NS Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: SOA
    description: "Query SOA Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: TTL
    description: "Query TTL Record"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: "ANY +noall +answer"
    description: "Query ALL DNS Records"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
  - name: "+nocomments +noquestion +noauthority +noadditional +nostats"
    description: "Query only answer section"
    flags:
      "-q, --query=": "Host name or IP address to query"
      "-t, --type=": "Type of the DNS record being queried (A, MX, NS...)"
      "-n, --nameserver=": "Address of the nameserver to send packets to"
      "-class=": "Network class of the DNS record being queried (IN, CH, HS)"
      "--edns=": "Whether to OPT in to EDNS (disable, hide, show)"
      "--txid=": "Set the transaction ID to a specific value"
      "-Z=": "Set uncommon protocol-level tweaks"
      "-U, --udp": "Use the DNS protocol over UDP"
      "-T, --tcp": "Use the DNS protocol over TCP"
      "-S, --tls": "Use the DNS-over-TLS protocol"
      "-H, --https": "Use the DNS-over-HTTPS protocol"
      "-1, --short": "Short mode: display nothing but the first result"
      "-J, --json": "Display the output as JSON"
      "--color=": {description: "When to colourise the output (always, automatic, never)", nargs: 2}
      "--seconds": "Do not format durations, display them as seconds"
      "--time": "Print how long the response took to arrive"
