# Converted from argc's httm.sh by tools/argc-to-spec. Do not edit by hand.
name: httm
flags:
  "-b, --browse": "interactive browse and search a specified directory to display unique file versions."
  "-s, --select=": "interactive browse and search a specified directory to display unique file versions."
  "-r, --restore=": "interactive browse and search a specified directory to display unique file versions."
  "-d, --deleted=": "show deleted files in interactive modes."
  "-R, --recursive": "recurse into the selected directory to find more files."
  "-a, --alt-replicated": "automatically discover locally replicated datasets and list their snapshots as well."
  "-p, --preview=": "user may specify a command to preview snapshots while in a snapshot selection view."
  "--uniqueness=": "comparing file versions solely on the basis of size and modify time (the default \"metadata\" behavior) may return what appear to be \"false positives\", in the sense that, modify time is not a precise measure of whether a file has actually changed."
  "-e, --exact": "use exact pattern matching for searches in the interactive modes (in contrast to the default fuzzy searching)."
  "-S, --snap=": "snapshot a file/s most immediate mount."
  "--list-snaps=": "display snapshots names for a file."
  "--prune": "prune all snapshot/s which contain the input file/s on that file's most immediate mount via \"zfs destroy\"."
  "--roll-forward=": "traditionally 'zfs rollback' is a destructive operation, whereas httm roll-forward is non-destructive."
  "-m, --file-mount=": "by default, display the all mount point/s of all dataset/s which contain/s the input file/s."
  "-l, --last-snap=": "automatically select and print the path of last-in-time unique snapshot version for the input file."
  "-n, --raw": "display the snapshot locations only, without extraneous information, delimited by a NEWLINE character."
  "-0, --zero": "display the snapshot locations only, without extraneous information, delimited by a NULL character."
  "--not-so-pretty": "display the ordinary output, but tab delimited, without any pretty border lines."
  "--json": "display the ordinary output, but as formatted JSON."
  "--omit-ditto": "omit display of the snapshot version which may be identical to the live version."
  "--no-filter": "by default, in the interactive modes, httm will filter out files residing upon non-supported datasets (like ext4, tmpfs, procfs, sysfs, or devtmpfs, etc.), and within any \"common\" snapshot paths."
  "--no-hidden": "do not show information regarding hidden files and directories (those that start with a '.') in the recursive or interactive modes."
  "--one-filesystem": "limit recursive search to file and directories on the same filesystem/device as the target directory."
  "--no-traverse": "in recursive mode, don't traverse symlinks."
  "--no-live": "only display information concerning snapshot versions (display no information regarding live versions of files or directories)."
  "--alt-store=": "give priority to discovered alternative backups stores, like Restic, and Time Machine."
  "--no-snap": "only display information concerning 'pseudo-live' versions in any Display Recursive mode (in --deleted, --recursive, but non-interactive modes)."
  "--map-aliases=": "manually map a local directory (eg."
  "--num-versions=": "detect and display the number of unique versions available (e.g. one, \"1\", version is available if either a snapshot version exists, and is identical to live version, or only a live version exists)."
  "--utc": "use UTC for date display and timestamps"
  "--no-clones": "by default, when copying files from snapshots, httm will first attempt a zero copy \"reflink\" clone on systems that support it."
  "--debug": "print configuration and debugging info"
  "--install-zsh-hot-keys": "install zsh hot keys to the users home directory, and then exit"
  "-h, --help": "Print help"
  "-V, --version": "Print version"
completion:
  flag:
    select: [path, contents, preview]
    restore: [copy, copy-and-preserve, overwrite, yolo, guard]
    deleted: [all, single, only]
    uniqueness: [all, no-filter, metadata, contents]
    file-mount: [source, target, mount, directory, device, dataset, relative-path, relative, relpath]
    last-snap: [any, ditto, no-ditto, no-ditto-exclusive, no-ditto-inclusive, none, without]
    alt-store: [restic, timemachine]
    num-versions: [all, graph, single, single-no-snap, single-with-snap, multiple]
