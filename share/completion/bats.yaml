# Converted from argc's bats.sh by tools/argc-to-spec. Do not edit by hand.
name: bats
flags:
  "-c, --count": "Count test cases without running any tests"
  "--code-quote-style=": "A two character string of code quote delimiters or 'custom' which requires setting $BATS_BEGIN_CODE_QUOTE and $BATS_END_CODE_QUOTE."
  "--line-reference-format": "Controls how file/line references e.g. in stack traces are printed: - comma_line (default): a.bats, line 1"
  "-f, --filter=": "Only run tests that match the regular expression"
  "--filter-status=": "Only run tests with the given status in the last completed (no CTRL+C/SIGINT) run."
  "--filter-tags=": "Only run tests that match all the tags in the list (&&)."
  "-F, --formatter=": "Switch between formatters: pretty (default), tap (default w/o term), tap13, junit, /<absolute path to formatter>"
  "--gather-test-outputs-in=": "Gather the output of failing *and* passing tests as files in directory (if existing, must be empty)"
  "-h, --help": "Display this help message"
  "-j, --jobs=": "Number of parallel jobs (requires GNU parallel or shenwei356/rush)"
  "--parallel-binary-name": "Name of parallel binary"
  "--no-tempdir-cleanup": "Preserve test output temporary directory"
  "--no-parallelize-across-files": "Serialize test file execution instead of running them in parallel (requires --jobs >1)"
  "--no-parallelize-within-files": "Serialize test execution within files instead of running them in parallel (requires --jobs >1)"
  "--report-formatter=": "Switch between reporters (same options as --formatter)"
  "-o, --output=": "Directory to write report files (must exist)"
  "-p, --pretty": "Shorthand for \"--formatter pretty\""
  "--print-output-on-failure": "Automatically print the value of `$output` on failed tests"
  "-r, --recursive": "Include tests in subdirectories"
  "--show-output-of-passing-tests": "Print output of passing tests"
  "-t, --tap": "Shorthand for \"--formatter tap\""
  "-T, --timing": "Add timing information to tests"
  "-x, --trace": "Print test commands as they are executed (like `set -x`)"
  "--verbose-run": "Make `run` print `$output` by default"
  "-v, --version": "Display the version number"
