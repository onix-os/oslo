# Converted from argc's grpcurl.sh by tools/argc-to-spec. Do not edit by hand.
name: grpcurl
flags:
  "-H=": "Additional headers in 'name: value' format."
  "-allow-unknown-fields": "When true, the request contents, if 'json' format is used, allows unknown fields to be present."
  "-alts": "Use Application Layer Transport Security (ALTS) when connecting to server."
  "-alts-handshaker-service=": "If set, this server will be used to do the ATLS handshaking."
  "-alts-target-service-account=": "The full email address of the service account that the server is expected to be using when ALTS is used."
  "-authority=": "The authoritative name of the remote server."
  "-cacert=": "File containing trusted root certificates for verifying the server."
  "-cert=": "File containing client certificate (public key), to present to the server."
  "-connect-timeout=": "The maximum time, in seconds, to wait for connection to be established."
  "-d=": "Data for request contents."
  "-emit-defaults": "Emit default values for JSON-encoded responses."
  "-expand-headers": "If set, headers may use '${NAME}' syntax to reference environment variables."
  "-format=": "The format of request data."
  "-format-error": "When a non-zero status is returned, format the response using the value set by the -format flag ."
  "-help": "Print usage instructions and exit."
  "-import-path=": "The path to a directory from which proto sources can be imported, for use with -proto flags."
  "-insecure": "Skip server certificate and domain verification."
  "-keepalive-time=": "If present, the maximum idle time in seconds, after which a keepalive probe is sent."
  "-key=": "File containing client private key, to present to the server."
  "-max-msg-sz=": "The maximum encoded size of a response message, in bytes, that grpcurl will accept."
  "-max-time=": "The maximum total time the operation can take, in seconds."
  "-msg-template": "When describing messages, show a template of input data."
  "-plaintext": "Use plain-text HTTP/2 when connecting to server (no TLS)."
  "-proto=": "The name of a proto source file."
  "-protoset=": "The name of a file containing an encoded FileDescriptorSet."
  "-protoset-out=": "The name of a file to be written that will contain a FileDescriptorSet proto."
  "-reflect-header=": "Additional reflection headers in 'name: value' format."
  "-rpc-header=": "Additional RPC headers in 'name: value' format."
  "-servername=": "Override server name when validating TLS certificate."
  "-unix": "Indicates that the server address is the path to a Unix domain socket."
  "-use-reflection": "When true, server reflection will be used to determine the RPC schema."
  "-user-agent=": "If set, the specified value will be added to the User-Agent header set by the grpc-go library."
  "-v": "Enable verbose output."
  "-version": "Print version."
  "-vv": "Enable very verbose output (includes timing data)."
completion:
  positional:
    - []
    - [list, describe]
