# Converted from argc's netcat.sh by tools/argc-to-spec. Do not edit by hand.
name: netcat
flags:
  "-4": "Use IPv4 addresses only."
  "-6": "Use IPv6 addresses only."
  "-b": "Allow broadcast."
  "-C": "Send CRLF as line-ending."
  "-D": "Enable debugging on the socket."
  "-d": "Do not attempt to read from stdin."
  "-F": "Pass the first connected socket using sendmsg(2) to stdout and exit."
  "-h": "Print out the nc help text and exit."
  "-I=": "Specify the size of the TCP receive buffer."
  "-i=": "Sleep for interval seconds between lines of text sent and received."
  "-k": "When a connection is completed, listen for another one."
  "-l": "Listen for an incoming connection rather than initiating a connection to a remote host."
  "-M=": "Set the TTL / hop limit of outgoing packets."
  "-m=": "Ask the kernel to drop incoming packets whose TTL / hop limit is under minttl."
  "-N": "shutdown(2) the network socket after EOF on the input."
  "-n": "Do not perform domain name resolution."
  "-O=": "Specify the size of the TCP send buffer."
  "-P=": "Specifies a username to present to a proxy server that requires authentication."
  "-p=": "Specify the source port nc should use, subject to privilege restrictions and availability."
  "-q=": "after EOF on stdin, wait the specified number of seconds and then quit."
  "-r": "Choose source and/or destination ports randomly instead of sequentially within a range or in the order that the system assigns them."
  "-S": "Enable the RFC 2385 TCP MD5 signature option."
  "-s=": "Set the source address to send packets from, which is useful on machines with multiple interfaces."
  "-T=": "Change the IPv4 TOS/IPv6 traffic class value."
  "-t": "Send RFC 854 DON'T and WON'T responses to RFC 854 DO and WILL requests."
  "-U": "Use UNIX-domain sockets."
  "-u": "Use UDP instead of TCP."
  "-V=": "Set the routing table to be used."
  "-v": "Produce more verbose output."
  "-W=": "Terminate after receiving recvlimit packets from the network."
  "-w=": "Connections which cannot be established or are idle timeout after timeout seconds."
  "-X=": "Use proxy_protocol when talking to the proxy server."
  "-x=": "Connect to destination using a proxy at proxy_address and port."
  "-Z": "DCCP mode."
  "-z": "Only scan for listening daemons, without sending any data to them."
  "-help": "Print help"
  "-version": "Print version"
