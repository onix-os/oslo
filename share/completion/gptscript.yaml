# Converted from argc's gptscript.sh by tools/argc-to-spec. Do not edit by hand.
name: gptscript
flags:
  "--cache-dir=": "Directory to store cache (default: $XDG_CACHE_HOME/gptscript) ($GPTSCRIPT_CACHE_DIR)"
  "--chat-state=": "The chat state to continue, or null to start a new chat and return the state ($GPTSCRIPT_CHAT_STATE)"
  "-C, --chdir=": "Change current working directory ($GPTSCRIPT_CHDIR)"
  "--color": "Use color in output (default true) ($GPTSCRIPT_COLOR)"
  "--config=": "Path to GPTScript config file ($GPTSCRIPT_CONFIG)"
  "--confirm": "Prompt before running potentially dangerous commands ($GPTSCRIPT_CONFIRM)"
  "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
  "--credential-override=*": {description: "Credentials to override (ex: --credential-override github.com/example/cred-tool:API_TOKEN=1234) ($GPTSCRIPT_CREDENTIAL_OVERRIDE)", nargs: -1}
  "--debug": "Enable debug logging ($GPTSCRIPT_DEBUG)"
  "--debug-messages": "Enable logging of chat completion calls ($GPTSCRIPT_DEBUG_MESSAGES)"
  "--default-model=": "Default LLM model to use ($GPTSCRIPT_DEFAULT_MODEL) (default \"gpt-4o\")"
  "--default-model-provider=": "Default LLM model provider to use, this will override OpenAI settings ($GPTSCRIPT_DEFAULT_MODEL_PROVIDER)"
  "--disable-cache": "Disable caching of LLM API responses ($GPTSCRIPT_DISABLE_CACHE)"
  "--disable-tui": "Don't use chat TUI but instead verbose output ($GPTSCRIPT_DISABLE_TUI)"
  "--dump-state=": "Dump the internal execution state to a file ($GPTSCRIPT_DUMP_STATE)"
  "--events-stream-to=": "Stream events to this location, could be a file descriptor/handle (e.g. fd://2), filename, or named pipe (e.g. \\\\.\\pipe\\my-pipe) ($GPTSCRIPT_EVENTS_STREAM_TO)"
  "--force-chat": "Force an interactive chat session if even the top level tool is not a chat tool ($GPTSCRIPT_FORCE_CHAT)"
  "--force-sequential": "Force parallel calls to run sequentially ($GPTSCRIPT_FORCE_SEQUENTIAL)"
  "-h, --help": "help for gptscript"
  "-f, --input=": "Read input from a file (\"-\" for stdin) ($GPTSCRIPT_INPUT_FILE)"
  "--list-models": "List the models available and exit ($GPTSCRIPT_LIST_MODELS)"
  "--list-tools": "List built-in tools and exit ($GPTSCRIPT_LIST_TOOLS)"
  "--no-trunc": "Do not truncate long log messages ($GPTSCRIPT_NO_TRUNC)"
  "--openai-api-key=": "OpenAI API KEY ($OPENAI_API_KEY)"
  "--openai-base-url=": "OpenAI base URL ($OPENAI_BASE_URL)"
  "--openai-org-id=": "OpenAI organization ID ($OPENAI_ORG_ID)"
  "-o, --output=": "Save output to a file, or - for stdout ($GPTSCRIPT_OUTPUT)"
  "-q, --quiet": "No output logging (set --quiet=false to force on even when there is no TTY) ($GPTSCRIPT_QUIET)"
  "--save-chat-state-file=": "A file to save the chat state to so that a conversation can be resumed with --chat-state ($GPTSCRIPT_SAVE_CHAT_STATE_FILE)"
  "--sub-tool=": "Use tool of this name, not the first tool in file ($GPTSCRIPT_SUB_TOOL)"
  "--ui": "Launch the UI ($GPTSCRIPT_UI)"
  "-v, --version": "version for gptscript"
  "--workspace=": "Directory to use for the workspace, if specified it will not be deleted on exit ($GPTSCRIPT_WORKSPACE)"
commands:
  - name: credential
    description: "List stored credentials"
    flags:
      "--all-contexts": "List credentials for all contexts ($GPTSCRIPT_CREDENTIAL_ALL_CONTEXTS)"
      "-h, --help": "help for credential"
      "--show-env-vars": "Show names of environment variables in each credential ($GPTSCRIPT_CREDENTIAL_SHOW_ENV_VARS)"
      "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
    commands:
      - name: delete
        description: "Delete a stored credential"
        flags:
          "-h, --help": "help for delete"
          "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
      - name: show
        description: "Show the secret value of a stored credential"
        flags:
          "-h, --help": "help for show"
          "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
  - name: eval
    flags:
      "--chat": "Enable chat ($GPTSCRIPT_EVAL_CHAT)"
      "-h, --help": "help for eval"
      "--internal-prompt": "($GPTSCRIPT_EVAL_INTERNAL_PROMPT)"
      "--json": "Output JSON ($GPTSCRIPT_EVAL_JSON)"
      "--max-tokens=": "Maximum number of tokens to output ($GPTSCRIPT_EVAL_MAX_TOKENS)"
      "--model=": "The model to use ($GPTSCRIPT_EVAL_MODEL)"
      "--temperature=": "Set the temperature, \"creativity\" ($GPTSCRIPT_EVAL_TEMPERATURE)"
      "--tools=*": {description: "Tools available to call ($GPTSCRIPT_EVAL_TOOLS)", nargs: -1}
      "--cache-dir=": "Directory to store cache (default: $XDG_CACHE_HOME/gptscript) ($GPTSCRIPT_CACHE_DIR)"
      "-C, --chdir=": "Change current working directory ($GPTSCRIPT_CHDIR)"
      "--color": "Use color in output (default true) ($GPTSCRIPT_COLOR)"
      "--config=": "Path to GPTScript config file ($GPTSCRIPT_CONFIG)"
      "--confirm": "Prompt before running potentially dangerous commands ($GPTSCRIPT_CONFIRM)"
      "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
      "--credential-override=*": {description: "Credentials to override (ex: --credential-override github.com/example/cred-tool:API_TOKEN=1234) ($GPTSCRIPT_CREDENTIAL_OVERRIDE)", nargs: -1}
      "--debug": "Enable debug logging ($GPTSCRIPT_DEBUG)"
      "--debug-messages": "Enable logging of chat completion calls ($GPTSCRIPT_DEBUG_MESSAGES)"
      "--default-model=": "Default LLM model to use ($GPTSCRIPT_DEFAULT_MODEL) (default \"gpt-4o\")"
      "--default-model-provider=": "Default LLM model provider to use, this will override OpenAI settings ($GPTSCRIPT_DEFAULT_MODEL_PROVIDER)"
      "--disable-cache": "Disable caching of LLM API responses ($GPTSCRIPT_DISABLE_CACHE)"
      "--dump-state=": "Dump the internal execution state to a file ($GPTSCRIPT_DUMP_STATE)"
      "--events-stream-to=": "Stream events to this location, could be a file descriptor/handle (e.g. fd://2), filename, or named pipe (e.g. \\\\.\\pipe\\my-pipe) ($GPTSCRIPT_EVENTS_STREAM_TO)"
      "-f, --input=": "Read input from a file (\"-\" for stdin) ($GPTSCRIPT_INPUT_FILE)"
      "--no-trunc": "Do not truncate long log messages ($GPTSCRIPT_NO_TRUNC)"
      "--openai-api-key=": "OpenAI API KEY ($OPENAI_API_KEY)"
      "--openai-base-url=": "OpenAI base URL ($OPENAI_BASE_URL)"
      "--openai-org-id=": "OpenAI organization ID ($OPENAI_ORG_ID)"
      "-o, --output=": "Save output to a file, or - for stdout ($GPTSCRIPT_OUTPUT)"
      "-q, --quiet": "No output logging (set --quiet=false to force on even when there is no TTY) ($GPTSCRIPT_QUIET)"
      "--workspace=": "Directory to use for the workspace, if specified it will not be deleted on exit ($GPTSCRIPT_WORKSPACE)"
  - name: fmt
    flags:
      "-h, --help": "help for fmt"
      "-w, --write": "Write output to file instead of stdout ($GPTSCRIPT_FMT_WRITE)"
      "--cache-dir=": "Directory to store cache (default: $XDG_CACHE_HOME/gptscript) ($GPTSCRIPT_CACHE_DIR)"
      "-C, --chdir=": "Change current working directory ($GPTSCRIPT_CHDIR)"
      "--color": "Use color in output (default true) ($GPTSCRIPT_COLOR)"
      "--config=": "Path to GPTScript config file ($GPTSCRIPT_CONFIG)"
      "--confirm": "Prompt before running potentially dangerous commands ($GPTSCRIPT_CONFIRM)"
      "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
      "--credential-override=*": {description: "Credentials to override (ex: --credential-override github.com/example/cred-tool:API_TOKEN=1234) ($GPTSCRIPT_CREDENTIAL_OVERRIDE)", nargs: -1}
      "--debug": "Enable debug logging ($GPTSCRIPT_DEBUG)"
      "--debug-messages": "Enable logging of chat completion calls ($GPTSCRIPT_DEBUG_MESSAGES)"
      "--default-model=": "Default LLM model to use ($GPTSCRIPT_DEFAULT_MODEL) (default \"gpt-4o\")"
      "--default-model-provider=": "Default LLM model provider to use, this will override OpenAI settings ($GPTSCRIPT_DEFAULT_MODEL_PROVIDER)"
      "--disable-cache": "Disable caching of LLM API responses ($GPTSCRIPT_DISABLE_CACHE)"
      "--dump-state=": "Dump the internal execution state to a file ($GPTSCRIPT_DUMP_STATE)"
      "--events-stream-to=": "Stream events to this location, could be a file descriptor/handle (e.g. fd://2), filename, or named pipe (e.g. \\\\.\\pipe\\my-pipe) ($GPTSCRIPT_EVENTS_STREAM_TO)"
      "-f, --input=": "Read input from a file (\"-\" for stdin) ($GPTSCRIPT_INPUT_FILE)"
      "--no-trunc": "Do not truncate long log messages ($GPTSCRIPT_NO_TRUNC)"
      "--openai-api-key=": "OpenAI API KEY ($OPENAI_API_KEY)"
      "--openai-base-url=": "OpenAI base URL ($OPENAI_BASE_URL)"
      "--openai-org-id=": "OpenAI organization ID ($OPENAI_ORG_ID)"
      "-o, --output=": "Save output to a file, or - for stdout ($GPTSCRIPT_OUTPUT)"
      "-q, --quiet": "No output logging (set --quiet=false to force on even when there is no TTY) ($GPTSCRIPT_QUIET)"
      "--workspace=": "Directory to use for the workspace, if specified it will not be deleted on exit ($GPTSCRIPT_WORKSPACE)"
  - name: getenv
    description: "Looks up an environment variable for use in GPTScript tools"
    flags:
      "-h, --help": "help for getenv"
      "--cache-dir=": "Directory to store cache (default: $XDG_CACHE_HOME/gptscript) ($GPTSCRIPT_CACHE_DIR)"
      "-C, --chdir=": "Change current working directory ($GPTSCRIPT_CHDIR)"
      "--color": "Use color in output (default true) ($GPTSCRIPT_COLOR)"
      "--config=": "Path to GPTScript config file ($GPTSCRIPT_CONFIG)"
      "--confirm": "Prompt before running potentially dangerous commands ($GPTSCRIPT_CONFIRM)"
      "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
      "--credential-override=*": {description: "Credentials to override (ex: --credential-override github.com/example/cred-tool:API_TOKEN=1234) ($GPTSCRIPT_CREDENTIAL_OVERRIDE)", nargs: -1}
      "--debug": "Enable debug logging ($GPTSCRIPT_DEBUG)"
      "--debug-messages": "Enable logging of chat completion calls ($GPTSCRIPT_DEBUG_MESSAGES)"
      "--default-model=": "Default LLM model to use ($GPTSCRIPT_DEFAULT_MODEL) (default \"gpt-4o\")"
      "--default-model-provider=": "Default LLM model provider to use, this will override OpenAI settings ($GPTSCRIPT_DEFAULT_MODEL_PROVIDER)"
      "--disable-cache": "Disable caching of LLM API responses ($GPTSCRIPT_DISABLE_CACHE)"
      "--dump-state=": "Dump the internal execution state to a file ($GPTSCRIPT_DUMP_STATE)"
      "--events-stream-to=": "Stream events to this location, could be a file descriptor/handle (e.g. fd://2), filename, or named pipe (e.g. \\\\.\\pipe\\my-pipe) ($GPTSCRIPT_EVENTS_STREAM_TO)"
      "-f, --input=": "Read input from a file (\"-\" for stdin) ($GPTSCRIPT_INPUT_FILE)"
      "--no-trunc": "Do not truncate long log messages ($GPTSCRIPT_NO_TRUNC)"
      "--openai-api-key=": "OpenAI API KEY ($OPENAI_API_KEY)"
      "--openai-base-url=": "OpenAI base URL ($OPENAI_BASE_URL)"
      "--openai-org-id=": "OpenAI organization ID ($OPENAI_ORG_ID)"
      "-o, --output=": "Save output to a file, or - for stdout ($GPTSCRIPT_OUTPUT)"
      "-q, --quiet": "No output logging (set --quiet=false to force on even when there is no TTY) ($GPTSCRIPT_QUIET)"
      "--workspace=": "Directory to use for the workspace, if specified it will not be deleted on exit ($GPTSCRIPT_WORKSPACE)"
  - name: parse
    flags:
      "-h, --help": "help for parse"
      "-p, --pretty-print": "Indent the json output ($GPTSCRIPT_PARSE_PRETTY_PRINT)"
      "--cache-dir=": "Directory to store cache (default: $XDG_CACHE_HOME/gptscript) ($GPTSCRIPT_CACHE_DIR)"
      "-C, --chdir=": "Change current working directory ($GPTSCRIPT_CHDIR)"
      "--color": "Use color in output (default true) ($GPTSCRIPT_COLOR)"
      "--config=": "Path to GPTScript config file ($GPTSCRIPT_CONFIG)"
      "--confirm": "Prompt before running potentially dangerous commands ($GPTSCRIPT_CONFIRM)"
      "--credential-context=": "Context name in which to store credentials ($GPTSCRIPT_CREDENTIAL_CONTEXT) (default \"default\")"
      "--credential-override=*": {description: "Credentials to override (ex: --credential-override github.com/example/cred-tool:API_TOKEN=1234) ($GPTSCRIPT_CREDENTIAL_OVERRIDE)", nargs: -1}
      "--debug": "Enable debug logging ($GPTSCRIPT_DEBUG)"
      "--debug-messages": "Enable logging of chat completion calls ($GPTSCRIPT_DEBUG_MESSAGES)"
      "--default-model=": "Default LLM model to use ($GPTSCRIPT_DEFAULT_MODEL) (default \"gpt-4o\")"
      "--default-model-provider=": "Default LLM model provider to use, this will override OpenAI settings ($GPTSCRIPT_DEFAULT_MODEL_PROVIDER)"
      "--disable-cache": "Disable caching of LLM API responses ($GPTSCRIPT_DISABLE_CACHE)"
      "--dump-state=": "Dump the internal execution state to a file ($GPTSCRIPT_DUMP_STATE)"
      "--events-stream-to=": "Stream events to this location, could be a file descriptor/handle (e.g. fd://2), filename, or named pipe (e.g. \\\\.\\pipe\\my-pipe) ($GPTSCRIPT_EVENTS_STREAM_TO)"
      "-f, --input=": "Read input from a file (\"-\" for stdin) ($GPTSCRIPT_INPUT_FILE)"
      "--no-trunc": "Do not truncate long log messages ($GPTSCRIPT_NO_TRUNC)"
      "--openai-api-key=": "OpenAI API KEY ($OPENAI_API_KEY)"
      "--openai-base-url=": "OpenAI base URL ($OPENAI_BASE_URL)"
      "--openai-org-id=": "OpenAI organization ID ($OPENAI_ORG_ID)"
      "-o, --output=": "Save output to a file, or - for stdout ($GPTSCRIPT_OUTPUT)"
      "-q, --quiet": "No output logging (set --quiet=false to force on even when there is no TTY) ($GPTSCRIPT_QUIET)"
      "--workspace=": "Directory to use for the workspace, if specified it will not be deleted on exit ($GPTSCRIPT_WORKSPACE)"
