# Converted from argc's mktemp.sh by tools/argc-to-spec. Do not edit by hand.
name: mktemp
flags:
  "-d, --directory": "create a directory, not a file"
  "-u, --dry-run": "do not create anything; merely print a name (unsafe)"
  "-q, --quiet": "suppress diagnostics about file/dir-creation failure"
  "--suffix=": "append SUFF to TEMPLATE; SUFF must not contain a slash."
  "-p, --tmpdir=": "interpret TEMPLATE relative to DIR; if DIR is not specified, use $TMPDIR if set, else /tmp."
  "-t": "interpret TEMPLATE as a single file name component, relative to a directory: $TMPDIR, if set; else the directory specified via -p; else /tmp [deprecated]"
  "--help": "display this help and exit"
  "--version": "output version information and exit"
