# Converted from argc's gphoto2.sh by tools/argc-to-spec. Do not edit by hand.
name: gphoto2
flags:
  "--debug": "Turn on debugging output."
  "--debug-logfile": "FILENAME (since 2.3.0) The logfile to write the debugging info to, if --debug is given."
  "--debug-loglevel=": "LEVEL (since 2.5.5) The verbosity of debug logging."
  "--hook-script": "FILENAME (new after 2.3.0) Execute the hook script FILENAME every time a certain event happens within gphoto2."
  "-q, --quiet": "Quiet output (default=verbose)."
  "-v, --version": "Display version and exit."
  "-h, --help": "Display options and short description."
  "--usage": "Display a short usage message."
  "--list-cameras": "List supported camera models."
  "--list-ports": "List supported port devices."
  "--stdout": "Send file to stdout."
  "--stdout-size": "Print filesize before data."
  "--parsable": "Outputs the filelist in a line based format that could be passed into a shellscript or other filter."
  "--auto-detect": "List auto-detected cameras and the ports to which they are connected."
  "--port=": "Specify port device."
  "--speed=": "Specify serial transfer speed."
  "--camera=": "Specify camera model."
  "--filename=": "When downloading files from the camera, specify the file name or file name pattern to use when storing the downloaded file on the local disk."
  "--filenumber=": "If you specify the filename using the --filename option and use the %n pattern, this pattern usually starts at 1."
  "--usbid=": "(Expert only) Override USB IDs."
  "-a, --abilities": "Display the camera and driver abilities specified in the libgphoto2 driver."
  "-f, --folder=": "Specify camera folder (default=\"/\")."
  "-R, --recurse": "Recursion (default for download)."
  "--no-recurse": "No recursion (default for deletion)."
  "-l, --list-folders": "List folders in folder."
  "-L, --list-files": "List files in folder."
  "-m, --mkdir=": "Create a directory."
  "-r, --rmdir=": "Remove a directory."
  "-n, --num-files": "Display number of files."
  "-p, --get-file=": "Get files given in range."
  "-P, --get-all-files": "Get all files from folder."
  "-t, --get-thumbnail=": "Get thumbnails given in range."
  "-T, --get-all-thumbnails": "Get all thumbnails from folder."
  "--get-raw-data=": "Get raw data given in range."
  "--get-all-raw-data": "Get all raw data from folder."
  "--get-audio-data=": "Get audio data given in range."
  "--get-all-audio-data": "Get all audio data from folder."
  "--upload-metadata=": "Upload meta data for the specific file, taken from a file prefix with meta_ ."
  "--get-metadata=": "Get meta data given in range."
  "--get-all-metadata": "Get all meta data from folder."
  "--force-overwrite": "Overwrite files without asking."
  "--skip-existing": "Skip files if they exist already on the local directory."
  "--new": "Only get not already downloaded files."
  "-d, --delete-file=": "Delete files given in range."
  "-D, --delete-all-files": "Delete all files in folder (defaults to --no-recurse)."
  "-u, --upload-file=": "Upload a file to camera."
  "--capture-preview": "Capture a quick preview."
  "-B--bulb=": "Do a bulb capture for the specified amount of seconds."
  "--show-preview": "Capture a quick preview and displays it in the terminal using Ascii Art (if aalib was used during build)."
  "-F, --frames=": "Number of frames to capture in one run."
  "-I, --interval=": "Time between capture of multiple frames."
  "--reset-interval": "Setting this option will reset the time interval to the value given by the -I|--interval option when a SIGUSR1 signal is received in time-lapse mode."
  "--capture-image": "Capture an image and keep it on the camera."
  "--capture-image-and-download": "Capture an image and download it immediately to the computer."
  "--trigger-capture": "Triggers the capture an image and return."
  "--keep": "When doing --capture-image-and-download or interval capture, this option will keep the images on the memory card of the camera."
  "--no-keep": "When doing --capture-image-and-download or interval capture, this option will not keep the images on the memory card of the camera after downloading them during capture."
  "--keep-raw": "When doing --capture-image-and-download or interval capture, this option will keep the RAW images on the memory card of the camera, but still download the JPEG images."
  "--capture-movie=": "Capture a movie."
  "--capture-sound": "Capture an audio clip."
  "--capture-tethered": "SECONDS, MILLISECONDS, COUNT or MATCHSTRING Lets gphoto2 wait for notifications from the camera that an object was added."
  "--wait-event": "SECONDS, MILLISECONDS, COUNT or MATCHSTRING, --wait-event-and-download SECONDS, MILLISECONDS, COUNT or MATCHSTRING Lets gphoto2 wait for notifications from the camera for various events."
  "--show-info=": "Show information for a single or multiple images, like width, height, size and/or the capture time"
  "--config": "Starts a ncurses based text configuration menu."
  "--list-config": "List all configuration entries."
  "--list-all-config": "List all configuration entries and their values and choices."
  "--get-config=": "Get the specified configuration entry."
  "--set-config=": "Set the specified configuration entry."
  "--set-config-index=": "Set the specified configuration entry by specifying the index into the list of choices for the configuration value."
  "--set-config-value=": "Set the specified configuration entry by specifying its new value."
  "--reset": "Resets the specified (or autodetected) USB port."
  "--storage-info": "Display information about the camera's storage media."
  "--summary": "Summary of camera status."
  "--manual": "Camera driver manual."
  "--about": "About the camera driver."
  "--shell": "Start the gphoto2 shell, an interactive environment."
completion:
  flag:
    debug-loglevel: [error, debug, data, all]
