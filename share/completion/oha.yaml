# Converted from argc's oha.sh by tools/argc-to-spec. Do not edit by hand.
name: oha
flags:
  "-n=": "Number of requests to run."
  "-c=": "Number of connections to run concurrently."
  "-p=": "Number of parallel requests to send on HTTP/2."
  "-z=": "Duration of application to send requests."
  "-q=": "Rate limit for all, in queries per second (QPS)"
  "--burst-delay=": "Introduce delay between a predefined number of requests."
  "--burst-rate=": "Rates of requests for burst."
  "--rand-regex-url": "Generate URL by rand_regex crate but dot is disabled for each query e.g. http://127.0.0.1/[a-z][a-z][0-9]."
  "--max-repeat=": "A parameter for the '--rand-regex-url'."
  "--latency-correction": "Correct latency to avoid coordinated omission problem."
  "--no-tui": "No realtime tui"
  "-j, --json": "Print results as JSON"
  "--fps=": "Frame per second for tui."
  "-m, --method=": "HTTP method [default: GET]"
  "-H=": "Custom HTTP header."
  "-t=": "Timeout for each request."
  "-A=": "HTTP Accept Header."
  "-d=": "HTTP request body."
  "-D=": "HTTP request body from file."
  "-T=": Content-Type.
  "-a=": "Basic authentication, username:password"
  "--http-version=": "HTTP version."
  "--http2": "Use HTTP/2."
  "--host=": "HTTP Host header"
  "--disable-compression": "Disable compression."
  "-r, --redirect=": "Limit for number of Redirect."
  "--disable-keepalive": "Disable keep-alive, prevents re-use of TCP connections between different HTTP requests."
  "--no-pre-lookup": "*Not* perform a DNS lookup at beginning to cache it"
  "--ipv6": "Lookup only ipv6."
  "--ipv4": "Lookup only ipv4."
  "--insecure": "Accept invalid certs."
  "--connect-to=": "Override DNS resolution and default port numbers with strings like 'example.org:443:localhost:8443'"
  "--disable-color": "Disable the color scheme."
  "--unix-socket=": "Connect to a unix socket instead of the domain in the URL."
  "--vsock-addr=": "Connect to a VSOCK socket using 'cid:port' instead of the domain in the URL."
  "--stats-success-breakdown": "Include a response status code successful or not successful breakdown for the time histogram and distribution statistics"
  "-h, --help": "Print help"
  "-V, --version": "Print version"
