# Converted from Fig's rm.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: rm
description: "Remove directory entries"
flags:
  "-r, -R": "Recursive. Attempt to remove the file hierarchy rooted in each file argument"
  "-P": "Overwrite regular files before deleting them"
  "-d": "Attempt to remove directories as well as other types of files"
  "-f": "⚠️ Attempt to remove the files without prompting for confirmation"
  "-i": "Request confirmation before attempting to remove each file"
  "-v": "Be verbose when deleting files"
completion:
  positionalany: ["$directories", "$files"]
