# Converted from argc's xplr.sh by tools/argc-to-spec. Do not edit by hand.
name: xplr
flags:
  "--force-focus": "Focuses on the given <PATH>, even if it is a directory"
  "-h, --help": "Prints help information"
  "-m, --pipe-msg-in": "Helps safely passing messages to the active xplr session, use %%, %s and %q as the placeholders"
  "-M, --print-msg-in": "Like --pipe-msg-in, but prints the message instead of passing to the active xplr session"
  "--print-pwd-as-result": "Prints the present working directory when quitting with `PrintResultAndQuit`"
  "--read-only": "Enables read-only mode (config.general.read_only)"
  "--read0": "Reads paths separated using the null character (\\0)"
  "--write0": "Prints paths separated using the null character (\\0)"
  "-0, --null": "Combines --read0 and --write0"
  "-V, --version": "Prints version information"
  "-c, --config=": "Specifies a custom config file (default is \"$HOME/.config/xplr/init.lua\")"
  "-C, --extra-config=*": {description: "Specifies extra config files to load", nargs: -1}
  "--on-load=*": {description: "Sends messages when xplr loads", nargs: -1}
  "--vroot=": "Treats the specified path as the virtual root"
