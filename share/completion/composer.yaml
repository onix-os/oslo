# Converted from argc's composer.sh by tools/argc-to-spec. Do not edit by hand.
name: composer
flags:
  "-h, --help": "Display help for the given command."
  "-q, --quiet": "Do not output any message"
  "-V, --version": "Display this application version"
  "--ansi": "Force (or disable --no-ansi) ANSI output"
  "--no-ansi": "Force (or disable --no-ansi) ANSI output"
  "-n, --no-interaction": "Do not ask any interactive question"
  "--profile": "Display timing and memory usage information"
  "--no-plugins": "Whether to disable plugins."
  "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
  "-d, --working-dir=": "If specified, use the given directory as working directory."
  "--no-cache": "Prevent use of the cache"
  "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
commands:
  - name: about
    description: "Shows a short information about Composer"
    flags:
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: archive
    description: "Creates an archive of this composer package"
    flags:
      "-f, --format=": "Format of the resulting archive: tar, tar.gz, tar.bz2 or zip (default tar)"
      "--dir=": "Write the archive to this directory"
      "--file=": "Write the archive with the given file name."
      "--ignore-filters": "Ignore filters when saving package"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
    completion:
      flag:
        format: [tar, tar.gz, tar.gz2, zip]
  - name: audit
    description: "Checks for security vulnerability advisories for installed packages"
    flags:
      "--no-dev": "Disables auditing of require-dev packages."
      "-f, --format=": "Output format."
      "--locked": "Audit based on the lock file instead of the installed packages."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: browse
    aliases: [home]
    description: "Opens the package's repository URL or homepage in your browser"
    flags:
      "-H, --homepage": "Open the homepage instead of the repository URL."
      "-s, --show": "Only show the homepage or repository URL."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: bump
    description: "Increases the lower limit of your composer.json requirements to the currently installed versions"
    flags:
      "-D, --dev-only": "Only bump requirements in \"require-dev\"."
      "-R, --no-dev-only": "Only bump requirements in \"require\"."
      "--dry-run": "Outputs the packages to bump, but will not execute anything."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: check-platform-reqs
    description: "Check that platform requirements are satisfied"
    flags:
      "--no-dev": "Disables checking of require-dev packages requirements."
      "--lock": "Checks requirements only from the lock file, not from installed packages."
      "-f, --format=": "Format of the output: text or json [default: \"text\"]"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: clear-cache
    aliases: [clearcache, cc]
    description: "Clears composer's internal package cache"
    flags:
      "--gc": "Only run garbage collection, not a full cache clear"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: completion
    description: "Dump the shell completion script"
    flags:
      "--debug": "Tail the completion debug log"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
    completion:
      positional:
        - [bash, zsh, fish]
  - name: config
    description: "Sets config options"
    flags:
      "-g, --global": "Apply command to the global config file"
      "-e, --editor": "Open editor"
      "-a, --auth": "Affect auth config file (only used for --editor)"
      "--unset": "Unset the given setting-key"
      "-l, --list": "List configuration settings"
      "-f, --file=": "If you want to choose a different composer.json or config.json"
      "--absolute": "Returns absolute paths when fetching *-dir config values instead of relative"
      "-j, --json": "JSON decode the setting value, to be used with extra.* keys"
      "-m, --merge": "Merge the setting value with the current value, to be used with extra.* keys in combination with --json"
      "--append": "When adding a repository, append it (lowest priority) to the existing ones instead of prepending it (highest priority)"
      "--source": "Display where the config value is loaded from"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: create-project
    description: "Creates new project from a package into given directory"
    flags:
      "-s, --stability=": "Minimum-stability allowed (unless a version is specified)."
      "--prefer-source": "Forces installation from package sources when possible, including VCS information."
      "--prefer-dist": "Forces installation from package dist (default behavior)."
      "--prefer-install=": "Forces installation from package dist|source|auto (auto chooses source for dev versions, dist for the rest)."
      "--repository=": "Add custom repositories to look the package up, either by URL or using JSON arrays (multiple values allowed)"
      "--repository-url=": "DEPRECATED: Use --repository instead."
      "--add-repository": "Add the custom repository in the composer.json."
      "--dev": "Enables installation of require-dev packages (enabled by default, only present for BC)."
      "--no-dev": "Disables installation of require-dev packages."
      "--no-custom-installers": "DEPRECATED: Use no-plugins instead."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "--no-progress": "Do not output download progress."
      "--no-secure-http": "Disable the secure-http config option temporarily while installing the root package."
      "--keep-vcs": "Whether to prevent deleting the vcs folder."
      "--remove-vcs": "Whether to force deletion of the vcs folder without prompting."
      "--no-install": "Whether to skip installation of the package dependencies."
      "--no-audit": "Whether to skip auditing of the installed package dependencies (can also be set via the COMPOSER_NO_AUDIT=1 env var)."
      "--audit-format=": "Audit output format."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "--ask": "Whether to ask for project directory."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: depends
    aliases: [why]
    description: "Shows which packages cause the given package to be installed"
    flags:
      "-r, --recursive": "Recursively resolves up to the root package"
      "-t, --tree": "Prints the results as a nested tree"
      "--locked": "Read dependency information from composer.lock"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: diagnose
    description: "Diagnoses the system to identify common errors"
    flags:
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: dump-autoload
    aliases: [dumpautoload]
    description: "Dumps the autoloader"
    flags:
      "-o, --optimize": "Optimizes PSR0 and PSR4 packages to be loaded with classmaps too, good for production."
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu": "Use APCu to cache found/not-found classes."
      "--apcu-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "--dry-run": "Outputs the operations but will not execute anything."
      "--dev": "Enables autoload-dev rules."
      "--no-dev": "Disables autoload-dev rules."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "--strict-psr": "Return a failed status code (1) if PSR-4 or PSR-0 mapping errors are present."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: exec
    description: "Executes a vendored binary/script"
    flags:
      "-l, --list": ""
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: fund
    description: "Discover how to help fund the maintenance of your dependencies"
    flags:
      "-f, --format=": "Format of the output: text or json [default: \"text\"]"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: global
    description: "Allows running commands in the global composer dir ($COMPOSER_HOME)"
    flags:
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: init
    description: "Creates a basic composer.json file in current directory"
    flags:
      "--name=": "Name of the package"
      "--description=": "Description of package"
      "--author=": "Author name of package"
      "--type=": "Type of package (e.g. library, project, metapackage, composer-plugin)"
      "--homepage=": "Homepage of package"
      "--require=": "Package to require with a version constraint, e.g. foo/bar:1.0.0 or foo/bar=1.0.0 or \"foo/bar 1.0.0\" (multiple values allowed)"
      "--require-dev=": "Package to require for development with a version constraint, e.g. foo/bar:1.0.0 or foo/bar=1.0.0 or \"foo/bar 1.0.0\" (multiple values allowed)"
      "-s, --stability=": "Minimum stability"
      "-l, --license=": "License of package"
      "--repository=": "Add custom repositories, either by URL or using JSON arrays (multiple values allowed)"
      "-a, --autoload=": "Add PSR-4 autoload mapping."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
    completion:
      flag:
        type: [library, project, metapackage, composer-plugin]
        stability: [stable, RC, beta, alpha, dev]
  - name: install
    aliases: [i]
    description: "Installs the project dependencies from the composer.lock file if present, or falls back on the composer.json"
    flags:
      "--prefer-source": "Forces installation from package sources when possible, including VCS information."
      "--prefer-dist": "Forces installation from package dist (default behavior)."
      "--prefer-install=": "Forces installation from package dist|source|auto (auto chooses source for dev versions, dist for the rest)."
      "--dry-run": "Outputs the operations but will not execute anything (implicitly enables --verbose)."
      "--download-only": "Download only, do not install packages."
      "--dev": "DEPRECATED: Enables installation of require-dev packages (enabled by default, only present for BC)."
      "--no-suggest": "DEPRECATED: This flag does not exist anymore."
      "--no-dev": "Disables installation of require-dev packages."
      "--no-autoloader": "Skips autoloader generation"
      "--no-progress": "Do not output download progress."
      "--no-install": "Do not use, only defined here to catch misuse of the install command."
      "--audit": "Run an audit after installation is complete."
      "--audit-format=": "Audit output format."
      "-o, --optimize-autoloader": "Optimize autoloader during autoloader dump"
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu-autoloader": "Use APCu to cache found/not-found classes."
      "--apcu-autoloader-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: licenses
    description: "Shows information about licenses of dependencies"
    flags:
      "-f, --format=": "Format of the output: text, json or summary [default: \"text\"]"
      "--no-dev": "Disables search in require-dev packages."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: list
    description: "List commands"
    flags:
      "-h, --help": ""
  - name: outdated
    description: "Shows a list of installed packages that have updates available, including their latest version"
    flags:
      "-o, --outdated": "Show only packages that are outdated (this is the default, but present here for compat with `show`"
      "-a, --all": "Show all installed packages with their latest versions"
      "--locked": "Shows updates for packages from the lock file, regardless of what is currently in vendor dir"
      "-D, --direct": "Shows only packages that are directly required by the root package"
      "--strict": "Return a non-zero exit code when there are outdated packages"
      "-M, --major-only": "Show only packages that have major SemVer-compatible updates."
      "-m, --minor-only": "Show only packages that have minor SemVer-compatible updates."
      "-p, --patch-only": "Show only packages that have patch SemVer-compatible updates."
      "-A, --sort-by-age": "Displays the installed version's age, and sorts packages oldest first."
      "-f, --format=": "Format of the output: text or json [default: \"text\"]"
      "--ignore=": "Ignore specified package(s)."
      "--no-dev": "Disables search in require-dev packages."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: prohibits
    aliases: [why-not]
    description: "Shows which packages prevent the given package from being installed"
    flags:
      "-r, --recursive": "Recursively resolves up to the root package"
      "-t, --tree": "Prints the results as a nested tree"
      "--locked": "Read dependency information from composer.lock"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: reinstall
    description: "Uninstalls and reinstalls the given package names"
    flags:
      "--prefer-source": "Forces installation from package sources when possible, including VCS information."
      "--prefer-dist": "Forces installation from package dist (default behavior)."
      "--prefer-install=": "Forces installation from package dist|source|auto (auto chooses source for dev versions, dist for the rest)."
      "--no-autoloader": "Skips autoloader generation"
      "--no-progress": "Do not output download progress."
      "-o, --optimize-autoloader": "Optimize autoloader during autoloader dump"
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu-autoloader": "Use APCu to cache found/not-found classes."
      "--apcu-autoloader-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: remove
    aliases: [rm, uninstall]
    description: "Removes a package from the require or require-dev"
    flags:
      "--dev": "Removes a package from the require-dev section."
      "--dry-run": "Outputs the operations but will not execute anything (implicitly enables --verbose)."
      "--no-progress": "Do not output download progress."
      "--no-update": "Disables the automatic update of the dependencies (implies --no-install)."
      "--no-install": "Skip the install step after updating the composer.lock file."
      "--no-audit": "Skip the audit step after updating the composer.lock file (can also be set via the COMPOSER_NO_AUDIT=1 env var)."
      "--audit-format=": "Audit output format."
      "--update-no-dev": "Run the dependency update with the --no-dev option."
      "-w, --update-with-dependencies": "Allows inherited dependencies to be updated with explicit dependencies."
      "-W, --update-with-all-dependencies": "Allows all inherited dependencies to be updated, including those that are root requirements."
      "--with-all-dependencies": "Alias for --update-with-all-dependencies"
      "--no-update-with-dependencies": "Does not allow inherited dependencies to be updated with explicit dependencies."
      "-m, --minimal-changes": "During an update with -w/-W, only perform absolutely necessary changes to transitive dependencies (can also be set via the COMPOSER_MINIMAL_CHANGES=1 env var)."
      "--unused": "Remove all packages which are locked but not required by any other package."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "-o, --optimize-autoloader": "Optimize autoloader during autoloader dump"
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu-autoloader": "Use APCu to cache found/not-found classes."
      "--apcu-autoloader-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: require
    aliases: [r]
    description: "Adds required packages to your composer.json and installs them"
    flags:
      "--dev": "Add requirement to require-dev."
      "--dry-run": "Outputs the operations but will not execute anything (implicitly enables --verbose)."
      "--prefer-source": "Forces installation from package sources when possible, including VCS information."
      "--prefer-dist": "Forces installation from package dist (default behavior)."
      "--prefer-install=": "Forces installation from package dist|source|auto (auto chooses source for dev versions, dist for the rest)."
      "--fixed": "Write fixed version to the composer.json."
      "--no-suggest": "DEPRECATED: This flag does not exist anymore."
      "--no-progress": "Do not output download progress."
      "--no-update": "Disables the automatic update of the dependencies (implies --no-install)."
      "--no-install": "Skip the install step after updating the composer.lock file."
      "--no-audit": "Skip the audit step after updating the composer.lock file (can also be set via the COMPOSER_NO_AUDIT=1 env var)."
      "--audit-format=": "Audit output format."
      "--update-no-dev": "Run the dependency update with the --no-dev option."
      "-w, --update-with-dependencies": "Allows inherited dependencies to be updated, except those that are root requirements."
      "-W, --update-with-all-dependencies": "Allows all inherited dependencies to be updated, including those that are root requirements."
      "--with-dependencies": "Alias for --update-with-dependencies"
      "--with-all-dependencies": "Alias for --update-with-all-dependencies"
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "--prefer-stable": "Prefer stable versions of dependencies (can also be set via the COMPOSER_PREFER_STABLE=1 env var)."
      "--prefer-lowest": "Prefer lowest versions of dependencies (can also be set via the COMPOSER_PREFER_LOWEST=1 env var)."
      "-m, --minimal-changes": "During an update with -w/-W, only perform absolutely necessary changes to transitive dependencies (can also be set via the COMPOSER_MINIMAL_CHANGES=1 env var)."
      "--sort-packages": "Sorts packages when adding/updating a new dependency"
      "-o, --optimize-autoloader": "Optimize autoloader during autoloader dump"
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu-autoloader": "Use APCu to cache found/not-found classes."
      "--apcu-autoloader-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: run-script
    aliases: [run]
    description: "Runs the scripts defined in composer.json"
    flags:
      "--timeout=": "Sets script timeout in seconds, or 0 for never."
      "--dev": "Sets the dev mode."
      "--no-dev": "Disables the dev mode."
      "-l, --list": "List scripts."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: search
    description: "Searches for packages"
    flags:
      "-N, --only-name": "Search only in package names"
      "-O, --only-vendor": "Search only for vendor / organization names, returns only \"vendor\" as result"
      "-t, --type=": "Search for a specific package type"
      "-f, --format=": "Format of the output: text or json [default: \"text\"]"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: show
    aliases: [info]
    description: "Shows information about packages"
    flags:
      "--all": "List all packages"
      "--locked": "List all locked packages"
      "-i, --installed": "List installed packages only (enabled by default, only present for BC)."
      "-p, --platform": "List platform packages only"
      "-a, --available": "List available packages only"
      "-s, --self": "Show the root package information"
      "-N, --name-only": "List package names only"
      "-P, --path": "Show package paths"
      "-t, --tree": "List the dependencies as a tree"
      "-l, --latest": "Show the latest version"
      "-o, --outdated": "Show the latest version but only for packages that are outdated"
      "--ignore=": "Ignore specified package(s)."
      "-M, --major-only": "Show only packages that have major SemVer-compatible updates."
      "-m, --minor-only": "Show only packages that have minor SemVer-compatible updates."
      "--patch-only": "Show only packages that have patch SemVer-compatible updates."
      "-A, --sort-by-age": "Displays the installed version's age, and sorts packages oldest first."
      "-D, --direct": "Shows only packages that are directly required by the root package"
      "--strict": "Return a non-zero exit code when there are outdated packages"
      "-f, --format=": "Format of the output: text or json [default: \"text\"]"
      "--no-dev": "Disables search in require-dev packages."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: status
    description: "Shows a list of locally modified packages"
    flags:
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: suggests
    description: "Shows package suggestions"
    flags:
      "--by-package": "Groups output by suggesting package (default)"
      "--by-suggestion": "Groups output by suggested package"
      "-a, --all": "Show suggestions from all dependencies, including transitive ones"
      "--list": "Show only list of suggested package names"
      "--no-dev": "Exclude suggestions from require-dev packages"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: update
    aliases: [u, upgrade]
    description: "Updates your dependencies to the latest version according to composer.json, and updates the composer.lock file"
    flags:
      "--with=": "Temporary version constraint to add, e.g. foo/bar:1.0.0 or foo/bar=1.0.0 (multiple values allowed)"
      "--prefer-source": "Forces installation from package sources when possible, including VCS information."
      "--prefer-dist": "Forces installation from package dist (default behavior)."
      "--prefer-install=": "Forces installation from package dist|source|auto (auto chooses source for dev versions, dist for the rest)."
      "--dry-run": "Outputs the operations but will not execute anything (implicitly enables --verbose)."
      "--dev": "DEPRECATED: Enables installation of require-dev packages (enabled by default, only present for BC)."
      "--no-dev": "Disables installation of require-dev packages."
      "--lock": "Overwrites the lock file hash to suppress warning about the lock file being out of date without updating package versions."
      "--no-install": "Skip the install step after updating the composer.lock file."
      "--no-audit": "Skip the audit step after updating the composer.lock file (can also be set via the COMPOSER_NO_AUDIT=1 env var)."
      "--audit-format=": "Audit output format."
      "--no-autoloader": "Skips autoloader generation"
      "--no-suggest": "DEPRECATED: This flag does not exist anymore."
      "--no-progress": "Do not output download progress."
      "-w, --with-dependencies": "Update also dependencies of packages in the argument list, except those which are root requirements."
      "-W, --with-all-dependencies": "Update also dependencies of packages in the argument list, including those which are root requirements."
      "-o, --optimize-autoloader": "Optimize autoloader during autoloader dump."
      "-a, --classmap-authoritative": "Autoload classes from the classmap only."
      "--apcu-autoloader": "Use APCu to cache found/not-found classes."
      "--apcu-autoloader-prefix=": "Use a custom prefix for the APCu autoloader cache."
      "--ignore-platform-req=": "Ignore a specific platform requirement (php & ext- packages)."
      "--ignore-platform-reqs": "Ignore all platform requirements (php & ext- packages)."
      "--prefer-stable": "Prefer stable versions of dependencies (can also be set via the COMPOSER_PREFER_STABLE=1 env var)."
      "--prefer-lowest": "Prefer lowest versions of dependencies (can also be set via the COMPOSER_PREFER_LOWEST=1 env var)."
      "-m, --minimal-changes": "During a partial update with -w/-W, only perform absolutely necessary changes to transitive dependencies (can also be set via the COMPOSER_MINIMAL_CHANGES=1 env var)."
      "-i, --interactive": "Interactive interface with autocompletion to select the packages to update."
      "--root-reqs": "Restricts the update to your first degree dependencies."
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
  - name: validate
    description: "Validates a composer.json and composer.lock"
    flags:
      "--no-check-all": "Do not validate requires for overly strict/loose constraints"
      "--check-lock": "Check if lock file is up to date (even when config.lock is false)"
      "--no-check-lock": "Do not check if lock file is up to date"
      "--no-check-publish": "Do not check for publish errors"
      "--no-check-version": "Do not report a warning if the version field is present"
      "-A, --with-dependencies": "Also validate the composer.json of all installed dependencies"
      "--strict": "Return a non-zero exit code for warnings as well as errors"
      "-h, --help": "Display help for the given command."
      "-q, --quiet": "Do not output any message"
      "-V, --version": "Display this application version"
      "--ansi": "Force (or disable --no-ansi) ANSI output"
      "--no-ansi": "Force (or disable --no-ansi) ANSI output"
      "-n, --no-interaction": "Do not ask any interactive question"
      "--profile": "Display timing and memory usage information"
      "--no-plugins": "Whether to disable plugins."
      "--no-scripts": "Skips the execution of all scripts defined in composer.json file."
      "-d, --working-dir=": "If specified, use the given directory as working directory."
      "--no-cache": "Prevent use of the cache"
      "-v, --verbose*": "Increase the verbosity of messages: 1 for normal output, 2 for more verbose output and 3 for debug"
