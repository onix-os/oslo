# Converted from argc's jar.sh by tools/argc-to-spec. Do not edit by hand.
name: jar
flags:
  "-c, --create": "Create the archive."
  "-i, --generate-index=": "Generate index information for the specified jar archives."
  "-t, --list": "List the table of contents for the archive"
  "-u, --update": "Update an existing jar archive"
  "-x, --extract": "Extract named (or all) files from the archive"
  "-d, --describe-module": "Print the module descriptor, or automatic module name"
  "--validate": "Validate the contents of the jar archive."
  "-C=": "Change to the specified directory and include the following file"
  "-f, --file=": "The archive file name."
  "--release=": "Places all following files in a versioned directory of the jar (i.e."
  "-v, --verbose": "Generate verbose output on standard output"
  "-e, --main-class=": "The application entry point for stand-alone applications bundled into a modular, or executable, jar archive"
  "-m, --manifest=": "Include the manifest information from the given manifest file"
  "-M, --no-manifest": "Do not create a manifest file for the entries"
  "--module-version=": "The module version, when creating a modular jar, or updating a non-modular jar"
  "--hash-modules=": "Compute and record the hashes of modules matched by the given pattern and that depend upon directly or indirectly on a modular jar being created or a non-modular jar being updated"
  "-p, --module-path": "Location of module dependence for generating the hash"
  "-0, --no-compress": "Store only; use no ZIP compression"
  "--date=": "The timestamp in ISO-8601 extended offset date-time with optional time-zone format, to use for the timestamps of entries, e.g. \"2022-02-12T12:30:00-05:00\""
  "-h, --help=": "Give this, or optionally the compatibility, help"
  "--help-extra": "Give help on extra options"
  "--version": "Print program version"
