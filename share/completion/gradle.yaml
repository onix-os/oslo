# Converted from Fig's gradle.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: gradle
description: "Gradle is an open-source build automation tool that is designed to be flexible enough to build almost any type of software"
flags:
  "-?, -h, --help": "Shows a help message with all available CLI options"
  "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
  "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
  "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
  "--scan": "Generate a build scan with detailed performance diagnostics"
  "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
  "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
  "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
  "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
  "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
  "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
  "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
  "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
  "--no-parallel": "Disables --parallel"
  "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
  "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
  "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
  "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
  "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
  "--no-daemon": "Disables --daemon"
  "--foreground": "Starts the Gradle Daemon in a foreground process"
  "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
  "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
  "-q, --quiet": "Log errors only"
  "-w, --warn": "Set log level to warn"
  "-i, --info": "Set log level to info"
  "-d, --debug": "Log in debug mode (includes normal stacktrace)"
  "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
  "--console=": "Specifies which type of console output to generate"
  "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
  "--warning-mode=": "Specifies how to log warning"
  "--include-build": "Run the build as a composite, including the specified build"
  "--offline": "Specifies that the build should operate without accessing network resources"
  "--refresh-dependencies": "Refresh the state of dependencies"
  "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
  "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
  "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
  "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
  "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
  "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
  "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
  "-p, --project-dir=": "Specifies the start directory for Gradle"
  "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
  "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
  "-I, --init-script": "Specifies an initialization script"
  "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
  "-Dorg.gradle.jvmargs=": "Set JVM arguments"
  "-Dorg.gradle.java.home=": "Set JDK home dir"
exclusiveflags:
  - [build-cache, no-build-cache]
  - [no-build-cache, build-cache]
  - [configure-on-demand, configure-on-demand]
  - [no-configure-on-demand, configure-on-demand]
  - [parallel, no-parallel]
  - [no-parallel, parallel]
  - [watch-fs, no-watch-fs]
  - [no-watch-fs, watch-fs]
  - [daemon, no-daemon]
  - [no-daemon, daemon]
completion:
  flag:
    priority: [normal, low]
    Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
    Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
    console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
    Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
    warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
    build-file: ["$files"]
    settings-file: ["$files"]
    gradle-user-home: ["$directories"]
    project-dir: ["$directories"]
    project-cache-dir: ["$directories"]
    Dorg.gradle.java.home: ["$directories"]
commands:
  - name: build
    description: "Compute all outputs"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: run
    description: "Run applications"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: check
    description: "Run all checks"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: clean
    description: "Clear the contents of the build directory"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: projects
    description: "List of all sub-projects"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: tasks
    description: "List of main tasks of the selected project"
    flags:
      "--all": "By default, this report shows only those tasks which have been assigned to a task group. You can obtain more information in the task listing using the --all option"
      "--group=": "If you need to be more precise, you can display only the tasks from a specific group"
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: help
    description: "Display task usage information"
    flags:
      "--task=!": ""
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: dependencies
    description: "Gives you a list of the dependencies of the selected project, broken down by configuration"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: buildEnvironment
    description: "Visualises the buildscript dependencies of the selected project"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: dependencyInsight
    description: "Gives you an insight into a particular dependency (or dependencies) that match specified input"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: properties
    description: "Gives you a list of the properties of the selected project"
    flags:
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: "--status"
    description: "(Standalone command) List running and recently stopped Gradle daemons. Only displays daemons of the same Gradle version"
  - name: "--stop"
    description: "(Standalone command) Stop all Gradle Daemons of the same version"
  - name: init
    description: "Create new Gradle builds, with new or existing projects"
    flags:
      "--type=": "Specify project type"
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        type: [basic, java-library, java-application]
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: wrapper
    description: "Generates a script, gradlew, that invokes a declared version of Gradle, downloading it beforehand if necessary"
    flags:
      "--gradle-version=": "The Gradle version used for downloading and executing the Wrapper"
      "--distribution-type=": "The Gradle distribution type used for the Wrapper"
      "--gradle-distribution-url=": "The full URL pointing to Gradle distribution ZIP file"
      "--gradle-distribution-sha256-sum=": "The SHA256 hash sum used for verifying the downloaded Gradle distribution"
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        distribution-type: [bin, all]
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
  - name: test
    description: "Run a test task"
    flags:
      "--continuous": "Allows you to automatically re-execute the requested tasks when task inputs change"
      "--rerun-tasks": "This will force test and all task dependencies of test to execute"
      "--continue": "Gradle will execute every task to be executed where all of the dependencies for that task completed without failure"
      "-?, -h, --help": "Shows a help message with all available CLI options"
      "-v, --version": "Prints Gradle, Groovy, Ant, JVM, and operating system version information"
      "-S, --full-stacktrace": "Print out the full (very verbose) stacktrace for any exceptions"
      "-s, --stacktrace": "Print out the stacktrace also for user exceptions (e.g. compile error)"
      "--scan": "Generate a build scan with detailed performance diagnostics"
      "-Dorg.gradle.debug": "Debug Gradle client (non-Daemon) process. Gradle will wait for you to attach a debugger at localhost:5005 by default"
      "-Dorg.gradle.daemon.debug": "Debug Gradle Daemon process"
      "--build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--no-build-cache": "Toggles the Gradle build cache. Gradle will try to reuse outputs from previous builds. Default is off"
      "--configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--no-configure-on-demand": "Toggles Configure-on-demand. Only relevant projects are configured in this build run. Default is off"
      "--max-workers=": "Sets maximum number of workers that Gradle may use. Default is number of processors"
      "--parallel": "Build projects in parallel. For limitations of this option, see Parallel Project Execution. Default is off"
      "--no-parallel": "Disables --parallel"
      "--priority=": "Specifies the scheduling priority for the Gradle daemon and all processes launched by it"
      "--profile": "Generates a high-level performance report in the $buildDir/reports/profile directory. --scan is preferred"
      "--watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--no-watch-fs": "Toggles watching the file system. When enabled Gradle re-uses information it collects about the file system between builds. Enabled by default on operating systems where Gradle supports this feature"
      "--daemon": "Use the Gradle Daemon to run the build. Starts the daemon if not running or existing daemon busy. Default is on"
      "--no-daemon": "Disables --daemon"
      "--foreground": "Starts the Gradle Daemon in a foreground process"
      "-Dorg.gradle.daemon.idletimeout=": "Gradle Daemon will stop itself after this number of milliseconds of idle time"
      "-Dorg.gradle.logging.level=": "Set logging level via Gradle properties"
      "-q, --quiet": "Log errors only"
      "-w, --warn": "Set log level to warn"
      "-i, --info": "Set log level to info"
      "-d, --debug": "Log in debug mode (includes normal stacktrace)"
      "-Dorg.gradle.console=": "Specify console mode via Gradle properties"
      "--console=": "Specifies which type of console output to generate"
      "-Dorg.gradle.warning.mode=": "Specify warning mode via Gradle properties"
      "--warning-mode=": "Specifies how to log warning"
      "--include-build": "Run the build as a composite, including the specified build"
      "--offline": "Specifies that the build should operate without accessing network resources"
      "--refresh-dependencies": "Refresh the state of dependencies"
      "--dry-run": "Run Gradle with all task actions disabled. Use this to show which task would have executed"
      "--write-locks": "Indicates that all resolved configurations that are lockable should have their lock state persisted"
      "--update-locks=": {description: "Indicates that versions for the specified modules have to be updated in the lock file", nargs: -1}
      "--no-rebuild": "Do not rebuild project dependencies. Useful for debugging and fine-tuning buildSrc, but can lead to wrong results. Use with caution"
      "-b, --build-file=": "Specifies the build file. For example: gradle --build-file=foo.gradle"
      "-c, --settings-file=": "Specifies the settings file. For example: gradle --settings-file=somewhere/else/settings.gradle"
      "-g, --gradle-user-home=": "Specifies the Gradle user home directory. The default is the .gradle directory in the user’s home directory"
      "-p, --project-dir=": "Specifies the start directory for Gradle"
      "--project-cache-dir=": "Specifies the project-specific cache directory. Default value is .gradle in the root project directory"
      "-D, --system-prop=": "Sets a system property of the JVM, for example -Dmyprop=myvalue"
      "-I, --init-script": "Specifies an initialization script"
      "-P, --project-prop=": "Sets a project property of the root project, for example -Pmyprop=myvalue"
      "-Dorg.gradle.jvmargs=": "Set JVM arguments"
      "-Dorg.gradle.java.home=": "Set JDK home dir"
    exclusiveflags:
      - [build-cache, no-build-cache]
      - [no-build-cache, build-cache]
      - [configure-on-demand, configure-on-demand]
      - [no-configure-on-demand, configure-on-demand]
      - [parallel, no-parallel]
      - [no-parallel, parallel]
      - [watch-fs, no-watch-fs]
      - [no-watch-fs, watch-fs]
      - [daemon, no-daemon]
      - [no-daemon, daemon]
    completion:
      flag:
        priority: [normal, low]
        Dorg.gradle.logging.level: [quiet, warn, lifecycle, info, debug]
        Dorg.gradle.console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        console: ["auto\tEnables color and other rich output in the console output when the build process is attached to a console, or to generate plain text only when not attached to a console", "plain\tDisables all color and other rich output in the console output. This is the default when Gradle is not attached to a terminal", "rich\tEnables color and other rich output in the console output, regardless of whether the build process is not attached to a console", "verbose\tEnables color and other rich output like the rich, but output task names and outcomes at the lifecycle log level, as is done by default in Gradle 3.5 and earlier"]
        Dorg.gradle.warning.mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        warning-mode: ["all\tLog all warnings", "fail\tLog all warnings and fail the build if there are any warnings", "summary\tSuppress all warnings and log a summary at the end of the build", "none\tSuppress all warnings, including the summary at the end of the build"]
        build-file: ["$files"]
        settings-file: ["$files"]
        gradle-user-home: ["$directories"]
        project-dir: ["$directories"]
        project-cache-dir: ["$directories"]
        Dorg.gradle.java.home: ["$directories"]
