# Converted from argc's ghz.sh by tools/argc-to-spec. Do not edit by hand.
name: ghz
flags:
  "-h, --help": "Show context-sensitive help (also try --help-long and --help-man)."
  "--config=": "Path to the JSON or TOML config file that specifies all the test run settings."
  "--proto=": "The Protocol Buffer .proto file."
  "--protoset=": "The compiled protoset file."
  "--call=": "A fully-qualified method name in 'package.Service/method' or 'package.Service.Method' format."
  "-i, --import-paths=": "Comma separated list of proto import paths."
  "--cacert=": "File containing trusted root certificates for verifying the server."
  "--cert=": "File containing client certificate (public key), to present to the server."
  "--key=": "File containing client private key, to present to the server."
  "--cname=": "Server name override when validating TLS certificate - useful for self signed certs."
  "--skipTLS": "Skip TLS client verification of the server's certificate chain and host name."
  "--insecure": "Use plaintext and insecure connection."
  "--authority=": "Value to be used as the :authority pseudo-header."
  "--async": "Make requests asynchronous as soon as possible."
  "-r, --rps=": "Requests per second (RPS) rate limit for constant load schedule."
  "--load-schedule=": "Specifies the load schedule."
  "--load-start=": "Specifies the RPS load start value for step or line schedules."
  "--load-step=": "Specifies the load step value or slope value."
  "--load-end=": "Specifies the load end value for step or line load schedules."
  "--load-step-duration=": "Specifies the load step duration value for step load schedule."
  "--load-max-duration=": "Specifies the max load duration value for step or line load schedule."
  "-c, --concurrency=": "Number of request workers to run concurrently for const concurrency schedule."
  "--concurrency-schedule=": "Concurrency change schedule."
  "--concurrency-start=": "Concurrency start value for step and line concurrency schedules."
  "--concurrency-end=": "Concurrency end value for step and line concurrency schedules."
  "--concurrency-step=": "Concurrency step / slope value for step and line concurrency schedules."
  "--concurrency-step-duration=": "Specifies the concurrency step duration value for step concurrency schedule."
  "--concurrency-max-duration=": "Specifies the max concurrency adjustment duration value for step or line concurrency schedule."
  "-n, --total=": "Number of requests to run."
  "-t, --timeout=": "Timeout for each request."
  "-z, --duration=": "Duration of application to send requests."
  "-x, --max-duration=": "Maximum duration of application to send requests with n setting respected."
  "--duration-stop=": "Specifies how duration stop is reported."
  "-d, --data=": "The call data as stringified JSON."
  "-D, --data-file=": "File path for call data JSON file."
  "-b, --binary": "The call data comes as serialized binary message or multiple count-prefixed messages read from stdin."
  "-B, --binary-file=": "File path for the call data as serialized binary message or multiple count-prefixed messages."
  "-m, --metadata=": "Request metadata as stringified JSON."
  "-M, --metadata-file=": "File path for call metadata JSON file."
  "--stream-interval=": "Interval for stream requests between message sends."
  "--stream-call-duration=": "Duration after which client will close the stream in each streaming call."
  "--stream-call-count=": "Count of messages sent, after which client will close the stream in each streaming call."
  "--stream-dynamic-messages": "In streaming calls, regenerate and apply call template data on every message send."
  "--reflect-metadata=": "Reflect metadata as stringified JSON used only for reflection request."
  "-o, --output=": "Output path."
  "-O, --format=": "Output format."
  "--skipFirst=": "Skip the first X requests when doing the results tally."
  "--count-errors": "Count erroneous (non-OK) resoponses in stats calculations."
  "--connections=": "Number of connections to use."
  "--connect-timeout=": "Connection timeout for the initial connection dial."
  "--keepalive=": "Keepalive time duration."
  "--name=": "User specified name for the test."
  "--tags=": "JSON representation of user-defined string tags."
  "--cpus=": "Number of cpu cores to use."
  "--debug=": "The path to debug log file."
  "-e, --enable-compression": "Enable Gzip compression on requests."
  "--lb-strategy=": "Client load balancing strategy."
  "--max-recv-message-size=": "Maximum message size the client can receive."
  "--max-send-message-size=": "Maximum message size the client can send."
  "--disable-template-functions": "Do not use and execute any template functions in call template data."
  "--disable-template-data": "Do not use and execute any call template data."
  "-v, --version": "Show application version."
completion:
  flag:
    format: [summary, csv, json, pretty, html, influx-summary, influx-details]
