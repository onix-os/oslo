# Converted from Fig's esbuild.ts by scripts/fig-to-spec.ts. Do not edit by hand.
name: esbuild
description: "An extremely fast JavaScript bundler"
flags:
  "--bundle": "Bundle all dependencies into the output files"
  "--define?": "Replace variable names with a literal value, eg. --define:DEBUG=true"
  "--external?": "Exclude modules from the build"
  "--format?": "The output format"
  "--loader?": "For a given file extension, specify a loader"
  "--minify": "Minify the output (sets all the --minify-* options)"
  "--outdir?": "The output directory for multiple entrypoints"
  "--outfile?": "The output file for one entrypoint"
  "--platform?": "The platform target"
  "--serve?": "Start a local HTTP server on this host:port"
  "--splitting": "Enable code splitting"
  "--target?": "Set the environment target. Can be a particular ES version or browser version, eg. chrome101"
  "--watch?": "Rebuild on file system changes"
  "--allow-overwrite": "Allow output files to overwrite input files"
  "--analyze?": "Print a report about the contents of the bundle"
  "--asset-names?": "Path template for 'file' loader files"
  "--banner?": "Text to be prepended to each output file type"
  "--charset?": "Use UTF-8 instead of escaped codepoints in ASCII"
  "--chunk-names?": "Path template to use for code splitting chunks"
  "--color?": "Force use of terminal colors"
  "--drop?": "Remove certain constructs"
  "--entry-names?": "Path template to use for entry point output paths"
  "--footer?": "Text to be appended to each file type"
  "--global-name?": "The name of the global if using --format=iife"
  "--ignore-annotations": "Enable this to work with packages that have incorrect tree-shaking annotations"
  "--inject?": "Import the file into all input files, automatically replace matching globals"
  "--jsx-factory?": "What to use for the JSX factory"
  "--jsx-fragment?": "What to use for the JS Fragment factory"
  "--jsx?": "Preserve JSX instead of transforming"
  "--jsx-dev": "Toggles development mode for the automatic runtime"
  "--jsx-import-source?": "Overrides the root import for runtime functions (default: react)"
  "--keep-names": "Preserve 'name' on functions and classes"
  "--legal-comments?": "Where to place legal comments"
  "--log-level?": "Set the log level"
  "--log-limit?": "Maximum message count, 0 to disable"
  "--log-override?": "For a particular identifier, set the log level"
  "--main-fields?": "Override the main file order in package.json"
  "--mangle-cache?": "Save 'mangle props' decisions to a JSON file"
  "--mangle-props?": "Rename all properties matching a regular expression"
  "--mangle-quoted?": "Enable mangling (renaming) quoted properties"
  "--metafile?": "Write metadata about the build to a JSON file"
  "--minify-whitespace": "Remove unnecessary whitespace in output files"
  "--minify-identifiers": "Shorten identifiers in output files"
  "--minify-syntax": "Use equivalent but shorter syntax in output files"
  "--out-extension?": "Use a custom output extension for each extension"
  "--outbase?": "Base path used to determine entrypoint output paths, for multiple entrypoints"
  "--preserve-symlinks": "Disable symlink resolution"
  "--public-path?": "Set the base URL for the 'file' loader"
  "--pure?": "Mark the name as a pure function for tree shaking"
  "--reserve-props?": "Do not mangle these properties"
  "--resolve-extensions?": "Comma-separated list of implicit extensions"
  "--servedir?": "What to serve in addition to the generated output files"
  "--source-root?": "Set the sourceRoot field in generated source maps"
  "--sourcefile?": "Set the source file for the source map if there's no file name to use"
  "--sourcemap?": "Generate source maps?"
  "--sources-content?": "Omit the sourcesContent field in generated source maps"
  "--supported?": "Consider a given syntax to be supported"
  "--tree-shaking?": "Force tree shaking on or off"
  "--tsconfig?": "Use this TypeScript config instead of the default"
  "--version": "Print the current version and exit"
completion:
  flag:
    format: [iife, cjs, esm]
    outdir: ["$directories"]
    outfile: ["$files"]
    platform: [browser, node, neutral]
    watch: [forever]
    analyze: [verbose]
    color: ["true", "false"]
    drop: [console, debugger]
    inject: ["$files"]
    jsx-factory: [React.createElement, h, preact.h]
    jsx-fragment: [React.Fragment, Fragment]
    jsx: ["preserve\tPreserve JSX instead of transforming", "automatic\tUse React's new automatic JSX transform"]
    legal-comments: [none, inline, eof, linked, external]
    log-level: [verbose, debug, info, warning, error, silent]
    mangle-cache: ["$files"]
    mangle-quoted: ["true", "false"]
    metafile: ["$files"]
    outbase: ["$directories"]
    public-path: ["$directories"]
    servedir: ["$directories"]
    sourcemap: ["external\tGenerate a separate .map.js file with no comment", "inline\tAppend source maps to JS files"]
    sources-content: ["false"]
    tree-shaking: ["true", "false"]
    tsconfig: ["$files"]
  positionalany: ["$files"]
