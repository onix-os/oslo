# Converted from argc's xsv.sh by tools/argc-to-spec. Do not edit by hand.
name: xsv
flags:
  "--list": "List all commands available."
  "-h, --help": "Display this message"
  "--version": "Print version info and exit"
commands:
  - name: cat
    description: "Concatenate by row or column"
    flags:
      "-p, --pad": "When concatenating columns, this flag will cause all records to appear."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will NOT be interpreted as column names."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
    completion:
      positional:
        - [rows, columns]
  - name: count
    description: "Count records"
    flags:
      "-h, --help": "Display this message"
      "-n, --no-headers": "When set, the first row will not be included in the count."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: fixlengths
    description: "Makes all records have same length"
    flags:
      "-l, --length=": "Forcefully set the length of each record."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: flatten
    description: "Show one field per line"
    flags:
      "-c, --condense=": "Limits the length of each field to the value specified."
      "-s, --separator=": "A string of characters to write after each record."
      "-h, --help": "Display this message"
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: fmt
    description: "Format CSV output (change field delimiter)"
    flags:
      "-t, --out-delimiter=": "The field delimiter for writing CSV data."
      "--crlf": "Use '\\r\\n' line endings in the output."
      "--ascii": "Use ASCII field and record separators."
      "--quote=": "The quote character to use."
      "--quote-always": "Put quotes around every value."
      "--escape=": "The escape character to use."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: frequency
    description: "Show frequency tables"
    flags:
      "-s, --select=": "Select a subset of columns to compute frequencies for."
      "-l, --limit=": "Limit the frequency table to the N most common items."
      "-a, --asc": "Sort the frequency tables in ascending order by count."
      "--no-nulls": "Don't include NULLs in the frequency table."
      "-j, --jobs=": "The number of jobs to run in parallel."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will NOT be included in the frequency table."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: headers
    description: "Show header names"
    flags:
      "-j, --just-names": "Only show the header names (hide column index)."
      "--intersect": "Shows the intersection of all headers in all of the inputs given."
      "-h, --help": "Display this message"
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: index
    description: "Create CSV index for faster access"
    flags:
      "-o, --output=": "Write index to <file> instead of <input>.idx."
      "-h, --help": "Display this message"
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: input
    description: "Read CSV data with special quoting rules"
    flags:
      "--quote=": "The quote character to use."
      "--escape=": "The escape character to use."
      "--no-quoting": "Disable quoting completely."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: join
    description: "Join CSV files"
    flags:
      "--no-case": "When set, joins are done case insensitively."
      "--left": "Do a 'left outer' join."
      "--right": "Do a 'right outer' join."
      "--full": "Do a 'full outer' join."
      "--cross": "USE WITH CAUTION."
      "--nulls": "When set, joins will work on empty fields."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: sample
    description: "Randomly sample CSV data"
    flags:
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will be consider as part of the population to sample from."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: search
    description: "Search CSV data with regexes"
    flags:
      "-i, --ignore-case": "Case insensitive search."
      "-s, --select=": "Select the columns to search."
      "-v, --invert-match": "Select only rows that did not match"
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: select
    description: "Select columns from CSV"
    flags:
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: slice
    description: "Slice records from CSV"
    flags:
      "-s, --start=": "The index of the record to slice from."
      "-e, --end=": "The index of the record to slice to."
      "-l, --len=": "The length of the slice (can be used instead of --end)."
      "-i, --index=": "Slice a single record (shortcut for -s N -l 1)."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: sort
    description: "Sort CSV data"
    flags:
      "-s, --select=": "Select a subset of columns to sort."
      "-N, --numeric": "Compare according to string numerical value"
      "-R, --reverse": "Reverse order"
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will not be interpreted as headers."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: split
    description: "Split CSV data into many files"
    flags:
      "-s, --size=": "The number of records to write into each chunk."
      "-j, --jobs=": "The number of spliting jobs to run in parallel."
      "--filename=": "A filename template to use when constructing the names of the output files."
      "-h, --help": "Display this message"
      "-n, --no-headers": "When set, the first row will NOT be interpreted as column names."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: stats
    description: "Compute basic statistics"
    flags:
      "-s, --select=": "Select a subset of columns to compute stats for."
      "--everything": "Show all statistics available."
      "--mode": "Show the mode."
      "--cardinality": "Show the cardinality."
      "--median": "Show the median."
      "--nulls": "Include NULLs in the population size for computing mean and standard deviation."
      "-j, --jobs=": "The number of jobs to run in parallel."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-n, --no-headers": "When set, the first row will NOT be interpreted as column names."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
  - name: table
    description: "Align CSV data into columns"
    flags:
      "-w, --width=": "The minimum width of each column."
      "-p, --pad=": "The minimum number of spaces between each column."
      "-c, --condense=": "Limits the length of each field to the value specified."
      "-h, --help": "Display this message"
      "-o, --output=": "Write output to <file> instead of stdout."
      "-d, --delimiter=": "The field delimiter for reading CSV data."
