# Converted from argc's naabu.sh by tools/argc-to-spec. Do not edit by hand.
name: naabu
flags:
  "-host=*": {description: "hosts to scan ports for (comma-separated)", nargs: -1}
  "-l, -list=": "list of hosts to scan ports (file)"
  "-exclude-hosts=": "hosts to exclude from the scan (comma-separated)"
  "-eh=": "hosts to exclude from the scan (comma-separated)"
  "-exclude-file=": "list of hosts to exclude from scan (file)"
  "-ef=": "list of hosts to exclude from scan (file)"
  "-p, -port=": "ports to scan (80,443, 100-200)"
  "-top-ports=": "top ports to scan (default 100)"
  "-tp=": "top ports to scan (default 100)"
  "-exclude-ports=": "ports to exclude from scan (comma-separated)"
  "-ep=": "ports to exclude from scan (comma-separated)"
  "-ports-file=": "list of ports to scan (file)"
  "-pf=": "list of ports to scan (file)"
  "-port-threshold=": "port threshold to skip port scan for the host"
  "-pts=": "port threshold to skip port scan for the host"
  "-exclude-cdn": "skip full port scans for CDN/WAF (only scan for port 80,443)"
  "-ec": "skip full port scans for CDN/WAF (only scan for port 80,443)"
  "-display-cdn": "display cdn in use"
  "-cdn": "display cdn in use"
  "-c=": "general internal worker threads (default 25)"
  "-rate=": "packets to send per second (default 1000)"
  "-up": "update naabu to latest version"
  "-update": "update naabu to latest version"
  "-duc": "disable automatic naabu update check"
  "-disable-update-check": "disable automatic naabu update check"
  "-o, -output=": "file to write output to (optional)"
  "-j, -json": "write output in JSON lines format"
  "-csv": "write output in csv format"
  "-config=": "path to the naabu configuration file (default $HOME/.config/naabu/config.yaml)"
  "-scan-all-ips": "scan all the IP's associated with DNS record"
  "-sa": "scan all the IP's associated with DNS record"
  "-ip-version=*": {description: "ip version to scan of hostname - (default 4) (default [\"4\"])", nargs: -1}
  "-iv=*": {description: "ip version to scan of hostname - (default 4) (default [\"4\"])", nargs: -1}
  "-s, -scan-type=": "type of port scan (SYN/CONNECT) (default \"s\")"
  "-source-ip=": "source ip and port (x.x.x.x:yyy - might not work on OSX)"
  "-interface-list": "list available interfaces and public ip"
  "-il": "list available interfaces and public ip"
  "-i, -interface=": "network Interface to use for port scan"
  "-nmap": "invoke nmap scan on targets (nmap must be installed) - Deprecated"
  "-nmap-cli=": "nmap command to run on found results (example: -nmap-cli 'nmap -sV')"
  "-r=": "list of custom resolver dns resolution (comma separated or from file)"
  "-proxy=": "socks5 proxy (ip[:port] / fqdn[:port]"
  "-proxy-auth=": "socks5 proxy authentication (username:password)"
  "-resume": "resume scan using resume.cfg"
  "-stream": "stream mode (disables resume, nmap, verify, retries, shuffling, etc)"
  "-passive": "display passive open ports using shodan internetdb api"
  "-irt=": "timeout on input read (default 3m0s)"
  "-input-read-timeout=": "timeout on input read (default 3m0s)"
  "-no-stdin": "Disable Stdin processing"
  "-sn": "Perform Only Host Discovery"
  "-host-discovery": "Perform Only Host Discovery"
  "-Pn": "Skip Host discovery"
  "-skip-host-discovery": "Skip Host discovery"
  "-ps=*": {description: "TCP SYN Ping (host discovery needs to be enabled)", nargs: -1}
  "-probe-tcp-syn=*": {description: "TCP SYN Ping (host discovery needs to be enabled)", nargs: -1}
  "-pa=*": {description: "TCP ACK Ping (host discovery needs to be enabled)", nargs: -1}
  "-probe-tcp-ack=*": {description: "TCP ACK Ping (host discovery needs to be enabled)", nargs: -1}
  "-pe": "ICMP echo request Ping (host discovery needs to be enabled)"
  "-probe-icmp-echo": "ICMP echo request Ping (host discovery needs to be enabled)"
  "-pp": "ICMP timestamp request Ping (host discovery needs to be enabled)"
  "-probe-icmp-timestamp": "ICMP timestamp request Ping (host discovery needs to be enabled)"
  "-pm": "ICMP address mask request Ping (host discovery needs to be enabled)"
  "-probe-icmp-address-mask": "ICMP address mask request Ping (host discovery needs to be enabled)"
  "-arp": "ARP ping (host discovery needs to be enabled)"
  "-arp-ping": "ARP ping (host discovery needs to be enabled)"
  "-nd": "IPv6 Neighbor Discovery (host discovery needs to be enabled)"
  "-nd-ping": "IPv6 Neighbor Discovery (host discovery needs to be enabled)"
  "-rev-ptr": "Reverse PTR lookup for input ips"
  "-sD": "Service Discovery"
  "-service-discovery": "Service Discovery"
  "-sV": "Service Version"
  "-service-version": "Service Version"
  "-retries=": "number of retries for the port scan (default 3)"
  "-timeout=": "millisecond to wait before timing out (default 1000)"
  "-warm-up-time=": "time in seconds between scan phases (default 2)"
  "-ping": "ping probes for verification of host"
  "-verify": "validate the ports again with TCP verification"
  "-health-check": "run diagnostic check up"
  "-hc": "run diagnostic check up"
  "-debug": "display debugging information"
  "-v, -verbose": "display verbose output"
  "-no-color": "disable colors in CLI output"
  "-nc": "disable colors in CLI output"
  "-silent": "display only results in output"
  "-version": "display version of naabu"
  "-stats": "display stats of the running scan (deprecated)"
  "-si=": "number of seconds to wait between showing a statistics update (deprecated) (default 5)"
  "-stats-interval=": "number of seconds to wait between showing a statistics update (deprecated) (default 5)"
  "-mp=": "port to expose naabu metrics on (default 63636)"
  "-metrics-port=": "port to expose naabu metrics on (default 63636)"
  "-h, -help": "Print help"
completion:
  flag:
    top-ports: [full, 100, 1000]
    tp: [full, 100, 1000]
    ip-version: [4, 6]
    iv: [4, 6]
