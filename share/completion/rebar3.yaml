# Converted from argc's rebar3.sh by tools/argc-to-spec. Do not edit by hand.
name: rebar3
flags:
  "-h, --help": "Print this help."
  "-v, --version": "Show version information."
commands:
  - name: alias
    description: "List aliases' definitions."
    flags:
      "-h, --help": ""
  - name: as
    description: "Higher order provider for running multiple tasks in a sequence as a certain profiles."
    flags:
      "-h, --help": ""
  - name: clean
    description: "Remove compiled beam files from apps."
    flags:
      "-a, --all": "Clean all apps include deps"
      "--apps": "Clean a specific list of apps or dependencies"
      "-p, --profile": "Clean under profile."
      "-h, --help": "Print help"
  - name: compile
    description: "Compile apps .app.src and .erl files."
    flags:
      "-d, --deps_only": "Only compile dependencies, no project apps will be built."
      "-h, --help": "Print help"
  - name: completion
    description: "Generate completion file for your shell."
    flags:
      "-a, --aliases": "Comma separated list of OS level aliases on which rebar3 completion will be triggered (e.g. \"rebar\" or \"r3\")."
      "-f, --file": "Completion file name."
      "-s, --shell": "Shell type, 'bash' or 'zsh'."
      "-h, --help": "Print help"
  - name: cover
    description: "Perform coverage analysis."
    flags:
      "-r, --reset": "Reset all coverdata."
      "-v, --verbose": "Print coverage analysis."
      "-m, --min_coverage": "Mandate a coverage percentage required to succeed (0..100)"
      "-h, --help": "Print help"
  - name: ct
    description: "Run Common Tests."
    flags:
      "--dir": "List of additional directories containing test suites"
      "--suite": "List of test suites to run"
      "--group": "List of test groups to run"
      "--case": "List of test cases to run"
      "--label": "Test label"
      "--config": "List of config files"
      "--spec": "List of test specifications"
      "--join_specs": "Merge all test specifications and perform a single test run"
      "--allow_user_terms": "Allow user defined config values in config files"
      "--logdir": "Log folder"
      "--logopts": "Options for common test logging"
      "--verbosity": Verbosity
      "-c, --cover": "Generate cover data [default: false]"
      "--cover_export_name": "Base name of the coverdata file to write"
      "--repeat": "How often to repeat tests"
      "--duration": "Max runtime (format: HHMMSS)"
      "--until": "Run until (format: HHMMSS)"
      "--force_stop": "Force stop on test timeout"
      "--basic_html": "Show basic HTML"
      "--stylesheet": "CSS stylesheet to apply to html output"
      "--decrypt_key": "Path to key for decrypting config"
      "--decrypt_file": "Path to file containing key for decrypting config"
      "--abort_if_missing_suites": "Abort if suites are missing [default: true]"
      "--multiply_timetraps": ""
      "--scale_timetraps": "Scale timetraps"
      "--create_priv_dir": "Create priv dir"
      "--include": "Directories containing additional include files"
      "--readable": "Shows test case names and only displays logs to shell on failures"
      "-v, --verbose": "Verbose output"
      "--name": "Gives a long name to the node"
      "--sname": "Gives a short name to the node"
      "--setcookie": "Sets the cookie if the node is distributed"
      "--sys_config": "List of application config files"
      "--compile_only": "Compile modules in the project with the test configuration but do not run the tests"
      "--retry": "Experimental feature."
      "--fail_fast": "Experimental feature."
      "-h, --help": "Print help"
  - name: deps
    description: "List dependencies"
    flags:
      "-h, --help": ""
  - name: dialyzer
    description: "Run the Dialyzer analyzer on the project."
    flags:
      "-u, --update-plt": "Enable updating the PLT."
      "-s, --succ-typings": "Enable success typing analysis."
      "--base-plt-location": "The location of base PLT file, defaults to $HOME/.cache/rebar3"
      "--plt-location": "The location of the PLT file, defaults to the profile's base directory"
      "--plt-prefix": "The prefix to the PLT file, defaults to \"rebar3\""
      "-a, --app": "Perform success typing analysis of a single application"
      "--base-plt-prefix": "The prefix to the base PLT file, defaults to \"rebar3\""
      "--statistics": "Print information about the progress of execution."
      "-h, --help": "Print help"
  - name: do
    description: "Higher order provider for running multiple tasks in a sequence."
    flags:
      "-h, --help": ""
  - name: edoc
    description: "Generate documentation using edoc."
    flags:
      "-h, --help": ""
  - name: escriptize
    description: "Generate escript archive."
    flags:
      "-a, --main-app": "Specify the name of the application to build an escript for."
      "-h, --help": "Print help"
  - name: eunit
    description: "Run EUnit Tests."
    flags:
      "--app": "Comma separated list of application test suites to run."
      "--application": "Comma separated list of application test suites to run."
      "-c, --cover": "Generate cover data."
      "--cover_export_name": "Base name of the coverdata file to write"
      "-p, --profile": "Show the slowest tests."
      "-d, --dir": "Comma separated list of dirs to load tests from."
      "-f, --file": "Comma separated list of files to load tests from."
      "-m, --module": "Comma separated list of modules to load tests from."
      "-t, --test": "Comma separated list of tests to run."
      "-s, --suite": "Comma separated list of modules to load tests from."
      "-g, --generator": "Comma separated list of generators to load tests from."
      "-v, --verbose": "Verbose output."
      "--name": "Gives a long name to the node"
      "--sname": "Gives a short name to the node"
      "--sys_config": "List of application config files"
      "--setcookie": "Sets the cookie if the node is distributed"
      "-h, --help": "Print help"
  - name: get-deps
    description: "Fetch dependencies."
    flags:
      "-h, --help": ""
  - name: new
    description: "Create new project from templates."
    flags:
      "-f, --force": "overwrite existing files"
      "-h, --help": "Print help"
  - name: path
    description: "Print paths to build dirs in current profile."
    flags:
      "--app": "Comma separated list of applications to return paths for."
      "--base": "Return the `base' path of the current profile."
      "--bin": "Return the `bin' path of the current profile."
      "--ebin": "Return all `ebin' paths of the current profile's applications."
      "--lib": "Return the `lib' path of the current profile."
      "--priv": "Return the `priv' path of the current profile's applications."
      "-s, --separator": "In case of multiple return paths, the separator character to use to join them."
      "--src": "Return the `src' path of the current profile's applications."
      "--rel": "Return the `rel' path of the current profile."
      "-h, --help": "Print help"
  - name: pkgs
    description: "List information for a package."
    flags:
      "-h, --help": ""
  - name: release
    description: "Build release of project."
    flags:
      "--all": "If true runs the command against all configured releases"
      "-n, --relname": "Specify the name for the release that will be generated"
      "-v, --relvsn": "Specify the version for the release"
      "-u, --upfrom": "Only valid with relup target, specify the release to upgrade from"
      "-o, --output-dir": "The output directory for the release."
      "-h, --help": "Print usage"
      "-l, --lib-dir": "Additional dir that should be searched for OTP Apps"
      "-d, --dev-mode": "Symlink the applications and configuration into the release instead of copying"
      "-i, --include-erts": "If true include a copy of erts used to build with, if a path include erts at that path."
      "-a, --override": "Provide an app name and a directory to override in the form <appname>:<app directory>"
      "-c, --config": "The path to a config file [default: ]"
      "--overlay_vars": "Path to a file of overlay variables"
      "--vm_args": "Path to a file to use for vm.args"
      "--sys_config": "Path to a file to use for sys.config"
      "--system_libs": "Boolean or path to dir of Erlang system libs"
      "--version": "Print relx version"
      "-r, --root": "The project root directory"
      "-m, --relnames": "Like --all, but only build the releases in the list, e.g. --relnames rel1,rel2"
  - name: relup
    description: "Create relup of releases."
    flags:
      "--all": "If true runs the command against all configured releases"
      "-n, --relname": "Specify the name for the release that will be generated"
      "-v, --relvsn": "Specify the version for the release"
      "-u, --upfrom": "Only valid with relup target, specify the release to upgrade from"
      "-o, --output-dir": "The output directory for the release."
      "-h, --help": "Print usage"
      "-l, --lib-dir": "Additional dir that should be searched for OTP Apps"
      "-d, --dev-mode": "Symlink the applications and configuration into the release instead of copying"
      "-i, --include-erts": "If true include a copy of erts used to build with, if a path include erts at that path."
      "-a, --override": "Provide an app name and a directory to override in the form <appname>:<app directory>"
      "-c, --config": "The path to a config file [default: ]"
      "--overlay_vars": "Path to a file of overlay variables"
      "--vm_args": "Path to a file to use for vm.args"
      "--sys_config": "Path to a file to use for sys.config"
      "--system_libs": "Boolean or path to dir of Erlang system libs"
      "--version": "Print relx version"
      "-r, --root": "The project root directory"
      "-m, --relnames": "Like --all, but only build the releases in the list, e.g. --relnames rel1,rel2"
  - name: report
    description: "Provide a crash report to be sent to the rebar3 issues page."
    flags:
      "-h, --help": ""
  - name: shell
    description: "Run shell with project apps and deps in path."
    flags:
      "--config": "Path to the config file to use."
      "--name": "Gives a long name to the node."
      "--sname": "Gives a short name to the node."
      "--setcookie": "Sets the cookie if the node is distributed."
      "--script": "Path to an escript file to run before starting the project apps."
      "--apps": "A list of apps to boot before starting the shell."
      "-r, --relname": "Name of the release to use as a template for the shell session."
      "-v, --relvsn": "Version of the release to use for the shell session."
      "--start-clean": "Cancel any applications in the 'apps' list or release."
      "--env-file": "Path to file of os environment variables to setup before expanding vars in config files."
      "--user_drv_args": "For versions of Erlang prior to 26, this option can be used to pass arguments to the user_drv start function for creating custom shells."
      "--eval": "Erlang term(s) to execute after the apps have been started, but before the shell is presented to the user."
      "-h, --help": "Print help"
  - name: tar
    description: "Tar archive of release built of project."
    flags:
      "--all": "If true runs the command against all configured releases"
      "-n, --relname": "Specify the name for the release that will be generated"
      "-v, --relvsn": "Specify the version for the release"
      "-u, --upfrom": "Only valid with relup target, specify the release to upgrade from"
      "-o, --output-dir": "The output directory for the release."
      "-h, --help": "Print usage"
      "-l, --lib-dir": "Additional dir that should be searched for OTP Apps"
      "-d, --dev-mode": "Symlink the applications and configuration into the release instead of copying"
      "-i, --include-erts": "If true include a copy of erts used to build with, if a path include erts at that path."
      "-a, --override": "Provide an app name and a directory to override in the form <appname>:<app directory>"
      "-c, --config": "The path to a config file [default: ]"
      "--overlay_vars": "Path to a file of overlay variables"
      "--vm_args": "Path to a file to use for vm.args"
      "--sys_config": "Path to a file to use for sys.config"
      "--system_libs": "Boolean or path to dir of Erlang system libs"
      "--version": "Print relx version"
      "-r, --root": "The project root directory"
      "-m, --relnames": "Like --all, but only build the releases in the list, e.g. --relnames rel1,rel2"
  - name: tree
    description: "Print dependency tree."
    flags:
      "-v, --verbose": "Print repo and branch/tag/ref for git and hg deps"
      "-h, --help": "Print help"
  - name: unlock
    description: "Unlock dependencies."
    flags:
      "-a, --all": "Unlock all dependencies and remove the lock file."
      "-h, --help": "Print help"
  - name: update
    description: "Update package index."
    flags:
      "-h, --help": ""
  - name: upgrade
    description: "Upgrade dependencies."
    flags:
      "-a, --all": "Upgrade all dependencies."
      "-h, --help": "Print help"
  - name: version
    description: "Print version for rebar and current Erlang."
    flags:
      "-h, --help": ""
  - name: xref
    description: "Run cross reference analysis."
    flags:
      "-h, --help": ""
  - name: experimental
    flags:
      "-h, --help": ""
    commands:
      - name: manifest
        description: "Produce a project manifest"
        flags:
          "-h, --help": ""
      - name: vendor
        description: "Turns dependencies into top-level apps"
        flags:
          "-h, --help": ""
  - name: local
    flags:
      "-h, --help": ""
    commands:
      - name: install
        description: "Extract libs from rebar3 escript along with a run script."
        flags:
          "-h, --help": ""
      - name: upgrade
        description: "Download latest rebar3 escript and extract."
        flags:
          "-h, --help": ""
  - name: plugins
    flags:
      "-h, --help": ""
    commands:
      - name: list
        description: "List local and global plugins for this project"
        flags:
          "-h, --help": ""
      - name: upgrade
        description: "Upgrade plugins"
        flags:
          "-h, --help": ""
