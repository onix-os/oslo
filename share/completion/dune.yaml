# Converted from argc's dune.sh by tools/argc-to-spec. Do not edit by hand.
name: dune
flags:
  "--help=": "Show this help in format FMT."
  "--version": "Show version information."
completion:
  flag:
    help: [auto, pager, groff, plain]
commands:
  - name: build
    description: "Build the given targets, or the default ones if none are given."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: cache
    description: "COMMAND … Manage Dune's shared cache of build artifacts."
    flags:
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
    commands:
      - name: clear
        description: "Clear the Dune cache."
        flags:
          "-h, --help": ""
      - name: size
        description: "Query the size of the Dune cache."
        flags:
          "-h, --help": ""
      - name: trim
        description: "Trim the Dune cache."
        flags:
          "-h, --help": ""
  - name: clean
    description: "Clean the project."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: coq
    description: "COMMAND … Command group related to Coq."
    flags:
      "-h, --help": ""
  - name: describe
    description: "Describe the workspace."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--context=": "Build context to use."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--format=": "FORMAT must be either sexp or csexp"
      "--lang=": "Behave the same as this version of Dune."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--sanitize-for-tests": "Sanitize the absolute paths in workspace items, and the associated UIDs, so that the output is reproducible."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--with-deps": "Whether the dependencies between modules should be printed."
      "--with-pps": "Whether the dependencies towards ppx-rewriters (that are called at compile time) should be taken into account."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
    commands:
      - name: aliases
        description: "Print aliases in a given directory."
        flags:
          "-h, --help": ""
      - name: contexts
        description: "List the build contexts available in the workspace."
        flags:
          "-h, --help": ""
      - name: env
        description: "Print the environment of a directory."
        flags:
          "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
          "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
          "--build-info": "Show build information."
          "--display-separate-messages": "Separate error messages with a blank line."
          "--error-reporting=": "Controls when the build errors are reported."
          "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
          "--field=": "Only print this field."
          "--file-watcher=": "Mechanism to detect changes in the source."
          "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
          "--sandbox=": "Set sandboxing mode."
          "--stop-on-first-error": "Stop the build as soon as an error is encountered."
          "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
          "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
          "--always-show-command-line": "Always show the full command lines of programs executed by dune."
          "--auto-promote": "Automatically promote files."
          "--build-dir=": "Specified build directory."
          "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
          "--cache-check-probability=": ""
          "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
          "--config-file=": "Load this configuration file instead of the default one."
          "--debug-artifact-substitution": "Print debugging info about artifact substitution"
          "--debug-backtraces": "Always print exception backtraces."
          "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
          "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
          "--debug-digests": "Explain why Dune decides to re-digest some files"
          "--debug-load-dir": "Print debugging info about directory loading"
          "--default-target=": "Set the default target that is used when none is specified to dune build."
          "--diff-command=": "Shell command to use to diff files."
          "--disable-promotion": "Disable all promotion rules"
          "--display=": "Control the display mode of Dune."
          "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
          "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
          "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
          "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
          "--help=": "Show this help in format FMT."
          "--ignore-lock-dir": "Ignore dune.lock/ directory."
          "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
          "--instrument-with=": "Enable instrumentation by BACKENDS."
          "-j=": "Run no more than JOBS commands simultaneously."
          "--no-buffer": "Do not buffer the output of commands executed by dune."
          "--no-config": "Do not load the configuration file"
          "--no-print-directory": "Suppress \"Entering directory\" messages."
          "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
          "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
          "--print-metrics": "Print out various performance metrics after every build."
          "--profile=": "Select the build profile, for instance dev or release."
          "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
          "--release": "Put dune into a reproducible release mode."
          "--require-dune-project-file=": "Fail if a dune-project file is missing."
          "--root=": "Use this directory as workspace root instead of guessing it."
          "--store-orig-source-dir": "Store original source location in dune-package metadata."
          "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
          "--trace-extended": "Output extended trace data (requires trace-file)."
          "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
          "--verbose": "Same as --display verbose"
          "--version": "Show version information."
          "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
          "--workspace=": "Use this specific workspace file instead of looking it up."
          "-x=": "Cross-compile using this toolchain."
        completion:
          flag:
            sandbox: [none, symlink, copy, hardlink]
            help: [auto, pager, groff, plain]
            terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
      - name: external-lib-deps
        description: "Print out external libraries needed to build the project."
        flags:
          "-h, --help": ""
      - name: installed-libraries
        description: "Print out libraries installed on the system."
        flags:
          "-h, --help": ""
      - name: opam-files
        description: "Print information about the opam files that have been discovered."
        flags:
          "-h, --help": ""
      - name: package-entries
        description: "prints information about the entries per package."
        flags:
          "-h, --help": ""
      - name: pkg
        description: "COMMAND … Subcommands related to package management"
        flags:
          "-h, --help": ""
      - name: pp
        description: "Build a given FILE and print the preprocessed output."
        flags:
          "-h, --help": ""
      - name: rules
        description: "Dump rules."
        flags:
          "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
          "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
          "--build-info": "Show build information."
          "--display-separate-messages": "Separate error messages with a blank line."
          "--error-reporting=": "Controls when the build errors are reported."
          "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
          "--file-watcher=": "Mechanism to detect changes in the source."
          "-m, --makefile": "Output the rules in Makefile syntax."
          "-o=": "Output to a file instead of stdout."
          "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
          "-r, --recursive": "Print all rules needed to build the transitive dependencies of the given targets."
          "--sandbox=": "Set sandboxing mode."
          "--stop-on-first-error": "Stop the build as soon as an error is encountered."
          "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
          "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
          "--always-show-command-line": "Always show the full command lines of programs executed by dune."
          "--auto-promote": "Automatically promote files."
          "--build-dir=": "Specified build directory."
          "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
          "--cache-check-probability=": ""
          "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
          "--config-file=": "Load this configuration file instead of the default one."
          "--debug-artifact-substitution": "Print debugging info about artifact substitution"
          "--debug-backtraces": "Always print exception backtraces."
          "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
          "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
          "--debug-digests": "Explain why Dune decides to re-digest some files"
          "--debug-load-dir": "Print debugging info about directory loading"
          "--default-target=": "Set the default target that is used when none is specified to dune build."
          "--diff-command=": "Shell command to use to diff files."
          "--disable-promotion": "Disable all promotion rules"
          "--display=": "Control the display mode of Dune."
          "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
          "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
          "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
          "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
          "--help=": "Show this help in format FMT."
          "--ignore-lock-dir": "Ignore dune.lock/ directory."
          "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
          "--instrument-with=": "Enable instrumentation by BACKENDS."
          "-j=": "Run no more than JOBS commands simultaneously."
          "--no-buffer": "Do not buffer the output of commands executed by dune."
          "--no-config": "Do not load the configuration file"
          "--no-print-directory": "Suppress \"Entering directory\" messages."
          "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
          "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
          "--print-metrics": "Print out various performance metrics after every build."
          "--profile=": "Select the build profile, for instance dev or release."
          "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
          "--release": "Put dune into a reproducible release mode."
          "--require-dune-project-file=": "Fail if a dune-project file is missing."
          "--root=": "Use this directory as workspace root instead of guessing it."
          "--store-orig-source-dir": "Store original source location in dune-package metadata."
          "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
          "--trace-extended": "Output extended trace data (requires trace-file)."
          "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
          "--verbose": "Same as --display verbose"
          "--version": "Show version information."
          "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
          "--workspace=": "Use this specific workspace file instead of looking it up."
          "-x=": "Cross-compile using this toolchain."
        completion:
          flag:
            sandbox: [none, symlink, copy, hardlink]
            help: [auto, pager, groff, plain]
            terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
      - name: targets
        description: "Print targets in a given directory."
        flags:
          "-h, --help": ""
      - name: workspace
        description: "Print a description of the workspace's structure."
        flags:
          "-h, --help": ""
  - name: diagnostics
    description: "Fetch and return errors from the current build."
    flags:
      "-h, --help": ""
  - name: exec
    description: "Execute a command in a similar environment as if installation was performed."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--context=": "Run the command in this build context."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--no-build": "don't rebuild target before executing"
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: external-lib-deps
    description: "Moved to dune describe external-lib-deps."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--missing": unused
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--sexp": unused
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "--unstable-by-dir": unused
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: fmt
    description: "Format source code."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--preview": "Just print the changes that would be made without actually applying them."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: format-dune-file
    description: "Format dune files."
    flags:
      "--dune-version=": "Which version of Dune language to use."
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
  - name: init
    description: "COMMAND … Command group for initializing Dune components."
    flags:
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
    commands:
      - name: executable
        description: "A binary executable."
        flags:
          "-h, --help": ""
      - name: library
        description: "An OCaml library."
        flags:
          "-h, --help": ""
      - name: project
        description: "A project is a predefined composition of components arranged in a standard directory structure."
        flags:
          "-h, --help": ""
      - name: test
        description: "A test harness."
        flags:
          "-h, --help": ""
  - name: install
    description: "Install packages defined in the workspace."
    flags:
      "-h, --help": ""
  - name: installed-libraries
    description: "Print out libraries installed on the system."
    flags:
      "-h, --help": ""
  - name: internal
    description: "COMMAND …"
    flags:
      "-h, --help": ""
    commands:
      - name: dune-internal
        flags:
          "-h, --help": ""
  - name: monitor
    description: "Connect to a Dune RPC server and monitor it."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--quit-on-disconnect": "Quit if the connection to the server is lost."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: ocaml
    description: "COMMAND … Command group related to OCaml."
    flags:
      "-h, --help": ""
  - name: ocaml-merlin
    description: "Start a merlin configuration server."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--context=": "Select the Dune build context that will be used to return information"
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: pkg
    description: "COMMAND … Experimental package management"
    flags:
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
    commands:
      - name: lock
        description: "Create a lockfile"
        flags:
          "-h, --help": ""
      - name: outdated
        description: "Check for outdated packages"
        flags:
          "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
          "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
          "--all": "Check all lock directories in the workspace for outdated packages."
          "--build-info": "Show build information."
          "--display-separate-messages": "Separate error messages with a blank line."
          "--error-reporting=": "Controls when the build errors are reported."
          "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
          "--file-watcher=": "Mechanism to detect changes in the source."
          "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
          "--sandbox=": "Set sandboxing mode."
          "--stop-on-first-error": "Stop the build as soon as an error is encountered."
          "--transitive": "Check for outdated packages in transitive dependencies"
          "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
          "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
          "--always-show-command-line": "Always show the full command lines of programs executed by dune."
          "--auto-promote": "Automatically promote files."
          "--build-dir=": "Specified build directory."
          "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
          "--cache-check-probability=": ""
          "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
          "--config-file=": "Load this configuration file instead of the default one."
          "--debug-artifact-substitution": "Print debugging info about artifact substitution"
          "--debug-backtraces": "Always print exception backtraces."
          "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
          "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
          "--debug-digests": "Explain why Dune decides to re-digest some files"
          "--debug-load-dir": "Print debugging info about directory loading"
          "--default-target=": "Set the default target that is used when none is specified to dune build."
          "--diff-command=": "Shell command to use to diff files."
          "--disable-promotion": "Disable all promotion rules"
          "--display=": "Control the display mode of Dune."
          "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
          "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
          "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
          "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
          "--help=": "Show this help in format FMT."
          "--ignore-lock-dir": "Ignore dune.lock/ directory."
          "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
          "--instrument-with=": "Enable instrumentation by BACKENDS."
          "-j=": "Run no more than JOBS commands simultaneously."
          "--no-buffer": "Do not buffer the output of commands executed by dune."
          "--no-config": "Do not load the configuration file"
          "--no-print-directory": "Suppress \"Entering directory\" messages."
          "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
          "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
          "--print-metrics": "Print out various performance metrics after every build."
          "--profile=": "Select the build profile, for instance dev or release."
          "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
          "--release": "Put dune into a reproducible release mode."
          "--require-dune-project-file=": "Fail if a dune-project file is missing."
          "--root=": "Use this directory as workspace root instead of guessing it."
          "--store-orig-source-dir": "Store original source location in dune-package metadata."
          "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
          "--trace-extended": "Output extended trace data (requires trace-file)."
          "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
          "--verbose": "Same as --display verbose"
          "--version": "Show version information."
          "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
          "--workspace=": "Use this specific workspace file instead of looking it up."
          "-x=": "Cross-compile using this toolchain."
        completion:
          flag:
            sandbox: [none, symlink, copy, hardlink]
            help: [auto, pager, groff, plain]
            terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
      - name: print-solver-env
        description: "Print a description of the environment that would be used to solve dependencies and then exit without attempting to solve the dependencies or generate the lockfile."
        flags:
          "-h, --help": ""
      - name: validate-lockdir
        description: "Validate that a lockdir contains a solution for local packages"
        flags:
          "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
          "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
          "--all": "Check all lock directories in the workspace for outdated packages."
          "--build-info": "Show build information."
          "--display-separate-messages": "Separate error messages with a blank line."
          "--error-reporting=": "Controls when the build errors are reported."
          "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
          "--file-watcher=": "Mechanism to detect changes in the source."
          "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
          "--sandbox=": "Set sandboxing mode."
          "--stop-on-first-error": "Stop the build as soon as an error is encountered."
          "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
          "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
          "--always-show-command-line": "Always show the full command lines of programs executed by dune."
          "--auto-promote": "Automatically promote files."
          "--build-dir=": "Specified build directory."
          "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
          "--cache-check-probability=": ""
          "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
          "--config-file=": "Load this configuration file instead of the default one."
          "--debug-artifact-substitution": "Print debugging info about artifact substitution"
          "--debug-backtraces": "Always print exception backtraces."
          "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
          "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
          "--debug-digests": "Explain why Dune decides to re-digest some files"
          "--debug-load-dir": "Print debugging info about directory loading"
          "--default-target=": "Set the default target that is used when none is specified to dune build."
          "--diff-command=": "Shell command to use to diff files."
          "--disable-promotion": "Disable all promotion rules"
          "--display=": "Control the display mode of Dune."
          "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
          "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
          "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
          "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
          "--help=": "Show this help in format FMT."
          "--ignore-lock-dir": "Ignore dune.lock/ directory."
          "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
          "--instrument-with=": "Enable instrumentation by BACKENDS."
          "-j=": "Run no more than JOBS commands simultaneously."
          "--no-buffer": "Do not buffer the output of commands executed by dune."
          "--no-config": "Do not load the configuration file"
          "--no-print-directory": "Suppress \"Entering directory\" messages."
          "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
          "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
          "--print-metrics": "Print out various performance metrics after every build."
          "--profile=": "Select the build profile, for instance dev or release."
          "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
          "--release": "Put dune into a reproducible release mode."
          "--require-dune-project-file=": "Fail if a dune-project file is missing."
          "--root=": "Use this directory as workspace root instead of guessing it."
          "--store-orig-source-dir": "Store original source location in dune-package metadata."
          "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
          "--trace-extended": "Output extended trace data (requires trace-file)."
          "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
          "--verbose": "Same as --display verbose"
          "--version": "Show version information."
          "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
          "--workspace=": "Use this specific workspace file instead of looking it up."
          "-x=": "Cross-compile using this toolchain."
        completion:
          flag:
            sandbox: [none, symlink, copy, hardlink]
            help: [auto, pager, groff, plain]
            terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: promotion
    description: "COMMAND … Control how changes are propagated back to source code."
    flags:
      "-h, --help": ""
  - name: rpc
    description: "COMMAND … Dune's RPC mechanism."
    flags:
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
    commands:
      - name: build
        description: "build a given target (requires dune to be running in passive watching mode)"
        flags:
          "-h, --help": ""
      - name: ping
        description: "Ping the build server running in the current directory"
        flags:
          "-h, --help": ""
      - name: status
        description: "show active connections"
        flags:
          "-h, --help": ""
  - name: rules
    description: "Dump rules."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "-m, --makefile": "Output the rules in Makefile syntax."
      "-o=": "Output to a file instead of stdout."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "-r, --recursive": "Print all rules needed to build the transitive dependencies of the given targets."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: runtest
    aliases: [test]
    description: "Run tests."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: show
    description: "COMMAND … Command group for showing information about the workspace"
    flags:
      "-h, --help": ""
  - name: shutdown
    description: "Cancel and shutdown any builds in the current workspace."
    flags:
      "-h, --help": ""
  - name: subst
    description: "Substitute watermarks in source files."
    flags:
      "--build-info": "Show build information."
      "--debug-backtraces": "Always print exception backtraces."
      "--help=": "Show this help in format FMT."
      "--version": "Show version information."
    completion:
      flag:
        help: [auto, pager, groff, plain]
  - name: top
    description: "Print a list of toplevel directives for including directories and loading cma files."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--context=": "Select context where to build/run utop."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: uninstall
    description: "Uninstall packages defined in the workspace."
    flags:
      "-h, --help": ""
  - name: upgrade
    description: "Upgrade projects across major Dune versions."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
  - name: utop
    description: "Load library in utop."
    flags:
      "--action-stderr-on-success=": "Same as --action-stdout-on-success but for standard error instead of standard output."
      "--action-stdout-on-success=": "Specify how to deal with the standard output of actions when they succeed."
      "--build-info": "Show build information."
      "--context=": "Select context where to build/run utop."
      "--display-separate-messages": "Separate error messages with a blank line."
      "--error-reporting=": "Controls when the build errors are reported."
      "-f, --force": "Force actions associated to aliases to be re-executed even if their dependencies haven't changed."
      "--file-watcher=": "Mechanism to detect changes in the source."
      "--passive-watch-mode": "Similar to [--watch], but only start a build when instructed externally by an RPC."
      "--sandbox=": "Set sandboxing mode."
      "--stop-on-first-error": "Stop the build as soon as an error is encountered."
      "-w, --watch": "Instead of terminating build after completion, wait continuously for file changes."
      "--wait-for-filesystem-clock": "Dune digest file contents for better incrementally."
      "--always-show-command-line": "Always show the full command lines of programs executed by dune."
      "--auto-promote": "Automatically promote files."
      "--build-dir=": "Specified build directory."
      "--cache=": "Enable or disable Dune cache (either enabled or disabled)."
      "--cache-check-probability=": ""
      "--cache-storage-mode=": "Dune cache storage mode (one of auto, hardlink or copy)."
      "--config-file=": "Load this configuration file instead of the default one."
      "--debug-artifact-substitution": "Print debugging info about artifact substitution"
      "--debug-backtraces": "Always print exception backtraces."
      "--debug-cache=": "Show debug messages on cache misses for the given cache layers."
      "--debug-dependency-path": "In case of error, print the dependency path from the targets on the command line to the rule that failed."
      "--debug-digests": "Explain why Dune decides to re-digest some files"
      "--debug-load-dir": "Print debugging info about directory loading"
      "--default-target=": "Set the default target that is used when none is specified to dune build."
      "--diff-command=": "Shell command to use to diff files."
      "--disable-promotion": "Disable all promotion rules"
      "--display=": "Control the display mode of Dune."
      "--dump-gc-stats=": "Dump the garbage collector stats to a file after the build is complete."
      "--dump-memo-graph=": "Dump the dependency graph to a file after the build is complete."
      "--dump-memo-graph-format=": "Set the file format used by --dump-memo-graph"
      "--dump-memo-graph-with-timing": "Re-run each cached node in the Memo graph after building and include the run duration in the output of --dump-memo-graph."
      "--help=": "Show this help in format FMT."
      "--ignore-lock-dir": "Ignore dune.lock/ directory."
      "--ignore-promoted-rules": "Ignore rules with (mode promote), except ones with (only ...)."
      "--instrument-with=": "Enable instrumentation by BACKENDS."
      "-j=": "Run no more than JOBS commands simultaneously."
      "--no-buffer": "Do not buffer the output of commands executed by dune."
      "--no-config": "Do not load the configuration file"
      "--no-print-directory": "Suppress \"Entering directory\" messages."
      "--only-packages=": "Ignore stanzas referring to a package that is not in PACKAGES."
      "-p, --for-release-of-packages=": "Shorthand for --release --only-packages PACKAGE."
      "--print-metrics": "Print out various performance metrics after every build."
      "--profile=": "Select the build profile, for instance dev or release."
      "--promote-install-files=": "Promote any generated <package>.install files to the source tree."
      "--release": "Put dune into a reproducible release mode."
      "--require-dune-project-file=": "Fail if a dune-project file is missing."
      "--root=": "Use this directory as workspace root instead of guessing it."
      "--store-orig-source-dir": "Store original source location in dune-package metadata."
      "--terminal-persistence=": "Change how the log of build results are displayed to the console between rebuilds while in --watch mode."
      "--trace-extended": "Output extended trace data (requires trace-file)."
      "--trace-file=": "Output trace data in catapult format (compatible with chrome://tracing)."
      "--verbose": "Same as --display verbose"
      "--version": "Show version information."
      "--watch-exclusions=": "Adds a POSIX regular expression that will exclude matching directories from `dune build --watch`."
      "--workspace=": "Use this specific workspace file instead of looking it up."
      "-x=": "Cross-compile using this toolchain."
    completion:
      flag:
        sandbox: [none, symlink, copy, hardlink]
        help: [auto, pager, groff, plain]
        terminal-persistence: [preserve, clear-on-rebuild, clear-on-rebuild-and-flush-history]
