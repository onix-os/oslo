# Converted from argc's yq.sh by tools/argc-to-spec. Do not edit by hand.
name: yq
flags:
  "-C, --colors": "force print with colors"
  "--csv-auto-parse": "parse CSV YAML/JSON values (default true)"
  "--csv-separator=": "CSV Separator character (default ,)"
  "-e, --exit-status": "set exit status if there are no matches or null or false is returned"
  "--expression=": "forcibly set the expression argument."
  "--from-file=": "Load expression from specified file."
  "-f, --front-matter=": "(extract|process) first input as yaml front-matter."
  "--header-preprocess": "Slurp any header comments and separators before processing expression."
  "-h, --help": "help for yq"
  "-I, --indent=": "sets indent level for output (default 2)"
  "-i, --inplace": "update the file in place of first file given."
  "-p, --input-format=": "parse format for input."
  "--lua-globals": "output keys as top-level global variables"
  "--lua-prefix=": "prefix (default \"return \")"
  "--lua-suffix=": "suffix (default \";\\n\")"
  "--lua-unquoted": "output unquoted string keys (e.g. {foo=\"bar\"})"
  "-M, --no-colors": "force print with no colors"
  "-N, --no-doc": "Don't print document separators (---)"
  "-0, --nul-output": "Use NUL char to separate values."
  "-n, --null-input": "Don't read input, simply evaluate the expression given."
  "-o, --output-format=": "output format type."
  "-P, --prettyPrint": "pretty print, shorthand for '... style = \"\"'"
  "--properties-array-brackets": "use [x] in array paths (e.g. for SpringBoot)"
  "--properties-separator=": "separator to use between keys and values (default \" = \")"
  "-s, --split-exp=": "print each result (or doc) into a file named (exp)."
  "--split-exp-file=": "Use a file to specify the split-exp expression."
  "--string-interpolation": "Toggles strings interpolation of \\(exp) (default true)"
  "--tsv-auto-parse": "parse TSV YAML/JSON values (default true)"
  "-r, --unwrapScalar": "unwrap scalar, print the value with no quotes, colors or comments."
  "-v, --verbose": "verbose mode"
  "-V, --version": "Print version information and quit"
  "--xml-attribute-prefix=": "prefix for xml attributes (default \"+@\")"
  "--xml-content-name=": "name for xml content (if no attribute name is present)."
  "--xml-directive-name=": "name for xml directives (e.g. <!DOCTYPE thing cat>) (default \"+directive\")"
  "--xml-keep-namespace": "enables keeping namespace after parsing attributes (default true)"
  "--xml-proc-inst-prefix=": "prefix for xml processing instructions (e.g. <?xml version=\"1\"?>) (default \"+p_\")"
  "--xml-raw-token": "enables using RawToken method instead Token."
  "--xml-skip-directives": "skip over directives (e.g. <!DOCTYPE thing cat>)"
  "--xml-skip-proc-inst": "skip over process instructions (e.g. <?xml version=\"1\"?>)"
  "--xml-strict-mode": "enables strict parsing of XML."
completion:
  flag:
    input-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, lua, l]
    output-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, shell, s, lua, l]
commands:
  - name: completion
    description: "Generate the autocompletion script for the specified shell"
    flags:
      "-h, --help": ""
    completion:
      positional:
        - [bash, zsh, fish, powershell]
  - name: eval
    description: "(default) Apply the expression to each document in each yaml file in sequence"
    flags:
      "-h, --help": "help for eval"
      "-C, --colors": "force print with colors"
      "--csv-auto-parse": "parse CSV YAML/JSON values (default true)"
      "--csv-separator=": "CSV Separator character (default ,)"
      "-e, --exit-status": "set exit status if there are no matches or null or false is returned"
      "--expression=": "forcibly set the expression argument."
      "--from-file=": "Load expression from specified file."
      "-f, --front-matter=": "(extract|process) first input as yaml front-matter."
      "--header-preprocess": "Slurp any header comments and separators before processing expression."
      "-I, --indent=": "sets indent level for output (default 2)"
      "-i, --inplace": "update the file in place of first file given."
      "-p, --input-format=": "parse format for input."
      "--lua-globals": "output keys as top-level global variables"
      "--lua-prefix=": "prefix (default \"return \")"
      "--lua-suffix=": "suffix (default \";\\n\")"
      "--lua-unquoted": "output unquoted string keys (e.g. {foo=\"bar\"})"
      "-M, --no-colors": "force print with no colors"
      "-N, --no-doc": "Don't print document separators (---)"
      "-0, --nul-output": "Use NUL char to separate values."
      "-n, --null-input": "Don't read input, simply evaluate the expression given."
      "-o, --output-format=": "output format type."
      "-P, --prettyPrint": "pretty print, shorthand for '... style = \"\"'"
      "--properties-array-brackets": "use [x] in array paths (e.g. for SpringBoot)"
      "--properties-separator=": "separator to use between keys and values (default \" = \")"
      "-s, --split-exp=": "print each result (or doc) into a file named (exp)."
      "--split-exp-file=": "Use a file to specify the split-exp expression."
      "--string-interpolation": "Toggles strings interpolation of \\(exp) (default true)"
      "--tsv-auto-parse": "parse TSV YAML/JSON values (default true)"
      "-r, --unwrapScalar": "unwrap scalar, print the value with no quotes, colors or comments."
      "-v, --verbose": "verbose mode"
      "--xml-attribute-prefix=": "prefix for xml attributes (default \"+@\")"
      "--xml-content-name=": "name for xml content (if no attribute name is present)."
      "--xml-directive-name=": "name for xml directives (e.g. <!DOCTYPE thing cat>) (default \"+directive\")"
      "--xml-keep-namespace": "enables keeping namespace after parsing attributes (default true)"
      "--xml-proc-inst-prefix=": "prefix for xml processing instructions (e.g. <?xml version=\"1\"?>) (default \"+p_\")"
      "--xml-raw-token": "enables using RawToken method instead Token."
      "--xml-skip-directives": "skip over directives (e.g. <!DOCTYPE thing cat>)"
      "--xml-skip-proc-inst": "skip over process instructions (e.g. <?xml version=\"1\"?>)"
      "--xml-strict-mode": "enables strict parsing of XML."
    completion:
      flag:
        input-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, lua, l]
        output-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, shell, s, lua, l]
  - name: eval-all
    description: "Loads _all_ yaml documents of _all_ yaml files and runs expression once"
    flags:
      "-h, --help": "help for eval-all"
      "-C, --colors": "force print with colors"
      "--csv-auto-parse": "parse CSV YAML/JSON values (default true)"
      "--csv-separator=": "CSV Separator character (default ,)"
      "-e, --exit-status": "set exit status if there are no matches or null or false is returned"
      "--expression=": "forcibly set the expression argument."
      "--from-file=": "Load expression from specified file."
      "-f, --front-matter=": "(extract|process) first input as yaml front-matter."
      "--header-preprocess": "Slurp any header comments and separators before processing expression."
      "-I, --indent=": "sets indent level for output (default 2)"
      "-i, --inplace": "update the file in place of first file given."
      "-p, --input-format=": "parse format for input."
      "--lua-globals": "output keys as top-level global variables"
      "--lua-prefix=": "prefix (default \"return \")"
      "--lua-suffix=": "suffix (default \";\\n\")"
      "--lua-unquoted": "output unquoted string keys (e.g. {foo=\"bar\"})"
      "-M, --no-colors": "force print with no colors"
      "-N, --no-doc": "Don't print document separators (---)"
      "-0, --nul-output": "Use NUL char to separate values."
      "-n, --null-input": "Don't read input, simply evaluate the expression given."
      "-o, --output-format=": "output format type."
      "-P, --prettyPrint": "pretty print, shorthand for '... style = \"\"'"
      "--properties-array-brackets": "use [x] in array paths (e.g. for SpringBoot)"
      "--properties-separator=": "separator to use between keys and values (default \" = \")"
      "-s, --split-exp=": "print each result (or doc) into a file named (exp)."
      "--split-exp-file=": "Use a file to specify the split-exp expression."
      "--string-interpolation": "Toggles strings interpolation of \\(exp) (default true)"
      "--tsv-auto-parse": "parse TSV YAML/JSON values (default true)"
      "-r, --unwrapScalar": "unwrap scalar, print the value with no quotes, colors or comments."
      "-v, --verbose": "verbose mode"
      "--xml-attribute-prefix=": "prefix for xml attributes (default \"+@\")"
      "--xml-content-name=": "name for xml content (if no attribute name is present)."
      "--xml-directive-name=": "name for xml directives (e.g. <!DOCTYPE thing cat>) (default \"+directive\")"
      "--xml-keep-namespace": "enables keeping namespace after parsing attributes (default true)"
      "--xml-proc-inst-prefix=": "prefix for xml processing instructions (e.g. <?xml version=\"1\"?>) (default \"+p_\")"
      "--xml-raw-token": "enables using RawToken method instead Token."
      "--xml-skip-directives": "skip over directives (e.g. <!DOCTYPE thing cat>)"
      "--xml-skip-proc-inst": "skip over process instructions (e.g. <?xml version=\"1\"?>)"
      "--xml-strict-mode": "enables strict parsing of XML."
    completion:
      flag:
        input-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, lua, l]
        output-format: [auto, a, yaml, y, json, j, props, p, csv, c, tsv, t, xml, x, base64, uri, toml, shell, s, lua, l]
