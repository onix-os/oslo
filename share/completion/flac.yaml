# Converted from argc's flac.sh by tools/argc-to-spec. Do not edit by hand.
name: flac
flags:
  "-v, --version": "Show the flac version number"
  "-h, --help": "Show basic usage and a list of all options"
  "-H, --explain": "Show detailed explanation of usage and all options"
  "-d, --decode": "Decode (the default behavior is to encode)"
  "-t, --test": "Test a flac encoded file (same as -d except no decoded file is written)"
  "-a, --analyze": "Analyze a FLAC encoded file (same as -d except an analysis file is written)"
  "-c, --stdout": "Write output to stdout"
  "-s, --silent": "Silent mode (do not write runtime encode/decode statistics to stderr)"
  "--totally-silent": "Do not print anything of any kind, including warnings or errors."
  "--no-utf8-convert": "Do not convert tags from local charset to UTF-8."
  "-w, --warnings-as-errors": "Treat all warnings as errors (which cause flac to terminate with a non-zero exit code)."
  "-f, --force": "Force overwriting of output files."
  "-o, --output-name=": "Force the output file name (usually flac just changes the extension)."
  "--output-prefix=": "Prefix each output file name with the given string."
  "--delete-input-file": "Automatically delete the input file after a successful encode or decode."
  "--preserve-modtime": "Output files have their timestamps/permissions set to match those of their inputs (this is default)."
  "--keep-foreign-metadata": "If encoding, save WAVE, RF64, or AIFF non-audio chunks in FLAC metadata."
  "--keep-foreign-metadata-if-present": "Like --keep-foreign-metadata, but without throwing an error if foreign metadata cannot be found or restored, instead printing a warning."
  "--skip=": "Skip over the first number of samples of the input."
  "--until=": "Stop at the given sample number for each input file."
  "--ogg": "When encoding, generate Ogg FLAC output instead of native FLAC."
  "--serial-number=": "When used with --ogg, specifies the serial number to use for the first Ogg FLAC stream, which is then incremented for each additional stream."
  "--residual-text": "Includes the residual signal in the analysis file."
  "--residual-gnuplot": "Generates a gnuplot file for every subframe; each file will contain the residual distribution of the subframe."
  "--cue=": "Set the beginning and ending cuepoints to decode."
  "-F, --decode-through-errors": "By default flac stops decoding with an error and removes the partially decoded file if it encounters a bitstream error."
  "--apply-replaygain-which-is-not-lossless=": "Applies ReplayGain values while decoding."
  "-V, --verify": "Verify a correct encoding by decoding the output in parallel and comparing to the original"
  "--lax": "Allow encoder to generate non-Subset files."
  "--replay-gain": "Calculate ReplayGain values and store them as FLAC tags, similar to vorbisgain."
  "--cuesheet=": "Import the given cuesheet file and store it in a CUESHEET metadata block."
  "--picture=": "Import a picture and store it in a PICTURE metadata block."
  "--ignore-chunk-sizes": "When encoding to flac, ignore the file size headers in WAV and AIFF files to attempt to work around problems with over-sized or malformed files."
  "-S, --seekpoint=": "Include a point or points in a SEEKTABLE."
  "-P, --padding=": "Tell the encoder to write a PADDING metadata block of the given length (in bytes) after the STREAMINFO block."
  "-T, --tag=": "Add a FLAC tag."
  "--tag-from-file=": "Like --tag, except FILENAME is a file whose contents will be read verbatim to set the tag value."
  "-b, --blocksize=": "Specify the blocksize in samples."
  "-m, --mid-side": "Try mid-side coding for each frame (stereo input only)"
  "-M, --adaptive-mid-side": "Adaptive mid-side coding for all frames (stereo input only)"
  "-0, --compression-level-0": "Synonymous with -l 0 -b 1152 -r 3 --no-mid-side"
  "-1, --compression-level-1": "Synonymous with -l 0 -b 1152 -M -r 3"
  "-2, --compression-level-2": "Synonymous with -l 0 -b 1152 -m -r 3"
  "-3, --compression-level-3": "Synonymous with -l 6 -b 4096 -r 4 --no-mid-side"
  "-4, --compression-level-4": "Synonymous with -l 8 -b 4096 -M -r 4"
  "-5, --compression-level-5": "Synonymous with -l 8 -b 4096 -m -r 5"
  "-6, --compression-level-6": "Synonymous with -l 8 -b 4096 -m -r 6 -A subdivide_tukey(2)"
  "-7, --compression-level-7": "Synonymous with -l 12 -b 4096 -m -r 6 -A subdivide_tukey(2)"
  "-8, --compression-level-8": "Synonymous with -l 12 -b 4096 -m -r 6 -A subdivide_tukey(3)"
  "--fast": "Fastest compression."
  "--best": "Highest compression."
  "-e, --exhaustive-model-search": "Do exhaustive model search (expensive!)"
  "-A, --apodization=": "Window audio data with given the apodization function."
  "-l, --max-lpc-order=": "Specifies the maximum LPC order."
  "-p, --qlp-coeff-precision-search": "Do exhaustive search of LP coefficient quantization (expensive!)."
  "-q, --qlp-coeff-precision=": "Precision of the quantized linear-predictor coefficients, 0 => let encoder decide (min is 5, default is 0)"
  "-r, --rice-partition-order=": "Set the [min,]max residual partition order (0..15)."
  "--endian=": "Set the byte order for samples"
  "--channels=": "Set number of channels."
  "--bps=": "Set bits per sample."
  "--sample-rate=": "Set sample rate (in Hz)."
  "--sign=": "Set the sign of samples."
  "--input-size=": "Specify the size of the raw input in bytes."
  "--force-raw-format": "Force input (when encoding) or output (when decoding) to be treated as raw samples (even if filename ends in .wav)."
  "--force-aiff-format": ""
  "--force-rf64-format": ""
  "--force-wave64-format=": "Force the decoder to output AIFF/RF64/WAVE64 format respectively."
  "--force-legacy-wave-format": ""
  "--force-extensible-wave-format=": "Instruct the decoder to output a WAVE file with WAVE_FORMAT_PCM and WAVE_FORMAT_EXTENSIBLE respectively."
  "--force-aiff-c-none-format": ""
  "--force-aiff-c-sowt-format=": "Instruct the decoder to output an AIFF-C file with format NONE and sowt respectively."
  "--no-adaptive-mid-side": ""
  "--no-cued-seekpoints": ""
  "--no-decode-through-errors": ""
  "--no-delete-input-file": ""
  "--no-preserve-modtime": ""
  "--no-keep-foreign-metadata": ""
  "--no-exhaustive-model-search": ""
  "--no-force": ""
  "--no-lax": ""
  "--no-mid-side": ""
  "--no-ogg": ""
  "--no-padding": ""
  "--no-qlp-coeff-prec-search": ""
  "--no-replay-gain": ""
  "--no-residual-gnuplot": ""
  "--no-residual-text": ""
  "--no-seektable": ""
  "--no-silent": ""
  "--no-verify": ""
  "--no-warnings-as-errors": ""
completion:
  flag:
    picture: [FILENAME, SPECIFICATION]
    endian: [big, little]
    sign: [signed, unsigned]
