# Converted from argc's btm.sh by tools/argc-to-spec. Do not edit by hand.
name: btm
flags:
  "--autohide_time": "Automatically hides the time scale in graphs after being shown for a brief moment when zoomed in/out."
  "-b, --basic": "Hides graphs and uses a more basic look, largely inspired by htop's design."
  "-C, --config_location=": "Sets the location of the config file."
  "-t, --default_time_value=": "Default time value for graphs."
  "--default_widget_count=": "Sets the N'th selected widget type to use as the default widget."
  "--default_widget_type=": "Sets which widget type to use as the default widget."
  "--disable_click": "Disables mouse clicks from interacting with bottom."
  "-m, --dot_marker": "Uses a dot marker for graphs as opposed to the default braille marker."
  "-e, --expanded": "Expand the default widget upon starting the app."
  "--hide_table_gap": "Hides spacing between table headers and entries."
  "--hide_time": "Hides the time scale from being shown."
  "-r, --rate=": "Sets how often data is refreshed."
  "--retention=": "How far back data will be stored up to."
  "--show_table_scroll_position": "Shows the list scroll position tracker in the widget title for table widgets."
  "-d, --time_delta=": "The amount of time changed when zooming in/out."
  "-S, --case_sensitive": "Enables case sensitivity by default when searching for a process."
  "-u, --current_usage": "Calculates process CPU usage as a percentage of current usage rather than total usage."
  "--disable_advanced_kill": "Hides additional stopping options Unix-like systems."
  "-g, --group_processes": "Groups processes with the same name by default."
  "--process_memory_as_value": "Defaults to showing process memory usage by value."
  "--process_command": "Shows the full command name instead of the process name by default."
  "-R, --regex": "Enables regex by default while searching."
  "-T, --tree": "Makes the process widget use tree mode by default."
  "-n, --unnormalized_cpu": "Show process CPU% usage without averaging over the number of CPU cores."
  "-W, --whole_word": "Enables whole-word matching by default while searching."
  "-c, --celsius": "Use Celsius as the temperature unit."
  "-f, --fahrenheit": "Use Fahrenheit as the temperature unit."
  "-k, --kelvin": "Use Kelvin as the temperature unit."
  "-l, --cpu_left_legend": "Puts the CPU chart legend on the left side."
  "--default_cpu_entry=": "Sets which CPU entry type is selected by default."
  "-a, --hide_avg_cpu": "Hides the average CPU usage entry."
  "--memory_legend=": "Where to place the legend for the memory chart widget."
  "--enable_cache_memory": "Enables collecting and displaying cache and buffer memory."
  "--network_legend=": "Where to place the legend for the network chart widget."
  "--network_use_bytes": "Displays the network widget using bytes."
  "--network_use_binary_prefix": "Displays the network widget with binary prefixes (e.g. kibibits, mebibits) rather than a decimal prefixes (e.g. kilobits, megabits)."
  "--network_use_log": "Displays the network widget with a log scale."
  "--use_old_network_legend": "(DEPRECATED) Uses separate network widget legend."
  "--battery": "Shows the battery widget in default or basic mode, if there is as battery available."
  "--enable_gpu": "Enable collecting and displaying GPU usage."
  "--theme=": "Use a pre-defined color theme."
  "-h, --help": "Prints help info (for more details use '--help'."
  "-V, --version": "Prints version information."
completion:
  flag:
    default_widget_type: [cpu, mem, net, network, proc, process, processes, temp, temperature, disk, batt, battery]
    default_cpu_entry: [all, avg]
    memory_legend: [none, top-left, top, top-right, left, right, bottom-left, bottom, bottom-right]
    network_legend: [none, top-left, top, top-right, left, right, bottom-left, bottom, bottom-right]
