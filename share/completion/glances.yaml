# Converted from argc's glances.sh by tools/argc-to-spec. Do not edit by hand.
name: glances
flags:
  "-h, --help": "show this help message and exit"
  "-V, --version": "show program's version number and exit"
  "-d, --debug": "enable debug mode"
  "-C, --config=": "path to the configuration file"
  "-P, --plugins=": "path to additional plugin directory"
  "--modules-list": "display modules (plugins & exports) list and exit"
  "--module-list": "display modules (plugins & exports) list and exit"
  "--disable-plugin=": "disable plugin (comma-separated list or all)."
  "--disable-plugins=": "disable plugin (comma-separated list or all)."
  "--disable=": "disable plugin (comma-separated list or all)."
  "--enable-plugin=": "enable plugin (comma-separated list)"
  "--enable-plugins=": "enable plugin (comma-separated list)"
  "--enable=": "enable plugin (comma-separated list)"
  "--disable-process": "disable process module"
  "--disable-webui": "disable the Web Interface"
  "--light": "light mode for Curses UI (disable all but the top menu)"
  "--enable-light": "light mode for Curses UI (disable all but the top menu)"
  "-0, --disable-irix": "task's cpu usage will be divided by the total number of CPUs"
  "-1": "start Glances in per CPU mode"
  "--percpu": "start Glances in per CPU mode"
  "--per-cpu": "start Glances in per CPU mode"
  "-2, --disable-left-sidebar": "disable network, disk I/O, FS and sensors modules"
  "-3, --disable-quicklook": "disable quick look module"
  "-4, --full-quicklook": "disable all but quick look and load"
  "-5, --disable-top": "disable top menu (QL, CPU, MEM, SWAP and LOAD)"
  "-6, --meangpu": "start Glances in mean GPU mode"
  "--disable-history": "disable stats history"
  "--disable-bold": "disable bold mode in the terminal"
  "--disable-bg": "disable background colors in the terminal"
  "--enable-irq": "enable IRQ module"
  "--enable-process-extended": "enable extended stats on top process"
  "--disable-separator": "disable separator in the UI (between top and others modules)"
  "--disable-cursor": "disable cursor (process selection) in the UI"
  "--sort-processes=": "Sort processes by: cpu_percent, memory_percent, username, cpu_times, io_counters, name"
  "--programs": "Accumulate processes by program"
  "--program": "Accumulate processes by program"
  "--export=": "enable export module (comma-separated list)"
  "--export-csv-file=": "file path for CSV exporter"
  "--export-csv-overwrite": "overwrite existing CSV file"
  "--export-json-file=": "file path for JSON exporter"
  "--export-graph-path=": "Folder for Graph exporter"
  "--export-process-filter=": "set the export process filter (comman separated list of regular expression)"
  "-c, --client=": "connect to a Glances server by IPv4/IPv6 address or hostname"
  "-s, --server": "run Glances in server mode"
  "--browser": "start the client browser (list of servers)"
  "--disable-autodiscover": "disable autodiscover feature"
  "-p, --port=": "define the client/server TCP port [default: 61209]"
  "-B, --bind=": "bind server to the given IPv4/IPv6 address or hostname"
  "--username": "define a client/server username"
  "--password": "define a client/server password"
  "-u=": "use the given client/server username"
  "--snmp-community=": "SNMP community"
  "--snmp-port=": "SNMP port"
  "--snmp-version=": "SNMP version (1, 2c or 3)"
  "--snmp-user=": "SNMP username (only for SNMPv3)"
  "--snmp-auth=": "SNMP authentication key (only for SNMPv3)"
  "--snmp-force": "force SNMP mode"
  "-t, --time=": "set minimum refresh rate in seconds [default: 2 sec]"
  "-w, --webserver": "run Glances in web server mode (FastAPI, Uvicorn, Jinja2 libs needed)"
  "--cached-time=": "set the server cache time [default: 1 sec]"
  "--stop-after=": "stop Glances after n refresh"
  "--open-web-browser": "try to open the Web UI in the default Web browser"
  "-q, --quiet": "do not display the curses interface"
  "-f, --process-filter=": "set the process filter pattern (regular expression)"
  "--process-short-name": "force short name for processes name"
  "--process-long-name": "force long name for processes name"
  "--stdout=": "display stats to stdout, one stat per line (comma-separated list of plugins/plugins.attribute)"
  "--stdout-json=": "display stats to stdout, JSON format (comma-separated list of plugins/plugins.attribute)"
  "--stdout-csv=": "display stats to stdout, CSV format (comma-separated list of plugins/plugins.attribute)"
  "--issue": "test all plugins and exit (please copy/paste the output if you open an issue)"
  "--trace-malloc": "trace memory allocation and display it at the end of the process (python 3.4 or higher needed)"
  "--memory-leak": "test memory leak (python 3.4 or higher needed)"
  "--api-doc": "display fields descriptions"
  "--hide-kernel-threads": "hide kernel threads in the process list (not available on Windows)"
  "-b, --byte": "display network rate in bytes per second"
  "--diskio-show-ramfs": "show RAM Fs in the DiskIO plugin"
  "--diskio-iops": "show IO per second in the DiskIO plugin"
  "--fahrenheit": "display temperature in Fahrenheit (default is Celsius)"
  "--fs-free-space": "display FS free space instead of used"
  "--sparkline": "display sparklines instead of bar in the curses interface"
  "--disable-unicode": "disable unicode characters in the curses interface"
  "--hide-public-info": "hide public information (like public IP)"
  "--disable-check-update": "disable online Glances version ckeck"
  "--strftime=": "strftime format string for displaying current date in standalone mode"
completion:
  flag:
    sort-processes: [cpu_percent, memory_percent, username, cpu_times, io_counters, name]
