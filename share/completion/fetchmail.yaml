# Converted from argc's fetchmail.sh by tools/argc-to-spec. Do not edit by hand.
name: fetchmail
flags:
  "-?, --help": "Displays option help."
  "-V, --version": "Displays the version information for your copy of fetchmail."
  "-c, --check": "Return a status code to indicate whether there is mail waiting, without actually fetching or deleting mail (see EXIT CODES below)."
  "-s, --silent": "Silent mode."
  "-v, --verbose": "Verbose mode."
  "--nosoftbounce": "Hard bounce mode."
  "--softbounce": "Soft bounce mode."
  "-a": "Retrieve both old (seen) and new messages from the mail server."
  "--all": "Retrieve both old (seen) and new messages from the mail server."
  "--fetchall": "Retrieve both old (seen) and new messages from the mail server."
  "-k, --keep": "Keep retrieved messages on the remote mail server."
  "-K, --nokeep": "Delete retrieved messages from the remote mail server."
  "-F, --flush": "POP3/IMAP only."
  "--limitflush": "POP3/IMAP only, since version 6.3.0."
  "-p=": "Specify the protocol to use when communicating with the remote mail server."
  "--proto=": "Specify the protocol to use when communicating with the remote mail server."
  "--protocol=": "Specify the protocol to use when communicating with the remote mail server."
  "-U, --uidl": "Force UIDL use (effective only with POP3)."
  "--idle": "Enable IDLE use (effective only with IMAP)."
  "-P, --service=": "Since version 6.3.0."
  "--port=": "Obsolete version of --service that does not take service names."
  "--principal=": "The principal option permits you to specify a service principal for mutual authentication."
  "-t, --timeout=": "The timeout option allows you to set a server-non-response timeout in seconds."
  "--plugin=": "The plugin option allows you to use an external program to establish the TCP connection."
  "--plugout=": "Identical to the plugin option above, but this one is used for the SMTP connections."
  "-r, --folder=": "Causes a specified non-default mail folder on the mail server (or comma-separated list of folders) to be retrieved."
  "--tracepolls": "Tell fetchmail to poll trace information in the form 'polling account %s' and 'folder %s' to the Received line it generates, where the %s parts are replaced by the user's remote name, the poll label, and the folder (mailbox) where available (the Received header also normally includes the server's true name)."
  "--ssl": "Causes the connection to the mail server to be encrypted via SSL, by negotiating SSL directly after connecting (called SSL-wrapped mode, or Implicit TLS by RFC-8314)."
  "--sslcert=": "For certificate-based client authentication."
  "--sslkey=": "Specifies the file name of the client side private SSL key."
  "--sslproto=": "This option has a dual use, out of historic fetchmail behaviour."
  "--sslcertck": "--sslcertck causes fetchmail to require that SSL/TLS be used and disconnect unless it can successfully negotiate SSL or TLS, or if it cannot successfully verify and validate the certificate and follow it to a trust anchor (or trusted root certificate)."
  "--nosslcertck": "The opposite of --sslcertck, this is a discouraged option."
  "--sslcertfile=": "Sets the file fetchmail uses to look up local certificates."
  "--sslcertpath=": "Sets the directory fetchmail uses to look up local certificates."
  "--sslcommonname=": "Use of this option is discouraged."
  "--sslfingerprint=": "Specify the fingerprint of the server key (an MD5 hash of the key) in hexadecimal notation with colons separating groups of two digits."
  "-S, --smtphost=": "Specify a hunt list of hosts to forward mail to (one or more host names, comma-separated)."
  "--fetchdomains=": "In ETRN or ODMR mode, this option specifies the list of domains the server should ship mail for once the connection is turned around."
  "-D, --smtpaddress=": "Specify the domain to be appended to addresses in RCPT TO lines shipped to SMTP."
  "--smtpname=": "Specify the domain and user to be put in RCPT TO lines shipped to SMTP."
  "-Z, --antispam=": "Specifies the list of numeric SMTP errors that are to be interpreted as a spam-block response from the listener."
  "-m, --mda=": "This option lets fetchmail use a Message or Local Delivery Agent (MDA or LDA) directly, rather than forward via SMTP or LMTP."
  "--lmtp": "Cause delivery via LMTP (Local Mail Transfer Protocol)."
  "--bsmtp=": "Append fetched mail to a BSMTP file."
  "--bad-header=": "Specify how fetchmail is supposed to treat messages with bad headers, i.e., headers with bad syntax."
  "-l, --limit=": "Takes a maximum octet size argument, where 0 is the default and also the special value designating \"no limit\"."
  "-w, --warnings=": "Takes an interval in seconds."
  "-b, --batchlimit=": "Specify the maximum number of messages that will be shipped to an SMTP listener before the connection is deliberately torn down and rebuilt (defaults to 0, meaning no limit)."
  "-B, --fetchlimit=": "Limit the number of messages accepted from a given server in a single poll."
  "--fetchsizelimit=": "Limit the number of sizes of messages accepted from a given server in a single transaction."
  "--fastuidl=": "Do a binary instead of linear search for the first unseen UID."
  "-e, --expunge=": "Arrange for deletions to be made final after a given number of messages."
  "-u=": "Specifies the user identification to be used when logging in to the mail server."
  "--user=": "Specifies the user identification to be used when logging in to the mail server."
  "--username=": "Specifies the user identification to be used when logging in to the mail server."
  "-I, --interface=": "Require that a specific interface device be up and have a specific local or remote IPv4 (IPv6 is not supported by this option yet) address (or range) before polling."
  "-M, --monitor=": "Daemon mode can cause transient links which are automatically taken down after a period of inactivity (e.g., PPP links) to remain up indefinitely."
  "--auth=": "This option permits you to specify an authentication type (see USER AUTHENTICATION below for details)."
  "-f, --fetchmailrc=": "Specify a non-default name for the ~/.fetchmailrc run control file."
  "-i, --idfile=": "Specify an alternate name for the .fetchids file used to save message UIDs."
  "--pidfile=": "Override the default location of the PID file that is used as a lock file."
  "-n, --norewrite": "Normally, fetchmail edits RFC-822 address headers (To, From, Cc, Bcc, and Reply-To) in fetched mail so that any mail IDs local to the server are expanded to full addresses (@ and the mail server host name are appended)."
  "-E, --envelope=": "In the configuration file, an enhanced syntax is used: envelope [<count>] <line>"
  "-Q, --qvirtual=": "The string prefix assigned to this option will be removed from the user name found in the header specified with the envelope option (before doing multidrop name mapping or localdomain checking, if either is applicable)."
  "--configdump": "Parse the ~/.fetchmailrc file, interpret any command-line options specified, and dump a configuration report to standard output."
  "-y, --yydebug": "Enables parser debugging, this option is meant to be used by developers only."
  "-T, --netsec": "Removed before version 6.3.0, the required underlying inet6_apps library had been discontinued and is no longer available."
completion:
  flag:
    bad-header: [reject, accept]
